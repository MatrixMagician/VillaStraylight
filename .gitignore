# Go
/bin/
*.test
*.out
# Built villa binary (produced by `go build ./cmd/villa`)
/villa

# Env / secrets
.env

# Screenshots captured during phase UAT — evidence, not source.
/phase*.png

# Tool scratch output: the graphify export and the browser-automation
# working directory. Both are regenerable and neither belongs in the tree.
/graphify-out/
/.playwright-mcp/

# Editor / local tooling directories
/.vscode/
