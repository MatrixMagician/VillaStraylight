Build reports/reconciliation.xlsx from invoices_q2.csv and the bank export. Sheet "Bank" holds the bank export as a table. Sheet "Invoices" holds one row per invoice with columns invoice, client, date, amount_gbp, paid_gbp, outstanding_gbp, status, where paid_gbp is a SUMIF formula over the Bank sheet matching the invoice reference, outstanding_gbp is a formula, and a final row totals the outstanding with a SUM formula. Use python3 with openpyxl to write it. openpyxl stores no cached formula values, so recalculate the file with LibreOffice headless (soffice is installed, no display) and confirm the recalculated file has values by reading it back with openpyxl in data_only mode. Also render the recalculated workbook to PDF with soffice. Report the total outstanding you read back and whether it matches the CSV arithmetic you did yourself. Do not modify any existing file.
