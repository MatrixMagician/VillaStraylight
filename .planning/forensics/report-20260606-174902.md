# Forensic Report

**Generated:** 2026-06-06T17:49:02+01:00
**Problem:** No description supplied. Investigation centered on the most salient event of
the session — the executor **worktree base-mismatch HALT** during `/gsd-execute-phase 9`,
which forced a mid-run switch from parallel worktree isolation to sequential execution.

---

## Evidence Summary

### Git Activity
- **Last commit:** 2026-06-06 17:46:42 — "chore: gofmt-align installDeps struct fields (pre-existing drift)"
- **Commits (Phase 9 session):** 18, spanning 17:12 → 17:46 (~34 min), steady cadence, no abnormal gaps.
- **Uncommitted changes:** none (tracked). 10 untracked files = pre-existing legacy scaffold (`.env.example`, `.golangci.yml`, `cmd/villastraylight/`, `internal/{config,server}/`, `web/`, `.vscode/`, `.playwright-mcp/`, `graphify-out/`) — present since session start, out of scope.
- **Active worktrees:** 1 (main only). No orphaned agent worktrees — the two halted executors' worktrees were auto-reaped.

### Planning State
- **Current milestone:** v1.1 — ROCm Opt-In Backend (phases 6–10).
- **Current phase:** 09 — `villa bench` (Honest A/B) — built + off-hardware verified, status `human_needed` (awaiting on-hardware UAT).
- **Milestone progress:** 3/5 phases complete (6, 7, 8); Phase 9 in UAT-wait; Phase 10 not started.
- **Blockers:** none recorded. STATE.md `status: executing` is slightly stale-precise (the phase is actually in on-hardware UAT-wait, not active execution) — cosmetic, not a fault.

### Branch Topology (root of the primary anomaly)
- **Working branch:** `fix/phase-03-install-model-pull-config` @ `3d3f7e3`.
- **`origin/HEAD` = `origin/main`:** `5aa41dc` (v1.0 + generated docs).
- **Divergence:** branch is **343 commits AHEAD** of `origin/HEAD`, 3 behind. `5aa41dce` is **NOT an ancestor** of HEAD — it is a divergent base missing all of Phases 6–9 (~187k-line delete delta vs. branch).

### Artifact Completeness (Phase 9)
| Phase | PLAN | CONTEXT | RESEARCH | PATTERNS | VALIDATION | SUMMARY | REVIEW | VERIFICATION | UAT |
|-------|------|---------|----------|----------|------------|---------|--------|--------------|-----|
| 09 | ✅ ×3 | ⚪ n/a¹ | ✅ | ✅ | ✅ | ✅ ×3 | ✅ | ✅ | ✅ |

¹ No CONTEXT.md by design — phase was planned via `--auto` (research-only path, no discuss-phase). Not a gap.

---

## Anomalies Detected

### 1. Worktree base-mismatch HALT — Confidence: HIGH (tooling/topology; **correctly contained**)
**Evidence:**
- Both Wave-1 executors (09-01, 09-02) returned `FATAL: worktree base mismatch — HEAD is 5aa41dce…, expected 82a2678…` and `exit 42`, before reading any files or making any commits.
- `git rev-list --left-right --count origin/HEAD...HEAD` → branch 343 ahead / 3 behind; `5aa41dce` not an ancestor of HEAD.
- `git worktree list` → only the main worktree remains (halted agents' worktrees auto-reaped; no residue).

**Interpretation:** Claude Code's `Agent(isolation="worktree")` created the executor worktrees off `origin/HEAD` (`main` = `5aa41dc`) rather than the orchestrator's branch HEAD (`82a2678`). Because the working branch is 343 commits ahead of `main` and unmerged, that base is missing the entire v1.1 codebase. The GSD spawn-time `worktree_branch_check` guard (#48) detected the base mismatch and **failed closed** — exactly its purpose: it prevented executors from building Phase 9 on a v1.0 tree and merging a catastrophic ~1000-file wrong-base diff. **The guard worked; no bad code was produced.** Recovery was a clean switch to sequential, non-isolated execution on the (correct) main working tree.

### 2. Scope-touch outside phase domain — Confidence: LOW (benign, isolated)
**Evidence:** `cmd/villa/install.go` (not a Phase 9 file) was reformatted by `go fmt ./cmd/villa/...` during the WR-05 fix and committed as `3d3f7e3 "chore: gofmt-align…"`.
**Interpretation:** Pre-existing gofmt drift in an earlier-phase file, surfaced incidentally. Correctly isolated into a standalone `chore:` commit (not mixed into Phase 9 feature commits), whitespace-only, `go vet` clean. No action needed.

### Anomalies checked and NOT found
- **Stuck loop:** `cmd/villa/bench.go` appears in 6 commits and `internal/bench/bench.go` in 4, but each is a distinct, forward-progressing change (feat → test → WR-03 → WR-01 → WR-05 → chore) with varied messages — not repeated "fix:" churn. No loop.
- **Missing artifacts:** Phase 9 has the full artifact set. None missing.
- **Partial-plan drift:** all 3 plans have committed SUMMARY.md; no commits-without-summary.
- **Abandoned work / crash residue:** last commit 2 min before investigation, zero uncommitted tracked changes, zero orphan worktrees.
- **Test regression:** no `revert`/`broken`/`fail` commits; the `fix(09)` commits are code-review remediations, full suite 533 green.

---

## Root Cause Hypothesis

A single tooling/topology mismatch — **`isolation="worktree"` forks from `origin/HEAD`, but the work lives on a feature branch 343 commits ahead of `origin/HEAD`** — caused both parallel executors to spawn on a stale v1.0 base. This is not a project or planning failure: the `#48` base-check guard caught it pre-flight, and sequential non-isolated execution completed the phase correctly. The condition will recur on every `execute-phase` run on this branch until it is merged to `main`.

## Recommended Actions

1. **Default to sequential (non-worktree) execution while this branch is unmerged.** For the remaining Phase 10 `execute-phase` run on `fix/phase-03-install-model-pull-config`, set `workflow.use_worktrees=false` (or keep dispatching executors without `isolation="worktree"`). Saved as a graphmind `bug` memory this session so it's recalled next run.
2. **Or merge the branch to `main` first.** Once `origin/HEAD` advances to include Phases 6–9, worktree isolation will fork from a correct base and parallelism is safe again. The branch name (`fix/phase-03-…`) no longer matches its contents (all of v1.1) — consider renaming/PR-ing to reduce confusion.
3. **No recovery needed for Phase 9** — it is built, verified off-hardware, and clean. Proceed with on-hardware UAT: `/gsd-verify-work 9`.
4. **Optional:** refresh STATE.md `status` from `executing` → an `awaiting-UAT` precision (cosmetic).

---

## Upstream Tracking

Already logged upstream as **open-gsd/gsd-core#683** — "Worktrees Only work if you are
developing in the default branch" (OPEN, `bug`/`needs-info`, opened 2026-06-04). Exact
root-cause match. This session's independent Linux reproduction was posted as a
corroborating comment (clears the `needs-info` gap; original report was Windows-only).

---

*Report generated by `/gsd-forensics`. Paths relative for portability; no credentials present in evidence.*
