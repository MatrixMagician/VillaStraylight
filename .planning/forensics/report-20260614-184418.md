# Forensic Report

**Generated:** 2026-06-14T17:44:18Z
**Problem:** None specified — run as a general post-mortem health scan of the Phase 27 gap-closure cycle (execute-phase --gaps-only → code-review → fix).

---

## Evidence Summary

### Git Activity
- **Last commit:** 2026-06-14 18:40 — "docs(27): add code review fix report — WR-01/WR-02/WR-03 applied"
- **Commits (this cycle):** 14 since `7c8df10` (phase held open for gap-closure)
- **Time span:** 15:52 → 18:40 local (one ~2h gap 16:20→18:27 = user decision time between turns, not a crash)
- **Uncommitted tracked changes:** none (working tree clean)
- **Untracked:** only long-standing non-project files (`.env.example`, `.golangci.yml`, `.vscode/`, screenshots) — pre-existing, unrelated
- **Active worktrees:** 1 (main only) — no orphans; no `/tmp/sv-*` or `/tmp/gsd-worktree-*` sandboxes remain

### Planning State
- **Current milestone:** v1.4 Coding Agent (status: executing, 80% — 4/5 phases, 15/15 plans)
- **Current phase:** 28 — Agent Surfacing & Contracts — Not started (expected)
- **Last session:** Phase 27 complete (re-verified PASS 5/5)
- **Blockers:** none

### Artifact Completeness (v1.4)
| Phase | PLANs | SUMMARYs | VERIFICATION | Status |
|-------|-------|----------|--------------|--------|
| 24 | 4 | 4 | ✅ | passed |
| 25 | 2 | 2 | ✅ | passed |
| 26 | 3 | 3 | ✅ | passed |
| 27 | 6 | 6 | ✅ | passed (re-verified after gap closure) |
| 28 | — | — | — | not started |

Phase 27 also carries CONTEXT, RESEARCH, PATTERNS, DISCUSSION-LOG, VALIDATION, REVIEW, REVIEW-FIX — full set, no gaps.

### Anomaly checks run
- Stuck loop: **none** — top-edited files are tracking docs (ROADMAP ×5, STATE ×4) + gap-closure sources (verify_agent.go ×3, install_test.go ×3); each commit has a distinct purpose/message, not repeated "fix:" churn on one file.
- Missing artifacts: **none** — every completed phase has PLAN+SUMMARY+VERIFICATION.
- Partial-plan drift: **none** — every plan with production commits (27-01/02/03/05/06) has a SUMMARY; 27-04 was acceptance-only (0 code commits, SUMMARY present).
- Abandoned work / crash: **none** — clean tree, no sentinels, no `.continue-here.md`.
- Test regression: **none** — no "revert"/"fix test"/"broken" commits; `make check` green at each gate.

## Anomalies Detected

### Tooling friction: partial STATE.md/ROADMAP.md updates by `phase.complete` — Confidence MEDIUM
**Evidence:** `phase.complete 27` emitted two WARNINGs ("Current Phase Name not found", "Last Activity Description not found" → update skipped) and left ROADMAP phase-27 checkbox at `[~]` plus three stale "Phase 27" text fields in STATE.md, despite returning `state_updated:true`. The orchestrator hand-corrected them in commit `74e91cb`.
**Interpretation:** STATE.md/ROADMAP body format has drifted from what the `phase.complete` writer expects, so it silently skips some fields. Final state is correct (hand-fixed), but the partial update is a latent trap: an unattended/auto run would have left a `[~]` "in-progress" checkbox on a verified-complete phase.

### Tooling friction: `gsd-tools` not on PATH for spawned executors — Confidence MEDIUM
**Evidence:** the 27-06 executor reported `gsd-tools` was not on PATH; it fell back to `node ~/.claude/sdk/.../gsd-tools.js`, and some `state.*`/`roadmap.*`/`commit` handlers no-op'd (made no file changes) in that environment, so it applied STATE/ROADMAP/commit directly via Edit+git instead.
**Interpretation:** sequential (non-worktree) executors can't rely on the gsd-tools shim; they compensate with direct git, which works but means orchestrator-side verification of STATE/ROADMAP is required (and was done).

### WR-02 behavioral change to the PRIV-06 egress gate — Confidence HIGH — ACTIONABLE (open obligation)
**Evidence:** commit `e2f2bc6` (code-review `--fix`) dropped `-f` from the egress negative-control curl (`curl -sf` → `curl -s`) in `cmd/villa/verify_agent.go`. Off-hardware truth-table tests + `make check` + `TestSeamGrepGate` are green, and fail-closed is preserved, but the 27-04 on-hardware acceptance was run against the OLD `-sf` probe. The fixer marked it `fixed: requires human verification`.
**Interpretation:** not a defect — strictly more honest (a reachable-but-erroring host now reads egress-open → FAIL instead of being excused). But it is an untested-on-hardware change to the security verb the milestone depends on. This is the one genuine forward-looking item.

### Pre-existing repo-wide gofmt drift + `make fmt` PATH bug — Confidence LOW — RESOLVED this session
**Evidence:** 5 files carried committed gofmt drift from phases 24–26 (`bench_compare.go`, `verify_memory_test.go`, `internal/agent/render.go`, `validate.go`, `validate_test.go`); `make fmt` ran bare `gofmt -w .` which fails when gofmt isn't on PATH. Fixed in `b9b49ce` (gofmt repo-wide) and `d297bc1` (`make fmt` resolves gofmt via GOROOT).
**Interpretation:** historical hygiene gap (earlier-phase executors didn't gofmt; `make lint` falls back to `go vet` here so CI never caught it). Now clean.

## Root Cause Hypothesis

No workflow failure occurred. The Phase 27 gap-closure cycle succeeded end-to-end (gaps_found 2/5 → re-verified PASS 5/5; 3 code-review warnings applied). The detected "anomalies" are minor GSD-tooling friction — `phase.complete` partially updating drifted STATE/ROADMAP, and `gsd-tools` being absent from the executor PATH — both of which the orchestrator detected and corrected inline. The only open obligation is on-hardware re-confirmation of the WR-02 egress-probe behavioral change.

## Recommended Actions

1. **On-hardware re-confirm WR-02 (highest priority):** re-run `villa verify agent` on the gfx1151 box to confirm plain `curl -s` returns exit 0 for an HTTP response when egress is OPEN (→ correctly FAILs) and a connect error (curl 6/7/28 → blocked) when egress is blocked. Do this before milestone v1.4 close.
2. **STATE/ROADMAP format drift:** consider reconciling STATE.md/ROADMAP.md body format with what `gsd-tools phase.complete` expects (or treat orchestrator hand-verification of the phase checkbox + status fields as a required post-`phase.complete` step) so unattended/auto runs don't leave a `[~]` on a complete phase.
3. **No recovery action needed** — tree is clean, all artifacts present. Next normal step: `/gsd-discuss-phase 28` (last v1.4 phase) or `/gsd-secure-phase 27`.

---

*Report generated by `/gsd-forensics`. Paths redacted for portability.*
