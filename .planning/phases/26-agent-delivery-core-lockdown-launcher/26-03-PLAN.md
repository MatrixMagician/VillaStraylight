---
phase: 26-agent-delivery-core-lockdown-launcher
plan: 03
type: execute
wave: 3
depends_on: ["26-01", "26-02"]
files_modified:
  - internal/agent/crush-policy.json
  - internal/agent/agent_test.go
autonomous: false
requirements: [AGENT-01, AGENT-04]
user_setup:
  - service: github-crush-release
    why: "Download the pinned Crush v0.76.0 release tarball to compute the extracted-binary SHA-256 (the policy currently carries a sentinel)"
    dashboard_config:
      - task: "Outbound to github.com release CDN must be reachable during the sanctioned pin window to fetch crush_0.76.0_Linux_x86_64.tar.gz"
        location: "host network (one-time pin step)"
must_haves:
  truths:
    - "crush-policy.json carries the REAL extracted-binary SHA-256 in binarySha256 (sentinel replaced) — derived from the SHA-256-verified tarball on the gfx1151 box (Pitfall 6 / Q2)"
    - "binary-drift detection no longer degrades to typed-Unknown WARN — a tampered/wrong binary is now a confident drift signal (AGENT-04)"
    - "a real `villa code` launch against the live loopback endpoint either succeeds (Crush comes up wired to villa-llama) or the on-hardware observation is recorded honestly — no pass is fabricated"
  artifacts:
    - path: "internal/agent/crush-policy.json"
      provides: "the pinned binary SHA-256 for D-14a binary-drift"
      contains: "binarySha256"
  key_links:
    - from: "internal/agent/crush-policy.json"
      to: "internal/agent/drift.go"
      via: "DetectDrift compares the installed binary hash to the now-pinned binarySha256"
      pattern: "binarySha256"
---

<objective>
Close the two on-hardware-only items the pure plans deliberately left open: (Q2) compute and pin the EXTRACTED-binary SHA-256 of Crush v0.76.0 (the policy currently carries a sentinel because the binary hash cannot be derived off-hardware — it requires extracting the SHA-256-verified tarball), and verify a real `villa code` launch against the live loopback endpoint on the gfx1151 box. This mirrors the Phase-25 Task-3 on-hardware acceptance pattern.

Purpose: Until binarySha256 is real, binary-drift detection (AGENT-04 D-14a) degrades to a typed-Unknown WARN. Pinning the real hash upgrades it to a confident drift signal. The launch smoke confirms the first-run-rendered crush.json + lockdown launcher actually bring Crush up wired to villa-llama. Both are honesty gates — no synthetic value, no fabricated pass.

Output: a real binarySha256 in crush-policy.json, the binary-drift WARN-vs-FAIL test flipped to assert a confident signal, and a recorded on-hardware launch observation.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/26-agent-delivery-core-lockdown-launcher/26-CONTEXT.md
@.planning/phases/26-agent-delivery-core-lockdown-launcher/26-RESEARCH.md
@.planning/phases/26-agent-delivery-core-lockdown-launcher/26-01-SUMMARY.md
@.planning/phases/26-agent-delivery-core-lockdown-launcher/26-02-SUMMARY.md
@internal/agent/crush-policy.json
@internal/agent/drift.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pin the extracted-binary SHA-256 (Q2 / Pitfall 6) and flip the drift test to confident</name>
  <files>internal/agent/crush-policy.json, internal/agent/agent_test.go</files>
  <read_first>
    - 26-RESEARCH.md lines 120-138 (the verified tarball pin: sha256 0f66...1ec9, size 25155696, asset/url), 359-362 (Pitfall 6: binary hash != tarball hash)
    - internal/agent/install.go (the verified stream→extract seam from Plan 02 — reuse it to extract the verified binary)
    - 26-01-SUMMARY.md (the sentinel value to replace) and 26-02-SUMMARY.md (the install seam signature)
  </read_first>
  <action>
    On the gfx1151 box, fetch the pinned tarball and verify it BEFORE extraction, exactly as the Plan-01/02 gate does:
    1. Download `crush_0.76.0_Linux_x86_64.tar.gz` from the policy urlTemplate.
    2. Verify its SHA-256 == `0f66114171270485763ffbc96f63403de5d598124c4f3841bc478c3a3a0d1ec9` and size == 25155696 (D-03). Refuse if it differs — do NOT extract an unverified artifact, do NOT pin a hash from an unverified binary.
    3. Extract the `crush` binary (prefer the Plan-02 `agent.Install` seam so the same traversal-guarded path is exercised, or a one-off `tar -xzf` is acceptable for the pin step since it is operator-run, not the product path).
    4. Compute `sha256sum` of the extracted `crush` binary and record that hex value.

    Replace the `binarySha256` sentinel in crush-policy.json with the computed value. Record the exact command output (tarball verify + binary sha256) in the SUMMARY as evidence — do NOT fabricate the value.

    Flip the Plan-01 binary-drift test: with a now-REAL policy binarySha256, the test that previously asserted typed-Unknown WARN on the sentinel must now assert a CONFIDENT BinaryDrift=true when the installed hash differs and BinaryDrift=false (no WARN) when it matches. Keep one test case covering the sentinel-degradation path (in case of a future re-pin) by constructing a DriftInput with an empty/sentinel PolicyBinSHA rather than mutating the embedded policy.
  </action>
  <verify>
    <automated>cd /home/oliverh/repos/github/MatrixMagician/VillaStraylight && go test ./internal/agent/ -run 'TestPolicyLoad|TestBinaryDrift' -count=1 && make check</automated>
  </verify>
  <done>crush-policy.json carries the real extracted-binary SHA-256 (derived from the verified tarball, evidence in SUMMARY); binary-drift is now a confident signal; the sentinel-degradation case is preserved via a constructed input; make check green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: On-hardware `villa code` launch acceptance (AGENT-01/AGENT-03 end-to-end)</name>
  <what-built>
    `villa code` launcher (Plan 02) + the verified install seam + the first-run config render (Plan 01/02). With the binary installed at $XDG_DATA_HOME/villa/bin/crush and NO pre-existing ~/.config/crush/crush.json, the FIRST `villa code` should render the config from config.toml (config-absent → render-then-launch, not a false drift), set the three lockdown env vars, exec the villa-owned crush, and Crush should connect to the loopback villa-llama endpoint (http://127.0.0.1:8080/v1) using the villa- prefixed model id. A SECOND `villa code` (config now present + matching) should detect no drift and launch directly.
  </what-built>
  <how-to-verify>
    On the gfx1151 box (this is the live dev host — see MEMORY):
    1. `make build` to get a fresh `./villa`.
    2. Install the verified binary into $XDG_DATA_HOME/villa/bin/crush (via the Plan-02 install seam path, or place the binary extracted in Task 1). Do NOT hand-create ~/.config/crush/crush.json — the FIRST `villa code` must render it from config.toml (the config-absent first-run render path). If a stale crush.json exists from prior testing, remove it first to exercise the genuine first-run path.
    3. Run `villa code` ONCE (first run): confirm it renders ~/.config/crush/crush.json from config.toml (villa provider + villa- model id + both kill switches present afterward), reports NO config-drift and NO binary-absent, and proceeds to launch. A FALSE "config drift" or a blocking remediation at this first run is a real finding — record it.
    4. Ensure villa-llama is serving (chat mode is fine — D-12: `villa code` rides whatever is served; a coding-mode-OFF WARN is expected and correct).
    5. Run `villa code` (now config present + matching): expect a coding-mode-OFF WARN (pointing at `villa coding-mode enter`) if chat mode is on, then Crush's TUI launches and can complete a prompt against the local model. Verify NO telemetry/autoupdate fetch fires (the runtime egress NEGATIVE-CONTROL proof is Phase 27 — here just observe it launches wired to local inference and answers). Confirm the second run did NOT re-write or auto-correct the config (present-but-matching → no write; D-14).
    6. Record the observation honestly. If the live box is on a ROCm backend or any environment-scoped caveat applies (cf. Phase 25 Task-3 ROCm-vs-vulkan-radv scoping), record the caveat — do NOT fabricate a pass. A graceful, correct refusal/WARN is itself a valid recorded outcome.
  </how-to-verify>
  <resume-signal>Type "approved" with the recorded launch observation (first-run render + launch, and any caveat), or describe issues to address.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub release CDN → pin step | One-time outbound fetch of the pinned tarball to derive the binary hash. |
| installed binary → drift detector | The now-pinned binarySha256 is the trust anchor for binary-drift. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-14 | Tampering | pinning a hash from an unverified binary | mitigate | The pin step verifies the tarball SHA-256 BEFORE extraction; the binary hash is derived ONLY from the verified artifact (Task 1, D-03). |
| T-26-15 | Spoofing | a fabricated binarySha256 value | mitigate | The pin records the exact verify+sha256 command output as SUMMARY evidence; no synthetic value (Task 1, honesty constraint). |
| T-26-16 | Information Disclosure | a launched agent phoning home | accept (here) | Observed at launch (no telemetry/autoupdate fetch); the negative-control-first runtime egress + llama-down proofs are Phase 27 (PRIV-06) — explicitly out of scope here. |
</threat_model>

<verification>
- `go test ./internal/agent/ -count=1` — policy + drift suite green with the real binary hash.
- `make check` — full suite green.
- On-hardware: `villa code` first run renders the config then launches wired to local inference (or a recorded honest caveat) — checkpoint approval.
</verification>

<success_criteria>
- AGENT-01: the pin policy carries a real, verified-derivation binary SHA-256 (sentinel removed).
- AGENT-04: binary-drift is a confident signal (no longer typed-Unknown WARN on the pinned asset); the sentinel-degradation path remains covered via a constructed input.
- The on-hardware `villa code` launch is recorded honestly (first-run render + launch, pass or caveat), mirroring the Phase-25 Task-3 acceptance discipline.
</success_criteria>

<output>
Create `.planning/phases/26-agent-delivery-core-lockdown-launcher/26-03-SUMMARY.md` when done. Record: the verified tarball sha256 + the derived binary sha256 (with command evidence), and the on-hardware launch observation (first-run render path + launch, pass or caveat). This closes AGENT-01 and AGENT-04 fully.
</output>
