---
phase: 26-agent-delivery-core-lockdown-launcher
plan: 02
type: execute
wave: 2
depends_on: ["26-01"]
files_modified:
  - cmd/villa/code.go
  - cmd/villa/code_test.go
  - cmd/villa/root.go
  - internal/agent/install.go
  - internal/agent/install_test.go
  - internal/agent/agent.go
autonomous: true
requirements: [AGENT-01, AGENT-03, AGENT-04]
must_haves:
  truths:
    - "`villa code` is registered as a sibling of `villa coding-mode` and runs with cobra.NoArgs"
    - "`villa code` sets CRUSH_DISABLE_METRICS=1, DO_NOT_TRACK=1, and CRUSH_DISABLE_PROVIDER_AUTO_UPDATE=1 in the launch env before exec (D-11)"
    - "`villa code` execs the villa-owned binary at $XDG_DATA_HOME/villa/bin/crush explicitly — never a PATH lookup (D-05)"
    - "`villa code` never mutates the CodingMode toggle — TestNoAutoFlipStructuralGuard stays green; when coding mode is OFF it WARNs pointing at `villa coding-mode enter` but still launches (D-12)"
    - "binary absent at launch → graceful remediation pointing at the Phase-27 install addon, not a crash (D-13)"
    - "binary drift or config drift at launch → surfaced with remediation, EXIT, never silently auto-corrected (D-13/D-14)"
    - "the install seam streams the tarball, verifies SHA-256 BEFORE extraction, and extracts the crush binary confined to $XDG_DATA_HOME/villa/bin (D-03/AGENT-01)"
    - "cmd/villa/code.go and internal/agent hold no backend-marker literals — TestSeamGrepGate stays green"
  artifacts:
    - path: "cmd/villa/code.go"
      provides: "`villa code` thin cobra caller + runCode + liveAgentDeps + XDG path helpers"
      exports: ["newCode"]
    - path: "internal/agent/install.go"
      provides: "checksum-before-extract install seam (stream→verify→tar extract, traversal-guarded)"
      exports: ["Install"]
    - path: "internal/agent/agent.go"
      provides: "agent.Run() orchestration: load → drift/presence check → coding-mode WARN → launch"
      exports: ["Run"]
  key_links:
    - from: "cmd/villa/root.go"
      to: "newCode()"
      via: "root.AddCommand"
      pattern: "newCode\\("
    - from: "cmd/villa/code.go"
      to: "$XDG_DATA_HOME/villa/bin/crush"
      via: "syscall.Exec of the explicit villa-owned path (Deps.Launch)"
      pattern: "villa.*bin.*crush"
    - from: "cmd/villa/code.go"
      to: "agent.Run"
      via: "runCode calls agent.Run(liveAgentDeps())"
      pattern: "agent\\.Run"
---

<objective>
Wire the pure `internal/agent` core (Plan 01) to the host: the `villa code` launcher (`cmd/villa/code.go` + `liveAgentDeps()`), the `agent.Run()` pre-launch flow (presence + drift check → coding-mode WARN → belt-and-braces env lockdown → exec), the checksum-before-extract install seam (`internal/agent/install.go`), the LSP PATH probe, and the verb registration in `root.go`. Clones `cmd/villa/coding-mode.go` + `internal/download/download.go` verbatim in shape.

Purpose: This makes the agent feature usable end-to-end off-hardware (the launch flow, drift surfacing, and env lockdown are all unit-testable via injected Deps). It honors the Phase-25 no-auto-flip guard (D-12), execs the villa-owned binary explicitly (D-05), and verifies the tarball before extraction (D-03). Plan 03 records the on-hardware binary hash and runs a real launch smoke.

Output: `villa code` registered and locked-down; the install seam; `agent.Run` orchestration; tests for env lockdown, binary-absent remediation, drift surfacing, and the seam/no-auto-flip guards.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-agent-delivery-core-lockdown-launcher/26-CONTEXT.md
@.planning/phases/26-agent-delivery-core-lockdown-launcher/26-RESEARCH.md
@.planning/phases/26-agent-delivery-core-lockdown-launcher/26-PATTERNS.md
@.planning/phases/26-agent-delivery-core-lockdown-launcher/26-01-SUMMARY.md

# Verbatim analogs to clone (read the cited line ranges):
@cmd/villa/coding-mode.go
@internal/download/download.go
@internal/recall/store.go
@internal/config/villaconfig.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: agent.Run() flow + install seam (AGENT-01 live half, AGENT-04 wiring)</name>
  <files>internal/agent/agent.go, internal/agent/install.go, internal/agent/install_test.go</files>
  <read_first>
    - 26-01-SUMMARY.md (the Deps/Result contract + config-drift primitive chosen in Plan 01)
    - internal/agent/drift.go, internal/agent/render.go, internal/agent/policy.go (the Plan-01 cores this flow composes)
    - internal/download/download.go (lines 72-131 stream-hash-verify-rename; 251-268 assertInsideDir — clone for tar extract confinement)
    - 26-RESEARCH.md lines 272-282 (checksum-before-place), 99 (archive/tar+compress/gzip), 543 (V12 extraction confinement)
    - internal/codingmode/codingmode.go (lines 230-377 Run() body shape — the load→guard→act ordering to mirror, WITHOUT the transactional capture/prove/rollback)
  </read_first>
  <behavior>
    - Test (TestRunBinaryAbsent): with Deps.HashBinary reporting not-present, agent.Run returns a Result with BinaryAbsent=true + a remediation Reason naming the Phase-27 install addon; no Launch is called (D-13).
    - Test (TestRunDriftSurfaced): with HashBinary/ReadConfig producing drift vs the freshly-rendered reference, Run returns BinaryDrift/ConfigDrift set + remediation; Launch is NOT called; no write/repair happens (D-14).
    - Test (TestRunLaunchesClean): with a present, non-drifting binary + matching config, Run calls Deps.Launch exactly once with an env slice containing the three lockdown vars (assert via a fake Launch capturing env) and returns a clean Result.
    - Test (TestInstallVerifyBeforeExtract): Install refuses-with-remediation (and does NOT extract) when the streamed tarball SHA-256 != policy SHA-256; on a matching tarball it extracts only the `crush` entry into the target dir; a tar entry with `..`/absolute path is rejected (traversal guard).
  </behavior>
  <action>
    Implement `agent.Run(d Deps) Result` composing the Plan-01 cores (load→check→launch ordering mirrors codingmode.Run's load-then-guard shape, but with NO transactional capture/prove/rollback — `villa code` only reads state and launches):
    1. `d.LoadConfig()` → cfg (source of truth). On error, refuse-with-remediation.
    2. Probe LSP servers via `d.LookPath` for each known server (primary gopls; opportunistically pyright/rust-analyzer/typescript-language-server) → []LSPProbe.
    3. Freshly Render(cfg, probes) → reference bytes + Warnings (the rendered reference for config-drift, AND the bytes liveAgentDeps may write — see code.go task; Run itself does not write).
    4. `d.HashBinary()` → (sha, present). `d.ReadConfig()` → on-disk crush.json bytes. Build DriftInput{InstalledBinSHA, BinaryPresent, PolicyBinSHA from policy.Assets[platform].BinarySHA256, OnDiskConfig, RenderedConfig=reference} → DetectDrift.
    5. If BinaryAbsent → Result with remediation pointing at the Phase-27 install addon (graceful, D-13), return WITHOUT launching.
    6. If BinaryDrift || ConfigDrift → Result surfacing the signal + remediation (D-13/D-14), return WITHOUT launching or auto-correcting. BinaryDriftUnknown (sentinel hash) → carry a WARN, do NOT block.
    7. Carry LSP Warnings into the Result (D-10). Carry a coding-mode-OFF flag (cfg.CodingMode==false) so the caller WARNs but still launches (D-12 — the launch itself happens in the caller's Deps.Launch; Run returns a "ready to launch" Result the caller acts on, OR Run calls d.Launch with the lockdown env — pick the codingmode-style split: the pure core decides, the injected Launch executes). Lock this: Run builds the lockdown env ([]string with CRUSH_DISABLE_METRICS=1, DO_NOT_TRACK=1, CRUSH_DISABLE_PROVIDER_AUTO_UPDATE=1 — D-11) and calls `d.Launch(env)` only on the clean path; the env construction lives in the pure core so it is unit-testable.

    `install.go`: implement `Install(...)` (the live-but-package-local half — exact signature is discretion; it streams the asset, verifies, extracts). Clone download.go's stream-hash-verify discipline: stream bytes while rolling crypto/sha256, assert size + EqualFold(hex(sum), asset.SHA256) BEFORE any extraction (reuse/call the Plan-01 VerifyTarball), refuse-with-remediation on mismatch ("refusing to install an unverified Crush binary"). Only on verify-pass, extract via archive/tar+compress/gzip, pulling ONLY the `crush` binary entry, writing it to filepath.Join(binDir, "crush") with 0755 (dir 0700), confined by an assertInsideDir-style traversal guard (clone download.go:251-268). NEVER shell out to `tar`. Phase 27 wires this into the install addon; Plan 26-02 ships the verified seam + tests.

    Keep internal/agent literal-free of backend markers (TestSeamGrepGate).
  </action>
  <verify>
    <automated>cd /home/oliverh/repos/github/MatrixMagician/VillaStraylight && go test ./internal/agent/ -run 'TestRun|TestInstall' -count=1 && go test ./internal/inference -run TestSeamGrepGate -count=1</automated>
  </verify>
  <done>agent.Run handles absent/drift/clean paths (launch only on clean, lockdown env built in the pure core); Install verifies before extracting and confines extraction; traversal entries rejected; seam gate green.</done>
</task>

<task type="auto">
  <name>Task 2: `villa code` thin caller + liveAgentDeps + registration (AGENT-03/D-05/D-11/D-12/D-13)</name>
  <files>cmd/villa/code.go, cmd/villa/code_test.go, cmd/villa/root.go</files>
  <read_first>
    - cmd/villa/coding-mode.go (lines 21-37 file-doc + marker discipline; 211-264 cobra constructors; 271-328 Result→exit mapping; 337-477 liveCodingModeDeps closure shape)
    - 26-PATTERNS.md lines 145-223 (code.go assignment: thin caller, runCode returns int, liveAgentDeps field-by-field, XDG path helpers)
    - internal/config/villaconfig.go (lines 201-219 villaConfigDir via os.UserConfigDir — for ~/.config/crush/crush.json)
    - internal/recall/store.go (lines 141-149 storeRootDir fallback chain — clone for $XDG_DATA_HOME/villa/bin)
    - cmd/villa/root.go (line 35 AddCommand list — append newCode())
  </read_first>
  <action>
    Create `cmd/villa/code.go` cloning `coding-mode.go` structure. Open with a file-doc stating: `villa code` is the agent launcher; it is LITERAL-FREE of backend markers AND coding-flag literals (TestSeamGrepGate walks cmd/villa); it does NOT render units and does NOT auto-flip coding mode (honor TestNoAutoFlipStructuralGuard, D-12). It does NOT contain any `CodingMode = true|false` literal (the no-auto-flip structural guard regex anchors on that — keep it out).

    `newCode()`: cobra.Command{Use:"code", Short/Long describing the locked-down agent launch, Args: cobra.NoArgs, RunE → code := runCode(cmd, liveAgentDeps()); os.Exit(code); return nil}.

    `runCode(cmd, d *agent.Deps) int` (body returns int, no os.Exit — so tests assert output+code without a subprocess): call agent.Run(*d); branch on the typed Result → Fprintf(errOut,...) + exitBlocked for binary-absent (remediation pointing at the Phase-27 `villa install` agent addon), for drift (surface signal + remediation, never auto-correct), and Fprintf(out,...) for the coding-mode-OFF WARN (pointing at `villa coding-mode enter`) before the launch proceeds. On the clean path agent.Run calls Deps.Launch; runCode returns exitPass for any non-launching success and lets a Launch error map to exitBlocked. Print LSP Warnings to stderr as advisory (D-10), never blocking.

    `liveAgentDeps() *agent.Deps` (clone liveCodingModeDeps): fill each Deps field with the real host impl —
    - LoadConfig: config.LoadVilla
    - LookPath: func(bin) (string,bool){ p,err:=exec.LookPath(bin); return p, err==nil }  (references only, never installs — D-10)
    - ReadConfig: os.ReadFile(crushConfigPath())  // ~/.config/crush/crush.json via os.UserConfigDir + filepath.Join(base,"crush","crush.json")
    - HashBinary: sha256 of agentBinPath() (present=false when os.Stat errors — feeds BinaryAbsent)
    - WriteConfig: MkdirAll(dir,0700) + WriteFile(...,0600), traversal-guarded (the render output writer; used when no config exists or via a future explicit render — see note)
    - Launch: func(env []string) error { syscall.Exec(agentBinPath(), []string{"crush"}, env) }  — fixed-arg, no shell; execs the EXPLICIT villa-owned path (D-05), never a PATH lookup.

    Add path helpers in the live layer: `crushConfigPath()` (os.UserConfigDir → crush/crush.json), `agentBinDir()` (clone storeRootDir fallback chain: XDG_DATA_HOME/villa/bin → ~/.local/share/villa/bin → /var/tmp/villa/bin), `agentBinPath()` = filepath.Join(agentBinDir(),"crush").

    Register in root.go: append `newCode()` to the root.AddCommand(...) list (sibling of newCodingMode()).

    Write cmd/villa/code_test.go: drive runCode with fake agent.Deps —
    - TestCodeLockdownEnv: a fake Launch captures env; assert it contains the three lockdown vars (D-11) on the clean path.
    - TestCodeBinaryAbsent: fake HashBinary reports absent → exitBlocked + stderr remediation mentioning the install addon; Launch NOT called.
    - TestCodeDriftSurfaced: fake drift inputs → exitBlocked + remediation; Launch NOT called.
    - TestCodeCodingModeOffWarns: cfg.CodingMode=false → a stderr WARN pointing at `villa coding-mode enter` AND Launch still called (D-12).
  </action>
  <verify>
    <automated>cd /home/oliverh/repos/github/MatrixMagician/VillaStraylight && go test ./cmd/villa/ -run 'TestCode' -count=1 && go test ./cmd/villa -run TestNoAutoFlipStructuralGuard -count=1 && go test ./internal/inference -run TestSeamGrepGate -count=1</automated>
  </verify>
  <done>`villa code` registered + NoArgs; lockdown env set before exec; execs the explicit villa-owned path; binary-absent + drift surface remediation without launching/auto-correcting; coding-mode-OFF WARNs but still launches; TestNoAutoFlipStructuralGuard + TestSeamGrepGate green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub release CDN → host filesystem | Install seam streams + extracts the third-party tarball to $XDG_DATA_HOME/villa/bin. |
| villa process → crush process | `villa code` execs the binary, handing it the lockdown env. |
| user PATH → launcher | A user-installed `crush` on PATH must NOT be exec'd (D-05). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-07 | Tampering | install seam — unverified tarball reaching extraction | mitigate | Install verifies SHA-256 (via Plan-01 VerifyTarball) BEFORE any extraction; refuse-with-remediation on mismatch (Task 1, D-03). |
| T-26-08 | Tampering | tar extraction path traversal | mitigate | Extraction confined to $XDG_DATA_HOME/villa/bin via an assertInsideDir guard; `..`/absolute tar entries rejected; stdlib archive/tar, never shell `tar` (Task 1, V12). |
| T-26-09 | Elevation of Privilege | launching the wrong `crush` (PATH hijack) | mitigate | Deps.Launch execs the EXPLICIT villa-owned path agentBinPath(); never exec.LookPath / PATH resolution for the binary (Task 2, D-05). |
| T-26-10 | Information Disclosure | telemetry / autoupdate phone-home at launch | mitigate | Launch env sets CRUSH_DISABLE_METRICS=1, DO_NOT_TRACK=1, CRUSH_DISABLE_PROVIDER_AUTO_UPDATE=1 (Task 1/2, D-11) — belt-and-braces with the config kill switches (Plan 01); runtime egress proof is Phase 27. |
| T-26-11 | Tampering | silent drift auto-correction at launch | mitigate | runCode surfaces drift + remediation and EXITs; no write/repair path in the launch flow (Task 1/2, D-14). |
| T-26-12 | Tampering | command injection via launched args/env | mitigate | syscall.Exec is fixed-arg with no shell; env values are constant literals; no user-controlled string is interpolated (Task 2, no-shell-interpolation constraint). |
| T-26-13 | Elevation of Privilege | `villa code` auto-flipping coding mode | mitigate | code.go contains no `CodingMode = true\|false` literal; TestNoAutoFlipStructuralGuard stays green; coding-mode-OFF is a WARN, never a mutation (Task 2, D-12). |
</threat_model>

<verification>
- `go test ./cmd/villa/ -run TestCode -count=1` — launcher env/absent/drift/coding-off paths green.
- `go test ./cmd/villa -run TestNoAutoFlipStructuralGuard -count=1` — guard stays green with the new verb.
- `go test ./internal/inference -run TestSeamGrepGate -count=1` — no backend/coding literals in code.go or internal/agent.
- `go test ./internal/agent/ -count=1` — install verify-before-extract + Run flow green.
- `make check` — vet + full suite, no regressions; `villa code --help` lists the verb after `go build`.
</verification>

<success_criteria>
- AGENT-01 (live half): the install seam streams, verifies SHA-256 before extraction, and places the binary at the villa-owned path; autoupdate forced off by construction (no upgrade verb).
- AGENT-03: `villa code` applies the belt-and-braces env lockdown (three vars) before exec and execs the explicit villa-owned binary; coding-mode-OFF WARNs but still launches (D-12); no-auto-flip guard green.
- AGENT-04 (live half): drift (binary or config) is surfaced at launch with remediation and never auto-corrected.
- TestSeamGrepGate + TestNoAutoFlipStructuralGuard green; no new Go module dependency; `make check` green.
</success_criteria>

<output>
Create `.planning/phases/26-agent-delivery-core-lockdown-launcher/26-02-SUMMARY.md` when done. Record: the agentBinPath()/crushConfigPath() resolution, the exact lockdown env slice, the install seam signature, and confirm what remains for Plan 03 (the on-hardware binarySha256 pin + a real launch smoke).
</output>
