---
phase: 26-agent-delivery-core-lockdown-launcher
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/agent/agent.go
  - internal/agent/policy.go
  - internal/agent/crush-policy.json
  - internal/agent/version.go
  - internal/agent/render.go
  - internal/agent/drift.go
  - internal/agent/agent_test.go
  - internal/agent/testdata/crush.json.golden
autonomous: true
requirements: [AGENT-01, AGENT-02, AGENT-04]
must_haves:
  truths:
    - "The embedded crush-policy.json decodes with version v0.76.0, the linux/amd64 asset, its tarball SHA-256, and a binary-SHA-256 field (sentinel until the Plan-03 on-hardware pin)"
    - "A tarball whose SHA-256 != the policy tarball SHA-256 is refused-with-remediation by the pure verify gate; a matching one passes (fail-closed, D-03)"
    - "Render(cfg) produces byte-identical output for the same config (config-drift compare is a clean compare, D-14)"
    - "The rendered crush.json sets both kill switches, exactly one openai-compat loopback provider, a non-empty models[] with a villa- prefixed id, and an lsp block for found servers only"
    - "A missing LSP server (e.g. gopls not found) produces a WARN-with-remediation and an omitted lsp entry, never a BLOCK (D-10)"
    - "DetectDrift returns a report only (binary drift / config drift flags + remediation); the core has no write or auto-correct path (D-14)"
    - "internal/agent holds no backend-marker literals — TestSeamGrepGate stays green"
  artifacts:
    - path: "internal/agent/crush-policy.json"
      provides: "go:embed pin policy: version + per-platform asset (name/sha256/size/binarySha256) + url template"
      contains: "0f66114171270485763ffbc96f63403de5d598124c4f3841bc478c3a3a0d1ec9"
    - path: "internal/agent/policy.go"
      provides: "CrushPolicy/CrushAsset types + loadCrushPolicy (panic-on-malformed embed) + VerifyTarball gate"
      exports: ["CrushPolicy", "CrushAsset"]
    - path: "internal/agent/render.go"
      provides: "Render(cfg, probes) -> deterministic crush.json bytes + LSP warnings"
      exports: ["Render", "LSPProbe", "Warning"]
    - path: "internal/agent/drift.go"
      provides: "DetectDrift(DriftInput) -> DriftReport (pure compare, never auto-corrects)"
      exports: ["DetectDrift", "DriftInput", "DriftReport"]
    - path: "internal/agent/version.go"
      provides: "version comparator cloned from preflight floors.go"
    - path: "internal/agent/agent.go"
      provides: "Deps struct (injected host-effect func fields) + typed Result"
      exports: ["Deps", "Result"]
    - path: "internal/agent/testdata/crush.json.golden"
      provides: "render-determinism golden fixture (NEW append-only)"
  key_links:
    - from: "internal/agent/render.go"
      to: "config.VillaConfig"
      via: "Render reads Model/CoderModel/CoderAgentCtx as the source of truth"
      pattern: "config\\.VillaConfig"
    - from: "internal/agent/policy.go"
      to: "crush-policy.json"
      via: "go:embed"
      pattern: "go:embed crush-policy.json"
---

<objective>
Build the pure, `Deps`-injected `internal/agent` core: the `go:embed` Crush pin policy + checksum gate (D-02/D-03, AGENT-01), the deterministic `crush.json` renderer derived from `config.toml` (D-06/D-07/D-08/D-09/D-10, AGENT-02), the version comparator, and the drift detector (D-14, AGENT-04). Every file clones a shipped first-party analog (`internal/preflight/floors.go`, `internal/codingmode/codingmode.go`); no new patterns, no new dependencies (Go stdlib only).

Purpose: This is the off-hardware, fully-testable spine of the agent delivery feature. Plan 02 wires it to the host via `cmd/villa/code.go`; Plan 03 records the on-hardware binary hash. The core returns typed values, never `os.Exit`, never prints, and is literal-free of backend markers so `TestSeamGrepGate` stays green.

Output: package `internal/agent` (policy/render/drift/version + Deps/Result), an embedded `crush-policy.json`, a render-determinism golden, and a table-driven test suite covering AGENT-01/02/04.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-agent-delivery-core-lockdown-launcher/26-CONTEXT.md
@.planning/phases/26-agent-delivery-core-lockdown-launcher/26-RESEARCH.md
@.planning/phases/26-agent-delivery-core-lockdown-launcher/26-PATTERNS.md

# Verbatim analogs to clone (read the cited line ranges, do not re-derive):
@internal/preflight/floors.go
@internal/preflight/rocm-policy.json
@internal/codingmode/codingmode.go
@internal/download/download.go
@internal/config/villaconfig.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pin policy + version comparator + Deps/Result skeleton (AGENT-01)</name>
  <files>internal/agent/policy.go, internal/agent/crush-policy.json, internal/agent/version.go, internal/agent/agent.go, internal/agent/agent_test.go</files>
  <read_first>
    - internal/preflight/floors.go (lines 73-117 embed+loader pattern; lines 141-206 compareVersions/splitVersion — clone VERBATIM)
    - internal/preflight/rocm-policy.json (flat-JSON minimalism to mirror)
    - internal/download/download.go (lines 114-130 EqualFold checksum-before-place discipline; 251-268 assertInsideDir)
    - internal/codingmode/codingmode.go (lines 38-44 package doc + seam discipline; 123-205 Deps/Result shape)
    - 26-RESEARCH.md lines 153-200 (FROZEN crush.json schema) and 359-362 (Pitfall 6: tarball-hash != binary-hash)
  </read_first>
  <behavior>
    - Test: loadCrushPolicy() decodes crush-policy.json with Version "v0.76.0", Assets["linux/amd64"].Name == "crush_0.76.0_Linux_x86_64.tar.gz", SHA256 == "0f66114171270485763ffbc96f63403de5d598124c4f3841bc478c3a3a0d1ec9", Size == 25155696.
    - Test: a malformed embed would panic (assert via a helper that unmarshals a bad byte slice through the same decode path — do not corrupt the real embed).
    - Test (TestChecksumGate): VerifyTarball returns nil when the computed hex SHA-256 equals the policy SHA-256 (case-insensitive); returns a refuse-with-remediation error when it differs or the size differs — never a silent pass.
    - Test: compareVersions/splitVersion behave identically to the floors.go originals (-1/0/+1; tolerates "v0.76.0" prefix and distro suffixes).
  </behavior>
  <action>
    Create package `internal/agent` whose package doc (clone codingmode.go:1-38 tone) states: this is a pure core, all host effects are injected `Deps` func fields wired by liveAgentDeps in cmd/villa, it imports NEITHER internal/inference NOR internal/detect, and it holds NO backend marker tokens (TestSeamGrepGate walks internal/).

    `policy.go`: `//go:embed crush-policy.json` into a package var; define `CrushPolicy{Version string; Assets map[string]CrushAsset; URLTmpl string}` and `CrushAsset{Name; SHA256; Size uint64; BinarySHA256 string}` with json tags (per D-09 discretion; mirror PATTERNS.md lines 55-66). `loadCrushPolicy()` json.Unmarshal + panic-on-malformed (build-time data, never runtime input — clone floors.go:111-117). Add `VerifyTarball(asset CrushAsset, got []byte) error` (or a size+hex-sum signature) that computes crypto/sha256, hex-encodes, strings.EqualFold-compares against asset.SHA256 and checks size, returning a refuse-with-remediation error string on mismatch (clone download.go:114-123 wording) — this is the pure half of D-03; the live stream-and-place is Plan 02.

    `crush-policy.json`: flat JSON mirroring rocm-policy.json minimalism, per the verified pin in 26-RESEARCH.md lines 120-138: version "v0.76.0"; assets {"linux/amd64": {name, sha256: 0f66...1ec9, size: 25155696, binarySha256: "<SENTINEL>"}}; urlTemplate "https://github.com/charmbracelet/crush/releases/download/v0.76.0/{asset}". Set binarySha256 to a clearly-marked sentinel string (e.g. "UNPINNED-binary-sha256-set-by-26-03-on-hardware") — Plan 03 replaces it with the extracted-binary SHA-256 (Pitfall 6 / Q2). Document the sentinel in a JSON-adjacent comment in policy.go (JSON cannot carry comments).

    `version.go`: clone compareVersions + splitVersion VERBATIM from floors.go:146-206 (unexported is fine; the package uses them internally).

    `agent.go`: define the `Deps` struct (injected host-effect func fields — clone codingmode.go:127-165 shape; fields: LoadConfig, LookPath, ReadConfig, HashBinary, WriteConfig, Launch — see PATTERNS.md lines 97-106 for the exact field list and one-line docs) and a typed `Result` carrying discrete bools (BinaryDrift, ConfigDrift, BinaryAbsent), a Reason (refuse-with-remediation), and Warnings []Warning. Do NOT implement Run() yet beyond the type — the orchestration flow body lands with the live wiring contract in Task 3 of Plan 02; here define only the types so Plan 02 has the contract. Implements D-01 (pure core, injected seam) and the D-03 verify gate.

    NEVER paste image tags / Vulkan0 / ROCm0 / device args / --jinja / sampling flags anywhere in this package (TestSeamGrepGate). The policy knows a version + a release URL + checksums only.
  </action>
  <verify>
    <automated>cd /home/oliverh/repos/github/MatrixMagician/VillaStraylight && go test ./internal/agent/ -run 'TestPolicyLoad|TestChecksumGate|TestVersionCompare' -count=1</automated>
  </verify>
  <done>crush-policy.json embeds + decodes; VerifyTarball is fail-closed on mismatch/size; compareVersions matches floors.go; Deps/Result types compile; `go vet ./internal/agent/` clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Deterministic crush.json renderer + LSP WARN + golden (AGENT-02)</name>
  <files>internal/agent/render.go, internal/agent/agent_test.go, internal/agent/testdata/crush.json.golden</files>
  <read_first>
    - 26-RESEARCH.md lines 153-200 (FROZEN schema: $schema/options/providers/lsp/permissions; required Model fields; #2649 villa- prefix; empty-models trap), 397-431 (illustrative rendered crush.json), 350-353 (Pitfall 4 determinism), 457-460 (Open-Q1: base_url is the FIXED constant http://127.0.0.1:8080/v1, NOT a config port field)
    - 26-PATTERNS.md lines 115-132 (render.go assignment: ordered structs, fixed indent+trailing newline, golden-freeze the choice)
    - internal/config/villaconfig.go (VillaConfig fields: Model, CoderModel, CoderAgentCtx — the source of truth)
  </read_first>
  <behavior>
    - Test (TestRenderGolden): Render(cfg, probes) twice with the same inputs yields byte-identical output; the output equals testdata/crush.json.golden (refreezable via -update).
    - Test (TestRenderContract): the rendered JSON parses and has options.disable_metrics==true AND options.disable_provider_auto_update==true; exactly ONE provider; that provider type=="openai-compat", base_url=="http://127.0.0.1:8080/v1", a non-empty models[]; models[0].id starts with "villa-"; no provider other than the villa one.
    - Test (TestLSPMissingWarn): a probe with Found=false for gopls produces a Warning (Code "lsp_missing", remediation mentions installing it) AND no "go"/"gopls" key in the rendered lsp block; a Found=true probe renders the entry with a fixed-literal command and emits no warning. Render never returns a blocking error for a missing LSP server (D-10).
    - Test: NO rendered value contains "$(", backtick, or "${" (Pitfall 1 — metachar-free literals).
  </behavior>
  <action>
    Implement `Render(cfg config.VillaConfig, probes []LSPProbe) ([]byte, []Warning, error)` — pure, deterministic. Lock Open-Q1 option (ii): render-only. The provider `base_url` is the FIXED loopback literal `http://127.0.0.1:8080/v1` (the inference endpoint is a constant `serverPort=8080`, not a config field — 26-RESEARCH.md Q1). NOTE this loopback URL + the model id are the ONLY non-config literals the core may hold; they are not backend markers, so the seam gate stays green.

    Build the JSON via ordered Go structs (NOT bare maps) so field order is stable; for the `providers` and `lsp` objects (which are maps in the schema) either use a single-key struct-of-structs or rely on encoding/json's sorted-map-key guarantee and pin it with the golden. Use json.MarshalIndent with a FIXED indent ("  ") and a FIXED trailing newline. Render:
    - top-level `$schema` = "https://charm.land/crush.json"
    - `options`: disable_metrics=true, disable_provider_auto_update=true (D-07); set disable_default_providers=true and auto_lsp=false (Pitfall 2/5 — strengthens cloud-fallback prevention + keeps the lsp block authoritative for determinism).
    - `providers.villa`: name, type "openai-compat" (D-08), base_url the loopback literal, api_key "local" (non-secret dummy; existing internal/inference probe uses "local"), and a non-empty `models[]` (D-08 — empty triggers a startup /models fetch, Pitfall 3).
    - `models[0]`: id = "villa-" + the served-model id derived from cfg (D-09 villa- prefix; the served id is cfg.CoderModel when coding-mode coder is set, else cfg.Model — pick the served model the unit advertises); name a human label; context_window = cfg.CoderAgentCtx when set else a sane default; default_max_tokens a fraction of context; all cost_* = 0; can_reason/supports_attachments honest defaults (false/false acceptable).
    - `lsp`: for each probe with Found, emit `{<key>: {command: <fixed literal>}}` (NO $() — Pitfall 1); for each not-Found, append a Warning{Code:"lsp_missing", Msg: install-hint} and OMIT the entry (D-10, clone 26-RESEARCH.md lines 378-394).
    - `permissions`: render minimal or omit (default-prompt). Do NOT render an allow-all allowed_tools (Q3 — full STRIDE allowlist is Phase 27).

    Define `LSPProbe{Key, Command string; Found bool}` and `Warning{Code, Msg string}` exported types. `crush.json` is a DERIVED artifact of config.toml — never the authority (config-as-source-of-truth; drift is flagged in Task 3, never auto-corrected).

    Create testdata/crush.json.golden (NEW append-only fixture) via the package-level `var update = flag.Bool("update", ...)` convention (clone the orchestrate golden discipline). This golden pins render determinism (D-14 config-drift depends on byte-stability).
  </action>
  <verify>
    <automated>cd /home/oliverh/repos/github/MatrixMagician/VillaStraylight && go test ./internal/agent/ -run 'TestRenderGolden|TestRenderContract|TestLSPMissingWarn' -count=1</automated>
  </verify>
  <done>Render is byte-deterministic and matches the golden; both kill switches + single loopback provider + non-empty villa- prefixed models[] asserted; missing LSP WARNs (never blocks) and omits the entry; no $()/metachars in any rendered value.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Pure drift detector (AGENT-04)</name>
  <files>internal/agent/drift.go, internal/agent/agent_test.go</files>
  <read_first>
    - 26-PATTERNS.md lines 136-142 (drift.go assignment: handed bytes/hashes + freshly-rendered reference; two signals; never auto-correct)
    - 26-RESEARCH.md lines 350-353 (Pitfall 4: prefer parsed-semantic compare to tolerate whitespace, also flag raw-byte stale-format), 359-362 (Pitfall 6: binary hash vs tarball hash)
  </read_first>
  <behavior>
    - Test (TestBinaryDrift): DetectDrift with InstalledBinSHA != PolicyBinSHA sets BinaryDrift=true + a remediation Reason; equal hashes set BinaryDrift=false. When PolicyBinSHA is the UNPINNED sentinel, BinaryDrift degrades to a typed-Unknown WARN (not a confident FAIL) — never a false drift on a freshly, correctly installed binary (Pitfall 6).
    - Test (TestConfigDrift): DetectDrift with OnDiskConfig semantically != RenderedConfig sets ConfigDrift=true + remediation; semantically-equal but whitespace-different bytes do NOT set ConfigDrift=true (parsed-semantic compare, Pitfall 4) — optionally a softer stale-format flag.
    - Test: DetectDrift never writes, never repairs — it returns a report value only (D-14). (Assert by signature: no Deps/io args; pure inputs → DriftReport.)
  </behavior>
  <action>
    Implement `DetectDrift(in DriftInput) DriftReport` — pure, zero I/O (live reads are injected via Deps.ReadConfig / Deps.HashBinary in Plan 02). `DriftInput{InstalledBinSHA string; BinaryPresent bool; PolicyBinSHA string; OnDiskConfig []byte; RenderedConfig []byte}`. `DriftReport{BinaryAbsent bool; BinaryDrift bool; ConfigDrift bool; BinaryDriftUnknown bool; Reason string}`.

    Logic: (a) if !BinaryPresent → BinaryAbsent=true (Plan 02 maps this to the Phase-27 install remediation, D-13). (b) binary drift = installed binary SHA-256 != policy `binarySha256` (Pitfall 6 — compare against the BINARY hash, never the tarball sha256). If PolicyBinSHA is the sentinel/empty → set BinaryDriftUnknown=true (typed-Unknown WARN — do NOT report a false drift). (c) config drift: lock Open-Q4 the parsed-semantic way — unmarshal OnDiskConfig and RenderedConfig into the same generic structure (or into a normalizing struct) and compare with reflect.DeepEqual; equal-semantics-different-whitespace is NOT drift; differing semantics is ConfigDrift=true. Each non-clean signal carries a refuse-with-remediation Reason. NO auto-correct path exists anywhere in this file (D-14).
  </action>
  <verify>
    <automated>cd /home/oliverh/repos/github/MatrixMagician/VillaStraylight && go test ./internal/agent/ -run 'TestBinaryDrift|TestConfigDrift' -count=1 && go test ./internal/inference -run TestSeamGrepGate -count=1</automated>
  </verify>
  <done>Binary drift + config drift detected with remediation; sentinel binary hash → typed-Unknown WARN, not false drift; whitespace-only config change is not drift; no write/repair path; TestSeamGrepGate green (no backend literals in internal/agent).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub release CDN → villa | Third-party tarball bytes enter the host (Plan 02 streams them; this plan owns the verify gate). |
| config.toml → rendered crush.json | villa-controlled values rendered into a file Crush will `$(...)`-expand at load. |
| embedded crush-policy.json → core | Build-time data, NOT runtime attacker input (compiled in). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-01 | Tampering | downloaded Crush tarball (supply chain) | mitigate | `VerifyTarball` (Task 1) — crypto/sha256 hex EqualFold-compare against policy SHA-256 + size, refuse-with-remediation on mismatch (D-03); the live stream-place wires this BEFORE extraction in Plan 02. |
| T-26-02 | Elevation of Privilege / Tampering | rendered crush.json values (`$(...)` exec) | mitigate | Every rendered value is a fixed metachar-free literal (loopback URL, "local", "gopls"); test asserts no `$(`/backtick/`${` in any value (Task 2, Pitfall 1); global-only render (D-06). |
| T-26-03 | Information Disclosure | cloud-model fallback / extra providers | mitigate | Exactly ONE openai-compat provider + `disable_default_providers=true` + explicit non-empty `models[]` (Task 2, D-08, Pitfall 2/3); runtime egress proof is Phase 27. |
| T-26-04 | Information Disclosure | telemetry phone-home | mitigate | `disable_metrics`=true + `disable_provider_auto_update`=true in rendered config (Task 2, D-07); env-var belt is the launcher (Plan 02, D-11). |
| T-26-05 | Tampering | malformed embedded policy | accept | Build-time `panic` on malformed embed (loadCrushPolicy) — compiled-in data, no attacker-controlled path (V5). |
| T-26-06 | Tampering | silent binary/config drift | mitigate | `DetectDrift` surfaces both signals with remediation and has NO auto-correct path (Task 3, D-14); binary drift compares the BINARY hash not the tarball hash (Pitfall 6). |
| T-26-SC | Tampering | charmbracelet/crush release pin | mitigate | Package Legitimacy Audit (26-RESEARCH.md lines 140-151) verdict OK — well-known Charm org, pinned by version + SHA-256 over checksums.txt. No new go.mod dependency. |
</threat_model>

<verification>
- `go test ./internal/agent/ -count=1` — full new suite green (policy/checksum/version/render/golden/LSP/drift).
- `go test ./internal/inference -run TestSeamGrepGate -count=1` — no backend-marker leak into internal/agent.
- `make check` — vet + full suite (no regression in any existing package).
- Render determinism: running `go test ./internal/agent -run TestRenderGolden` twice with no -update produces no diff.
</verification>

<success_criteria>
- AGENT-01 (partial — pure half): the pin policy decodes; the checksum gate is fail-closed (the live stream-place lands in Plan 02, the binary hash in Plan 03).
- AGENT-02: crush.json renders as a deterministic derived artifact of config.toml with both kill switches, one loopback openai-compat provider, a non-empty villa- prefixed models[], and detected-only LSP entries with WARN-on-absence.
- AGENT-04 (partial — pure half): the drift detector flags binary + config drift with remediation and never auto-corrects (live reads wired in Plan 02; binary hash pinned in Plan 03).
- TestSeamGrepGate stays green; no new Go module dependency; `make check` green.
</success_criteria>

<output>
Create `.planning/phases/26-agent-delivery-core-lockdown-launcher/26-01-SUMMARY.md` when done. Record: the chosen config-drift compare primitive (parsed-semantic vs raw-byte), the exact villa- model-id prefix + how the served id is derived, and the binarySha256 sentinel value Plan 03 must replace.
</output>
