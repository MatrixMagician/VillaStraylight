---
phase: 06-rocm-backend-resolver-spine
plan: 03
type: execute
wave: 3
depends_on: ["06-02"]
files_modified:
  - cmd/villa/install.go
  - cmd/villa/lifecycle.go
  - cmd/villa/status.go
  - cmd/villa/dashboard.go
  - cmd/villa/model.go
  - cmd/villa/inference.go
  - internal/status/status.go
autonomous: true
requirements: [ROCM-04]
must_haves:
  truths:
    - "Every previously-hardcoded VulkanBackend() call site (8 physical occurrences across 7 non-test files) resolves the backend through BackendFor(cfg.Backend)"
    - "Each re-routed site surfaces a BackendFor error through its existing error path (no silent swallow, no new panic)"
    - "The start-time offload-assert receives the resolved backend's markers (validate path is no longer Vulkan-only)"
    - "The full v1.0 test suite stays green under the Vulkan default — the re-route is a behavior no-op (SC#4)"
  artifacts:
    - path: "cmd/villa/install.go"
      provides: "both install sites resolve via BackendFor(cfg.Backend), errors surfaced through render-fail/deps-wiring paths"
    - path: "internal/status/status.go"
      provides: "status render site resolves via BackendFor, error folded into the Report failure return"
    - path: "cmd/villa/inference.go"
      provides: "runValidation resolves the backend via BackendFor(config.LoadVilla().Backend) (A1 resolved) and threads markers into the start-time assert"
  key_links:
    - from: "cmd/villa/*.go + internal/status/status.go"
      to: "inference.BackendFor"
      via: "BackendFor(cfg.Backend) replaces inference.VulkanBackend()"
      pattern: "inference\\.BackendFor\\("
    - from: "cmd/villa/inference.go"
      to: "ValidateInput.Markers"
      via: "runValidation sets Markers from BackendFor(...).ResidencyProof()"
      pattern: "ResidencyProof\\(\\)"
---

<objective>
Re-route all 8 physical `inference.VulkanBackend()` occurrences across the 7
non-test files to `BackendFor(cfg.Backend)` (D-03/ROCM-04), each surfacing the
resolver error through its existing error path, and thread the resolved backend's
markers into the start-time offload-assert (`ValidateInput.Markers`). Prove the
re-route is a behavior no-op under the Vulkan default: the whole v1.0 suite stays
green (SC#4).

Purpose: This is the spine's payoff — config (`cfg.Backend`) now drives backend
selection everywhere, replacing the hardcoded Vulkan literal. Because the six TEST
helpers keep constructing `VulkanBackend()` directly (never via `BackendFor`) and
the config default is `"vulkan"`, every existing verdict is unchanged — that no-op
IS success criterion #4.

Output: 8 re-routed call sites; markers threaded into the validate path; `go vet
./... && go test ./...` green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-rocm-backend-resolver-spine/06-CONTEXT.md
@.planning/phases/06-rocm-backend-resolver-spine/06-RESEARCH.md
@.planning/phases/06-rocm-backend-resolver-spine/06-PATTERNS.md
@.planning/phases/06-rocm-backend-resolver-spine/06-VALIDATION.md
@.planning/phases/06-rocm-backend-resolver-spine/06-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-route the RenderInput.Backend sites (install, lifecycle, model, internal/status)</name>
  <files>cmd/villa/install.go, cmd/villa/lifecycle.go, cmd/villa/model.go, internal/status/status.go</files>
  <read_first>
    - cmd/villa/install.go:234 (RenderInput{Backend: inference.VulkanBackend()} — surrounding render-fail handling that returns exitBlocked; note cfg already in scope and cfg.Backend is SET from rec.Backend at install.go:227)
    - cmd/villa/lifecycle.go:66 (RenderInput{Backend: ...} — surrounding `return nil, "", fmt.Errorf(...%w)` style)
    - cmd/villa/model.go:361 (RenderInput{Backend: ...} inside a closure returning (bool, error))
    - internal/status/status.go:180 (RenderInput{Backend: ...} — surrounding ModelFile/Render error returns; the Report{Overall: StatusFail...} shape)
    - internal/inference/backend.go (BackendFor signature from Plan 02)
    - .planning/phases/06-rocm-backend-resolver-spine/06-PATTERNS.md (§7 Call Sites — verified line numbers + per-site error path)
  </read_first>
  <action>
    At each of these four RenderInput sites, replace `Backend: inference.VulkanBackend()`
    with a resolved backend. Resolve once near where `cfg` is already in scope:
    `b, err := inference.BackendFor(cfg.Backend)` (use the config var already loaded at
    each site; confirm the local cfg name), then set `Backend: b`. Surface err through the
    EXISTING error path per site (do not invent new control flow): install.go:234 →
    print to errOut and `return exitBlocked` matching the existing render-fail handling;
    lifecycle.go:66 → `return nil, "", fmt.Errorf("resolve backend: %w", err)`;
    model.go:361 → `return false, err` (the closure already returns (bool, error));
    internal/status/status.go:180 → `return Report{Overall: inference.StatusFail.String(),
    ..., err: err}` matching the surrounding Render/ModelFile error returns. Do not keep
    any literal `VulkanBackend()` at these sites. Do not change behavior under the Vulkan
    default (cfg.Backend is "vulkan" → BackendFor returns the unchanged Vulkan backend).
  </action>
  <verify>
    <automated>go build ./... && go test ./internal/status/ ./cmd/villa/ -run 'Render|Status|Lifecycle|Model|Install'</automated>
  </verify>
  <acceptance_criteria>
    - `go build ./...` compiles.
    - `grep -c 'inference.VulkanBackend()' cmd/villa/install.go cmd/villa/lifecycle.go cmd/villa/model.go internal/status/status.go` returns 0 for each (RenderInput sites re-routed). [install.go has a second occurrence handled in Task 2 — after Task 1 install.go still shows 1 until Task 2.]
    - Each site calls `inference.BackendFor(` with the in-scope config backend value.
    - The BackendFor error is returned/printed through the pre-existing error path at each site (no `_ = err`, no panic).
    - `go test ./internal/status/ ./cmd/villa/` passes for the affected tests (Vulkan-default no-op).
  </acceptance_criteria>
  <done>The four RenderInput.Backend sites resolve via BackendFor and surface errors through existing paths; package builds; affected tests green under the Vulkan default.</done>
</task>

<task type="auto">
  <name>Task 2: Re-route the Endpoint()/deps-wiring sites (install deps, status deps, dashboard) + inference.go validate path (A1 resolved)</name>
  <files>cmd/villa/install.go, cmd/villa/status.go, cmd/villa/dashboard.go, cmd/villa/inference.go</files>
  <read_first>
    - cmd/villa/install.go:632 (NewContainerRunner(VulkanBackend(), ...).Endpoint() — a deps-wiring helper, e.g. liveInstallDeps)
    - cmd/villa/status.go:135 (NewContainerRunner(VulkanBackend(), ...).Endpoint() — liveStatusDeps; note this helper already calls config.LoadVilla at status.go:137 `LoadConfig: config.LoadVilla` and status.go:168 `cfg, err := config.LoadVilla()`)
    - cmd/villa/dashboard.go:130 (NewContainerRunner(VulkanBackend(), ...).Endpoint() — folds liveStatusDeps; single resolution point; dashboard.go:132 `LoadConfig: config.LoadVilla`, :168 `cfg, err := config.LoadVilla()`)
    - cmd/villa/inference.go:100-130 (runValidation builds `backend := inference.VulkanBackend()` at line 113 around detect.Probe + recommend.Pick; the existing inference.Verdict{Status: FAIL,...} refusal at ~127)
    - internal/config/villaconfig.go:124 (`func LoadVilla() (VillaConfig, error)` — the standard config loader used by liveStatusDeps/liveDashboardDeps/liveLifecycleDeps/recommend.go; VillaConfig.Backend defaults to "vulkan")
    - internal/inference/validate.go:34-58,122 (ValidateInput now carries a Markers field per Plan 01; scrapeOffloadLog(stderr, in.Markers))
    - .planning/phases/06-rocm-backend-resolver-spine/06-PATTERNS.md (§Open Plan Decisions A1) and §RESEARCH Assumptions A1
  </read_first>
  <action>
    Endpoint()/deps sites (install.go:632, status.go:135, dashboard.go:130): resolve the
    backend ONCE in the deps-wiring helper where cfg is available (`b, err :=
    inference.BackendFor(cfg.Backend)`), surface err up through the deps constructor's
    return, and pass `b` into `NewContainerRunner(b, ...)`. dashboard.go reuses the
    liveStatusDeps single resolution point — do not resolve twice.

    inference.go:113 (runValidation) — A1 IS RESOLVED, use this exact mechanical recipe:
    runValidation currently takes no config and builds `backend := inference.VulkanBackend()`
    directly. There is NO cfg threaded into runValidation today, so DO NOT invent a caller-
    threading signature change. Instead load the villa config the SAME way every other live
    deps-wiring site does — call `config.LoadVilla()` (internal/config/villaconfig.go:124,
    the established loader used by liveStatusDeps status.go:168, liveDashboardDeps
    dashboard.go:168, liveLifecycleDeps lifecycle.go:162, and recommend.go:50). Concretely,
    at the top of runValidation replace `backend := inference.VulkanBackend()` with:
    `cfg, err := config.LoadVilla()` (handle err by returning the existing
    `inference.Verdict{Status: inference.StatusFail, Detail: "...", Remediation: "...",
    Provenance: "config.LoadVilla"}` refusal shape, mirroring the CR-02 refusal at ~127 —
    a config load failure is a FAIL, never a silent Vulkan default); then `backend, err :=
    inference.BackendFor(cfg.Backend)` and on err return the same Verdict{StatusFail}
    refusal shape (Detail names the bad backend value). This honors `backend = "rocm"` on
    `villa inference` while keeping the Vulkan default a no-op (LoadVilla returns
    Backend:"vulkan" by default → BackendFor returns the unchanged Vulkan backend). Add the
    `internal/config` import to inference.go if not already present.

    Then thread the markers into the start-time assert: set `Markers:
    backend.ResidencyProof()` on the ValidateInput the runValidation path constructs (the
    `in := inference.ValidateInput{...}` block), so validate.go:122's scrapeOffloadLog gets
    the correct per-backend markers. Remove the last literal VulkanBackend() at these sites.
  </action>
  <verify>
    <automated>go build ./... && go test ./cmd/villa/ ./internal/dashboard/</automated>
  </verify>
  <acceptance_criteria>
    - `go build ./...` compiles.
    - `grep -rc 'inference.VulkanBackend()' cmd/villa/ internal/status/ --include=*.go | grep -v '_test.go'` shows 0 non-test occurrences across all 7 files (all 8 physical sites re-routed).
    - inference.go runValidation loads config via `config.LoadVilla()`, resolves via `inference.BackendFor(cfg.Backend)`, and sets `Markers: backend.ResidencyProof()` on its ValidateInput (start-time assert is backend-aware).
    - A config-load failure and an unknown-backend value each return the existing `inference.Verdict{Status: inference.StatusFail, ...}` refusal shape (no silent Vulkan default, no panic).
    - The deps-wiring helpers resolve the backend once and surface BackendFor errors through their return (no swallow).
    - `go test ./cmd/villa/ ./internal/dashboard/` passes (Vulkan-default no-op).
    - The cfg source for inference.go:113 is `config.LoadVilla()` (A1 resolved — recorded in the SUMMARY).
  </acceptance_criteria>
  <done>All remaining Endpoint()/deps sites resolve via BackendFor; runValidation loads `config.LoadVilla()` then resolves via BackendFor and threads `backend.ResidencyProof()` markers into ValidateInput (A1 resolved with a deterministic recipe); no non-test VulkanBackend() literals remain; build + affected tests green.</done>
</task>

<task type="auto">
  <name>Task 3: Full-suite no-op proof (SC#4)</name>
  <files>internal/inference/seam_test.go</files>
  <read_first>
    - internal/inference/seam_test.go (TestSeamGrepGate — confirm it still passes after the re-route; the negative gate asserts backend literals don't leak outside the seam)
    - .planning/phases/06-rocm-backend-resolver-spine/06-VALIDATION.md (§Per-Task Verification Map — 6-noop-proof: `go test ./...`)
    - .planning/phases/06-rocm-backend-resolver-spine/06-PATTERNS.md (§7 Call Sites note — six test helpers keep injecting VulkanBackend() directly; that is why the no-op holds)
  </read_first>
  <action>
    Run the full suite to prove the 7-site re-route is a behavior no-op under the Vulkan
    default. If `go vet ./...` or `go test ./...` surfaces any regression, fix the offending
    re-route to restore byte-identical Vulkan behavior (do NOT change test assertions to make
    them pass — the no-op guarantee is the contract). Confirm the six test helpers
    (cmd/villa/status_test.go, internal/status/status_test.go, internal/dashboard/api_test.go,
    internal/inference/inference_test.go, internal/orchestrate/render_test.go) still construct
    VulkanBackend() directly and remain green — do not touch them. If the negative seam gate
    needs the rocm token verification added in Plan 02 to remain green, confirm it; otherwise
    no edit to seam_test.go is required (this task may close with zero source changes if the
    suite is already green — the deliverable is the proof).
  </action>
  <verify>
    <automated>go vet ./... && go test ./...</automated>
  </verify>
  <acceptance_criteria>
    - `go vet ./...` is clean.
    - `go test ./...` is fully green (every package, the v1.0 suite under the Vulkan default).
    - `grep -rc 'inference.VulkanBackend()' cmd/villa/ internal/status/ --include=*.go | grep -v '_test.go'` confirms 0 non-test occurrences remain.
    - The six VulkanBackend()-injecting test helpers are unchanged (no edits) and pass.
    - No test assertion was weakened to pass; any failure was fixed in the re-route source, not the test.
  </acceptance_criteria>
  <done>`go vet ./... && go test ./...` is green; the re-route is proven a behavior no-op under the Vulkan default (SC#4); no test helper was modified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config (cfg.Backend) → call sites | The config string now drives backend selection at 8 sites; an unresolvable value must fail closed through each existing error path, never default silently to a privileged backend. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-6-07 | Spoofing/Tampering | 8 re-routed call sites | mitigate | Each site routes through BackendFor (fail-closed, D-02) and surfaces the error via its pre-existing error path — no silent fallback. runValidation loads `config.LoadVilla()` and refuses (StatusFail) on a config-load or unknown-backend error rather than silently defaulting. The full-suite no-op proof confirms no behavior drift under the Vulkan default. |
| T-6-08 | Tampering | start-time offload-assert (validate path) | mitigate | runValidation threads `backend.ResidencyProof()` into ValidateInput.Markers so the install/`villa inference` assert is backend-correct, not Vulkan-only (closes the #1 trap end-to-end). |
| T-6-SC | Tampering | npm/pip/cargo installs | mitigate | No package-manager installs this plan (re-route only; no new deps — RESEARCH §Package Legitimacy Audit: no Go packages added). No checkpoint required. |
</threat_model>

<verification>
- `go vet ./... && go test ./...` is green (the SC#4 no-op proof).
- Zero non-test `inference.VulkanBackend()` occurrences remain across the 7 files.
- The start-time validate path is backend-aware (Markers threaded).
- runValidation resolves cfg via `config.LoadVilla()` (A1 resolved) and fails closed on a load/unknown-backend error.
- The six VulkanBackend()-injecting test helpers are untouched and green.
</verification>

<success_criteria>
- All 8 physical VulkanBackend() occurrences (7 non-test files) resolve through BackendFor(cfg.Backend) with errors surfaced through existing paths (ROCM-04 / SC#4).
- The start-time offload-assert receives the resolved backend's markers.
- runValidation's cfg source is `config.LoadVilla()` (A1 resolved deterministically).
- The full v1.0 test suite stays green under the Vulkan default (the re-route is a behavior no-op).
</success_criteria>

<output>
Create `.planning/phases/06-rocm-backend-resolver-spine/06-03-SUMMARY.md` when done.
</output>
