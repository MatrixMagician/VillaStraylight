---
phase: 06-rocm-backend-resolver-spine
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - internal/inference/backend_rocm.go
  - internal/inference/backend.go
  - internal/inference/backend_rocm_test.go
  - internal/inference/seam_test.go
  - internal/inference/offload_test.go
  - internal/inference/running_offload_test.go
  - internal/inference/testdata/load_tensors_rocm.txt
  - internal/inference/testdata/load_tensors_rocm_cpu.txt
  - internal/inference/testdata/load_tensors_rocm_fault.txt
  - internal/inference/testdata/rocm_devinfo_pass.stderr
  - internal/inference/testdata/rocm_offloaded_partial.stderr
autonomous: true
requirements: [ROCM-01, ROCM-02, ROCM-04]
must_haves:
  truths:
    - "Setting backend = rocm produces a ROCm Backend (rocm-7.2.4 digest-pinned image, kfd+dri devices, render group, ordered HSA/HIPBLASLT env) with no specifics leaking to callers"
    - "BackendFor fails closed on an unknown backend with an actionable error, never a silent Vulkan fallback; empty/vulkan → Vulkan"
    - "A grep-gate test fails if a refactor drops the ROCm0/HSA/kfd markers from backend_rocm.go"
    - "The ROCm offload-assert distinguishes real offload (ROCm0 N/N) from CPU fallback (FAIL), fault (FAIL), and unevaluable (WARN) via the descriptor + reused combine logic"
    - "The ROCm running verdict exercises the gpu_busy_percent residency signal (D-06): a Known non-zero busy fixture corroborates the ROCm0 N/N PASS, and an Unknown/absent busy reading is neutral-for-PASS (WARN-capable, never a false-FAIL) — driven by detect.GPUBusyPercentForTest against a temp drmRoot"
  artifacts:
    - path: "internal/inference/backend_rocm.go"
      provides: "backendROCm struct, rocmImage digest-pinned const, ContainerArgs delta, ResidencyProof() ROCm0 markers"
      contains: "ROCm0"
    - path: "internal/inference/backend.go"
      provides: "BackendFor(name) (Backend, error) resolver — the single polymorphism point"
      contains: "func BackendFor"
    - path: "internal/inference/backend_rocm_test.go"
      provides: "TestROCmContainerArgs, TestROCmImageDigestPinned, TestBackendFor"
    - path: "internal/inference/seam_test.go"
      provides: "TestROCmMarkerPresence positive grep-gate + verified negative seam coverage of the rocm image token"
    - path: "internal/inference/testdata/load_tensors_rocm.txt"
      provides: "ROCm0 N/N PASS fixture (with a ROCm_Host line to lock Pitfall 2)"
    - path: "internal/inference/running_offload_test.go"
      provides: "ROCm residency table cases incl. the gpu_busy_percent signal (D-06): Known non-zero corroborate-PASS + Unknown neutral-for-PASS via detect.GPUBusyPercentForTest"
      contains: "GPUBusyPercentForTest"
  key_links:
    - from: "internal/inference/backend.go"
      to: "backendROCm{}"
      via: "BackendFor(\"rocm\") returns backendROCm{}"
      pattern: "case \"rocm\""
    - from: "internal/inference/backend_rocm.go"
      to: "rocmImage digest"
      via: "Image() returns the @sha256-pinned rocm-7.2.4 const"
      pattern: "@sha256:2da150c1f025"
    - from: "internal/inference/running_offload_test.go"
      to: "detect.GPUBusyPercentForTest (gpu_busy_percent fixture)"
      via: "ROCm busy table cases inject a temp drmRoot gpu_busy_percent file (PASS-corroborate + Unknown neutral)"
      pattern: "GPUBusyPercentForTest"
---

<objective>
Add the ROCm backend and the polymorphic resolver: `backend_rocm.go`
(`backendROCm` — digest-pinned `rocm-7.2.4` image, `/dev/kfd`+`/dev/dri` devices,
`render` group, ordered `HSA_OVERRIDE_GFX_VERSION` then `ROCBLAS_USE_HIPBLASLT`
env, shared mandatory flags, ROCm0 ResidencyProof markers), `BackendFor(name)
(Backend, error)` (D-01/D-02, fail-closed), the positive ROCm-marker grep-gate
(D-10), and the ROCm fixtures + table cases exercising the descriptor-driven
scrapers from Plan 01 — INCLUDING the gpu_busy_percent residency signal (D-06):
fixtures that corroborate the ROCm0 N/N PASS with a Known non-zero busy reading
and prove an Unknown/absent busy reading is neutral-for-PASS (D-07/Q2).

Purpose: Plan 01 built the backend-neutral engine + the busy-signal fold; this plan
adds the second backend, the one place that maps a config string to it, and the
ROCm fixtures that prove the residency verdict (the 4-signal D-06 assert) end to
end off-hardware. Backend literals stay behind the seam (SC#4); the resolver fails
closed on unknown input. Vulkan stays the default — the v1.0 suite is still green.

Output: `backend_rocm.go`, `backend.go` (BackendFor), `backend_rocm_test.go`,
`TestROCmMarkerPresence`, 5 ROCm testdata fixtures, ROCm cases added to the
offload/running tests including the gpu_busy_percent signal cases (Vulkan cases
unchanged).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-rocm-backend-resolver-spine/06-CONTEXT.md
@.planning/phases/06-rocm-backend-resolver-spine/06-RESEARCH.md
@.planning/phases/06-rocm-backend-resolver-spine/06-PATTERNS.md
@.planning/phases/06-rocm-backend-resolver-spine/06-VALIDATION.md
@.planning/phases/06-rocm-backend-resolver-spine/06-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create backend_rocm.go (image, ContainerArgs delta, ResidencyProof ROCm markers)</name>
  <files>internal/inference/backend_rocm.go</files>
  <read_first>
    - internal/inference/backend_vulkan.go (the sibling exemplar — digest-pin comment 15-19, struct/assertion/ctor 55-69, ContainerArgs 81-103; shared consts hostPublishAddr/serverPort/containerModelsDir/llamaServerFlags/endpointURL are backend-neutral, reuse verbatim)
    - internal/inference/inference.go (ResidencyMarkers struct + Backend interface as extended in Plan 01)
    - .planning/phases/06-rocm-backend-resolver-spine/06-RESEARCH.md (§"ROCm ContainerArgs delta (D-09)" + §Standard Stack — the resolved digest)
    - .planning/phases/06-rocm-backend-resolver-spine/06-PATTERNS.md (§backend_rocm.go — full delta + the A3 render/video note)
  </read_first>
  <action>
    Mirror backend_vulkan.go. Define `const rocmImage =
    "docker.io/kyuz0/amd-strix-halo-toolboxes:rocm-7.2.4@sha256:2da150c1f0252f383b0b400f6cfa6630d3d34cf7c57132fe8445393b40531a89"`
    (NEVER rocm7-nightlies — 64 GB cap, D-08) with a provenance comment mirroring the
    Vulkan one: record skopeo resolve date 2026-06-05 + the RESEARCH legitimacy note.
    Define `type backendROCm struct{}`, `var _ Backend = backendROCm{}`,
    `func (backendROCm) Name() string { return "rocm" }`, `Image()` returns rocmImage.
    No exported ROCmBackend() ctor needed (BackendFor is the entry point). Implement
    `ContainerArgs(spec RunSpec)` as a delta over Vulkan (D-09): add `"--device",
    "/dev/kfd"` AND keep `"--device", "/dev/dri"`; add `"--group-add", "render"` in
    addition to the existing `"--group-add", "keep-groups"`; add ORDERED env
    `"--env", "HSA_OVERRIDE_GFX_VERSION=11.5.1"` then `"--env", "ROCBLAS_USE_HIPBLASLT=1"`;
    swap vulkanImage→rocmImage; reuse the shared consts and the `args = append(args,
    llamaServerFlags...)` line (-ngl 999 -fa 1 --no-mmap). Add a code comment noting
    A3 (render group per STACK.md; PITFALLS.md mentions render+video — byte-golden is
    Phase 7). Implement `func (backendROCm) ResidencyProof() ResidencyMarkers`
    returning DeviceToken "ROCm0", DeviceLabel "- ROCm", StartLogDevicePrefix
    "ggml_cuda_init:" (HIP reuses the cuda-init prefix; A2 MEDIUM-confidence),
    FaultString "Memory access fault by GPU node", RejectSoftwareRenderer false.
  </action>
  <verify>
    <automated>go build ./internal/inference/</automated>
  </verify>
  <acceptance_criteria>
    - `go build ./internal/inference/` compiles.
    - `var _ Backend = backendROCm{}` compile-time assertion is present and passes (proves the interface incl. ResidencyProof is satisfied).
    - backend_rocm.go contains the literal `@sha256:2da150c1f0252f383b0b400f6cfa6630d3d34cf7c57132fe8445393b40531a89`.
    - backend_rocm.go contains "ROCm0", "HSA_OVERRIDE_GFX_VERSION", "ROCBLAS_USE_HIPBLASLT=1", "/dev/kfd", "/dev/dri", "render".
    - `grep -c 'rocm7-nightlies' internal/inference/backend_rocm.go` returns 0.
  </acceptance_criteria>
  <done>backend_rocm.go exists, satisfies the Backend interface (incl. ResidencyProof), pins the resolved rocm-7.2.4 digest, and assembles the kfd+dri/render/ordered-env ContainerArgs delta behind the seam.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add BackendFor resolver + ROCm backend tests (ContainerArgs, digest, resolver)</name>
  <files>internal/inference/backend.go, internal/inference/backend_rocm_test.go</files>
  <read_first>
    - internal/inference/backend_vulkan.go (VulkanBackend() ctor shape 61-63)
    - internal/inference/inference_test.go (TestContainerArgsCarryMandatoryFlags 66-79; the digest-pin guard 76-77 — Contains-style, NOT a byte-golden)
    - internal/inference/backend_rocm.go (from Task 1)
    - .planning/phases/06-rocm-backend-resolver-spine/06-RESEARCH.md (§Pattern 2 — BackendFor body + the format-guard recommendation)
  </read_first>
  <action>
    Create backend.go in internal/inference with `func BackendFor(name string) (Backend,
    error)` (D-01): `case "", "vulkan"` → `backendVulkan{}, nil`; `case "rocm"` →
    `backendROCm{}, nil`; `default` → an actionable error (D-02; exact wording is
    Claude's Discretion, e.g. `fmt.Errorf("unknown inference backend %q: set backend =
    \"vulkan\" (default) or \"rocm\" in config.toml", name)`). NEVER a silent Vulkan
    fallback on unknown. Create backend_rocm_test.go with three tests: `TestROCmContainerArgs`
    — build `BackendFor("rocm")` (or backendROCm{}), call ContainerArgs with a RunSpec,
    `strings.Join(args, " ")`, assert Contains `--device /dev/kfd`, `--device /dev/dri`,
    `--group-add render`, `HSA_OVERRIDE_GFX_VERSION=11.5.1`, `ROCBLAS_USE_HIPBLASLT=1`,
    `-ngl 999`, `-fa 1`, `--no-mmap` (Contains, not a byte-golden — that's Phase 7).
    `TestROCmImageDigestPinned` — assert Image() contains `@sha256:` and a 64-hex-char
    digest. `TestBackendFor` — `""` and `"vulkan"` return a backend with Name()=="vulkan";
    `"rocm"` returns Name()=="rocm"; an unknown value returns a non-nil error whose
    message names the bad value (assert err != nil and the returned Backend is nil).
  </action>
  <verify>
    <automated>go test ./internal/inference/ -run 'TestBackendFor|TestROCmContainerArgs|TestROCmImageDigestPinned'</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/inference/ -run 'TestBackendFor|TestROCmContainerArgs|TestROCmImageDigestPinned'` passes.
    - `grep -c 'func BackendFor' internal/inference/backend.go` returns 1.
    - `TestBackendFor` asserts unknown → (nil Backend, non-nil error) and `""`/`"vulkan"` → vulkan, `"rocm"` → rocm.
    - `TestROCmImageDigestPinned` asserts `@sha256:` + 64 hex chars (passes now — real digest).
    - `TestROCmContainerArgs` asserts all of: /dev/kfd, /dev/dri, group-add render, HSA_OVERRIDE_GFX_VERSION=11.5.1, ROCBLAS_USE_HIPBLASLT=1, -ngl 999, -fa 1, --no-mmap.
  </acceptance_criteria>
  <done>BackendFor resolves "", vulkan, rocm and fails closed on unknown; the three ROCm backend tests are green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: ROCm fixtures + scrape table cases (incl. gpu_busy_percent signal, D-06) + positive marker grep-gate</name>
  <files>internal/inference/testdata/load_tensors_rocm.txt, internal/inference/testdata/load_tensors_rocm_cpu.txt, internal/inference/testdata/load_tensors_rocm_fault.txt, internal/inference/testdata/rocm_devinfo_pass.stderr, internal/inference/testdata/rocm_offloaded_partial.stderr, internal/inference/offload_test.go, internal/inference/running_offload_test.go, internal/inference/seam_test.go</files>
  <read_first>
    - internal/inference/testdata/load_tensors_vulkan.txt, load_tensors_cpu.txt, radv_devinfo_pass.stderr, offloaded_zero.stderr (fixture shapes to mirror)
    - internal/inference/running_offload_test.go + offload_test.go (table-test + readFixture + GTTUsedBytesForTest patterns; Markers + GPUBusyPercent now supplied per Plan 01)
    - internal/detect/gpu_amd.go (GPUBusyPercentForTest(drmRoot) at ~357-396 — reads a gpu_busy_percent file under a temp drmRoot, returns detect.Int; mirror the mem_info_gtt_used fixture pattern — NO new code in detect)
    - internal/inference/seam_test.go (TestSeamGrepGate 34-84; the container-image-literal regex at 39 already binds kyuz0|docker.io/)
    - .planning/phases/06-rocm-backend-resolver-spine/06-RESEARCH.md (§"ROCm residency PASS fixture" + Pitfalls 2/3/4 — exact line shapes; §Pattern 3 — busy input wired now, live read Phase 8; §Open Questions Q2)
    - .planning/phases/06-rocm-backend-resolver-spine/06-PATTERNS.md (§testdata fixtures table — what each fixture must assert)
  </read_first>
  <action>
    Create the 5 fixtures with exact line shapes from RESEARCH §"ROCm residency PASS
    fixture": `load_tensors_rocm.txt` — `ggml_cuda_init: found 1 ROCm devices`,
    `offloaded 65/65 layers to GPU`, a `ROCm0 model buffer size = N MiB` (N>0) line, AND
    a `ROCm_Host model buffer size` line (locks Pitfall 2 — ROCm0 must not match
    ROCm_Host). `load_tensors_rocm_cpu.txt` — only a `CPU model buffer size` line, no
    ROCm0 (FAIL). `load_tensors_rocm_fault.txt` — a `Memory access fault by GPU node`
    line present even alongside a partial ROCm0 line (fault dominates → FAIL, Pitfall 4).
    `rocm_devinfo_pass.stderr` — `ggml_cuda_init: found 1 ROCm devices` + a `- ROCm`
    device-info line + `offloaded 65/65` (start-time PASS). `rocm_offloaded_partial.stderr`
    — `offloaded 1/65 layers to GPU` (N<M → FAIL, Pitfall 3).

    Add ROCm table cases to running_offload_test.go (ROCm0 N/N PASS, CPU FAIL, fault FAIL,
    empty WARN), driving each with `backendROCm{}.ResidencyProof()` markers; leave EVERY
    existing Vulkan case unchanged. ADD the gpu_busy_percent signal cases (D-06): for the
    ROCm0 N/N PASS case, also supply a `GPUBusyPercent` reading via
    `detect.GPUBusyPercentForTest(t.TempDir-with-a-gpu_busy_percent-file)` — write a temp
    drmRoot containing a `gpu_busy_percent` file with a non-zero value (e.g. "37"), read it
    through `GPUBusyPercentForTest`, set it on `RunningOffloadInput.GPUBusyPercent`, and
    assert the verdict stays PASS (the busy signal CORROBORATES residency). Add a second
    case proving an Unknown/absent busy reading (zero-value detect.Int, or a temp drmRoot
    with NO gpu_busy_percent file) is NEUTRAL-for-PASS: the ROCm0 N/N residency PASS stays
    PASS (WARN-capable, never a false-FAIL — D-07/Q2). (The Known-zero→FAIL rule is unit-
    proven in Plan 01 Task 2; the live decode-time FAIL is deferred to Phase 8 — do NOT
    add a live-decode fixture here.)

    Add ROCm cases to offload_test.go (device+N/N PASS, partial 1/65 FAIL, CPU FAIL),
    driving each with `backendROCm{}.ResidencyProof()` markers; leave every existing Vulkan
    case unchanged. (The start-time path takes no busy signal — busy is running-path only.)

    In seam_test.go add `TestROCmMarkerPresence`: read backend_rocm.go, assert it contains
    each of "ROCm0", "HSA_OVERRIDE_GFX_VERSION", "/dev/kfd" (D-10; gate on ROCm0 NOT
    ggml_cuda — shared with CUDA path). VERIFY (don't duplicate) that the existing negative
    seam-gate regex covers the rocm image token (kyuz0|docker.io/ already match); optionally
    add `rocm-7.2.4|rocm7-nightlies` for explicit intent.
  </action>
  <verify>
    <automated>go test ./internal/inference/</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/inference/` is fully green (ROCm cases + all existing Vulkan cases).
    - `go test ./internal/inference/ -run TestROCmMarkerPresence` passes; deleting "ROCm0" from backend_rocm.go would make it fail (the gate is real).
    - `go test ./internal/inference/ -run TestSeamGrepGate` passes and its image-literal pattern matches the rocm image token (verified, not duplicated).
    - All 5 new testdata files exist under internal/inference/testdata/.
    - The ROCm fixtures exercise: ROCm0 N/N → PASS, CPU-only → FAIL, fault → FAIL, empty → WARN (running); device+N/N → PASS, partial 1/65 → FAIL, CPU → FAIL (start-time).
    - A running-path case feeds a Known non-zero `GPUBusyPercent` (via `detect.GPUBusyPercentForTest` against a temp drmRoot gpu_busy_percent file) and asserts the ROCm0 N/N verdict stays PASS (busy corroborates); a second case feeds an Unknown/absent busy and asserts the PASS is unchanged (neutral, never false-FAIL).
    - `grep -c 'GPUBusyPercentForTest' internal/inference/running_offload_test.go` >= 1 (the existing reader is used; no new detect code).
    - load_tensors_rocm.txt contains BOTH a `ROCm0` line and a `ROCm_Host` line (Pitfall 2 lock).
  </acceptance_criteria>
  <done>All 5 ROCm fixtures exist; both scrapers have ROCm table cases driven by the ROCm descriptor; the running verdict's gpu_busy_percent signal is exercised (Known non-zero corroborates PASS, Unknown neutral-for-PASS) via the existing detect.GPUBusyPercentForTest reader; the positive marker grep-gate passes and the negative seam gate is verified to cover the rocm image token; whole inference package green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config string → BackendFor | Untrusted/hand-edited `backend` value selects a privileged device path; an unknown value must not silently reach a privileged backend. |
| backend literals → callers | `/dev/kfd`, image digest, HSA env must stay seam-bound (one auditable file). |
| sysfs gpu_busy_percent → ROCm verdict | The ROCm residency proof's busy corroborator must never false-FAIL a residency-proven PASS on an idle/unavailable reading. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-6-03 | Spoofing/Tampering | BackendFor (D-02) | mitigate | Fail-closed: unknown backend → actionable error, never a silent Vulkan/privileged fallback. TestBackendFor asserts (nil, non-nil err) on unknown input. |
| T-6-04 | Tampering / Supply Chain | rocmImage const (V10) | mitigate | Digest-pinned `@sha256:2da150c1f025…` (never the bare `:rocm-7.2.4` tag; never rocm7-nightlies). TestROCmImageDigestPinned enforces a 64-hex pin; same provenance as the audited Vulkan image (RESEARCH §Package Legitimacy Audit). |
| T-6-05 | Information Disclosure / EoP | `/dev/kfd` device token (V12) | accept (this phase) | The token is ENCODED in ContainerArgs only — no unit is rendered or run in Phase 6. Live kfd exposure + SELinux narrowing forwarded to Phase 7 (render) / Phase 8 (run). Seam discipline + TestROCmMarkerPresence keep the privilege literal in one auditable file (V1). |
| T-6-06 | Tampering | seam leak of rocm literals (V1) | mitigate | Negative seam gate (verified to cover the rocm image token) + positive TestROCmMarkerPresence — a refactor that drops or leaks the markers fails CI. |
| T-6-09 | Tampering | ROCm gpu_busy_percent residency signal (D-06) | mitigate | ROCm fixtures prove the busy fold off-hardware: a Known non-zero reading (via GPUBusyPercentForTest) corroborates the ROCm0 N/N PASS; an Unknown/absent reading is neutral-for-PASS (WARN-capable, never a false-FAIL — D-07/Q2). The Known-zero→FAIL rule is unit-proven in Plan 01; the live decode-time read is the Phase-8 follow-on. |
| T-6-SC | Tampering | npm/pip/cargo installs | mitigate | No package-manager installs this plan (container image only, digest-pinned; RESEARCH §Package Legitimacy Audit: no Go packages added, slopcheck N/A to images). No checkpoint required. |
</threat_model>

<verification>
- `go vet ./internal/inference/ && go test ./internal/inference/` is green.
- `BackendFor("rocm").Image()` is digest-pinned; unknown input fails closed.
- The positive ROCm-marker gate and the negative seam gate both pass; backend literals are seam-bound.
- The ROCm running verdict exercises the gpu_busy_percent signal (corroborate-PASS + neutral-Unknown) via detect.GPUBusyPercentForTest.
- All Vulkan cases remain unchanged (regression from Plan 01 still green).
</verification>

<success_criteria>
- `backend_rocm.go` produces a ROCm backend (digest-pinned rocm-7.2.4, kfd+dri, render group, ordered HSA/HIPBLASLT env) behind the interface (ROCM-01).
- `BackendFor` is the single resolver, fail-closed on unknown (ROCM-01/04).
- The ROCm offload-assert distinguishes real offload from CPU fallback/fault/unevaluable via the descriptor (ROCM-02).
- The ROCm running verdict exercises the gpu_busy_percent residency signal (D-06): Known non-zero corroborates PASS, Unknown neutral-for-PASS (ROCM-02 / SC#2).
- A grep-gate fails if the ROCm0/HSA/kfd markers are dropped (ROCM-02 / SC#3).
- The whole `internal/inference` package builds and tests green with Vulkan unchanged.
</success_criteria>

<output>
Create `.planning/phases/06-rocm-backend-resolver-spine/06-02-SUMMARY.md` when done.
</output>
