---
phase: 24-coder-fit-math-catalog-on-hardware-model-qualification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/catalog/catalog.go
  - internal/catalog/seed.json
  - internal/catalog/load.go
  - internal/catalog/catalog_test.go
  - internal/catalog/testdata/schema3-external.json
autonomous: true
requirements: [CODER-01]

must_haves:
  truths:
    - "villa's embedded catalog contains three role:\"coder\" entries (qwen3-coder-30b-a3b, qwen3-coder-next-q4, qwen3-coder-next-q3) with revision-pinned shard URLs and template provenance"
    - "Existing 4 chat entries in seed.json are byte-untouched except the top-level schema_version/catalog_version bumps"
    - "An external schema-2 catalog file warns and falls back to the embedded seed; an external schema-3 catalog with coder fields round-trips through DisallowUnknownFields decode"
    - "Absent role decodes as chat; absent cache_reuse_safe decodes as false (fail-closed)"
  artifacts:
    - path: "internal/catalog/catalog.go"
      provides: "SupportedSchema = 3; CatalogModel fields Role, AgentCtx, CacheReuseSafe, AgentSampling, TemplateProvenance; AgentSampling struct"
      contains: "SupportedSchema = 3"
    - path: "internal/catalog/seed.json"
      provides: "schema_version 3 seed with 3 coder entries (verified weights/sha256/revisions)"
      contains: "\"role\": \"coder\""
    - path: "internal/catalog/catalog_test.go"
      provides: "schema-3 pin test, coder-entry seed tests, fail-closed default tests, external fallback/round-trip tests"
  key_links:
    - from: "internal/catalog/seed.json"
      to: "internal/catalog/catalog.go"
      via: "json decode with DisallowUnknownFields — struct fields + seed bump land in ONE commit (Pitfall 5)"
      pattern: "agent_ctx"
    - from: "internal/catalog/load.go"
      to: "SupportedSchema"
      via: "exact-match schema window (ext.SchemaVersion != SupportedSchema → warn + embedded seed)"
      pattern: "SupportedSchema"
---

<objective>
Catalog schema 2→3 (CODER-01): extend `CatalogModel` with the five append-only optional coder fields per D-01, ship three `role:"coder"` seed entries with revision-pinned GGUF artifacts and template provenance per D-02, and keep the chat-entry surface bit-for-bit untouched per D-03 — with the loader's exact-match schema window bumped and the whole change landing in one commit (DisallowUnknownFields trap).

Purpose: Phase 24's fit math (plan 24-02) and on-hardware qualification (plan 24-03) both consume these entries; Phase 25 renders `agent_ctx`/`cache_reuse_safe`/sampling from them.
Output: schema-3 catalog core + seed + tests, `make check` green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/24-coder-fit-math-catalog-on-hardware-model-qualification/24-CONTEXT.md
@.planning/phases/24-coder-fit-math-catalog-on-hardware-model-qualification/24-PATTERNS.md
@.planning/phases/24-coder-fit-math-catalog-on-hardware-model-qualification/24-RESEARCH.md
</context>

## Artifacts this phase produces (plan 24-01 share)

- `internal/catalog/catalog.go`: const `SupportedSchema = 3`; new `CatalogModel` fields `Role string` (json `role,omitempty`), `AgentCtx int` (json `agent_ctx,omitempty`), `CacheReuseSafe bool` (json `cache_reuse_safe,omitempty`), `AgentSampling AgentSampling` (json `agent_sampling,omitempty`... see Task 1 for pointer-vs-value decision), `TemplateProvenance string` (json `template_provenance,omitempty`); new exported struct `AgentSampling` with `Temperature float64` (json `temperature`), `TopP float64` (json `top_p`), `TopK int` (json `top_k`), `RepeatPenalty float64` (json `repeat_penalty`).
- `internal/catalog/seed.json`: `schema_version: 3`, bumped `catalog_version`, three new entry ids: `qwen3-coder-30b-a3b`, `qwen3-coder-next-q4`, `qwen3-coder-next-q3`.
- `internal/catalog/load.go`: coder-entry validation on the external-catalog path (refuse `role:"coder"` entries with `agent_ctx <= 0` or out-of-range sampling — refuse, never clamp).
- `internal/catalog/testdata/schema3-external.json`: external schema-3 round-trip fixture.
- New tests: `TestLoadSeedCoderEntries`, fail-closed default tests, external schema-2-falls-back / schema-3-round-trip tests.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CatalogModel schema-3 fields + SupportedSchema=3 + seed.json coder entries (one commit)</name>
  <files>internal/catalog/catalog.go, internal/catalog/seed.json, internal/catalog/catalog_test.go</files>
  <read_first>
    - internal/catalog/catalog.go (current CatalogModel shape, SupportedSchema doc-comment style, Shard struct convention, UnifiedMemorySafe/Bootstrap gating-comment style)
    - internal/catalog/seed.json (existing entry key order — coder entries copy it, inserting new keys after "bootstrap" and before "shards")
    - internal/catalog/catalog_test.go (TestLoadSeedDownloadMetadata lines ~52-83 — hard-pins SupportedSchema=2 at ~lines 57-58, MUST flip to 3 in this task; TestLoadSeedVerifiedDims lines ~88-109 map-walk pattern)
    - .planning/phases/24-coder-fit-math-catalog-on-hardware-model-qualification/24-PATTERNS.md (§internal/catalog sections — exact analog excerpts)
  </read_first>
  <behavior>
    - Test 1 (TestLoadSeedCoderEntries): Load("") succeeds; SupportedSchema == 3; exactly 3 entries have Role == "coder" with ids qwen3-coder-30b-a3b / qwen3-coder-next-q4 / qwen3-coder-next-q3; every coder entry has AgentCtx > 0, non-empty TemplateProvenance containing an "@" revision pin, and exactly one shard whose URL contains "/resolve/" followed by a 40-hex revision (never "/resolve/main/").
    - Test 2 (fail-closed defaults): a CatalogModel decoded from JSON WITHOUT role/cache_reuse_safe keys yields Role == "" and CacheReuseSafe == false (D-01 absent ⇒ chat / false).
    - Test 3 (chat entries untouched, D-03): all pre-existing entry ids still present with Role == "" and no AgentCtx.
    - Test 4 (per-entry expected values, TestLoadSeedVerifiedDims pattern): map-walk asserting per coder entry the exact weight_bytes / n_layers / n_kv_heads / head_dim / agent_ctx / tier_gb values from the table in action.
  </behavior>
  <action>
    Implement catalog schema 2→3 per D-01/D-02/D-03, all in this single task so struct + constant + seed + test-pin land together (RESEARCH Pitfall 5: DisallowUnknownFields rejects seed keys the struct does not yet have).

    catalog.go:
    - Bump `SupportedSchema` 2→3 and append a v3 paragraph to its doc comment in the existing v2-paragraph style, citing Phase 24 / D-01: v3 adds the optional coder-role fields (role, agent_ctx, cache_reuse_safe, agent_sampling, template_provenance).
    - Add to `CatalogModel`, each with a doc comment citing its decision ID (the Shards/UnifiedMemorySafe comment style): `Role string` json:"role,omitempty" (absent/empty ⇒ chat, D-01/D-03); `AgentCtx int` json:"agent_ctx,omitempty" (the agent-profile context the coder fit is computed at AND Phase 25 renders into --ctx-size, D-01/D-04); `CacheReuseSafe bool` json:"cache_reuse_safe,omitempty" (Go zero false = fail-closed, D-01; licensed only by the on-hardware probe, D-09); `AgentSampling *AgentSampling` json:"agent_sampling,omitempty" (pointer so absent stays absent on re-encode; if existing struct conventions prefer value types, a value struct is acceptable provided chat entries still emit no key); `TemplateProvenance string` json:"template_provenance,omitempty" (HF repo@revision + template origin, D-01/D-02).
    - New exported struct `AgentSampling` (the Shard nested-struct convention): `Temperature float64` json:"temperature", `TopP float64` json:"top_p", `TopK int` json:"top_k", `RepeatPenalty float64` json:"repeat_penalty".
    - Do NOT replicate the stale "NOTE (Assumption A2)" placeholder caveat for coder entries — their values are verified (24-PATTERNS.md note).

    seed.json (per D-02 every coder shard URL is `https://huggingface.co/{repo}/resolve/{revision}/{filename}` — never resolve/main; existing 4 chat entries gain NO keys; bump top-level "schema_version" to 3 and bump "catalog_version"):
    - Entry `qwen3-coder-30b-a3b` (all tiers): display_name "Qwen3-Coder-30B-A3B-Instruct (coder, all tiers)", quant UD-Q4_K_XL, weight_bytes 17665334432, n_layers 48, n_kv_heads 4, head_dim 128, kv_bytes_per_elem 2, default_ctx 65536, min_envelope_bytes 28000000000, tier_gb 64, unified_memory_safe true, backend_default vulkan, bootstrap false, role coder, agent_ctx 65536, cache_reuse_safe true (EXPECTED value — plan 24-03's probe licenses it; plan 24-04 reconciles), agent_sampling {temperature 0.7, top_p 0.8, top_k 20, repeat_penalty 1.05}, template_provenance "unsloth/Qwen3-Coder-30B-A3B-Instruct-GGUF@b17cb02dd882d5b6ab62fc777ad2995f19668350 (embedded GGUF chat template)", one shard: url https://huggingface.co/unsloth/Qwen3-Coder-30B-A3B-Instruct-GGUF/resolve/b17cb02dd882d5b6ab62fc777ad2995f19668350/Qwen3-Coder-30B-A3B-Instruct-UD-Q4_K_XL.gguf, filename Qwen3-Coder-30B-A3B-Instruct-UD-Q4_K_XL.gguf, sha256 2841aa314d916434860cfb8990347528dcdfe5c350dbcb9d1461dbee88ff2533, size_bytes 17665334432.
    - Entry `qwen3-coder-next-q4` (128 GB tier): quant UD-Q4_K_XL, weight_bytes 49608478720, n_layers 12 (FULL-ATTENTION layer count of the hybrid Qwen3NextForCausalLM — 48 ÷ full_attention_interval 4; RESEARCH Pattern 3 — note this rationale in display_name so nobody "fixes" 12 back to 48), n_kv_heads 2, head_dim 256, kv_bytes_per_elem 2, default_ctx 131072, min_envelope_bytes 60000000000, tier_gb 128, role coder, agent_ctx 131072, cache_reuse_safe ABSENT (fail-closed false — hybrid model, D-01), same agent_sampling preset (A4 — checked during qualification), template_provenance "unsloth/Qwen3-Coder-Next-GGUF@ce09c67b53bc8739eef83fe67b2f5d293c270632 (embedded GGUF chat template)", shard url https://huggingface.co/unsloth/Qwen3-Coder-Next-GGUF/resolve/ce09c67b53bc8739eef83fe67b2f5d293c270632/Qwen3-Coder-Next-UD-Q4_K_XL.gguf, sha256 4bb93f0a0221ef4ff963ca9094df629c8dfdfabc3b4fdd85c1a2e4c0624fce36, size_bytes 49608478720.
    - Entry `qwen3-coder-next-q3` (96 GB tier): same repo/revision/provenance, quant UD-Q3_K_XL, weight_bytes 36282685440, n_layers 12, n_kv_heads 2, head_dim 256, default_ctx 131072, min_envelope_bytes 45000000000, tier_gb 96, role coder, agent_ctx 131072, cache_reuse_safe ABSENT, same sampling, shard filename Qwen3-Coder-Next-UD-Q3_K_XL.gguf, sha256 91b928f28a2f4b76a0d9147f148311bfe8716ac8e495acc33eb7aace0ad76135, size_bytes 36282685440.

    catalog_test.go: write the four behavior tests FIRST (tdd), then update the existing `SupportedSchema = 2` pin in TestLoadSeedDownloadMetadata to 3 in this same commit.
  </action>
  <verify>
    <automated>go test ./internal/catalog/... -run 'Load|Schema|Coder|Seed' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "SupportedSchema = 3" internal/catalog/catalog.go` matches exactly one line.
    - `python3 -c "import json;c=json.load(open('internal/catalog/seed.json'));coders=[m for m in c['models'] if m.get('role')=='coder'];assert c['schema_version']==3;assert len(coders)==3;assert all('/resolve/main/' not in s['url'] for m in coders for s in m['shards']);assert {m['id'] for m in coders}=={'qwen3-coder-30b-a3b','qwen3-coder-next-q4','qwen3-coder-next-q3'};assert [m for m in coders if m['id']=='qwen3-coder-next-q4'][0]['n_layers']==12;print('OK')"` prints OK.
    - `git diff` over seed.json shows the existing chat entries unchanged except the two top-level version keys (D-03).
    - `go test ./internal/catalog/...` green, including the flipped schema pin.
  </acceptance_criteria>
  <done>Schema-3 struct + constant + seed + tests land together; 3 coder entries with revision-pinned shards and verified values; chat entries byte-untouched; catalog tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: External-catalog window fixtures + coder-entry validation (fail-closed, refuse-never-clamp)</name>
  <files>internal/catalog/load.go, internal/catalog/catalog_test.go, internal/catalog/testdata/schema3-external.json</files>
  <read_first>
    - internal/catalog/load.go (exact-match schema window ~lines 35-58; loadExternal DisallowUnknownFields ~lines 97-101; 1 MiB bounded reader; existing warning string formats)
    - internal/catalog/catalog_test.go (TestLoadVersionMismatchFallsBack ~lines 162-189 and its testdata/too-old-catalog.json fixture pattern)
    - internal/catalog/testdata/ (existing fixture files — reuse naming style)
  </read_first>
  <behavior>
    - Test 1 (schema-2 external falls back): an external catalog file with schema_version 2 produces a schema-mismatch warning and the embedded seed is returned (the v1→v2 precedent now exercised for 2-vs-3).
    - Test 2 (schema-3 external round-trip): an external schema-3 file containing a coder entry with all five new keys decodes successfully under DisallowUnknownFields and Load returns it (Pitfall 5 regression guard).
    - Test 3 (validation refuses, never clamps): an external schema-3 catalog containing a role:"coder" entry with agent_ctx 0 (and a variant with temperature outside (0,2] or top_p outside (0,1] or top_k < 0 or repeat_penalty outside (0,3]) is rejected with a warning naming the offending entry id and falls back to the embedded seed — the value is NEVER silently clamped (ASVS V5 control from 24-RESEARCH §Security Domain).
  </behavior>
  <action>
    Add `internal/catalog/testdata/schema3-external.json` (a minimal valid schema-3 catalog with one coder entry exercising every new key) and adjust/extend fixtures so the schema-2-falls-back branch is covered against the new constant.

    In load.go, extend the external-catalog path (loadExternal or its caller — wherever decode succeeds and before the catalog is returned) with a coder-entry validation pass per the existing warn-and-fall-back style: for each entry with Role == "coder", refuse the whole external catalog (warning + embedded-seed fallback, the exact mechanism the schema-mismatch branch uses) when agent_ctx <= 0 or agent_sampling values are out of range (temperature in (0,2], top_p in (0,1], top_k >= 0, repeat_penalty in (0,3] when an agent_sampling block is present). Refuse with a warning naming the entry id — never clamp, never partially accept (fail-closed discipline, D-01). The embedded seed is exempt (it is guarded by Task 1's tests, not runtime validation).

    Keep the 1 MiB bounded reader and DisallowUnknownFields untouched (existing DoS/input controls, T-24-03).
  </action>
  <verify>
    <automated>go test ./internal/catalog/... -count=1 && make check</automated>
  </verify>
  <acceptance_criteria>
    - `internal/catalog/testdata/schema3-external.json` exists with `"schema_version": 3` and one `"role": "coder"` entry.
    - Test run shows the schema-2-external case producing a fallback warning (assert via the returned warnings slice, the TestLoadVersionMismatchFallsBack pattern).
    - A coder entry with `"agent_ctx": 0` in an external catalog causes fallback-to-seed with a warning containing the entry id; no clamping code path exists (`grep -n "clamp" internal/catalog/load.go` returns no matches).
    - `make check` green (vet + full test suite — proves no other package broke on the schema bump).
  </acceptance_criteria>
  <done>External schema window proven for 2-vs-3 both directions; untrusted coder entries validated refuse-with-warning; full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| external catalog file → `catalog.Load` | hand-edited/user-supplied JSON crosses into the control plane |
| HF GGUF artifacts → catalog claims | seed.json asserts integrity facts (sha256/size/revision) later enforced by `internal/download` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-01 | Tampering | HF repo re-upload / template swap under a quant | mitigate | repo+revision-pinned shard URLs (`resolve/{40-hex}` never `resolve/main`) + per-shard sha256/size (D-02); Task 1 test asserts the URL form |
| T-24-02 | Tampering / Input validation | external catalog coder entries (`agent_ctx`, sampling) | mitigate | Task 2: refuse-with-warning + embedded-seed fallback, never clamp (ASVS V5); DisallowUnknownFields retained |
| T-24-03 | DoS | oversized external catalog | accept (existing control) | 1 MiB bounded reader unchanged |
| T-24-SC | Tampering | package installs | accept (n/a) | zero new Go module deps this plan (RESEARCH Package Legitimacy Audit: no packages installed; no [ASSUMED]/[SUS]) |
</threat_model>

<verification>
- `go test ./internal/catalog/... -count=1` green.
- `make check` green (no Go-side podman/image/backend literals added — `TestSeamGrepGate` stays green by construction; this plan touches only catalog data + structs).
- seed.json python assertion from Task 1 acceptance criteria prints OK.
</verification>

<success_criteria>
- Catalog schema 3 ships 3 coder entries with revision-pinned artifacts and template provenance (CODER-01, SC#1); chat entries untouched (D-03).
- Fail-closed defaults proven by tests (absent role ⇒ chat, absent cache_reuse_safe ⇒ false).
- External-catalog window + validation proven both directions.
</success_criteria>

<output>
Create `.planning/phases/24-coder-fit-math-catalog-on-hardware-model-qualification/24-01-SUMMARY.md` when done.
</output>
