---
phase: 24-coder-fit-math-catalog-on-hardware-model-qualification
plan: 04
type: execute
wave: 3
depends_on: ["24-02", "24-03"]
files_modified:
  - internal/catalog/seed.json
  - internal/catalog/catalog_test.go
  - .planning/phases/24-coder-fit-math-catalog-on-hardware-model-qualification/24-TOOLBOX-DECISION.md
  - .planning/phases/24-coder-fit-math-catalog-on-hardware-model-qualification/24-QUALIFICATION-EVIDENCE.md
autonomous: false
requirements: [CODER-01, CODER-03]

must_haves:
  truths:
    - "The frozen catalog reflects measured reality: cache_reuse_safe values match the probe evidence; agent_ctx/min_envelope values reconciled against measured KV/GTT; failed entries deleted or re-pinned, never shipped"
    - "The toolbox keep/re-pin decision is recorded as a numbered decision with on-box evidence (version proof, binary-string findings, functional agent-loop results) BEFORE the catalog freezes"
    - "The recommend golden (re-frozen once in plan 24-02) is NOT touched by reconciliation"
    - "make check is green over the final frozen catalog"
  artifacts:
    - path: ".planning/phases/24-coder-fit-math-catalog-on-hardware-model-qualification/24-TOOLBOX-DECISION.md"
      provides: "Numbered D-11 decision record (keep or digest-pinned re-pin) citing evidence files"
      contains: "Decision"
    - path: ".planning/phases/24-coder-fit-math-catalog-on-hardware-model-qualification/24-QUALIFICATION-EVIDENCE.md"
      provides: "Per-entry qualification summary table (verdict, measured KV vs computed, cache-reuse, disposition per D-10/D-12)"
    - path: "internal/catalog/seed.json"
      provides: "The frozen, measurement-reconciled schema-3 catalog"
  key_links:
    - from: ".planning/phases/24-coder-fit-math-catalog-on-hardware-model-qualification/qualification/*/verdict.md"
      to: "internal/catalog/seed.json"
      via: "reconciliation: cache_reuse_safe truth-up, delete/re-pin per verdict (D-10/D-12)"
      pattern: "cache_reuse"
    - from: "24-TOOLBOX-DECISION.md"
      to: "Phase 25 inference seam"
      via: "if re-pin: the digest change lands at internal/inference in Phase 25; this phase records the decision only"
      pattern: "sha256"
---

<objective>
Reconcile the catalog with measured reality and freeze it (CODER-03 SC#3-4 closure; CODER-01 final state): truth-up `cache_reuse_safe` from the probe evidence, apply D-10/D-12 dispositions (delete or re-pin any failed entry — never ship on hope), fold material measured-vs-computed deltas into the fit fields per D-09, and record the D-11 toolbox keep/re-pin decision with evidence — then get operator sign-off on the freeze.

Purpose: SC#4 demands the toolbox decision be recorded with evidence before the catalog freezes; Phase 25 consumes the frozen entries' agent_ctx/sampling/cache_reuse_safe and the decision record.
Output: final frozen schema-3 seed + decision record + evidence summary, full suite green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-coder-fit-math-catalog-on-hardware-model-qualification/24-03-SUMMARY.md
@.planning/phases/24-coder-fit-math-catalog-on-hardware-model-qualification/24-RESEARCH.md
</context>

## Artifacts this phase produces (plan 24-04 share)

- `24-TOOLBOX-DECISION.md` — the numbered D-11 decision record (expected verdict per research: KEEP digest sha256:9a74e555…/build 9496, contingent on the functional proofs; fallback candidate build 9579 digest-pinned if a parser/arch failure surfaced).
- `24-QUALIFICATION-EVIDENCE.md` — per-entry summary: verdict, measured KV vs computed, GTT footprint, cache-reuse result, D-10/D-12 disposition.
- Final reconciled `internal/catalog/seed.json` (+ updated expected-value tests).

<tasks>

<task type="auto">
  <name>Task 1: Reconcile measured values into the catalog (D-09/D-10/D-12 dispositions)</name>
  <files>internal/catalog/seed.json, internal/catalog/catalog_test.go</files>
  <read_first>
    - .planning/phases/24-coder-fit-math-catalog-on-hardware-model-qualification/qualification/qwen3-coder-30b-a3b/verdict.md
    - .planning/phases/24-coder-fit-math-catalog-on-hardware-model-qualification/qualification/qwen3-coder-next-q4/verdict.md
    - .planning/phases/24-coder-fit-math-catalog-on-hardware-model-qualification/qualification/qwen3-coder-next-q3/verdict.md
    - internal/catalog/seed.json (current coder entry values from plan 24-01)
    - internal/catalog/catalog_test.go (the per-entry expected-value map tests that must track any reconciled value)
  </read_first>
  <action>
    Apply the evidence to the catalog, per decision:

    - D-09 (measured reality): for each entry, compare measured KV at agent_ctx against the computed estimate. If the total measured footprint (weights+KV+constant state) exceeds the computed total materially (>5% of the computed total), fold the excess into that entry's `min_envelope_bytes` — NEVER into the ctx-proportional KV dims (RESEARCH Pattern 3: the DeltaNet recurrent constant belongs in the floor, not the formula). If measured KV is ~4x computed for a Next entry, the n_layers:12 encoding was wrong — treat as a FAIL requiring re-encoding plus a re-measure, not a silent edit.
    - cache_reuse_safe truth-up: set each entry's value to exactly the probe verdict (30B expected true, Next expected false — but the cache-reuse.txt evidence decides; D-11 input). Absent key encodes false (fail-closed encoding from 24-01).
    - D-10: any entry whose verdict.md is FAIL is DELETED from seed.json (or re-pinned to a different quant/revision ONLY if a concrete verified alternative artifact exists with sha256+size — never a placeholder). Record the disposition in the evidence summary (Task 2).
    - D-12: if either Next entry failed on arch/tool-call grounds, BOTH Next entries' shipping is re-evaluated — if the (re-)pinned image cannot prove the path, delete the Next entries and note that the 96/128 GB tiers ship 30B-A3B only (deletion over hope).
    - A4: if qualification surfaced sampling guidance differing for Next, update its agent_sampling values with the evidence citation.
    - Update catalog_test.go expected-value maps to match every reconciled value; if entries were deleted, update the coder-entry count/id assertions.
    - Do NOT touch cmd/villa/testdata/recommend.golden.json (the fixture is hand-built; the phase's single re-freeze already happened in plan 24-02). Do NOT touch any Go file outside internal/catalog tests.
  </action>
  <verify>
    <automated>make check && python3 -c "import json;c=json.load(open('internal/catalog/seed.json'));coders=[m for m in c['models'] if m.get('role')=='coder'];assert coders, 'no coder entries survived — requires explicit operator sign-off';assert all('/resolve/main/' not in s['url'] for m in coders for s in m['shards']);print('OK:',[m['id'] for m in coders])"</automated>
  </verify>
  <acceptance_criteria>
    - Every surviving coder entry's cache_reuse_safe state matches the literal verdict in its cache-reuse.txt/verdict.md.
    - No FAIL-verdict entry remains in seed.json; every disposition (keep/delete/re-pin) is traceable to a verdict file.
    - `git diff --stat` for this task touches only internal/catalog/seed.json and internal/catalog/catalog_test.go.
    - `make check` green (catalog tests track the reconciled values; recommend tests + golden untouched and green).
  </acceptance_criteria>
  <done>The catalog encodes measured reality with evidence-traceable dispositions; full suite green.</done>
</task>

<task type="auto">
  <name>Task 2: Record the D-11 toolbox decision + qualification evidence summary</name>
  <files>.planning/phases/24-coder-fit-math-catalog-on-hardware-model-qualification/24-TOOLBOX-DECISION.md, .planning/phases/24-coder-fit-math-catalog-on-hardware-model-qualification/24-QUALIFICATION-EVIDENCE.md</files>
  <read_first>
    - .planning/phases/24-coder-fit-math-catalog-on-hardware-model-qualification/24-RESEARCH.md (§Toolbox Vintage Findings — the verified static evidence table; §Assumptions A1; §Cache-Reuse Compatibility)
    - .planning/phases/24-coder-fit-math-catalog-on-hardware-model-qualification/qualification/qwen3-coder-30b-a3b/verdict.md
    - .planning/phases/24-coder-fit-math-catalog-on-hardware-model-qualification/qualification/qwen3-coder-next-q4/verdict.md
    - .planning/phases/24-coder-fit-math-catalog-on-hardware-model-qualification/qualification/qwen3-coder-next-q3/verdict.md
  </read_first>
  <action>
    Write `24-TOOLBOX-DECISION.md` as a numbered decision record (the project's decision-ID convention) covering D-11's three required checks, each with both its static and functional evidence cited:
    1. Qwen3-Next/DeltaNet arch support (llama.cpp PR #16095): static — `qwen3next` arch string verified in libllama.so (RESEARCH §Toolbox Vintage Findings); functional — Next entries loaded and generated on hardware (cite verdict files + server-version.txt build-9496 proofs).
    2. Tool-call parser vintage (Feb-2026 fixes): static — `Qwen3-Coder-` parser + `<tool_call>`/`<function=` XML markers present in libllama-common.so; functional — smoke tests returned well-formed string-arguments tool_calls, agent loops executed real multi-step tool use.
    3. Per-model `--cache-reuse` semantics: the per-entry probe results (degrade-warning presence/absence + second-turn cache_n evidence) and the resulting catalog cache_reuse_safe values.
    State the verdict explicitly on a line beginning "Decision:": KEEP digest sha256:9a74e555… (build 9496) — or, if any functional check failed on parser/arch grounds, a re-pin to the pre-identified fallback (the build-9579 digest, locally present) recorded here as the decision, noting the actual pin change lands at the internal/inference seam in Phase 25 (no Go-side image literals in Phase 24 — TestSeamGrepGate). Any re-pin is digest-pinned, never floating/nightly (v1.1 standing decision).

    Write `24-QUALIFICATION-EVIDENCE.md`: a per-entry table (entry id, agent_ctx, verdict, measured KV vs computed, total GTT footprint, cache-reuse verdict, D-10/D-12 disposition, evidence-file links) plus the A2 DeltaNet-constant observation and any min_envelope_bytes folds applied in Task 1.
  </action>
  <verify>
    <automated>grep -qi "^Decision:" .planning/phases/24-coder-fit-math-catalog-on-hardware-model-qualification/24-TOOLBOX-DECISION.md && grep -qi "qwen3-coder-30b-a3b" .planning/phases/24-coder-fit-math-catalog-on-hardware-model-qualification/24-QUALIFICATION-EVIDENCE.md</automated>
  </verify>
  <acceptance_criteria>
    - 24-TOOLBOX-DECISION.md contains all three D-11 checks, each citing at least one concrete evidence file path, and a single line starting "Decision:" naming keep-or-re-pin with the exact digest.
    - 24-QUALIFICATION-EVIDENCE.md has one row per qualified entry with verdict, measured-vs-computed KV, cache-reuse result, and disposition.
    - If the decision is re-pin: the record names the fallback digest explicitly and states the Phase 25 seam handoff; no Phase 24 Go file contains it (`git diff --stat` shows no internal/ or cmd/ Go changes from this task).
  </acceptance_criteria>
  <done>D-11 decision recorded with evidence before the freeze; the per-entry qualification story is summarized and traceable.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Operator approval of the catalog freeze</name>
  <files>internal/catalog/seed.json, .planning/phases/24-coder-fit-math-catalog-on-hardware-model-qualification/24-TOOLBOX-DECISION.md</files>
  <what-built>The reconciled, measurement-backed schema-3 catalog (cache_reuse_safe truth-up, D-10/D-12 dispositions applied), the numbered D-11 toolbox decision record with evidence, and the qualification evidence summary. Full suite green.</what-built>
  <action>Present the reconciled seed diff, the D-11 decision record, and the evidence summary to the operator for the freeze decision; pause execution until the operator responds. The catalog is not "frozen" (and the phase not complete) without this sign-off.</action>
  <how-to-verify>
    1. Read `24-TOOLBOX-DECISION.md` — confirm the Decision line is supported by the cited evidence and you agree with keep vs re-pin.
    2. Read `24-QUALIFICATION-EVIDENCE.md` — confirm every entry's disposition matches its verdict (no FAIL shipped; deletions/re-pins justified).
    3. Review `git diff` of `internal/catalog/seed.json` against the plan 24-01 state — confirm each reconciled value traces to evidence and the chat entries remain untouched.
    4. Run `make check` and `./villa recommend` — confirm the coder block renders with honest fit terms and residency on this box.
  </how-to-verify>
  <verify>
    <automated>make check</automated>
  </verify>
  <done>Operator typed "approved"; the catalog freeze is ratified against the evidence chain.</done>
  <resume-signal>Type "approved" to freeze the catalog and close the phase plans, or describe required changes (e.g., dispute a disposition, request a re-measure). This checkpoint is never auto-approved — it is the SC#1/SC#4 freeze gate.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| qualification evidence → shipped catalog claims | the freeze converts dev-time observations into product integrity claims (cache_reuse_safe, fit values, shipped entries) |
| decision record → Phase 25 seam | a wrong/unevidenced re-pin verdict would change the pinned image downstream |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-13 | Repudiation | catalog frozen without evidence linkage | mitigate | every disposition/value in the freeze traces to a named verdict/evidence file; blocking human checkpoint reviews the chain |
| T-24-14 | Tampering | unpinned URL or floating tag slipping into the reconciled seed | mitigate | automated python gate (no /resolve/main/ in coder shards); any re-pin is digest-pinned by decision-record rule |
| T-24-15 | Tampering | a dishonest cache_reuse_safe:true shipped (Phase 25 would render the flag) | mitigate | truth-up is a literal copy of the probe verdict; acceptance criterion cross-checks against cache-reuse.txt |
| T-24-SC | Tampering | package installs | accept (n/a) | zero installs this plan; no [ASSUMED]/[SUS] artifacts |
</threat_model>

<verification>
- `make check` green over the final frozen catalog; recommend golden byte-identical to its plan 24-02 freeze (`git log` shows no later commits touching it).
- seed.json python gate prints the surviving coder ids; no FAIL entries present.
- Decision record + evidence summary committed in the phase dir.
</verification>

<success_criteria>
- Catalog frozen reflecting measured reality — entries that failed are deleted or re-pinned, never shipped on hope (CODER-03 SC#3, D-09/D-10/D-12).
- Toolbox keep/re-pin decision recorded as a numbered decision with evidence BEFORE the freeze (SC#4, D-11).
- Operator approved the freeze at a blocking checkpoint.
</success_criteria>

<output>
Create `.planning/phases/24-coder-fit-math-catalog-on-hardware-model-qualification/24-04-SUMMARY.md` when done.
</output>
