---
phase: 24-coder-fit-math-catalog-on-hardware-model-qualification
plan: 02
type: execute
wave: 2
depends_on: ["24-01"]
files_modified:
  - internal/recommend/recommend.go
  - internal/recommend/coder.go
  - internal/recommend/coder_test.go
  - internal/recommend/recommend_test.go
  - cmd/villa/recommend.go
  - cmd/villa/recommend_test.go
  - cmd/villa/testdata/recommend.golden.json
autonomous: true
requirements: [CODER-02]

must_haves:
  truths:
    - "villa recommend computes a coder fit at each coder entry's catalog agent_ctx, after the embed reservation and chat fit, against the post-reservation envelope"
    - "Residency mode is a pure inequality output: swap when the best coder entry fits standalone, shared when none fits — including on the no-envelope refusal path (fits:false, residency:shared)"
    - "The --json output always contains a coder block directly above schema_version, on every path including refusals; schema_version is 3"
    - "The chat pick is bit-identical with coder entries present: pickBest skips role:\"coder\"; a --model override naming a coder entry warns-and-allows"
    - "Exactly ONE re-freeze of cmd/villa/testdata/recommend.golden.json happens in this phase"
  artifacts:
    - path: "internal/recommend/coder.go"
      provides: "pickCoder stage + CoderFit contract struct, reusing kvCacheBytes/addSaturating/headroomBytes verbatim"
      contains: "pickCoder"
    - path: "internal/recommend/recommend.go"
      provides: "recommendSchemaVersion = 3; Coder field above SchemaVersion (no omitempty); finalizeRecommendation stamps coder block on every path"
      contains: "recommendSchemaVersion = 3"
    - path: "cmd/villa/testdata/recommend.golden.json"
      provides: "schema-3 frozen contract with populated coder block"
      contains: "\"schema_version\": 3"
  key_links:
    - from: "internal/recommend/recommend.go"
      to: "internal/recommend/coder.go"
      via: "Pick computes the coder block after the envelope shrink; finalizeRecommendation stamps it on all 3 return paths"
      pattern: "finalizeRecommendation"
    - from: "internal/recommend/coder.go"
      to: "internal/catalog"
      via: "m.Role / m.AgentCtx / catalog fit dims from plan 24-01"
      pattern: "AgentCtx"
    - from: "cmd/villa/recommend.go"
      to: "internal/recommend.Recommendation.Coder"
      via: "JSON encoder (automatic) + gated human-table coder section"
      pattern: "Coder"
---

<objective>
Recommend schema 2→3 (CODER-02): add the coder fit stage to `Pick` per D-04/D-05/D-06, surface an always-stamped append-only `coder` block per D-07, keep the chat pick bit-identical per D-03, and land the phase's single deliberate golden re-freeze.

Purpose: Phase 25's swap verb consumes the residency-mode output; the dashboard/status never see this until Phase 28, so the --json golden is the only contract evolving here.
Output: pure-core coder fit + schema-3 recommend contract + ONE golden re-freeze, `make check` green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-coder-fit-math-catalog-on-hardware-model-qualification/24-CONTEXT.md
@.planning/phases/24-coder-fit-math-catalog-on-hardware-model-qualification/24-PATTERNS.md
@.planning/phases/24-coder-fit-math-catalog-on-hardware-model-qualification/24-01-SUMMARY.md
</context>

## Artifacts this phase produces (plan 24-02 share)

- `internal/recommend/coder.go` (new file, kv.go house style): func `pickCoder(c catalog.Catalog, envelope uint64) CoderFit`; exported struct `CoderFit` with fields/JSON keys: `Model string` `model`, `Quant string` `quant`, `AgentCtx int` `agent_ctx`, `WeightBytes uint64` `weight_bytes`, `KVCacheBytes uint64` `kv_cache_bytes`, `HeadroomBytes uint64` `headroom_bytes`, `TotalBytes uint64` `total_bytes`, `Fits bool` `fits`, `Residency string` `residency` (values `"swap"`/`"shared"`). NONE of these keys are omitempty.
- `internal/recommend/recommend.go`: `recommendSchemaVersion = 3`; `Recommendation.Coder CoderFit` json:"coder" placed directly above `SchemaVersion`; role filter in `pickBest`; warn-and-allow note in `pickOverride` for coder ids.
- `cmd/villa/recommend.go`: human-table "Coder (agent profile)" section.
- `cmd/villa/testdata/recommend.golden.json`: re-frozen once — `"coder": {…}` between `"memory_considered"` and `"schema_version": 3`.
- New CLI output field (JSON): top-level `coder` object (see CoderFit keys above).

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Coder fit stage, residency derivation, schema-3 contract in the pure core</name>
  <files>internal/recommend/recommend.go, internal/recommend/coder.go, internal/recommend/coder_test.go, internal/recommend/recommend_test.go</files>
  <read_first>
    - internal/recommend/recommend.go (recommendSchemaVersion ~lines 27-32; contract-field placement ~lines 103-115; finalizeRecommendation ~lines 209-227 and its 3 call sites ~lines 160/184/187; pickBest loop ~lines 275-332; pickOverride unsafe-override note ~lines 349-351; saturating total at ~line 377)
    - internal/recommend/kv.go (kvCacheBytes + addSaturating — reuse VERBATIM, no second formula)
    - internal/recommend/envelope.go (headroomBytes, headroomFraction)
    - internal/recommend/recommend_test.go (testCatalog builder ~lines 14-46; TestPickMemoryReservation ~lines 359-444; TestPickRefusalStampsMemoryFields ~lines 469-500; hasNote helper ~lines 502-509; existing SchemaVersion==2 assertions at ~lines 376-378, 483-485, 497-499 — ALL flip to 3 in this task)
    - internal/catalog/catalog.go (the schema-3 fields from plan 24-01: Role, AgentCtx, CacheReuseSafe)
    - .planning/phases/24-coder-fit-math-catalog-on-hardware-model-qualification/24-PATTERNS.md (§internal/recommend sections)
  </read_first>
  <behavior>
    - Test 1 (swap when fits, D-06): with a coder entry whose weights+KV@agent_ctx+headroom ≤ post-reservation envelope, Pick returns Coder.Fits true, Residency "swap", Model set, and the exact saturating fit terms (WeightBytes, KVCacheBytes computed by kvCacheBytes at AgentCtx, HeadroomBytes, TotalBytes).
    - Test 2 (shared when none fits, D-06): with only an oversized coder entry, Coder.Fits false, Residency "shared", Model "" — and the chat pick is unaffected.
    - Test 3 (fit at agent_ctx, never --ctx, D-04): Pick with Overrides{Ctx: <small value>} still computes the coder KV at the entry's AgentCtx — the override changes the chat KV only.
    - Test 4 (chat pick bit-identical, D-03): a catalog with chat + coder entries produces the same chat Model/Quant/Ctx/fit-terms as the same catalog without the coder entries; pickBest never selects a role:"coder" entry even when it is the largest fitting footprint.
    - Test 5 (refusal stamping, D-07/Pitfall 6): on the unknown-envelope refusal path (TestPickRefusalStampsMemoryFields shape), the coder block is present with Fits false and Residency "shared"; SchemaVersion == 3.
    - Test 6 (override warn-and-allow): Overrides{Model:"<coder id>"} resolves via FindByID, succeeds, and appends a note containing the entry id and the word "coder" (the unsafe-override note precedent).
    - Test 7: SchemaVersion == 3 stamped on every path (existing 2-assertions flipped).
    - Test 8 (reservation ordering, D-05): with memory enabled, the coder fit is evaluated against the post-reservation envelope (an entry fitting the raw envelope but not the reserved one yields shared).
  </behavior>
  <action>
    Write the behavior tests FIRST (coder_test.go for the new stage; flip existing schema pins in recommend_test.go), then implement:

    coder.go (new file, kv.go house style — doc comment citing D-04/D-05/D-06/D-07): `CoderFit` struct exactly as specified in "Artifacts" above; `pickCoder(c, envelope)` mirrors pickBest's eligibility loop with these deltas: skip entries where Role != "coder"; skip Bootstrap and !UnifiedMemorySafe identically; apply the MinEnvelopeBytes secondary floor identically; ctx is ALWAYS m.AgentCtx (D-04 — `effectiveCtx`/Overrides must not be reachable from this function's signature); total uses the saturating form addSaturating(addSaturating(m.WeightBytes, kvCacheBytes(m, m.AgentCtx)), headroomBytes(envelope)); best = largest fitting total (most-capable rule). Returns: best found ⇒ populated CoderFit with Fits true, Residency "swap"; none ⇒ zero-value terms with Fits false, Residency "shared" (the honest no-fit mirror of pickBest's note path — D-06: shared is the floor, never a refusal).

    recommend.go:
    - Bump `recommendSchemaVersion` to 3, appending a history sentence to its doc comment (2→3: append-only coder block, Phase 24, D-07).
    - Add `Coder CoderFit` json:"coder" (NOT omitempty — Pitfall 6; the omitempty ROCm advice fields are the WRONG precedent) directly ABOVE SchemaVersion, doc comment citing D-06/D-07 and noting always-stamped-including-refusals.
    - Stamping: compute the coder block in Pick after the embed-reservation envelope shrink (D-05 ordering: envelope → reservation → chat fit → coder fit) and pass it to finalizeRecommendation via an added parameter (3 call sites). On the no-envelope refusal path (the call site that never computes an envelope) pass the zero CoderFit with Fits false / Residency "shared" (D-06 conservative floor — swap requires a proven fit).
    - pickBest: add `if m.Role == "coder" { continue }` with a comment citing D-03 (chat path excludes coder entries; absent role ⇒ chat).
    - pickOverride: when the FindByID result has Role == "coder", warn-and-allow with a loud note in the unsafe-override style, e.g. naming the id and stating it is a role:"coder" entry being used on the chat pick only because the user overrode (Open Question 1 resolution: warn-and-allow, consistent with the D-07 v1.0 override philosophy). Note: the chat KV for an override still uses effectiveCtx — only pickCoder is agent_ctx-locked.
  </action>
  <verify>
    <automated>go test ./internal/recommend/... -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "recommendSchemaVersion = 3" internal/recommend/recommend.go` matches exactly one line.
    - `grep -n 'json:"coder"' internal/recommend/recommend.go` matches one line and that line does NOT contain `omitempty`.
    - In recommend.go the `Coder` field declaration appears on an earlier line than the `SchemaVersion` field declaration.
    - `grep -rn "effectiveCtx" internal/recommend/coder.go` returns no matches (D-04 enforcement).
    - All 8 behavior tests green; `go test ./internal/recommend/... -count=1` green.
  </acceptance_criteria>
  <done>Pure-core coder fit + residency derivation + schema-3 contract proven by table tests on every path including refusals; chat pick bit-identical.</done>
</task>

<task type="auto">
  <name>Task 2: Human-table coder section + the ONE golden re-freeze</name>
  <files>cmd/villa/recommend.go, cmd/villa/recommend_test.go, cmd/villa/testdata/recommend.golden.json</files>
  <read_first>
    - cmd/villa/recommend.go (renderRecommend ~lines 123-130 — JSON path needs NO change; gated embed-reservation row ~lines 158-163; gated ROCm advice block ~lines 178-187; gib() ~lines 216-218; fitsGlyph() ~lines 221-226; saveRecommendation ~lines 98-118 — do NOT persist coder fields this phase)
    - cmd/villa/recommend_test.go (fixtureRecommendation + TestRecommendJSONGolden ~lines 19-71; the SchemaVersion fixture comment ~lines 33-40; golden -update mechanics ~lines 52-62; TestRecommendTableShowsFitMath ~lines 75-86)
    - cmd/villa/detect_test.go (~line 13: the single package-level `update` flag — do NOT redeclare)
    - cmd/villa/testdata/recommend.golden.json (current frozen shape — target diff is a pure insertion of "coder": {…} between "memory_considered" and "schema_version", plus the 2→3 value change)
  </read_first>
  <behavior>
    - TestRecommendJSONGolden: fixture gains a POPULATED coder block (model qwen3-coder-30b-a3b, quant UD-Q4_K_XL, agent_ctx 65536, non-zero fit terms, fits true, residency "swap") and SchemaVersion 3 with an appended history sentence in the fixture comment; the frozen bytes exercise the full schema-3 shape.
    - Table test: rendered human output contains a "Coder (agent profile)" section with the model id, agent ctx, residency value, and a fits glyph; with an empty coder Model, the section renders a single honest line stating no coder model fits and residency is shared.
  </behavior>
  <action>
    cmd/villa/recommend.go: add a coder section to renderRecommendTable after the notes, following the gated ROCm-advice block shape. When rec.Coder.Model is non-empty render: model, quant, agent ctx, weight/kv/headroom/total fit rows via gib(), fits via fitsGlyph(), and the residency value. When Model is empty render one line stating no coder model fits the envelope and residency is "shared" (the agent rides the chat endpoint) — the JSON block is always stamped (D-07) but the human table renders compactly (CONTEXT Claude's-discretion item, resolved here). Do not touch saveRecommendation (coder persistence is Phase 25).

    cmd/villa/recommend_test.go: extend fixtureRecommendation with the populated CoderFit above and SchemaVersion 3; extend the table test with the new substrings.

    Re-freeze EXACTLY ONCE: run `go test ./cmd/villa -run Recommend -update`, then review `git diff cmd/villa/testdata/recommend.golden.json` — the diff must be a pure addition of the "coder" object between "memory_considered" and "schema_version" plus the schema value 2→3. Any other change to the golden is a contract violation (D-07: one re-freeze; plan 24-04 and all later phases must not touch this file).
  </action>
  <verify>
    <automated>go test ./cmd/villa -run Recommend -count=1 && make check</automated>
  </verify>
  <acceptance_criteria>
    - `python3 -c "import json,collections;d=json.load(open('cmd/villa/testdata/recommend.golden.json'),object_pairs_hook=collections.OrderedDict);k=list(d);assert d['schema_version']==3;assert 'coder' in d;assert k.index('coder')==k.index('schema_version')-1 and k.index('coder')==k.index('memory_considered')+1;assert d['coder']['residency']=='swap' and d['coder']['fits'] is True;print('OK')"` prints OK.
    - `git log --oneline -- cmd/villa/testdata/recommend.golden.json` shows exactly one new commit touching the golden in this phase.
    - Table test asserts the "Coder" section substrings; `make check` green.
    - `grep -rn "flag.Bool(\"update\"" cmd/villa/ | wc -l` is 1 (no redeclared flag).
  </acceptance_criteria>
  <done>Human table renders the coder section; the schema-3 contract is frozen in exactly one deliberate golden re-freeze; full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config.toml / CLI flags → Pick | user-controlled overrides (`--ctx`, `--model`) enter the fit math |
| Recommendation --json → Phase 25/28 consumers | the residency claim drives the future swap verb — a dishonest value becomes an OOM downstream |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-04 | Tampering | `--ctx` override leaking into the coder fit (wrong KV → wrong residency → Phase 25 OOM) | mitigate | pickCoder signature takes no Overrides; grep-gate acceptance criterion (no effectiveCtx in coder.go); Test 3 |
| T-24-05 | Repudiation | conditional/omitted coder block hides the residency basis on refusals | mitigate | no-omitempty + finalizeRecommendation stamping on all 3 paths + refusal-path test (D-07, Pitfall 6) |
| T-24-06 | Information disclosure | none new — pure core, no I/O, loopback posture unchanged | accept | no network/file surface added; `saveRecommendation` deliberately untouched |
| T-24-SC | Tampering | package installs | accept (n/a) | zero new dependencies; no [ASSUMED]/[SUS] packages |
</threat_model>

<verification>
- `go test ./internal/recommend/... ./cmd/villa -count=1` green; `make check` green.
- `TestSeamGrepGate` green (this plan adds no podman/image/backend literals — pure fit math + render).
- Golden diff reviewed: pure append above schema_version, value 2→3, nothing else.
</verification>

<success_criteria>
- Coder fit computed at agent-profile ctx after embed reservation + chat fit; residency is a pure inequality output, honest on refusals (CODER-02, SC#2; D-04/D-05/D-06/D-07).
- Chat recommendation bit-identical with coder entries present (D-03).
- Recommend schema 3 frozen with exactly one golden re-freeze.
</success_criteria>

<output>
Create `.planning/phases/24-coder-fit-math-catalog-on-hardware-model-qualification/24-02-SUMMARY.md` when done.
</output>
