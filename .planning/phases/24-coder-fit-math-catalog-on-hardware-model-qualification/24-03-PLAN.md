---
phase: 24-coder-fit-math-catalog-on-hardware-model-qualification
plan: 03
type: execute
wave: 2
depends_on: ["24-01"]
files_modified:
  - .planning/phases/24-coder-fit-math-catalog-on-hardware-model-qualification/qualification/qualify.sh
  - .planning/phases/24-coder-fit-math-catalog-on-hardware-model-qualification/qualification/crush.json
  - .planning/phases/24-coder-fit-math-catalog-on-hardware-model-qualification/qualification/qwen3-coder-30b-a3b/
  - .planning/phases/24-coder-fit-math-catalog-on-hardware-model-qualification/qualification/qwen3-coder-next-q4/
  - .planning/phases/24-coder-fit-math-catalog-on-hardware-model-qualification/qualification/qwen3-coder-next-q3/
autonomous: false
requirements: [CODER-03]

must_haves:
  truths:
    - "Each coder catalog entry has a real multi-step agent-in-the-loop tool-call run (Crush v0.76.0 read→edit→verify task) through llama-server --jinja on the PINNED toolbox digest, with an explicit PASS/FAIL verdict and evidence on disk"
    - "KV footprint and total GTT footprint are MEASURED at each entry's agent_ctx on the gfx1151 box and recorded against the computed estimates"
    - "Each entry has a cache-reuse probe verdict (true/false with journal + timings evidence) feeding cache_reuse_safe"
    - "Every qualification run records the served llama-server version, proving it ran by the pinned digest (build 9496), never the drifted tag"
    - "The live chat stack is restored and villa status is green after qualification"
  artifacts:
    - path: ".planning/phases/24-coder-fit-math-catalog-on-hardware-model-qualification/qualification/qwen3-coder-30b-a3b/verdict.md"
      provides: "PASS/FAIL verdict + measured KV/GTT vs computed + cache-reuse result for the 30B entry"
    - path: ".planning/phases/24-coder-fit-math-catalog-on-hardware-model-qualification/qualification/qwen3-coder-next-q4/verdict.md"
      provides: "verdict for Next-Q4 @131072 (D-12 ship gate input)"
    - path: ".planning/phases/24-coder-fit-math-catalog-on-hardware-model-qualification/qualification/qwen3-coder-next-q3/verdict.md"
      provides: "verdict for Next-Q3 @131072 (D-12 ship gate input)"
  key_links:
    - from: "qualification runs"
      to: "internal/catalog/seed.json entries"
      via: "villa model pull <entry-id> (sha256-verified pre-stage) — requires plan 24-01's entries"
      pattern: "qwen3-coder"
    - from: "verdict.md files"
      to: "plan 24-04 reconciliation + D-11 decision record"
      via: "measured KV / cache-reuse / PASS-FAIL evidence citations"
      pattern: "PASS|FAIL"
---

<objective>
On-hardware qualification (CODER-03, D-08/D-09/D-10 evidence gathering): run the 11-step protocol from 24-RESEARCH Pattern 4 for all three coder entries on this gfx1151 box — serve by pinned digest with `--jinja` at each entry's `agent_ctx`, measure KV + GTT, run the tool-call smoke test, drive a real Crush v0.76.0 read→edit→verify agent loop, probe `--cache-reuse`, capture evidence — then restore the chat stack.

Purpose: benchmark scores alone never qualify an entry (D-08); plan 24-04 reconciles the catalog and records the D-11 toolbox decision FROM this evidence.
Output: per-entry evidence directories + verdicts under the phase `qualification/` dir. NO product code changes (TestSeamGrepGate: all podman/digest literals live in these phase-dir scripts/docs only).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-coder-fit-math-catalog-on-hardware-model-qualification/24-RESEARCH.md
@.planning/phases/24-coder-fit-math-catalog-on-hardware-model-qualification/24-01-SUMMARY.md
</context>

## Artifacts this phase produces (plan 24-03 share)

- `qualification/qualify.sh` — the reproducible per-entry protocol script (serve-by-digest, measure, smoke, agent loop, cache-reuse probe). Phase-dir artifact, NOT product code.
- `qualification/crush.json` — the qualification provider config (kill switches on, loopback 127.0.0.1:8081, villa-unique model id).
- Per entry id (`qwen3-coder-30b-a3b`, `qwen3-coder-next-q4`, `qwen3-coder-next-q3`): `server-version.txt`, `kv-gtt.txt` (baseline/after/delta + `KV buffer size` log lines + residency lines), `smoke.json` (+ no-properties variant result), `crush-transcript.txt`, `cache-reuse.txt`, `verdict.md`.

## Operating rules (read before any host command)

1. **Digest, never tag (Pitfall 1):** every `podman run` uses `docker.io/kyuz0/amd-strix-halo-toolboxes@sha256:9a74e555c45864352a4077528836988d448e9f030fbab9f7376ea1c603ac7aad`. The local `vulkan-radv` tag has DRIFTED to build 9579; the pin is build 9496. Record `llama-server --version` output from inside every run.
2. **Fixed-arg commands only** — no shell interpolation of model names or user input; entry filenames come from the catalog you authored in 24-01.
3. **This box is the live host** — `systemctl --user stop villa-llama.service` before each serve (clean GTT baseline ~<2 GiB + avoids OOM on the 46 GiB Next-Q4 load, Pitfall 3); restart and confirm `villa status` after.
4. **Crush is a QUALIFICATION TOOL ONLY (D-08)** — fetched to /tmp, never installed system-wide, never shipped; delivery is Phase 26. `--yolo` is acceptable ONLY in the disposable /tmp workspace with `--cwd` pinned (Pitfall 7) — the villa repo is NEVER the cwd.
5. **Sanctioned outbound window:** the only network egress in this plan is the Crush tarball (GitHub, sha256-pinned) and the GGUF pulls through `./villa model pull` (HF, revision+sha256-pinned via the existing download core). Nothing else.

<tasks>

<task type="auto">
  <name>Task 1: Pre-stage the qualification harness (Crush, scratch repo, GGUFs, script)</name>
  <files>.planning/phases/24-coder-fit-math-catalog-on-hardware-model-qualification/qualification/qualify.sh, .planning/phases/24-coder-fit-math-catalog-on-hardware-model-qualification/qualification/crush.json</files>
  <read_first>
    - .planning/phases/24-coder-fit-math-catalog-on-hardware-model-qualification/24-RESEARCH.md (§Pattern 4 protocol steps 1-11, §Code Examples — tool-call smoke test, qual crush.json sketch, KV/GTT capture; §Qualification Artifacts table — Crush URL + sha256)
    - internal/inference/backend_vulkan.go (the seam's ContainerArgs/flags the qual serve command replicates — READ ONLY, do not modify)
    - internal/catalog/seed.json (the three coder entry ids + shard filenames from plan 24-01)
  </read_first>
  <action>
    Per D-08, stage the dev-time harness:

    1. Disk gate: assert ≥120 GB free on /home (`df`); abort with a report if not.
    2. Fetch Crush v0.76.0: download `https://github.com/charmbracelet/crush/releases/download/v0.76.0/crush_0.76.0_Linux_x86_64.tar.gz`, verify `sha256sum --check` against `0f66114171270485763ffbc96f63403de5d598124c4f3841bc478c3a3a0d1ec9` (hard abort on mismatch — refuse with remediation), extract to `/tmp/crush-qual/`. Confirm `command -v crush` is still empty (no system-wide install — D-08).
    3. Scratch workspace: create `/tmp/qual-repo` as a fresh git repo containing a tiny Go module with one intentionally failing test (a small function with an off-by-one bug + a test that catches it — the read→edit→verify task target). Commit it so agent edits are diffable.
    4. Author `qualification/crush.json` (copied into /tmp/qual-repo at run time): options `disable_metrics: true` and `disable_provider_auto_update: true` (kill switches), exactly one openai-compat provider with base_url `http://127.0.0.1:8081/v1`, villa-unique model id `villa-qwen3-coder-30b` (Pitfall 8 — model-id shadowing; per-entry the id/name/context_window are swapped in: 65536 for 30B, 131072 for Next), default_max_tokens 16384. Note: Crush may execute `$(…)` in a project-local crush.json — we author this file ourselves; never copy one from elsewhere.
    5. Author `qualification/qualify.sh`: the parameterized per-entry protocol (entry id, gguf filename, agent_ctx) implementing RESEARCH Pattern 4 steps 2-9 with the exact serve command: `podman run --rm --name villa-qual --device /dev/dri --group-add keep-groups --security-opt seccomp=unconfined -p 127.0.0.1:8081:8080 -v ~/.local/share/villa/models:/models:ro,z docker.io/kyuz0/amd-strix-halo-toolboxes@sha256:9a74e555c45864352a4077528836988d448e9f030fbab9f7376ea1c603ac7aad llama-server -m /models/<file>.gguf -c <agent_ctx> --host 0.0.0.0 --port 8080 -ngl 999 -fa 1 --no-mmap -lv 4 --metrics --jinja` (flags mirror the inference seam + the `--jinja` qualification delta; loopback-only publish on 8081). Script also captures `llama-server --version`, GTT baseline/after from `/sys/class/drm/card1/device/mem_info_gtt_used`, `KV buffer size` / `Vulkan0 model buffer` / `offloaded N/N` log lines, runs both smoke-test variants (standard tools request asserting string `arguments` per the llama.cpp #20198 class, and the no-`properties` variant which must not 500), and the cache-reuse probe restart with `--cache-reuse 256`.
    6. Build villa (`make build`) and pre-stage all three GGUFs through the existing verify path: `./villa model pull qwen3-coder-30b-a3b`, `./villa model pull qwen3-coder-next-q4`, `./villa model pull qwen3-coder-next-q3` (~104 GB total; sha256 verified by internal/download — any checksum mismatch is a hard FAIL for that entry, T-24-07).
  </action>
  <verify>
    <automated>echo "0f66114171270485763ffbc96f63403de5d598124c4f3841bc478c3a3a0d1ec9  /tmp/crush-qual-download/crush_0.76.0_Linux_x86_64.tar.gz" | sha256sum --check && /tmp/crush-qual/crush --version && ls ~/.local/share/villa/models/ | grep -c "Qwen3-Coder"</automated>
  </verify>
  <acceptance_criteria>
    - `sha256sum --check` on the Crush tarball passes; `/tmp/crush-qual/crush --version` reports 0.76.0; `command -v crush` returns nothing.
    - `go test ./...` inside /tmp/qual-repo exits non-zero with exactly one failing test (the seeded bug).
    - All three GGUF files exist under `~/.local/share/villa/models/` with sizes 17665334432 / 49608478720 / 36282685440 bytes and passed villa's sha256 verification.
    - `qualification/qualify.sh` and `qualification/crush.json` committed; crush.json contains `"disable_metrics": true`, `"disable_provider_auto_update": true`, and `127.0.0.1:8081`.
    - No Go source file changed in this task (`git status --porcelain -- internal cmd` is empty) — TestSeamGrepGate untouchable by construction.
  </acceptance_criteria>
  <done>Harness staged: verified Crush in /tmp, scratch repo with failing test, qual config + script committed, all three GGUFs pulled and verified.</done>
</task>

<task type="auto">
  <name>Task 2: Qualify qwen3-coder-30b-a3b @ agent_ctx 65536 (full protocol + cache-reuse probe)</name>
  <files>.planning/phases/24-coder-fit-math-catalog-on-hardware-model-qualification/qualification/qwen3-coder-30b-a3b/</files>
  <read_first>
    - .planning/phases/24-coder-fit-math-catalog-on-hardware-model-qualification/qualification/qualify.sh (the script authored in Task 1)
    - .planning/phases/24-coder-fit-math-catalog-on-hardware-model-qualification/24-RESEARCH.md (§Pattern 4 steps 2-11, PASS criteria step 7; §Fit Pre-Computations — expected KV 6.00 GiB f16 @65536, expected total ~22.5 GiB + headroom)
  </read_first>
  <action>
    Execute the protocol for the 30B entry (D-08/D-09), capturing every output into `qualification/qwen3-coder-30b-a3b/`:

    1. Quiesce: `systemctl --user stop villa-llama.service`; record quiesced GTT baseline (expect <2 GiB; if higher, find and stop the offender before proceeding — Pitfall 3).
    2. Serve by digest at ctx 65536 via qualify.sh; record `llama-server --version` into server-version.txt — MUST report build 9496/commit 94a220cd6 (T-24-07; a 9579 reading means the tag leaked in — abort and fix the script).
    3. Measure (D-09): `KV buffer size` log lines + GTT delta + residency lines (`Vulkan0 model buffer size`, `offloaded N/N`) into kv-gtt.txt. A partial offload (< N/N) is a FAIL for this entry/ctx — offload-asserting, never liveness. Record measured KV vs the computed 6.00 GiB estimate and total footprint vs ~26.2 GiB.
    4. Smoke (cheap disqualifier first): both curl variants from RESEARCH Code Examples into smoke.json — assert HTTP 200, well-formed tool_calls, STRING arguments; no-properties variant must not 500.
    5. Agent loop (D-08): copy qualification/crush.json (id villa-qwen3-coder-30b, context_window 65536) into /tmp/qual-repo; reset the repo to the seeded-bug commit; run `CRUSH_DISABLE_METRICS=1 DO_NOT_TRACK=1 /tmp/crush-qual/crush run --yolo --cwd /tmp/qual-repo "Run 'go test ./...', read the failing test and the source file it covers, fix the bug so the test passes, then run 'go test ./...' again and confirm it passes."` — tee stdout to crush-transcript.txt.
    6. Evaluate ALL PASS criteria (RESEARCH step 7): ≥3 distinct tool types actually executed (read/view, edit/write, bash); final `go test ./...` in /tmp/qual-repo green; zero 5xx on /v1/chat/completions while tools present (server log); no narrated tool calls (raw `<tool_call>`/`<function=` XML as assistant prose); no `ggml_vulkan: Device memory allocation` failure; session self-terminates.
    7. Cache-reuse probe (feeds cache_reuse_safe, D-09): restart the server with `--cache-reuse 256`, run a 2-turn prompt; record into cache-reuse.txt: presence/absence of `cache reuse is not supported - ignoring n_cache_reuse` / `cache_reuse is not supported by this context` in the log AND second-turn `timings.cache_n` from the API response. Verdict true requires BOTH no warning AND cache_n > 0 (expected: true for 30B — but evidence decides, never assumption).
    8. Stop the qual container. Write verdict.md: PASS/FAIL per criterion, measured-vs-computed KV table, cache-reuse verdict, server version line.
  </action>
  <verify>
    <automated>grep -v '^#' .planning/phases/24-coder-fit-math-catalog-on-hardware-model-qualification/qualification/qwen3-coder-30b-a3b/server-version.txt | grep -q "9496" && grep -qE "offloaded [0-9]+/[0-9]+" .planning/phases/24-coder-fit-math-catalog-on-hardware-model-qualification/qualification/qwen3-coder-30b-a3b/kv-gtt.txt && grep -qE "^VERDICT: (PASS|FAIL)" .planning/phases/24-coder-fit-math-catalog-on-hardware-model-qualification/qualification/qwen3-coder-30b-a3b/verdict.md</automated>
  </verify>
  <acceptance_criteria>
    - server-version.txt contains build 9496 (pinned digest proof).
    - kv-gtt.txt contains a `KV buffer size` line, a GTT baseline/after/delta in MiB, and full-offload residency lines.
    - smoke.json shows tool_calls with string arguments for the standard variant and a non-500 result for the no-properties variant.
    - crush-transcript.txt shows the multi-step loop; verdict.md enumerates each PASS criterion with evidence references and ends with a single `VERDICT: PASS` or `VERDICT: FAIL` line plus the cache-reuse true/false line.
  </acceptance_criteria>
  <done>30B-A3B has a complete evidence directory with an explicit verdict, measured KV, and a cache-reuse result.</done>
</task>

<task type="auto">
  <name>Task 3: Qualify qwen3-coder-next-q4 and qwen3-coder-next-q3 @ agent_ctx 131072; restore the chat stack</name>
  <files>.planning/phases/24-coder-fit-math-catalog-on-hardware-model-qualification/qualification/qwen3-coder-next-q4/, .planning/phases/24-coder-fit-math-catalog-on-hardware-model-qualification/qualification/qwen3-coder-next-q3/</files>
  <read_first>
    - .planning/phases/24-coder-fit-math-catalog-on-hardware-model-qualification/qualification/qwen3-coder-30b-a3b/verdict.md (the proven run shape from Task 2)
    - .planning/phases/24-coder-fit-math-catalog-on-hardware-model-qualification/24-RESEARCH.md (§Fit Pre-Computations — Next expected KV ~3.00 GiB @131072 per the n_layers:12 encoding; §Pattern 4 step 8 — a Next failure on arch/parser grounds triggers the re-pin evaluation against build 9579, digest-pinned)
  </read_first>
  <action>
    Repeat the Task 2 protocol per Next entry (Q4 first — the tightest fit: 46.2 GiB weights into the 62.5 GiB envelope; villa-llama MUST be stopped), with these deltas: ctx 131072; crush.json model id `villa-qwen3-coder-next` with context_window 131072; expected measured KV ~3.0 GiB — if measurement lands near 12 GiB the n_layers:12 hybrid encoding is wrong (Pitfall 2) and the entry FAILS reconciliation review. The measured-vs-computed delta also bounds the DeltaNet recurrent-state constant (A2): record it explicitly in verdict.md so plan 24-04 can fold a material constant into min_envelope_bytes.

    These two runs are the D-12 ship gate: arch support (model loads + generates under qwen3next), tool-call path (smoke + agent loop), and the cache-reuse probe (expected false for the hybrid — the journal warning string is the evidence). Any FAIL on arch/parser grounds: do NOT improvise a re-pin here — record the failure precisely (which criterion, which log lines); the keep/re-pin decision is plan 24-04's D-11 record, with build 9579 (locally present, digest-pinned) as the pre-identified fallback candidate.

    After both runs: stop the qual container, `systemctl --user start villa-llama.service`, and run `./villa status` — the chat stack must be green (OFFLOAD asserted, not just active) before this task is done.
  </action>
  <verify>
    <automated>grep -qE "^VERDICT: (PASS|FAIL)" .planning/phases/24-coder-fit-math-catalog-on-hardware-model-qualification/qualification/qwen3-coder-next-q4/verdict.md && grep -qE "^VERDICT: (PASS|FAIL)" .planning/phases/24-coder-fit-math-catalog-on-hardware-model-qualification/qualification/qwen3-coder-next-q3/verdict.md && ./villa status</automated>
  </verify>
  <acceptance_criteria>
    - Both Next evidence directories contain the same six files as the 30B directory, each verdict.md ending in a single `VERDICT:` line and a `cache-reuse:` line.
    - Each verdict.md records measured KV vs the 3.0 GiB computed value and the measured-vs-computed delta attributed to the DeltaNet constant (A2).
    - server-version.txt in both directories shows build 9496.
    - `./villa status` exits 0 with the inference service healthy and offload asserted (restored stack).
  </acceptance_criteria>
  <done>Both Next entries have explicit evidence-backed verdicts at 131072; the live chat stack is restored and green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Operator review of qualification evidence + restored stack</name>
  <files>.planning/phases/24-coder-fit-math-catalog-on-hardware-model-qualification/qualification/</files>
  <what-built>Three complete on-hardware qualification evidence directories (30B-A3B @65536, Next-Q4 @131072, Next-Q3 @131072): pinned-digest server version proofs, measured KV/GTT vs computed, tool-call smoke results, real Crush agent-loop transcripts with PASS/FAIL verdicts, and per-entry cache-reuse probe results. Chat stack restored.</what-built>
  <action>Present the three verdict.md files and the per-criterion evidence to the operator for review; pause execution until the operator responds. Do not proceed to plan 24-04 reconciliation on an unreviewed evidence set.</action>
  <how-to-verify>
    1. Open each `qualification/<entry-id>/verdict.md` and confirm the VERDICT line is supported by the cited evidence (transcript shows real read→edit→verify tool use, not narrated XML; offload was N/N; server version 9496).
    2. Spot-check one crush-transcript.txt: the agent actually ran go test, edited the source, and re-ran green.
    3. Run `./villa status` and confirm the chat stack is green with offload asserted.
    4. Confirm /tmp/crush-qual and /tmp/qual-repo contain the only Crush artifacts (nothing installed into PATH or shipped — D-08).
  </how-to-verify>
  <verify>
    <automated>ls .planning/phases/24-coder-fit-math-catalog-on-hardware-model-qualification/qualification/*/verdict.md | grep -c verdict</automated>
  </verify>
  <done>Operator typed "approved" (or every raised issue was resolved and the affected run redone and re-reviewed).</done>
  <resume-signal>Type "approved" to release the evidence to plan 24-04 (reconciliation + D-11 decision), or describe which entry's evidence is unconvincing / which run must be redone. This checkpoint is never auto-approved — it gates a catalog freeze on real-hardware truth.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| internet → host (sanctioned window) | Crush tarball (GitHub) + 3 GGUFs (HF) — the ONLY outbound in this plan |
| host → qual container | loopback-only publish 127.0.0.1:8081; models volume read-only |
| LLM output → /tmp/qual-repo (via crush --yolo) | auto-accepted tool calls from a local model |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-07 | Tampering | wrong/drifted toolbox build qualifying the catalog | mitigate | run by digest sha256:9a74e555… only; record llama-server --version per run; automated gate greps 9496 |
| T-24-08 | Tampering (supply chain) | Crush tarball / GGUF artifacts | mitigate | sha256sum --check (hard abort) for Crush; revision+sha256 verify via internal/download for GGUFs (RESEARCH legitimacy audit: all Approved, none [ASSUMED]/[SUS]) |
| T-24-09 | Elevation of privilege | prompt-injected tool calls under --yolo escaping the workspace | mitigate | disposable /tmp/qual-repo, --cwd pinned, villa repo never the cwd, self-authored crush.json (no `$(…)` from foreign files), workspace reviewed at the checkpoint |
| T-24-10 | Information disclosure | qualification agent phoning home | mitigate | CRUSH_DISABLE_METRICS=1 + DO_NOT_TRACK=1 env + both config kill switches (belt-and-braces; the full negative-control proof is Phase 27) |
| T-24-11 | Spoofing / exposure | qual server reachable off-box | mitigate | publish 127.0.0.1:8081 only (loopback-only bind discipline, PRIV-01 precedent) |
| T-24-12 | Tampering | shell interpolation in qualification commands | mitigate | fixed-arg commands; filenames sourced from the catalog, never interpolated user input |
| T-24-SC | Tampering | package installs | accept (n/a) | no package-manager installs; Crush is a /tmp-scoped dev tool, never installed or shipped (D-08) |
</threat_model>

<verification>
- All three evidence directories complete (six files each) with explicit VERDICT lines.
- Every server-version.txt shows build 9496 (digest proof).
- `./villa status` green after restoration; `git status --porcelain -- internal cmd` empty (zero product-code changes; TestSeamGrepGate untouched).
</verification>

<success_criteria>
- Every coder entry has a real agent-in-the-loop qualification result with measured KV at agent_ctx on this box (CODER-03, SC#3; D-08/D-09).
- Cache-reuse compatibility evidence captured per entry (D-11 input).
- Failures (if any) are recorded precisely for D-10/D-12 disposition in plan 24-04 — nothing is shipped on hope from this plan.
</success_criteria>

<output>
Create `.planning/phases/24-coder-fit-math-catalog-on-hardware-model-qualification/24-03-SUMMARY.md` when done.
</output>
