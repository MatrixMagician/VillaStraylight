---
phase: 11-address-v1-1-tech-debt-rocm-readiness-detect-probes-doc-reco
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/detect/gpu_amd.go
  - internal/detect/readiness_rocm.go
  - internal/detect/detect.go
  - internal/detect/readiness_rocm_test.go
autonomous: true
requirements: [DET-04, DASH-06]

must_haves:
  truths:
    - "firmwareDateOK() returns a real KnownBool when the linux-firmware date probes successfully (no longer an unconditional UnknownBool)"
    - "hsaOverrideViable() returns a real KnownBool derived from gfx-id + ROCm substrate (no longer an unconditional UnknownBool)"
    - "Off-hardware (gfx-id Unknown / rpm absent), both fields stay UNSET — no fabricated false (no-false-green, D-08)"
    - "A Known denylisted firmware date (20251125) or a Known sub-floor date (< 20260110) yields KnownBool(false)"
    - "A Known clear firmware date (>= 20260110, not denied) yields KnownBool(true)"
    - "cmd/villa/testdata/detect.golden.json stays byte-identical (fixture-driven, D-04) — TestJSONGolden passes WITHOUT -update"
    - "[MANUAL UAT] On a live gfx1151 ROCm host, villa detect --json shows firmware_date_ok:true + hsa_override_viable:true, and the Phase-10 status/dashboard ROCm-readiness badge reads `ready` (non-`unknown`) — closes DASH-06 SC#1 residual"
  artifacts:
    - path: "internal/detect/gpu_amd.go"
      provides: "firmwareDateProbe() (runTool rpm host probe), firmwareDatePolicyOK(date) seam, isYYYYMMDD() guard, rocmFirmwareFloor/rocmFirmwareDeny consts"
    - path: "internal/detect/readiness_rocm.go"
      provides: "real firmwareDateOK(date Str) + hsaOverrideViable(gfxID Str, rocmPresent Bool) bodies; extended computeROCmReadiness signature"
    - path: "internal/detect/readiness_rocm_test.go"
      provides: "TestFirmwareDateOK + TestHSAOverrideViable table tests; updated 5-arg call sites; extended TestComputeROCmReadinessOffHardware"
  key_links:
    - from: "internal/detect/detect.go Probe()"
      to: "computeROCmReadiness"
      via: "passes gpu.rocmPresent + firmwareDateProbe() into the extended call"
      pattern: "computeROCmReadiness\\(gpu\\.gfxID, kernel, gpu\\.rocmPresent, firmwareDateProbe\\(\\)"
    - from: "internal/detect/readiness_rocm.go firmwareDateOK"
      to: "firmwareDatePolicyOK (gpu_amd.go seam)"
      via: "policy compare; no firmware literal in readiness_rocm.go"
      pattern: "firmwareDatePolicyOK"
---

<objective>
Make `internal/detect/readiness_rocm.go`'s two stub probes real. Today `firmwareDateOK()` and `hsaOverrideViable()` return unconditional `UnknownBool`, so the Phase-10 `rocm_readiness` badge stays `unknown` even on a live ROCm host — leaving the DET-04 readiness fields permanently UNSET and the DASH-06 SC#1 "non-unknown readiness badge" sub-clause unmet.

This plan adds a fixed-arg `rpm` firmware-date probe + a detect-local floor/deny policy seam in `gpu_amd.go`, rewrites the two stub bodies in `readiness_rocm.go`, threads the new source facts through `computeROCmReadiness`, and adds table tests — all mirroring existing in-tree idioms (`igpuGfxID`/`runTool`, `kernelMeetsROCmFloor`/`compareVersionSegments`, `kernelFloorOK`/`rocminfoGfx1151`).

Purpose: Close the rocm_readiness detect-probe gap (the v1.1 milestone-audit functional follow-up) while preserving the no-false-green discipline (D-08) so off-hardware the fields stay honestly UNSET and the byte-frozen detect golden is unperturbed.
Output: Two real probes + extended compute signature + four updated test call sites + two new table tests + one extended regression test.
</objective>

<artifacts_this_phase_produces>
NEW symbols introduced by this plan (exclude from source-grounding drift checks):
- `firmwareDateProbe() Str` — gpu_amd.go (runTool rpm host probe)
- `firmwareDatePolicyOK(date string) bool` — gpu_amd.go (floor/deny compare seam)
- `isYYYYMMDD(s string) bool` — gpu_amd.go (8-digit all-digit parse guard)
- `rocmFirmwareFloor` const = "20260110" — gpu_amd.go
- `rocmFirmwareDeny` var = []string{"20251125"} — gpu_amd.go
- Extended signature: `computeROCmReadiness(gfxID Str, kernel Str, rocmPresent Bool, firmwareDate Str, resolvedImage string) ROCmReadiness`
- Rewritten bodies: `firmwareDateOK(date Str) Bool`, `hsaOverrideViable(gfxID Str, rocmPresent Bool) Bool` (signatures change from zero-arg)
- `TestFirmwareDateOK`, `TestHSAOverrideViable` — readiness_rocm_test.go

NO new HostProfile/ROCmReadiness fields, NO schema bump (stays 2), NO golden re-freeze (D-04).
</artifacts_this_phase_produces>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-address-v1-1-tech-debt-rocm-readiness-detect-probes-doc-reco/11-CONTEXT.md
@.planning/phases/11-address-v1-1-tech-debt-rocm-readiness-detect-probes-doc-reco/11-RESEARCH.md
@.planning/phases/11-address-v1-1-tech-debt-rocm-readiness-detect-probes-doc-reco/11-PATTERNS.md

# Source files (analogs + targets)
@internal/detect/gpu_amd.go
@internal/detect/readiness_rocm.go
@internal/detect/detect.go
@internal/detect/readiness_rocm_test.go
@internal/detect/value.go
@internal/preflight/rocm-policy.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add firmware-date probe + floor/deny policy seam in gpu_amd.go</name>
  <files>internal/detect/gpu_amd.go</files>
  <read_first>
    - internal/detect/gpu_amd.go — runTool (lines 235-250, fixed-arg bounded exec), capRaw (252-258), maxToolOutput const (~19), igpuGfxID probe shape (~424), the rocmKernelFloorTarget const + kernelMeetsROCmFloor (293-307), compareVersionSegments + splitNumericSegments (309-354) — these are the EXACT analogs to copy
    - internal/preflight/rocm-policy.json — the authoritative policy VALUES to duplicate detect-side: firmwareFloor 20260110, firmwareDeny ["20251125"]
    - internal/detect/value.go — Str / KnownStr / UnknownStr typed-Optional constructors
    - 11-RESEARCH.md Pattern 1 + Pattern 2 (firmware probe mechanics, live-verified `rpm -q --qf '%{VERSION}' linux-firmware` → `20260519`); Pitfall 2 (isYYYYMMDD guard before compare); the import-cycle confirmation (preflight imports detect → detect MUST NOT import preflight → duplicate values)
    - 11-PATTERNS.md "Analog A" + "Analog B"
  </read_first>
  <behavior>
    - firmwareDateProbe(): runTool absent/non-zero → UnknownStr(reason, capRaw(out)); output not a parseable YYYYMMDD → UnknownStr(reason, capRaw(out)); a clean 8-digit stamp → KnownStr(date, source)
    - isYYYYMMDD("20260519") == true; isYYYYMMDD("") == false; isYYYYMMDD("rawhide-snap") == false; isYYYYMMDD("2026051") == false (len != 8); isYYYYMMDD("2026051a") == false (non-digit)
    - firmwareDatePolicyOK("20251125") == false (denylist match); firmwareDatePolicyOK("20251231") == false (< floor 20260110); firmwareDatePolicyOK("20260110") == true (== floor); firmwareDatePolicyOK("20260519") == true (clear)
  </behavior>
  <action>
    Add to gpu_amd.go (the seam — the ONLY detect file allowed to carry firmware/kernel/image literals), placing the new helpers beside the kernel-floor precedent (~line 293-307):

    1. `firmwareDateProbe() Str` — mirror igpuGfxID exactly: call the EXISTING `runTool("rpm", "-q", "--qf", "%{VERSION}", "linux-firmware")` (fixed args, no shell, package name is a constant literal — no injection surface, ASVS V5). On `!ok` return `UnknownStr` with a reason like "rpm query for linux-firmware failed or rpm absent" and `capRaw(out)`. TrimSpace the output; if `!isYYYYMMDD(date)` return `UnknownStr` with reason "linux-firmware version not a parseable YYYYMMDD stamp" + `capRaw(out)` (no-false-green per D-01). Otherwise return `KnownStr(date, "rpm -q --qf %{VERSION} linux-firmware")`.

    2. `isYYYYMMDD(s string) bool` — guard: `len(s) == 8` AND every byte in `'0'..'9'`. This prevents a rawhide/snapshot VERSION from reaching the numeric compare (Pitfall 2).

    3. Two consts beside `rocmKernelFloorTarget` (DUPLICATE the values from rocm-policy.json — do NOT import preflight, that is a forbidden cycle per D-02 / RESEARCH import-cycle confirmation; this mirrors the established kernel-floor + image-tag duplication precedent): `const rocmFirmwareFloor = "20260110"` and `var rocmFirmwareDeny = []string{"20251125"}`. Add a comment citing CLAUDE.md §"Version Compatibility" (linux-firmware ≥ 20260110, avoid 20251125 — breaks ROCm) and that these mirror preflight's rocm-policy.json.

    4. `firmwareDatePolicyOK(date string) bool` — mirror kernelMeetsROCmFloor + rocmImagePolicyOK verdict ordering: iterate `rocmFirmwareDeny`, on match return false (denylist wins); then `return compareVersionSegments(date, rocmFirmwareFloor) >= 0`. Return a bare bool (the Bool wrapping happens in readiness_rocm.go's firmwareDateOK, per PATTERNS Analog B note). REUSE the existing `compareVersionSegments` — do NOT roll a new comparator.

    Do NOT add a firmware literal anywhere in readiness_rocm.go. No new deps. No fenced code in production beyond these helper bodies.
  </action>
  <verify>
    <automated>cd /home/oliverh/repos/github/MatrixMagician/VillaStraylight && go build ./internal/detect/ && go vet ./internal/detect/</automated>
  </verify>
  <acceptance_criteria>
    - gpu_amd.go compiles + vets clean: `go build ./internal/detect/ && go vet ./internal/detect/` exit 0
    - `grep -n 'func firmwareDateProbe' internal/detect/gpu_amd.go` matches
    - `grep -n 'func firmwareDatePolicyOK' internal/detect/gpu_amd.go` matches
    - `grep -n 'func isYYYYMMDD' internal/detect/gpu_amd.go` matches
    - `grep -n 'rocmFirmwareFloor\|rocmFirmwareDeny' internal/detect/gpu_amd.go` matches both consts
    - firmwareDateProbe uses the existing runTool (fixed args): `grep -n 'runTool("rpm"' internal/detect/gpu_amd.go` matches; NO `sh -c` / `exec.Command(.*sh` introduced
    - NO firmware-date literal leaked into readiness_rocm.go: `grep -E '2026[0-9]{4}|2025[0-9]{4}' internal/detect/readiness_rocm.go` returns nothing
    - detect does NOT import preflight: `grep -n 'internal/preflight' internal/detect/*.go` returns nothing
  </acceptance_criteria>
  <done>firmwareDateProbe + firmwareDatePolicyOK + isYYYYMMDD + the two policy consts exist in gpu_amd.go, build + vet clean, with no firmware literal in readiness_rocm.go and no detect→preflight import.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make firmwareDateOK + hsaOverrideViable real; thread facts through computeROCmReadiness + Probe()</name>
  <files>internal/detect/readiness_rocm.go, internal/detect/detect.go, internal/detect/readiness_rocm_test.go</files>
  <read_first>
    - internal/detect/readiness_rocm.go — the two stub bodies (firmwareDateOK ~52-58, hsaOverrideViable ~60-65), the computeROCmReadiness signature + body (~20-28), and the kernelFloorOK (~45-50) / rocminfoGfx1151 (~34-39) templates to mirror exactly
    - internal/detect/detect.go — the single computeROCmReadiness call site (line 36); gpu.gfxID + gpu.rocmPresent already on gpuInfo (rocmPresent populated by probeGPU)
    - internal/detect/readiness_rocm_test.go — ALL FOUR existing computeROCmReadiness call sites (TestComputeROCmReadinessOffHardware:10, TestKernelFloorKnownBelowFloor:35, TestKernelFloorUnknownKernel:51, TestImagePolicyOK:69 AND :74) — all use the 3-arg signature and MUST be updated to 5-arg or the package will not compile
    - internal/detect/value.go — KnownBool / UnknownBool
    - 11-RESEARCH.md "Threading the facts" + Pattern 3 (HSA pure derivation, NO os.Getenv) + Assumptions A1 (gate HSA Known-ness on gfxID.Known, NOT rocmPresent — rocmPresent is always Known so it cannot preserve off-hardware UNSET); Pitfall 4 (never read HSA_OVERRIDE_GFX_VERSION)
    - 11-PATTERNS.md readiness_rocm.go + detect.go + test sections (exact table cases)
  </read_first>
  <behavior>
    TestFirmwareDateOK (call firmwareDateOK directly):
    - KnownStr("20260519") → Known && Value==true
    - KnownStr("20260110") (== floor) → Known && Value==true
    - KnownStr("20251125") (denylist) → Known && Value==false
    - KnownStr("20251231") (sub-floor, not denied) → Known && Value==false
    - UnknownStr(...) → Known==false (UNSET)
    TestHSAOverrideViable (call hsaOverrideViable directly):
    - KnownStr("gfx1151") + KnownBool(true) → Known && Value==true
    - KnownStr("gfx1100") + KnownBool(true) → Known && Value==false (non-gfx1151)
    - KnownStr("gfx1151") + KnownBool(false) → Known && Value==false (no substrate)
    - UnknownStr(...) gfx-id + any rocmPresent → Known==false (UNSET, no-false-green)
    TestComputeROCmReadinessOffHardware (extended): with gfx-id Unknown + UnknownStr firmware date, both FirmwareDateOK.Known==false AND HSAOverrideViable.Known==false (regression guard, D-06).
  </behavior>
  <action>
    In readiness_rocm.go:
    1. Replace `firmwareDateOK()` stub with `firmwareDateOK(date Str) Bool` — mirror kernelFloorOK exactly: `if !date.Known { return UnknownBool("firmware date not probed (rpm absent or unparseable)", date.Raw) }`; else `return KnownBool(firmwareDatePolicyOK(date.Value), "firmware date vs floor/denylist")`. NO firmware literal in this file (firmwareDatePolicyOK is the gpu_amd.go seam).
    2. Replace `hsaOverrideViable()` stub with `hsaOverrideViable(gfxID Str, rocmPresent Bool) Bool` — pure derivation, NO I/O, NO os.Getenv (D-03 / Pitfall 4): `if !gfxID.Known { return UnknownBool("HSA override viability unevaluable (gfx id not enumerated)", gfxID.Raw) }`; else `return KnownBool(gfxID.Value == "gfx1151" && rocmPresent.Value, "gfx1151 + rocm substrate present (HSA 11.5.1 applies)")`. Gate Known-ness on gfxID.Known (per Assumptions A1 — rocmPresent is always Known so it cannot preserve off-hardware UNSET).
    3. Extend computeROCmReadiness to `(gfxID Str, kernel Str, rocmPresent Bool, firmwareDate Str, resolvedImage string)`: wire `HSAOverrideViable: hsaOverrideViable(gfxID, rocmPresent)` and `FirmwareDateOK: firmwareDateOK(firmwareDate)`; leave KernelFloorOK / RocminfoGfx1151 / ImagePolicyOK untouched.
    4. Update the now-inaccurate stub doc comments (the "not probed … advisory only off-hardware" prose on the two functions) to describe the live probe + the off-hardware UNSET path.

    In detect.go: change the line-36 call to `computeROCmReadiness(gpu.gfxID, kernel, gpu.rocmPresent, firmwareDateProbe(), resolvedROCmImage())` — keep ALL I/O in gpu_amd.go (firmwareDateProbe does the exec); Probe() only wires it, matching how it already passes gpu.gfxID / resolvedROCmImage().

    In readiness_rocm_test.go:
    5. Update ALL FOUR existing computeROCmReadiness call sites to the 5-arg signature (add a rocmPresent Bool + an UnknownStr firmware date — for the off-hardware/kernel tests pass `KnownBool(false, "test")` or `rocmPresent()` and an UnknownStr firmware date so existing assertions hold). This is mechanical-but-mandatory.
    6. Add `TestFirmwareDateOK` (table) calling firmwareDateOK directly per the behavior block.
    7. Add `TestHSAOverrideViable` (table) calling hsaOverrideViable directly per the behavior block.
    8. Extend TestComputeROCmReadinessOffHardware so its existing FirmwareDateOK/HSAOverrideViable UNSET assertions still hold under the new signature (pass gfx-id Unknown + UnknownStr firmware date).

    No fenced code in this action; copy the in-tree analog bodies near-verbatim.
  </action>
  <verify>
    <automated>cd /home/oliverh/repos/github/MatrixMagician/VillaStraylight && go test ./internal/detect/ -run 'TestFirmwareDateOK|TestHSAOverrideViable|TestComputeROCmReadinessOffHardware|TestKernelFloor|TestImagePolicyOK' -x && go test ./internal/detect/...</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/detect/...` passes (all four updated call sites compile; both new tests + the extended off-hardware test green)
    - readiness_rocm.go: firmwareDateOK no longer returns an unconditional UnknownBool — `grep -n 'func firmwareDateOK(date Str) Bool' internal/detect/readiness_rocm.go` matches AND `grep -n 'firmwareDatePolicyOK' internal/detect/readiness_rocm.go` matches
    - readiness_rocm.go: hsaOverrideViable no longer returns an unconditional UnknownBool — `grep -n 'func hsaOverrideViable(gfxID Str, rocmPresent Bool) Bool' internal/detect/readiness_rocm.go` matches
    - NO os.Getenv / HSA_OVERRIDE_GFX_VERSION read in detect: `grep -rn 'os.Getenv\|HSA_OVERRIDE_GFX_VERSION' internal/detect/` returns nothing
    - detect.go threads the new facts: `grep -n 'computeROCmReadiness(gpu.gfxID, kernel, gpu.rocmPresent, firmwareDateProbe()' internal/detect/detect.go` matches
    - `grep -c 'func TestFirmwareDateOK\|func TestHSAOverrideViable' internal/detect/readiness_rocm_test.go` == 2
    - NO firmware literal in readiness_rocm.go: `grep -E '2026[0-9]{4}|2025[0-9]{4}' internal/detect/readiness_rocm.go` returns nothing
  </acceptance_criteria>
  <done>Both probes are real and table-tested; computeROCmReadiness threads the new facts; Probe() wires firmwareDateProbe() + gpu.rocmPresent; all four legacy call sites updated; `go test ./internal/detect/...` green; no os.Getenv/firmware-literal leakage.</done>
</task>

<task type="auto">
  <name>Task 3: Golden + cross-package regression guard (detect golden byte-identical, badge fold green)</name>
  <files>(verification only — no source edits expected)</files>
  <read_first>
    - cmd/villa/detect_test.go — fixtureProfile() (lines 18-50, sets FirmwareDateOK/HSAOverrideViable to explicit UnknownBool) + TestJSONGolden (line ~55). This PROVES the golden is fixture-driven, NOT detect.Probe()-derived (D-04 / Pitfall 1) — the new probes cannot perturb it
    - cmd/villa/testdata/detect.golden.json — must stay byte-identical
    - internal/status/status.go foldROCmReadiness (~line 161) + internal/status/status_test.go TestFoldROCmReadiness (~279-293) — the unchanged worst-wins fold that flips the badge to `ready` automatically once leaves go Known
    - 11-RESEARCH.md "Existing consumers that flip automatically" + Pitfall 1 (do NOT re-freeze the golden)
  </read_first>
  <action>
    Pure regression verification — there should be NO source edits in this task (if a test fails it indicates a Task 1/2 defect to fix there, NOT a golden re-freeze).

    1. Confirm the detect golden is byte-identical WITHOUT -update: run TestJSONGolden and confirm pass. If it FAILS, the fix is in Task 1/2 (a probe leaked into the fixture path) — do NOT run with -update and do NOT re-freeze the golden (D-04 forbids it; the golden is fixture-driven so a real failure means a wiring bug).
    2. Confirm the Phase-10 badge fold is unchanged-and-green: run the status fold tests.
    3. Run the full per-wave suite (detect + cmd/villa + status + recommend) green.
    4. Run the full suite `go test ./...` green as the phase gate.

    Record in the SUMMARY: the golden stayed byte-identical (no -update used), and that the on-hardware badge=ready proof is a MANUAL UAT criterion (D-06), not automatable here.
  </action>
  <verify>
    <automated>cd /home/oliverh/repos/github/MatrixMagician/VillaStraylight && go test ./cmd/villa/ -run TestJSONGolden && go test ./internal/status/ -run TestFoldROCmReadiness && go test ./internal/detect/... ./cmd/villa/... ./internal/status/... ./internal/recommend/... && go test ./...</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./cmd/villa/ -run TestJSONGolden` passes WITHOUT any `-update` flag (golden byte-identical, D-04)
    - `git diff --stat cmd/villa/testdata/detect.golden.json` shows ZERO changes
    - `go test ./internal/status/ -run TestFoldROCmReadiness` passes (badge fold unchanged + green)
    - `go test ./...` exits 0 (phase gate)
    - NO source file under cmd/villa/ or internal/status/ or internal/recommend/ was modified by this plan (`git diff --name-only` lists only internal/detect/* files)
  </acceptance_criteria>
  <done>detect.golden.json is byte-identical with no -update; badge fold + full suite green; manual on-hardware UAT criterion recorded in SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host tool → detect | `rpm` stdout (`%{VERSION}`) is untrusted external tool output crossing into the probe |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Tampering / EoP | firmwareDateProbe shelling to rpm | mitigate | Use the EXISTING fixed-arg `runTool("rpm", "-q", "--qf", "%{VERSION}", "linux-firmware")` — never `sh -c`; the package name is a constant literal, no user-controlled arg → no command-injection surface (ASVS V5). |
| T-11-02 | Denial of Service | untrusted rpm stdout size | accept | `runTool` already bounds output to `maxToolOutput` (8 KiB) + `capRaw` bounds the raw provenance field; inherited, no new control needed. |
| T-11-03 | Data integrity (false-green) | firmware/HSA verdict | mitigate | No-false-green (D-08): unparseable/absent firmware → `UnknownBool` (UNSET); gfx-id Unknown → HSA UNSET. `isYYYYMMDD` guard rejects non-date stamps before the numeric compare so a rawhide VERSION degrades to UNSET, never a fabricated false. |
| T-11-04 | Information disclosure | HSA env secret | mitigate | `hsaOverrideViable` derives from host facts only; it MUST NOT read/log `os.Getenv("HSA_OVERRIDE_GFX_VERSION")` (Pitfall 4). Verified by the no-os.Getenv grep gate in Task 2 acceptance. |
| T-11-SC | Tampering | npm/pip/cargo installs | n/a | No package-manager installs in this plan — zero new dependencies (stdlib + in-tree helpers only). |

No `security_block_on: high` findings: the probe is read-only, fixed-arg, bounded, and adds no network/auth/secret surface.
</threat_model>

<verification>
- Off-hardware (CI/unit, D-06): `go test ./internal/detect/...` green — probe wiring + no-false-green (Unknown source → UNSET; Known-good → KnownBool(true); Known-bad denylist/sub-floor/non-gfx1151 → KnownBool(false)).
- Golden regression (D-04): `go test ./cmd/villa/ -run TestJSONGolden` passes with NO -update; `git diff` of detect.golden.json is empty.
- Badge fold (DASH-06 SC#1, off-hardware): `go test ./internal/status/ -run TestFoldROCmReadiness` green.
- Phase gate: `go test ./...` green.
- **On-hardware UAT (MANUAL, D-06 — host-dependent, gfx1151 ROCm host only):** with `backend=rocm`, `villa detect --json` shows `firmware_date_ok:true` + `hsa_override_viable:true`, and `villa status` / dashboard ROCm-readiness badge reads `ready` (not `unknown`). UAT-gated like Phases 8–10 — NOT an automated task.
</verification>

<success_criteria>
- `firmwareDateOK()` / `hsaOverrideViable()` are real probes (no longer unconditional UnknownBool), proven by TestFirmwareDateOK / TestHSAOverrideViable.
- Off-hardware fields stay UNSET (no-false-green), proven by the extended TestComputeROCmReadinessOffHardware + the byte-identical golden.
- `hostProfileSchemaVersion` stays 2; detect.golden.json byte-identical; no new fields (D-04).
- Firmware literals stay behind the gpu_amd.go seam; no detect→preflight import; no os.Getenv of the HSA env.
- Full suite green.
</success_criteria>

<output>
Create `.planning/phases/11-address-v1-1-tech-debt-rocm-readiness-detect-probes-doc-reco/11-01-SUMMARY.md` when done. Record: the golden stayed byte-identical (no -update), and the on-hardware badge=ready proof is a deferred MANUAL UAT criterion (D-06).
</output>
