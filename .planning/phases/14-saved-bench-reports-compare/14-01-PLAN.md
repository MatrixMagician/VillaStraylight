---
phase: 14-saved-bench-reports-compare
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/benchstore/benchstore.go
  - internal/benchstore/benchstore_test.go
  - internal/benchstore/testdata/record.golden
autonomous: true
requirements: [BENCH-03, BENCH-04]
must_haves:
  truths:
    - "The on-disk saved-bench-report JSONL record format is frozen (schema_version=1) by a byte-for-byte golden BEFORE any live writer path exists"
    - "pp and tg tok/s are stored as SEPARATE record fields end-to-end — there is no blended tok/s key anywhere in the record"
    - "VoidExhausted/Reason residency-void state round-trips write->read without loss"
    - "Two saved reports are Comparable iff model AND quant AND host gfx id all match; backend is allowed to differ (cross-backend compare is the intended use)"
    - "An UNKNOWN host fingerprint field yields 'not comparable', never a false-equal"
    - "internal/benchstore imports NEITHER internal/inference NOR internal/detect (TestSeamGrepGate stays green)"
  artifacts:
    - path: "internal/benchstore/benchstore.go"
      provides: "SavedReport record + savedReportSchemaVersion const, Fingerprint type, Comparable()/Compare() guard, Append()/Load() pure cores, Deps seam, benchReportsPath() helper, assertInsideDir guard copy"
      min_lines: 150
      exports: ["SavedReport", "Fingerprint", "Deps", "Append", "Load", "Comparable", "Compare", "CompareResult", "NotComparable"]
    - path: "internal/benchstore/benchstore_test.go"
      provides: "schema-version stability, no-blended-key grep, void round-trip, comparable matrix, unknown-host, per-metric delta, append-grows-via-seam tests"
      min_lines: 120
    - path: "internal/benchstore/testdata/record.golden"
      provides: "ONE frozen schema-1 JSONL saved-report record — the on-disk contract"
      contains: "schema_version"
  key_links:
    - from: "internal/benchstore/benchstore.go Append"
      to: "Deps.AppendLine"
      via: "injected func field (pure core marshals, seam does byte I/O)"
      pattern: "Deps\\.AppendLine|d\\.AppendLine"
    - from: "internal/benchstore/benchstore.go Compare"
      to: "Comparable(a.Fingerprint, b.Fingerprint)"
      via: "comparability guard gates delta computation"
      pattern: "Comparable\\("
---

<objective>
Create the pure `internal/benchstore` core that owns the on-disk saved-bench-report contract: the `SavedReport` JSONL record type with `savedReportSchemaVersion = 1`, the pp/tg-separate fields, the `VoidExhausted`/`Reason` residency-void state, the comparability `Fingerprint` + guard, the `Append()`/`Load()` marshal/parse cores behind an injected `Deps` seam, and the XDG path + 0600/0700 traversal-guarded write helper. Freeze the record format with its own byte-for-byte golden (schema 1) in THIS wave, BEFORE any live writer path exists, so the on-disk format can never silently drift into a migration.

Purpose: BENCH-03 requires a versioned saved report (pp/tg separate, residency-void recorded); BENCH-04 requires a comparability guard. Both rest on this contract, which the ROADMAP mandates freezing first.
Output: `internal/benchstore/{benchstore.go,benchstore_test.go}` + `internal/benchstore/testdata/record.golden`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-saved-bench-reports-compare/14-RESEARCH.md
@.planning/phases/14-saved-bench-reports-compare/14-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define the SavedReport contract + Fingerprint + schema_version=1 + JSONL marshal/parse, and freeze the record golden</name>
  <files>internal/benchstore/benchstore.go, internal/benchstore/benchstore_test.go, internal/benchstore/testdata/record.golden</files>
  <read_first>
    - internal/benchstore/benchstore.go (the file being created — read after first write if iterating)
    - internal/detect/profile.go (lines 1-60: hostProfileSchemaVersion const + SchemaVersion-as-LAST-field append-only discipline — the exact pattern to clone for savedReportSchemaVersion)
    - internal/bench/bench.go (lines 107-155: Stats MedianPP/StddevPP/MedianTG/StddevTG + ABResult DeltaPP/DeltaTG + Result.VoidExhausted/Reason — the pp/tg-separate shapes this record persists)
    - cmd/villa/bench.go (lines 50-53: `benchPrompt` is a FIXED reproducibility constant — not user content; lines 326-333: BenchSpec construction wiring `Prompt: benchPrompt`; lines 379-416: the `bench --json` emission shape — `benchEntry`/`benchConditions` (reps/warmup/n_predict/seed/temp — NOTE: `bench --json` does NOT expose a `prompt` key) + `benchSide`/`benchAB` JSON tags `prompt_per_sec`/`predicted_per_sec`/`delta_prompt_per_sec`/`delta_predicted_per_sec` — re-declare these tags in benchstore types so on-disk numbers match `bench --json` byte-for-byte)
    - cmd/villa/bench_test.go (lines 463-498: golden freeze with the shared `*update` flag + the "no blended tok/s key" grep assertion to clone)
    - internal/detect/profile_test.go (lines 15-17: schema-version const assertion to clone)
  </read_first>
  <behavior>
    - Test: `savedReportSchemaVersion == 1` (const assert, mirroring detect/profile_test.go).
    - Test: marshaling a fully-populated SavedReport (single mode) then re-parsing yields an equal struct (round-trip), with VoidExhausted=true and Reason preserved.
    - Test: the marshaled record JSON contains `prompt_per_sec`, `predicted_per_sec`, `void_exhausted`, `schema_version` and contains NEITHER `tok_per_sec` NOR `tokens_per_sec` NOR `throughput` (no-blended grep over the marshaled bytes).
    - Test: `schema_version` is the LAST field emitted (byte-frozen golden encodes the field order).
    - Golden: `internal/benchstore/testdata/record.golden` is byte-stable — running the freeze test with `-update` writes it, re-running without `-update` matches.
  </behavior>
  <action>
    Create package `benchstore` in `internal/benchstore/benchstore.go`. Declare `const savedReportSchemaVersion = 1` with a doc comment stating it is the on-disk contract self-version, bumped ONLY on an incompatible change, new fields appended ABOVE `SchemaVersion` (clone the detect/profile.go discipline). Imports are stdlib ONLY (`encoding/json`, `os`, `bufio`, `bytes`, `path/filepath`, `strings`, `fmt`, `time`) — import NEITHER `internal/inference` NOR `internal/detect` NOR `cmd/villa` (Pitfall 2 / seam gate).

    Define exported types with explicit `json:` tags (re-declare, do NOT import cmd/villa benchSide/benchAB):
    - `SavedReport`: fields in this order — `CapturedAt string json:"captured_at"` (RFC3339), `Mode string json:"mode"` ("single"|"ab"), `Spec SavedSpec json:"spec"`, `Single *SavedSide json:"single,omitempty"`, `AB *SavedAB json:"ab,omitempty"`, `VoidExhausted bool json:"void_exhausted"`, `Reason string json:"reason,omitempty"`, `Fingerprint Fingerprint json:"fingerprint"`, and LAST `SchemaVersion int json:"schema_version"`.
    - `SavedSpec`: the reproducible spec — `Prompt string json:"prompt"`, `Reps int json:"reps"`, `Warmup int json:"warmup"`, `NPredict int json:"n_predict"`, `Seed int json:"seed"`, `Temp float64 json:"temp"`, `ABTarget string json:"ab_target,omitempty"`. DECISION (resolve now — the record golden frozen in THIS task bakes this in permanently; reversal would be a schema migration): PERSIST the `Prompt` field carrying the FIXED reproducibility constant `bench.BenchSpec.Prompt` (the `benchPrompt` literal at cmd/villa/bench.go:50-53 — e.g. "Summarize the water cycle in three concise sentences."). This is the ROADMAP mandate "persist the full BenchSpec + env fingerprint" and is NOT a no-content violation: `benchPrompt` is a fixed, in-repo constant, never user/response text (T-14-02 covers prompt/response CONTENT, not this fixed benchmark stimulus). Note `bench --json` itself does NOT emit a `prompt` key (its `benchConditions` is reps/warmup/n_predict/seed/temp only) — the saved record is a SUPERSET of the wire JSON for reproducibility, which is intended (the on-disk store reproduces the run; `bench --json` reports it). The frozen `record.golden` MUST therefore contain the `"prompt"` key with the fixed constant value.
    - `SavedSide`: `Backend string json:"backend"`, `PromptPerSec float64 json:"prompt_per_sec"`, `PromptStddev float64 json:"prompt_per_sec_stddev"`, `PredictedPerSec float64 json:"predicted_per_sec"`, `PredictedStddev float64 json:"predicted_per_sec_stddev"`, `Kept int json:"kept"`, `Void int json:"void"` (byte-identical tags to cmd/villa benchSide).
    - `SavedAB`: `From string json:"from"`, `To string json:"to"`, `A SavedSide json:"a"`, `B SavedSide json:"b"`, `DeltaPromptPerSec float64 json:"delta_prompt_per_sec"`, `DeltaPredictedPerSec float64 json:"delta_predicted_per_sec"`.
    - `Fingerprint`: `Model string json:"model"`, `Quant string json:"quant"`, `Ctx int json:"ctx"`, `Backend string json:"backend"`, `HostGfxID string json:"host_gfx_id"`, `KernelVersion string json:"kernel_version,omitempty"` (A2/A4: ctx included as a comparability field, kernel recorded but secondary — see Task 2 for which block).

    Add a constructor or builder `NewSavedReport(...)` (or a plain struct-literal path documented for the cmd tier) that stamps `SchemaVersion: savedReportSchemaVersion`. Add `func Marshal(r SavedReport) ([]byte, error)` returning ONE compact JSON line terminated with `'\n'` (use `json.Marshal` then append `'\n'`; JSONL = one object per line, NOT indented). The record golden is the COMPACT one-line form.

    Write the freeze test in `benchstore_test.go`: declare a package-level `var update = flag.Bool("update", false, "update golden files")` (this package owns its own `*update`; do NOT reference cmd/villa's). Build a deterministic SavedReport (fixed CapturedAt string, e.g. "2026-06-07T12:00:00Z", single mode, VoidExhausted=true, Reason set, fully-populated Fingerprint), marshal it, and golden-compare against `internal/benchstore/testdata/record.golden` with the `*update` write-or-compare idiom cloned from cmd/villa/bench_test.go:463-480. Add the const-assert test and the no-blended grep over the marshaled bytes.
  </action>
  <verify>
    <automated>go test ./internal/benchstore/... -run 'SchemaVersion|RecordGolden|NoBlended|VoidRoundTrip' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `internal/benchstore/benchstore.go` contains `const savedReportSchemaVersion = 1`
    - `internal/benchstore/benchstore.go` defines `type SavedReport struct` whose LAST field is `SchemaVersion int json:"schema_version"`
    - `go test ./internal/benchstore/... -run RecordGolden -count=1` exits 0 (golden created via `-update` first, then matches)
    - `grep -E 'tok_per_sec|tokens_per_sec|throughput' internal/benchstore/testdata/record.golden` returns NO matches (no blended key)
    - `grep -c '"prompt_per_sec"' internal/benchstore/testdata/record.golden` and `grep -c '"predicted_per_sec"' internal/benchstore/testdata/record.golden` are each >= 1 (pp/tg separate)
    - `grep -c '"prompt":' internal/benchstore/testdata/record.golden` is >= 1 — the frozen `schema_version=1` record PERSISTS the fixed `Prompt` reproducibility constant (deterministic decision per ROADMAP "persist the full BenchSpec"); the value is the in-repo `benchPrompt` constant, never user content
    - `grep -RnE 'internal/(inference|detect)' internal/benchstore/benchstore.go` returns NO matches (import purity)
  </acceptance_criteria>
  <done>Record contract type + schema_version=1 frozen by a byte-for-byte golden; pp/tg stored separately; void state round-trips; package imports no inference/detect.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Comparability guard (Comparable/Compare) + Deps seam + Append/Load + XDG path helper with traversal guard</name>
  <files>internal/benchstore/benchstore.go, internal/benchstore/benchstore_test.go</files>
  <read_first>
    - internal/benchstore/benchstore.go (the file being extended — Task 1 output)
    - internal/bench/bench.go (lines 81-105: Deps-of-func-fields; lines 124-135 + 157-175: ABResult/abResult per-metric delta — the separation idiom Compare clones)
    - internal/config/villaconfig.go (lines 22-27: configFileMode 0o600 / configDirMode 0o700; lines 223-240: assertInsideDir clean+abs+filepath.Rel `..` guard — copy this guard shape into benchstore, do NOT import config for it; lines 130-136: LoadVilla returns defaults when file absent — mirror for ReadAll on os.IsNotExist)
    - cmd/villa/model.go (lines 112-124: modelsDir XDG_DATA_HOME -> ~/.local/share -> /var/tmp resolver chain — clone for benchReportsPath)
    - internal/detect/profile.go (lines 33, 50: IGPUGfxID/KernelVersion are typed-Optional Str with .Known — informs the unknown-host sentinel decision, even though benchstore takes plain strings)
  </read_first>
  <behavior>
    - Test (comparable matrix): two Fingerprints with identical Model+Quant+Ctx+HostGfxID but DIFFERENT Backend are Comparable (backend is NOT a blocker — cross-backend compare is the point). Each single-field mismatch (model | quant | host) yields Comparable==false with the differing field named in the returned slice.
    - Test (unknown host): a Fingerprint with HostGfxID=="" (or a defined unknown sentinel) is NOT comparable to any other (even an identical-empty one) — no false-equal.
    - Test (per-metric delta): `Compare(a, b)` on a comparable single-mode pair returns DeltaPP and DeltaTG as TWO separate figures (b.PredictedPerSec - a.PredictedPerSec etc.); a non-comparable pair returns a `NotComparable` result carrying the differing fields and NO numeric delta.
    - Test (Append via seam): `Append(deps, report)` marshals one line and calls `deps.AppendLine` exactly once with bytes ending in `'\n'`; a buffer-backed Deps grows by one line per Append (append-only).
    - Test (Load via seam): `Load(deps)` parses N JSONL lines into []SavedReport via `deps.ReadAll`; a corrupt/unparseable line is skipped (not a panic) and earlier records survive; an absent store (ReadAll returns nil,nil) yields an empty slice and no error.
    - Test (path safety): the `benchReportsPath()` helper resolves under `$XDG_DATA_HOME/villa/` when set (use `t.Setenv("XDG_DATA_HOME", t.TempDir())`); the traversal guard refuses a path resolving outside its dir.
  </behavior>
  <action>
    Extend `internal/benchstore/benchstore.go`.

    Comparability (clone RESEARCH Code Examples / PATTERNS guard): `func Comparable(a, b Fingerprint) (bool, []string)` — append "model" if a.Model != b.Model; "quant" if a.Quant != b.Quant; "ctx" if a.Ctx != b.Ctx (A2 — ctx is a comparability field); "host" if `a.HostGfxID == "" || b.HostGfxID == "" || a.HostGfxID != b.HostGfxID` (UNKNOWN host = not comparable, no false-equal — Pitfall 4). Backend is DELIBERATELY NOT a blocker (A3 — that IS the comparison). Document the backend-allowed decision in the doc comment so the design choice is explicit and golden-tested. Return `(len(diff)==0, diff)`.

    `type CompareResult struct { Comparable bool; DifferingFields []string; DeltaPromptPerSec float64; DeltaPredictedPerSec float64; A *SavedReport; B *SavedReport }` and `type NotComparable struct { DifferingFields []string }` (or fold "not comparable" into CompareResult.Comparable=false — choose ONE; document it). `func Compare(a, b SavedReport) CompareResult`: if `!Comparable(a.Fingerprint, b.Fingerprint)` return a result with Comparable=false + DifferingFields and ZERO deltas (never fabricate). Else compute DeltaPromptPerSec and DeltaPredictedPerSec as SEPARATE figures from the comparable sides' median pp/tg (use Single when mode=="single", else the appropriate AB side — document which side Compare reads; recommend comparing the primary measured side of each report). NEVER a blended delta.

    `Deps` struct of func fields (clone bench.Deps shape): `AppendLine func(line []byte) error`, `ReadAll func() ([]byte, error)`, `Now func() time.Time`. `func Append(d Deps, r SavedReport) error` stamps `r.SchemaVersion`, sets `r.CapturedAt` from `d.Now()` if empty (RFC3339), marshals via Task-1 Marshal, calls `d.AppendLine(line)`. `func Load(d Deps) ([]SavedReport, error)` calls `d.ReadAll`, scans with `bufio.Scanner` over the bytes (bound the buffer; skip-and-continue on a per-line `json.Unmarshal` error — fail closed per line, never panic), returns the parsed slice; nil bytes => empty slice, nil error.

    Path helper (cmd tier will call it via the live Deps in Plan 02, but define it here so the resolver lives with the contract): `func benchReportsPath() string` cloning model.go:modelsDir — `$XDG_DATA_HOME/villa/bench-reports.jsonl` -> `~/.local/share/villa/bench-reports.jsonl` -> `/var/tmp/villa/bench-reports.jsonl`. Carry a LOCAL copy of `assertInsideDir(path, dir string) error` (the config guard shape: clean+abs+filepath.Rel, reject `..`/abs) — do NOT import internal/config for it (config's is unexported; Pitfall 2 / keep purity). Add `const (storeFileMode os.FileMode = 0o600; storeDirMode os.FileMode = 0o700)`.

    Add the matrix/unknown-host/delta/append/load/path tests to `benchstore_test.go` (table-driven). Pure-core tests back `Deps` with a `bytes.Buffer` (AppendLine appends to it, ReadAll returns its bytes) — no XDG touched. The path-safety test uses `t.Setenv("XDG_DATA_HOME", t.TempDir())`.
  </action>
  <verify>
    <automated>go test ./internal/benchstore/... -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `internal/benchstore/benchstore.go` contains `func Comparable(a, b Fingerprint) (bool, []string)`
    - `internal/benchstore/benchstore.go` contains `func Compare(` and `func Append(` and `func Load(`
    - `go test ./internal/benchstore/... -run 'Comparable|UnknownHost|CompareDelta|Append|Load' -count=1` exits 0
    - The comparable test proves: identical model+quant+ctx+host but differing backend => Comparable==true (backend not a blocker)
    - The unknown-host test proves: HostGfxID=="" => Comparable==false (no false-equal)
    - `grep -n 'O_APPEND' internal/benchstore/benchstore.go` OR the live append wiring is deferred to Plan 02 — benchReportsPath + assertInsideDir + 0600/0700 consts present in this file
    - `go vet ./internal/benchstore/...` exits 0
  </acceptance_criteria>
  <done>Comparability guard refuses on model/quant/ctx/host mismatch and on unknown host (no false-equal); backend may differ; Append/Load round-trip through the injected seam; deltas are per-metric; path helper + traversal guard + 0600/0700 present; package stays pure (no inference/detect import).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host filesystem → benchstore store path | the resolved `$XDG_DATA_HOME/villa/bench-reports.jsonl` path must stay inside the XDG dir |
| on-disk JSONL → Load parser | hand-edited / malformed report lines are untrusted input that must fail closed, never panic |

## STRIDE Threat Register (ASVS L1; block on high severity)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Tampering | benchReportsPath / store write path | mitigate | local `assertInsideDir` (clean+abs+filepath.Rel `..` check) guard before any write; store joined under XDG only; `storeFileMode 0o600` / `storeDirMode 0o700` (the live append wiring in Plan 02 enforces the modes; the guard + consts ship here) |
| T-14-02 | Information Disclosure | record fields | mitigate | persist ONLY numeric timings + reproducible spec + fingerprint + the FIXED `benchPrompt` constant — NO user prompt text and NO model response content; the persisted `Prompt` is an in-repo benchmark constant (not user data); 0600 file keeps it owner-only |
| T-14-03 | Denial of Service | Load JSONL parser | mitigate | `bufio.Scanner` with a bounded buffer; per-line `json.Unmarshal` error is skipped-and-continued (fail closed per line), a corrupt/partial trailing line never panics or poisons earlier records |
| T-14-04 | Spoofing/Integrity | comparability guard | mitigate | UNKNOWN host fingerprint field => NOT comparable (no false-equal misleading delta) — preserves the no-false-green posture |
| T-14-SC | Tampering | npm/pip/cargo installs | accept | no package installs in this phase — stdlib only (`encoding/json`/`os`/`bufio`), `go.mod` unchanged; RESEARCH Package Legitimacy Audit = N/A |
</threat_model>

<verification>
- `go test ./internal/benchstore/... -count=1` green (schema-version, golden, no-blended, void round-trip, comparable matrix, unknown-host, per-metric delta, append/load, path safety).
- `go test ./internal/inference -run SeamGrepGate -count=1` stays green (benchstore leaks no backend marker / imports no inference/detect).
- `go vet ./internal/benchstore/...` clean.
- The record golden `internal/benchstore/testdata/record.golden` exists and is byte-stable (the on-disk contract, frozen BEFORE any writer).
</verification>

<success_criteria>
- `savedReportSchemaVersion == 1`; the record format is golden-frozen before any live writer (BENCH-03 contract locked).
- pp and tg are separate record fields; no blended key in the golden (BENCH-03).
- `VoidExhausted`/`Reason` round-trip (BENCH-03 residency-void recorded).
- `Comparable` blocks on model/quant/ctx/host mismatch and on unknown host; permits a backend difference (BENCH-04 guard).
- `internal/benchstore` imports neither `internal/inference` nor `internal/detect`; `TestSeamGrepGate` green.
</success_criteria>

<output>
Create `.planning/phases/14-saved-bench-reports-compare/14-01-SUMMARY.md` when done.
</output>
