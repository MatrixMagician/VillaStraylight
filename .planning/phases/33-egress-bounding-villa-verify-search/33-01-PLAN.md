---
phase: 33-egress-bounding-villa-verify-search
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - cmd/villa/verify_search.go
  - cmd/villa/verify_search_test.go
  - internal/websafe/ssrf_test.go
autonomous: true
requirements: [PRIV-08]
must_haves:
  truths:
    - "The pure verdict core maps probe outcomes to PASS / FAIL / REJECT with the negative control asserted FIRST"
    - "A canary still reachable under the bound is a FAIL (never a fabricated PASS)"
    - "A canary already unreachable unguarded, or an unreachable allowlist, is a REJECT (honest infra-fail), not a PASS or FAIL"
    - "The curl-exit classifier maps 6/7/28 to blocked, exit 0 to reachable, any other to REJECT"
    - "A planted-injection page comes back stripped + fenced + flagged via the shipped websafe guard (in-process, no network)"
    - "SSRF internal-host cases (169.254.169.254, 127.0.0.1, localhost, villa-*) are rejected via the shipped ssrf.go guard"
  artifacts:
    - path: "cmd/villa/verify_search.go"
      provides: "searchProof 3-state verdict, evalSearchVerify pure core, classifySearchProbe, in-process family drivers"
      contains: "func evalSearchVerify"
    - path: "cmd/villa/verify_search_test.go"
      provides: "truth-table tests for the pure core incl. the inverse-framing trap + REJECT cases, classifier table, family (b) in-process test, and the pure-core secretQueryBlocked FAIL case (the REAL family-(d) driver + TestSearchSecretQuery + live wiring land in Plan 02)"
      contains: "func TestEvalSearchVerify"
    - path: "internal/websafe/ssrf_test.go"
      provides: "explicit internal-host SSRF case (family c) if not already covered"
      contains: "169.254.169.254"
  key_links:
    - from: "cmd/villa/verify_search.go"
      to: "internal/websafe/websafe.go"
      via: "NewLoader(Deps{Client}, DefaultBounds()).Load(...) asserts Page.Verdict + fenced Content (family b)"
      pattern: "websafe\\.NewLoader"
    - from: "cmd/villa/verify_search.go"
      to: "internal/websafe/ssrf.go"
      via: "SafeClient(DefaultBounds()) refuses an internal URL (family c live-wiring assertion)"
      pattern: "websafe\\.SafeClient"
---

<objective>
Build the testable spine of `villa verify search`: the 3-state PASS/FAIL/REJECT verdict type, the pure inverse-framing `evalSearchVerify` core (negative-control-FIRST), the curl-exit `classifySearchProbe` classifier, and the two in-process assertion families that need no live host — (b) planted-injection page stripped+fenced+flagged via the shipped `internal/websafe` guard, and (c) SSRF internal-host rejection via the shipped `ssrf.go` guard. Every piece here is unit-testable off-hardware; the live netns/nft seam and cobra wiring land in Plan 02.

Purpose: PRIV-08's honesty math — the load-bearing, easy-to-invert truth table that SC2 forbids fabricating — must be pinned by tests FIRST, independent of any live bound. This is the part where a false-green is most dangerous, so it is isolated, pure, and TDD'd.

Output: `cmd/villa/verify_search.go` (pure core + classifier + in-process family drivers, NO cobra/exec yet), `cmd/villa/verify_search_test.go` (truth-table + classifier + family tests), and an explicit internal-host SSRF case in `internal/websafe/ssrf_test.go`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-egress-bounding-villa-verify-search/33-RESEARCH.md
@.planning/phases/33-egress-bounding-villa-verify-search/33-PATTERNS.md
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: 3-state verdict + pure inverse-framing core + curl-exit classifier (RED then GREEN)</name>
  <files>cmd/villa/verify_search.go, cmd/villa/verify_search_test.go</files>
  <read_first>
    - cmd/villa/verify_agent.go:43-103 — `evalAgentVerify` (the pure negative-control-FIRST core; clone its shape, extend PASS/FAIL to PASS/FAIL/REJECT)
    - cmd/villa/verify_agent.go:105-148 — curl-exit constants (`curlExitCouldNotResolve=6`, `curlExitFailedToConnect=7`, `curlExitOperationTimeout=28`) + `classifyEgressProbe` (clone the 6/7/28-vs-everything-else map; here the everything-else branch is REJECT, not FAIL)
    - cmd/villa/install_memory.go:188 — `memoryProof` struct shape (PASS/FAIL only — INSUFFICIENT here; introduce searchProof with 3 states)
    - cmd/villa/preflight.go:42-44 — AUTHORITATIVE exit constants `exitPass=0`, `exitWarn=2`, `exitBlocked=1` (do NOT invent a 4th; the verdict→exit map lands in Plan 02)
    - cmd/villa/verify_agent_test.go:30-282 — `TestEvalAgentVerify`, `TestEvalAgentVerifyNegativeControlFirst`, `TestClassifyEgressProbe`, `TestEvalAgentVerifyInfraErrorFails` (the truth-table test precedents to clone)
    - .planning/phases/33-egress-bounding-villa-verify-search/33-RESEARCH.md — "Code Examples" → the full `evalSearchVerify(...)` skeleton (use the locked order verbatim) + "Pattern 3" (the locked truth-table order) + "Pitfall 1/3"
    - .planning/phases/33-egress-bounding-villa-verify-search/33-PATTERNS.md:58-91 — searchProof/searchStatus enum shape + classifier constants
  </read_first>
  <behavior>
    - Test (REJECT, positive control fails): allowlistReaches returns (false, nil) ⇒ searchReject ("allowlisted upstream not reachable; cannot conduct the proof"). allowlistReaches returns (false, err) ⇒ searchReject.
    - Test (REJECT, negative control already-down — the empty-netns trap): allowlistReaches (true,nil), canaryUnguarded returns (false, nil) ⇒ searchReject ("canary already unreachable unguarded — cannot prove a block"). canaryUnguarded returns (_, err) ⇒ searchReject.
    - Test (FAIL, the inversion trap SC2 forbids): allowlist+canary controls pass, boundThen returns (canaryReachable=true, allowlistReachable=true, nil) ⇒ searchFail ("canary STILL reachable under the bound — block ineffective"). MUST NOT be searchPass.
    - Test (REJECT, blanket block): boundThen returns (canaryReachable=false, allowlistReachable=false, nil) ⇒ searchReject ("the bound blocked the allowlist too — not an allowlist"). boundThen returns (_,_,err) ⇒ searchReject.
    - Test (FAIL, families): controls all pass + bound correct (canary blocked, allowlist reachable), but injectionFlagged returns not-all-true ⇒ searchFail; ssrfBlocked false ⇒ searchFail; secretQueryBlocked (false,nil) or (_,err) ⇒ searchFail.
    - Test (PASS, all hold): controls pass, canary blocked + allowlist reachable under bound, injection stripped+fenced+flagged, ssrf blocked, secret-query contained ⇒ searchPass with a detail string.
    - Test (classifier): classifySearchProbe — exit 6/7/28 ⇒ blocked=true; exit 0 (externalErr==nil) ⇒ reachable (blocked=false, err=nil); any other nonzero / never-started ⇒ err (REJECT-bound), NEVER blocked=true.
    - Test (every non-PASS verdict): detail string is non-empty (refuse-with-remediation invariant).
  </behavior>
  <action>
    Introduce `searchStatus int` with `searchPass`, `searchFail`, `searchReject` iota constants and `searchProof{status searchStatus; detail string}` per 33-PATTERNS.md:58-91. Write the truth-table tests FIRST (RED) in verify_search_test.go cloning the `TestEvalAgentVerify*` shape, encoding every case in the behavior block — the inversion trap (canary-still-reachable-under-bound ⇒ FAIL, never PASS) and the two REJECT classes (env-broken/control-down vs ineffective-blanket) are the load-bearing cases; do not omit them.

    Then implement `evalSearchVerify` GREEN, cloning `evalAgentVerify` (verify_agent.go:62) and following the locked order from 33-RESEARCH.md "Code Examples" EXACTLY: (1) allowlistReaches → REJECT on err-or-false; (2) canaryUnguarded → REJECT on err, REJECT on false (the empty-netns trap — cannot prove a block of something already unreachable); (3) boundThen → FAIL if canary still reachable (the inversion trap — NEVER a fabricated PASS, per D-decision "ineffective block is REJECTED, never a fabricated PASS" SC2; note a still-reachable canary is the ineffective-BLOCK case which is a FAIL security violation, while a broken/blanket env is REJECT), REJECT if allowlist not reachable under bound; (4) families injectionFlagged/ssrfBlocked/secretQueryBlocked → FAIL on any violation; (5) PASS only if all hold. The signature takes injected probe funcs (allowlistReaches/canaryUnguarded/boundThen/injectionFlagged/ssrfBlocked/secretQueryBlocked) so it is host-free — DO NOT call exec or net here.

    Implement `classifySearchProbe(sanityOrControlErr error, externalExitCode int, externalErr error) (blocked bool, err error)` cloning `classifyEgressProbe` (verify_agent.go:131) with the constants `curlExitCouldNotResolve=6`, `curlExitFailedToConnect=7`, `curlExitOperationTimeout=28`: exit 0 ⇒ blocked=false; 6/7/28 ⇒ blocked=true; default ⇒ error (which the caller maps to REJECT), never blocked=true. Every non-PASS searchProof detail carries refuse-with-remediation text naming the next step. Run `go test ./cmd/villa/ -run TestEvalSearchVerify -count=1` and `-run TestClassifySearchProbe` to GREEN.
  </action>
  <verify>
    <automated>go test ./cmd/villa/ -run 'TestEvalSearchVerify|TestClassifySearchProbe' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./cmd/villa/ -run 'TestEvalSearchVerify|TestClassifySearchProbe' -count=1` passes.
    - `grep -c 'func evalSearchVerify' cmd/villa/verify_search.go` returns 1.
    - `grep -c 'searchReject' cmd/villa/verify_search.go` returns >= 2 (REJECT used for both the env-broken and the blanket-block paths).
    - `grep -vE '^[[:space:]]*//' cmd/villa/verify_search.go | grep -cE 'exec\.Command|http\.Get|net\.Dial' ` returns 0 (the pure core does NO host I/O).
    - `grep -c 'func TestEvalSearchVerifyInverse\|STILL reachable under the bound' cmd/villa/verify_search_test.go` returns >= 1 (the inversion trap is explicitly tested).
  </acceptance_criteria>
  <done>The 3-state verdict, the pure inverse-framing core, and the curl-exit classifier exist and are green; the inversion trap and both REJECT classes are pinned by tests; the core does zero host I/O.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: in-process family (b) injection + family (c) SSRF assertions, no network</name>
  <files>cmd/villa/verify_search.go, cmd/villa/verify_search_test.go, internal/websafe/ssrf_test.go</files>
  <read_first>
    - internal/websafe/websafe.go:31-65 — `Deps{Client *http.Client}`, `Page{Content,Source,Title,Verdict}`, `NewLoader(deps,bounds)`, `(*Loader).Load(ctx,urls) []Page`
    - internal/websafe/verdict.go:12-20 — `Verdict{Detected bool; Rules []string}`
    - internal/websafe/fence.go:44-60 — the `[UNTRUSTED_WEB_CONTENT nonce=...]` provenance fence marker format
    - internal/websafe/ssrf.go:52-160 — `DefaultBounds`, `ipRejected(netip.Addr)`, `hostRejected(host)`, `control(network,address,_)`, `SafeClient(Bounds)` (ipRejected/hostRejected/control are UNEXPORTED → family-c unit test lives in package websafe)
    - internal/websafe/ssrf_test.go:21-130 — existing `TestSSRFRejectSet`, `TestHostRejected`, `TestControlConnectTime` (extend with an explicit internal-host case if not present)
    - internal/websafe/websafe_test.go:240-262 — precedent for asserting `Content` contains `UNTRUSTED_WEB_CONTENT` after Load
    - .planning/phases/33-egress-bounding-villa-verify-search/33-RESEARCH.md — "Pattern 5" (family b in-process) + "Pattern 6" (family c) + "Family (b) in-process injection assertion" Code Example
    - .planning/phases/33-egress-bounding-villa-verify-search/33-PATTERNS.md:131-161 — family (b)/(c) analog mapping
  </read_first>
  <behavior>
    - Test family (b): a stub `*http.Client` (custom RoundTripper) returns a planted HTML page containing active markup plus an imperative injection sentence; driving `NewLoader(Deps{Client: stub}, DefaultBounds()).Load(ctx, []url)` returns a Page where Verdict.Detected==true AND len(Verdict.Rules)>0 (flagged), Content does NOT contain the active-markup tag (stripped), and Content contains "UNTRUSTED_WEB_CONTENT nonce=" (fenced). The injectionFlagged driver returns (stripped, fenced, flagged) all true for this input.
    - Test family (c) in package websafe: `ipRejected(netip.MustParseAddr("169.254.169.254"))==true`, `ipRejected(127.0.0.1)==true`, `hostRejected("villa-searxng")==true`, `hostRejected("localhost")==true`, and `control` returns a non-nil SSRF error for an internal connect address. (Add only the cases not already covered by the existing ssrf_test.go tests.)
    - Test family (c) via exported wiring in cmd/villa: `SafeClient(DefaultBounds())` refuses (returns an error, no successful response) when driven against an internal-host URL; the ssrfBlocked driver returns true for the internal-host case.
  </behavior>
  <action>
    In verify_search.go add two in-process family drivers (still no live netns/nft): `injectionFlagged()` builds a `websafe.NewLoader(websafe.Deps{Client: <injected stub>}, websafe.DefaultBounds())`, calls `Load`, and returns `(stripped, fenced, flagged bool)` by inspecting `Page.Verdict.Detected`, `len(Page.Verdict.Rules)`, absence of active markup in `Page.Content`, and presence of the `UNTRUSTED_WEB_CONTENT nonce=` fence (use `strings.Contains`). `ssrfBlocked()` drives `websafe.SafeClient(websafe.DefaultBounds())` against an internal-host URL and returns true iff the request is refused. Keep the planted-injection page string in verify_search_test.go (test input), not in the production file — and do NOT place the literal `UNTRUSTED_WEB_CONTENT nonce=` only inside a comment that an acceptance grep negative-checks (here it is a positive `Contains` arg, so it stays in real code).

    In verify_search_test.go add `TestSearchInjectionFlagged` cloning the websafe_test.go Load assertion (stub RoundTripper returning the planted page; assert detected+rules+stripped+fenced). Add a test that the ssrfBlocked driver returns true for an internal URL via SafeClient.

    In internal/websafe/ssrf_test.go (package websafe), add an explicit internal-host case asserting `ipRejected(netip.MustParseAddr("169.254.169.254"))` and `hostRejected("villa-searxng")` if the existing TestSSRFRejectSet/TestHostRejected do not already cover them — extend, do not duplicate. Run the suites to GREEN.
  </action>
  <verify>
    <automated>go test ./cmd/villa/ -run 'TestSearchInjectionFlagged|TestSearchSSRF' -count=1 && go test ./internal/websafe/ -run 'TestSSRF|TestHostRejected|TestControl' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./cmd/villa/ -run 'TestSearchInjectionFlagged|TestSearchSSRF' -count=1` passes.
    - `go test ./internal/websafe/ -run 'TestSSRF|TestHostRejected|TestControl' -count=1` passes.
    - `grep -c 'websafe.NewLoader' cmd/villa/verify_search.go` returns >= 1 (family b reuses the shipped guard).
    - `grep -c 'websafe.SafeClient' cmd/villa/verify_search.go` returns >= 1 (family c reuses the shipped SSRF guard).
    - `grep -c '169.254.169.254' internal/websafe/ssrf_test.go` returns >= 1.
    - `go vet ./cmd/villa/ ./internal/websafe/` exits 0.
  </acceptance_criteria>
  <done>Families (b) and (c) assert in-process against the shipped Phase-31/32 guard with no network; the internal-host SSRF case is explicit; all suites green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fetched web page → guard core | untrusted HTML/markdown crosses into the websafe sanitizer/classifier (family b input) |
| canary/allowlist host outcome → verdict core | probe results cross into the PASS/FAIL/REJECT decision (the honesty-bearing boundary) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-01 | Repudiation / Info-disclosure | `evalSearchVerify` verdict math | mitigate | Negative-control-FIRST + ineffective-block ⇒ FAIL, broken/blanket env ⇒ REJECT; the inversion trap (canary-still-reachable ⇒ PASS) is forbidden and explicitly unit-tested (`STILL reachable under the bound` case). |
| T-33-02 | Spoofing the verdict | empty/unroutable probe environment | mitigate | allowlist-reachable + canary-reachable-unguarded positive controls REJECT an unroutable env instead of false-passing (Pitfall 1). Pinned by REJECT-case tests. |
| T-33-04 | Tampering (indirect prompt injection) | family (b) injection assertion | mitigate | Reuse the shipped GUARD-02/03/04 strip+fence+flag core via `websafe.Loader`; assert Detected+Rules+stripped+fenced (flag-not-block, never claims elimination). |
| T-33-05 | Info-disclosure (SSRF) | family (c) SSRF assertion | mitigate | Assert `ipRejected(169.254.169.254/127.0.0.1)`, `hostRejected(villa-*/localhost)`, and `SafeClient` connect-time refusal of internal hosts. |
</threat_model>

<verification>
- `go test ./cmd/villa/ -run 'TestEvalSearchVerify|TestClassifySearchProbe|TestSearchInjectionFlagged|TestSearchSSRF' -count=1` all pass.
- `go test ./internal/websafe/ -run 'TestSSRF|TestHostRejected|TestControl' -count=1` passes.
- `go vet ./cmd/villa/ ./internal/websafe/` exits 0.
- The pure core does zero host I/O (grep gate in Task 1 acceptance).
</verification>

<success_criteria>
- The 3-state PASS/FAIL/REJECT verdict and the pure inverse-framing core exist and are green.
- The inversion trap (canary-still-reachable-under-bound ⇒ FAIL, never PASS) and both REJECT classes (env-broken/control-down, ineffective-blanket) are pinned by tests.
- Families (b) injection and (c) SSRF assert in-process against the shipped guard with no live host.
- Nothing in this plan touches `internal/orchestrate/openwebui.go`, `internal/config/villaconfig.go`, or the OWUI goldens.
</success_criteria>

<output>
Create `.planning/phases/33-egress-bounding-villa-verify-search/33-01-SUMMARY.md` when done
</output>
