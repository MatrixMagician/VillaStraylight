---
phase: 33-egress-bounding-villa-verify-search
plan: 02
type: execute
wave: 2
depends_on: ["33-01"]
files_modified:
  - cmd/villa/verify_search.go
  - cmd/villa/verify_search_test.go
  - cmd/villa/verify.go
  - cmd/villa/testdata/verify-search.json.golden
  - internal/orchestrate/openwebui_test.go
autonomous: true
requirements: [PRIV-07, PRIV-08, PRIV-09]
must_haves:
  truths:
    - "`villa verify search` is registered under the `verify` group and dispatches to runVerifySearch"
    - "With web search OFF (web_search_enabled=false) the command exits 0 with a 'nothing to verify' message — not a silent skip"
    - "The verdict maps PASS→0 (exitPass), FAIL→1 (exitBlocked), REJECT→2 (exitWarn) — no 4th exit code"
    - "`verify search --json` emits a byte-frozen schema-v1 contract guarded by a golden fixture"
    - "The OWUI web-search-OFF render stays byte-identical to v1.4 (PRIV-07 regression)"
    - "The OWUI outbound-kill env keys are present in BOTH the web-off and web-on goldens (PRIV-09 regression) and are NOT re-added"
  artifacts:
    - path: "cmd/villa/verify_search.go"
      provides: "searchVerifyDeps seam, liveSearchVerify live wiring, newVerifySearch, runVerifySearch (gate + exit map + human/--json render)"
      contains: "func runVerifySearch"
    - path: "cmd/villa/testdata/verify-search.json.golden"
      provides: "byte-frozen verify search --json contract, schema version 1"
      contains: "schema"
    - path: "internal/orchestrate/openwebui_test.go"
      provides: "regression assertion: kill-env keys present in both OWUI goldens (PRIV-09), web-off byte-identical (PRIV-07)"
      contains: "HF_HUB_OFFLINE"
  key_links:
    - from: "cmd/villa/verify.go"
      to: "cmd/villa/verify_search.go"
      via: "newVerify() AddCommand(newVerifySearch())"
      pattern: "newVerifySearch\\(\\)"
    - from: "cmd/villa/verify_search.go"
      to: "internal/config/villaconfig.go"
      via: "liveLoadedConfig().WebSearchEnabled gates the command (read-only)"
      pattern: "WebSearchEnabled"
    - from: "cmd/villa/verify_search.go"
      to: "internal/orchestrate/memory.go"
      via: "orchestrate.EmbedImage() supplies the probe helper image (no re-typed literal — TestSeamGrepGate)"
      pattern: "orchestrate\\.EmbedImage"
---

<objective>
Wire `villa verify search` to the host and the CLI: the `searchVerifyDeps` seam + `liveSearchVerify` live wiring that composes the canary/allowlist probes and the transient rootless-netns nft bound behind injected funcs, the cobra `newVerifySearch`/`runVerifySearch` with the web-search gate and the PASS/FAIL/REJECT→exit map, and a byte-frozen `--json` contract (schema v1). Plus the two assert-only regression guards: PRIV-07 (OWUI web-off render byte-identical to v1.4) and PRIV-09 (the outbound-kill env present in both goldens — assert, never re-add).

Purpose: Turn the Plan-01 pure core into an operator-runnable verb, register it, and lock the opt-in/default-off and outbound-kill invariants by regression assertion (the env + config are already shipped — this phase ASSERTS them). The live netns/nft mechanics are exercised off-hardware by an injected seam here and validated on real hardware in Plan 03.

Output: `cmd/villa/verify_search.go` completed (live seam + cobra + render), `cmd/villa/verify.go` (registration), `cmd/villa/testdata/verify-search.json.golden` (schema v1), `cmd/villa/verify_search_test.go` (registration, gate, exit-map, --json golden tests), `internal/orchestrate/openwebui_test.go` (PRIV-07/PRIV-09 regression assertions).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-egress-bounding-villa-verify-search/33-RESEARCH.md
@.planning/phases/33-egress-bounding-villa-verify-search/33-PATTERNS.md
@.planning/phases/33-egress-bounding-villa-verify-search/33-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: live seam + cobra registration + gate + exit-map (searchVerifyDeps, liveSearchVerify, newVerifySearch, runVerifySearch)</name>
  <files>cmd/villa/verify_search.go, cmd/villa/verify_search_test.go, cmd/villa/verify.go</files>
  <read_first>
    - cmd/villa/verify_agent.go:150-247 — `liveAgentVerify` (the four-layer live seam; clone the two-layer negative-control + classifier composition and the `runLlamaDownControl` deferred-restore precedent for any rootless-netns touch)
    - cmd/villa/verify_agent.go:283-361 — `verifyAgentDeps`, `liveVerifyAgentDeps`, `newVerifyAgent`, `runVerifyAgent` (clone the Deps seam, the live wiring, the cobra command, and the gate+exit-map run body)
    - cmd/villa/verify.go:42-143 — `verifyMemoryDeps`/`liveVerifyMemoryDeps`, `newVerify` (the verify parent that AddCommands subcommands at :75-77), `newVerifyMemory`, `runVerifyMemory` (the gate-OFF-exits-0 + FAIL→exitBlocked pattern)
    - cmd/villa/install_memory.go:127-141 — `liveLoadedConfig()`, `liveLoadedMemoryEnabled()` (clone a `liveLoadedWebSearchEnabled` failing soft to false)
    - cmd/villa/install_memory.go:239-421 — `memoryProofNetwork="villa"`, `runProbeCurl`, `runProbeCurlCode`, `extractExitCode` (REUSE for the canary/allowlist probes; helper image from `orchestrate.EmbedImage()`)
    - internal/config/villaconfig.go:134-138 — `WebSearchEnabled bool` toml `web_search_enabled` (the gate source — read-only, already shipped)
    - internal/orchestrate/searxng.go:87-99 — `searxngEngines` (duckduckgo/brave/wikipedia/wikidata) — the sanctioned allowlist the allowlist-host is derived from
    - cmd/villa/preflight.go:42-44 — exit constants `exitPass=0`, `exitBlocked=1`, `exitWarn=2`
    - .planning/phases/33-egress-bounding-villa-verify-search/33-RESEARCH.md — "Pattern 4" (the verified `unshare -rn` + `nft -f -` ruleset, fixed-arg, no shell), "Pitfall 1/2/4/6", "Don't Hand-Roll" table, "Environment Availability" (honest REJECT when nft/unshare absent)
    - .planning/phases/33-egress-bounding-villa-verify-search/33-PATTERNS.md:93-127, 165-173 — WR-02 (omit curl -f), Deps seam shape, gate-OFF body, the bound live-seam mapping
  </read_first>
  <action>
    Add `searchVerifyDeps` cloning `verifyAgentDeps` (verify_agent.go:285): `loadedWebSearchEnabled func() bool` (gate; live = `liveLoadedWebSearchEnabled`), `loadedConfig func() config.VillaConfig` (live = `liveLoadedConfig`), and `verifyFn func(ctx, deps searchVerifyDeps) searchProof` (live = `liveSearchVerify`). Add `liveVerifySearchDeps()` wiring them, and `liveLoadedWebSearchEnabled()` cloning `liveLoadedMemoryEnabled` but reading `config.LoadVilla().WebSearchEnabled`, failing soft to false.

    Implement `liveSearchVerify(ctx, deps) searchProof` composing the six injected probe funcs into the Plan-01 `evalSearchVerify`. Use `orchestrate.EmbedImage()` for the probe helper image (NEVER a re-typed image literal — `TestSeamGrepGate`) and `memoryProofNetwork` for the probe network. Build the canary/allowlist probes from `runProbeCurlCode` with `"-s", "--max-time", "5"` and classify via Plan-01 `classifySearchProbe` — OMIT `curl -f` (WR-02: a 4xx/5xx host is REACHABLE; -f would misclassify it as REJECT and excuse open egress). Build the transient bound with a fixed-arg `unshare -rn` + `nft -f -` reading the ruleset from STDIN (per 33-RESEARCH Pattern 4: `table inet villabound { chain output { type filter hook output priority 0; policy drop; oif "lo" accept; ct state established,related accept; ip daddr <ALLOW> accept } }`), allowlist IPs `netip`-validated before formatting — NO shell interpolation, NO composed shell string. If `nft`/`unshare` are absent or the bound cannot be applied/torn-down, return searchReject with literal remediation (typed-Unknown → never a false PASS). If a rootless-netns is touched, wrap apply/teardown in a `defer`-always restore (the `runLlamaDownControl` precedent) and downgrade a restore failure to REJECT. The exact rootless-netns attach point (arch A vs B) is validated on-hardware in Plan 03; the seam is mechanism-agnostic and the pure core is unchanged either way.

    Add `newVerifySearch()` cloning `newVerifyAgent` (verify_agent.go:317) with `Use: "search"`, a Short/Long describing the negative-control-first inverse-framed bounded-outbound proof, a `--json` bool flag, and a `RunE` that calls `os.Exit(runVerifySearch(cmd, args, liveVerifySearchDeps()))`. Register it in `verify.go:75-77`: add `cmd.AddCommand(newVerifySearch())` after `newVerifyAgent()`.

    Implement `runVerifySearch(cmd, args, deps) int` cloning `runVerifyAgent` (verify_agent.go:345): gate-OFF (`!deps.loadedWebSearchEnabled()`) prints "verify search: web search is not enabled (web_search_enabled=false) — nothing to verify ..." and returns `exitPass` (NOT a silent skip). Otherwise run `deps.verifyFn`, then map the searchProof: `searchPass→exitPass(0)`, `searchFail→exitBlocked(1)` (print the refuse-with-remediation detail to stderr), `searchReject→exitWarn(2)` (print the honest infra-fail detail to stderr) — per the locked exit map; do NOT invent a 4th code. (The `--json` render is wired in Task 2; here leave a render hook.) Add tests `TestVerifySearchRegistered` (clone verify_agent_test.go:355) and `TestRunVerifySearchGate` (clone :379 — OFF exits 0) plus an exit-map table test driving the three searchProof states through `runVerifySearch` with a fake `verifyFn`.
  </action>
  <verify>
    <automated>go test ./cmd/villa/ -run 'TestVerifySearchRegistered|TestRunVerifySearchGate|TestRunVerifySearchExit' -count=1 && go test ./internal/inference/ -run TestSeamGrepGate -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./cmd/villa/ -run 'TestVerifySearchRegistered|TestRunVerifySearchGate|TestRunVerifySearchExit' -count=1` passes.
    - `go test ./internal/inference/ -run TestSeamGrepGate -count=1` passes (no leaked image literal in verify_search.go).
    - `grep -c 'newVerifySearch()' cmd/villa/verify.go` returns 1.
    - `grep -c 'orchestrate.EmbedImage' cmd/villa/verify_search.go` returns >= 1 AND `grep -vE '^[[:space:]]*//' cmd/villa/verify_search.go | grep -cE 'sh -c|bash -c' ` returns 0 (no shell interpolation).
    - `grep -c '"-f"' cmd/villa/verify_search.go` returns 0 (WR-02: curl -f omitted on the reachability probe).
    - `grep -cE 'exitWarn|exitBlocked|exitPass' cmd/villa/verify_search.go` returns >= 3 (the full PASS/FAIL/REJECT exit map present; no new const).
  </acceptance_criteria>
  <done>`villa verify search` is registered, gated on web_search_enabled (OFF exits 0), maps PASS/FAIL/REJECT to exitPass/exitBlocked/exitWarn, builds the transient bound fixed-arg with no shell interpolation and a seam-locked image, and REJECTs honestly when tooling is absent.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: --json byte-frozen golden (schema v1) + PRIV-07/PRIV-09 regression assertions</name>
  <files>cmd/villa/verify_search.go, cmd/villa/verify_search_test.go, cmd/villa/testdata/verify-search.json.golden, internal/orchestrate/openwebui_test.go</files>
  <read_first>
    - cmd/villa/recommend_test.go:65-90 — `TestRecommendJSONGolden` (the byte-frozen golden test shape; `*update` regenerate path; `bytes.Equal` compare)
    - cmd/villa/detect_test.go:13 — `var update = flag.Bool("update", ...)` (package-level — REUSE, do not redeclare)
    - cmd/villa/testdata/recommend.golden.json — golden fixture format precedent
    - internal/orchestrate/openwebui.go:155-170 — the base env kill-set block (`HF_HUB_OFFLINE=1`, `ANONYMIZED_TELEMETRY=False`, `DO_NOT_TRACK=True`, `SCARF_NO_ANALYTICS=True`, `OFFLINE_MODE=True`, `ENABLE_VERSION_UPDATE_CHECK=False`) — ASSERT-ONLY, never modify (re-adding breaks the frozen golden)
    - internal/orchestrate/openwebui_test.go:19-100 — existing OWUI render tests (`unitByName`, the on/off render fixtures) to extend with the regression asserts
    - internal/orchestrate/render_test.go:249-374 — `TestRenderOpenWebUITelemetryFrozen`, `TestRenderOpenWebUIContainerGolden` (the existing frozen/golden guards; extend, do not re-freeze)
    - .planning/phases/33-egress-bounding-villa-verify-search/33-RESEARCH.md — "Pitfall 5" (do NOT re-add the env), "Phase Requirements" table (PRIV-07/PRIV-09 are regression assertions), "Assumptions Log A5" (verify --json is greenfield → schema v1)
    - .planning/phases/33-egress-bounding-villa-verify-search/33-PATTERNS.md:177-216 — golden test shape + "PRIV-09 regression (assert, never modify)"
  </read_first>
  <action>
    Define a small `--json` view struct for verify search (e.g. fields: schema int = 1, verdict string PASS/FAIL/REJECT, detail string, and per-family booleans/states — at planner-executor discretion within an append-only-friendly shape) marshaled deterministically with `encoding/json`. In `runVerifySearch`, when the `--json` flag is set, marshal the verdict view to stdout instead of the human line; keep the same exit-code map. Use the AUTHORITATIVE schema version 1 (greenfield — no prior verify json, per A5).

    Add `TestVerifySearchJSON` cloning `TestRecommendJSONGolden` (recommend_test.go:65): build a deterministic searchProof (via the fake verifyFn), render `--json` to a buffer, and compare byte-for-byte against `testdata/verify-search.json.golden`; reuse the package-level `*update` flag to (re)freeze. Generate the golden once with `go test ./cmd/villa/ -run TestVerifySearchJSON -update` and commit it. Keep the contract byte-frozen append-only thereafter.

    Add the PRIV-09 regression assertion in `internal/orchestrate/openwebui_test.go`: assert the six kill-env keys (`HF_HUB_OFFLINE`, `ANONYMIZED_TELEMETRY`, `DO_NOT_TRACK`, `SCARF_NO_ANALYTICS`, `OFFLINE_MODE`, `ENABLE_VERSION_UPDATE_CHECK`) are present in BOTH the web-search-OFF and web-search-ON rendered `villa-openwebui.container` units — a guard that the kill-set is never silently dropped or gated. Add (or confirm) the PRIV-07 regression: the web-search-OFF render stays byte-identical to the v1.4 `villa-openwebui.container.golden` (extend the existing golden assertion; do NOT re-freeze). DO NOT modify openwebui.go, villaconfig.go, or the two OWUI goldens — assertion-only. Run the suites.
  </action>
  <verify>
    <automated>go test ./cmd/villa/ -run TestVerifySearchJSON -count=1 && go test ./internal/orchestrate/ -run 'TestRenderOpenWebUI' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./cmd/villa/ -run TestVerifySearchJSON -count=1` passes.
    - `go test ./internal/orchestrate/ -run 'TestRenderOpenWebUI' -count=1` passes.
    - `test -f cmd/villa/testdata/verify-search.json.golden` succeeds AND `grep -c '"schema"' cmd/villa/testdata/verify-search.json.golden` returns >= 1 (schema v1 present).
    - `git diff --exit-code internal/orchestrate/openwebui.go internal/config/villaconfig.go internal/orchestrate/testdata/villa-openwebui.container.golden` shows NO changes (assert-only; the env/config/goldens are untouched).
    - `grep -c 'HF_HUB_OFFLINE' internal/orchestrate/openwebui_test.go` returns >= 1 (PRIV-09 regression assertion present).
    - `make check` passes (full vet + test suite green).
  </acceptance_criteria>
  <done>`verify search --json` emits a byte-frozen schema-v1 contract; PRIV-07 (web-off byte-identical) and PRIV-09 (kill-env present in both goldens) are guarded by regression assertions; openwebui.go/villaconfig.go/the OWUI goldens are untouched; full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator CLI → verify command | flag/config input crosses into the gated verb |
| verify command → host (unshare/nft/curl/podman) | the live seam shells fixed-arg execs to apply the transient bound and probe egress |
| allowlist IPs → nft ruleset | netip-validated values cross into the rule text (no shell) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-03 | Tampering (shell/command injection) | `liveSearchVerify` host-exec seam | mitigate | Fixed-arg `exec.Command`; `nft -f -` ruleset on STDIN (no composed shell string); allowlist IPs netip-validated before entering the rule; helper image only from `orchestrate.EmbedImage()` (TestSeamGrepGate). Asserted by grep (no `sh -c`, no re-typed literal). |
| T-33-06 | Denial of service | bound left applied after a crash | mitigate | Prefer the ephemeral `unshare -rn` subprocess (auto-teardown); if the rootless-netns is touched, deferred-always restore with REJECT-on-restore-failure (Pitfall 6 / `runLlamaDownControl` precedent). |
| T-33-07 | Info-disclosure (probe misclassification) | curl reachability probe | mitigate | OMIT `curl -f` (WR-02): a reachable-but-erroring host reads as reachable (egress open ⇒ FAIL), never excused as a REJECT. Asserted by grep (`"-f"` count 0). |
| T-33-08 | Info-disclosure (lazy OWUI HF pull / telemetry) | OWUI env kill-set | mitigate | Already killed in base env (unconditional, both goldens); PRIV-09 regression asserts the keys are never dropped/gated; on-hardware no-HF-pull proof in Plan 03. |
| T-33-09 | Tampering (silent scope reduction of opt-in) | web-off render | mitigate | PRIV-07 regression: web-search-OFF render byte-identical to v1.4 golden; the gate reads the already-shipped `WebSearchEnabled` (no schema change). |
| T-33-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs this phase (RESEARCH Package Legitimacy Audit: zero external packages; host tools are OS-provided). |
</threat_model>

<verification>
- `go test ./cmd/villa/ -run 'TestVerifySearchRegistered|TestRunVerifySearchGate|TestRunVerifySearchExit|TestVerifySearchJSON' -count=1` all pass.
- `go test ./internal/orchestrate/ -run 'TestRenderOpenWebUI' -count=1` passes.
- `go test ./internal/inference/ -run TestSeamGrepGate -count=1` passes.
- `make check` passes.
- `git diff --exit-code internal/orchestrate/openwebui.go internal/config/villaconfig.go internal/orchestrate/testdata/villa-openwebui.container.golden` shows no changes.
</verification>

<success_criteria>
- `villa verify search` is registered, gated on web_search_enabled, and maps PASS/FAIL/REJECT to exitPass/exitBlocked/exitWarn (no 4th code).
- The live seam builds the transient bound fixed-arg with no shell interpolation and a seam-locked image, and REJECTs honestly when tooling is absent.
- `verify search --json` emits a byte-frozen schema-v1 contract.
- PRIV-07 (web-off byte-identical) and PRIV-09 (kill-env in both goldens) are guarded by regression assertions; the env block, config schema, and OWUI goldens are untouched.
</success_criteria>

<output>
Create `.planning/phases/33-egress-bounding-villa-verify-search/33-02-SUMMARY.md` when done
</output>
