---
phase: 33-egress-bounding-villa-verify-search
plan: 03
type: execute
wave: 3
depends_on: ["33-02"]
files_modified:
  - cmd/villa/verify_search.go
  - cmd/villa/verify_search_test.go
autonomous: false
requirements: [PRIV-08, PRIV-09]
user_setup:
  - service: host-tooling
    why: "The bound-mechanics + real verify run require the live Strix Halo host with podman rootless-netns, nft, unshare, and a running web-search-enabled villa stack"
must_haves:
  truths:
    - "On real hardware, the off-allowlist canary is reachable UNGUARDED and UNREACHABLE under the bound (a real nft block, not an unroutable netns)"
    - "An allowlisted SearXNG-upstream host stays reachable under the bound (proves an allowlist, not a blanket block)"
    - "`villa verify search` returns PASS on the live web-search-enabled stack; an ineffective/unroutable bound returns REJECT (exit 2), never a fabricated PASS"
    - "A real web search under the bound makes NO outbound HuggingFace pull (the kill env is effective)"
  artifacts:
    - path: "cmd/villa/verify_search.go"
      provides: "the rootless-netns attach point (arch A vs B) finalized to the mechanism proven on hardware"
      contains: "func liveSearchVerify"
  key_links:
    - from: "cmd/villa/verify_search.go"
      to: "host: unshare -rn + nft + podman rootless-netns"
      via: "the live bound is applied where container egress actually flows (Pitfall 1)"
      pattern: "unshare"
---

<objective>
Validate the live bound mechanics on the real Strix Halo host and run the real `villa verify search` end-to-end. This resolves RESEARCH Open Q2 (which rootless-netns attach point — architecture A vs B) and Open Q1 (whether any web-search-required weight needs pre-staging), and proves the central PRIV-08 honesty property on actual hardware: the canary is genuinely reachable unguarded and genuinely blocked by the nft rule (not by an absent route), the allowlist stays reachable, and an unroutable/ineffective bound REJECTs instead of false-passing. Also confirms PRIV-09 on hardware: a real web search under the bound triggers no outbound HuggingFace pull.

Purpose: The pure core and seam are testable off-hardware (Plans 01/02), but the load-bearing trap (Pitfall 1: an empty `unshare -rn` netns blocks everything trivially) can only be disproven on a host where egress actually flows. This task pins the attach point to the mechanism that makes the negative control real, and captures the operator-visible PASS/REJECT behavior.

Output: the finalized rootless-netns attach point in `liveSearchVerify` (whichever of arch A/B is proven on hardware), and a human-verified record that the canary inverse-framing, the allowlist-still-reachable control, the live PASS, and the no-HF-pull check all hold.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-egress-bounding-villa-verify-search/33-RESEARCH.md
@.planning/phases/33-egress-bounding-villa-verify-search/33-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: finalize the rootless-netns attach point on hardware (arch A vs B)</name>
  <files>cmd/villa/verify_search.go, cmd/villa/verify_search_test.go</files>
  <read_first>
    - cmd/villa/verify_search.go (from Plan 02) — the `liveSearchVerify` bound seam to finalize
    - .planning/phases/33-egress-bounding-villa-verify-search/33-RESEARCH.md — "Pitfall 1" (architectures A and B; prefer A — apply the bound where podman's rootless-netns egress flows), "Pattern 4" (the verified `unshare -rn nft -f -` ruleset), "Open Questions" Q2, "Environment Availability" (nft 1.1.6, unshare 2.41.5, podman 5.8.2 netavark+pasta), "Common Pitfalls" Pitfall 2 (rootless egress is L4 via pasta — apply inside the rootless netns, not the host FORWARD chain)
    - cmd/villa/install_memory.go:384 — `runProbeCurlCode` (the probe-container exec mechanism that traverses the bounded namespace)
    - .planning/phases/33-egress-bounding-villa-verify-search/33-PATTERNS.md:165-173 — the bound live-seam mapping + teardown precedent
  </read_first>
  <action>
    On the live host, prototype both bound architectures from 33-RESEARCH Pitfall 1: (A) apply the nft drop in podman's rootless-netns (where the `villa` bridge + default route live) and probe with a `--network villa` helper container via `runProbeCurlCode`, so the canary is genuinely reachable unguarded and blocked by the RULE; (B) build real egress (veth + default route) inside an `unshare -rn` netns before applying the bound. Pick the architecture that (i) makes the canary genuinely reachable UNGUARDED, (ii) blocks it by the nft rule under the bound, (iii) keeps an allowlisted SearXNG-upstream host reachable under the bound, and (iv) tears the bound down on EVERY exit path (prefer the ephemeral subprocess; if the rootless-netns is touched, deferred-always restore with REJECT-on-restore-failure, per Pitfall 6). Finalize `liveSearchVerify` to that mechanism with fixed-arg `exec.Command` and `nft -f -` on STDIN (no shell interpolation), allowlist IPs netip-validated, helper image only from `orchestrate.EmbedImage()`. The pure core from Plan 01 is unchanged. If the chosen mechanism reveals a new pure-classification edge, add a unit test for it. Record which architecture was chosen and the verified canary/allowlist outcomes in the SUMMARY.
  </action>
  <verify>
    <automated>make check</automated>
  </verify>
  <acceptance_criteria>
    - `make check` passes (full vet + test suite green after the attach-point finalization).
    - `go test ./internal/inference/ -run TestSeamGrepGate -count=1` passes (no leaked image literal).
    - `grep -vE '^[[:space:]]*//' cmd/villa/verify_search.go | grep -cE 'sh -c|bash -c' ` returns 0 (no shell interpolation in the finalized seam).
    - The SUMMARY records the chosen architecture (A or B) and the on-host canary-reachable-unguarded / canary-blocked-under-bound / allowlist-still-reachable outcomes.
  </acceptance_criteria>
  <done>The rootless-netns attach point is finalized to the on-hardware-proven mechanism; the canary is reachable unguarded and blocked by the nft rule (not an absent route); the suite is green and the seam stays shell-free + seam-locked.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: on-hardware checkpoint — real verify search PASS + no-HF-pull confirmation</name>
  <action>Operator-driven on-hardware verification of `villa verify search` on the live Strix Halo host (cannot run off-host). Follow the how-to-verify steps below; this is a blocking human checkpoint — do not auto-advance.</action>
  <what-built>The real `villa verify search` egress-bound proof on the live Strix Halo host: the transient rootless-netns nft bound, the inverse-framed canary/allowlist controls, and the PRIV-09 no-outbound-HF-pull confirmation under a real web search.</what-built>
  <how-to-verify>
    1. Ensure the web-search-enabled stack is up: `./villa install` (with `web_search_enabled=true`) then confirm `villa status` shows villa-searxng + villa-websafe healthy.
    2. Run the proof: `./villa verify search`. EXPECTED: it prints a PASS line and exits 0. (Check `echo $?` == 0.)
    3. Confirm the inverse framing is real, not an unroutable trick: the proof's own output (or `./villa verify search --json`) should show the off-allowlist canary was reachable UNGUARDED and UNREACHABLE under the bound, and an allowlisted SearXNG-upstream host stayed reachable under the bound. (If the canary was NOT reachable unguarded, or the allowlist was blocked, the proof must REJECT with exit 2 — NOT a fabricated PASS.)
    4. Negative-control sanity (optional but recommended): temporarily make the bound ineffective (e.g. add the canary IP to the allowlist) and re-run — EXPECTED: REJECT/FAIL, exit nonzero, never a green PASS. Restore afterward.
    5. PRIV-09 no-HF-pull: run a REAL web search through Open WebUI (a query that triggers grounding) while watching outbound — EXPECTED: grounding works AND no outbound HuggingFace pull occurs (the `HF_HUB_OFFLINE=1` + telemetry kill env is effective). Confirm whether any web-search-required weight needed pre-staging (RESEARCH Open Q1 / Assumption A2 default: "none needed" — the villa-embed `nomic-embed-text-v1.5` is already staged). If grounding breaks for a missing weight, that is a CONTEXT-change escalation, not a silent fix.
    6. Confirm the bound left no residue: after the verb exits, real web search still works (the bound was verify-time-only and torn down) and no nft table named `villabound` lingers.
  </how-to-verify>
  <resume-signal>Type "approved" once verify search PASSes on hardware with a real (reachable-unguarded → blocked-under-bound) canary, the allowlist stays reachable, no HF pull occurs under a real web search, and the bound tears down cleanly — or describe what you observed (e.g. which architecture, any REJECT, any missing pre-staged weight).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live container egress → nft bound | real outbound traffic crosses the transient rule in the rootless-netns |
| OWUI web-search grounding → outbound | a real search may lazily attempt an HF pull (must be killed) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-02 | Spoofing the verdict | the empty-netns false-block (Pitfall 1) | mitigate | On-hardware: the bound is applied where egress actually flows; the canary is verified reachable UNGUARDED first, so a block is proven by the RULE, not an absent route. An unroutable env REJECTs (exit 2), never a fabricated PASS. Verified by the human checkpoint. |
| T-33-06 | Denial of service | bound left applied after the verb | mitigate | Verify-time-only + ephemeral teardown; the checkpoint confirms no `villabound` nft table lingers and real web search still works after. |
| T-33-08 | Info-disclosure (lazy OWUI HF pull) | OWUI web-search grounding | mitigate | The base kill env (`HF_HUB_OFFLINE=1` + telemetry switches) is effective; the checkpoint runs a REAL web search and confirms no outbound HF pull (PRIV-09 / Open Q1). |
</threat_model>

<verification>
- `make check` passes after the attach-point finalization.
- On-hardware: `./villa verify search` returns PASS (exit 0) with a genuinely-reachable-unguarded → blocked-under-bound canary and a still-reachable allowlist.
- An ineffective/unroutable bound returns REJECT (exit 2), never a fabricated PASS (human-verified negative control).
- A real web search under the bound makes no outbound HuggingFace pull; grounding works (no missing pre-staged weight) — or any gap is escalated as a CONTEXT change.
- No `villabound` nft table or residual bound remains after the verb exits.
</verification>

<success_criteria>
- The rootless-netns attach point is finalized to the on-hardware-proven mechanism (arch A or B recorded).
- PRIV-08 is proven on real hardware: inverse-framed negative-control-first, real nft block, REJECT (never fabricated PASS) on an ineffective bound.
- PRIV-09 is confirmed on hardware: no outbound HF pull under a real web search; weight pre-staging question resolved.
- The bound is verify-time-only and tears down cleanly with no residue.
</success_criteria>

<output>
Create `.planning/phases/33-egress-bounding-villa-verify-search/33-03-SUMMARY.md` when done
</output>
