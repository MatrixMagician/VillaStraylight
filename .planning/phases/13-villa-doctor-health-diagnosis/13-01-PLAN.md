---
phase: 13-villa-doctor-health-diagnosis
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/doctor/doctor.go
  - internal/doctor/doctor_test.go
autonomous: true
requirements: [DOCTOR-01, DOCTOR-02, DOCTOR-03]

must_haves:
  truths:
    - "doctor.Aggregate folds preflight findings, status health, the per-service offload Verdict, and a drift Plan into one worst-wins Report"
    - "D-02 honored: doctor defines its OWN Report type with reportSchemaVersion=1 and never extends the byte-frozen status.Report (git diff --stat internal/status/ shows no change)"
    - "D-04 honored: the worst-wins Overall maps to the AUTHORITATIVE shipped preflight exit constants (exitPass 0 / exitWarn 2 / exitBlocked 1) — confident BLOCK/residency-FAIL to exitBlocked, WARN/drift to exitWarn; never the inverted ROADMAP parenthetical"
    - "A confident offload FAIL dominates a HealthReady (no false-green over a health-200)"
    - "A non-empty Plan.Changed produces a drift WARN finding"
    - "Every non-healthy Finding carries non-empty Remediation text"
    - "A down / not-installed stack (status error, absent unit dir) degrades to a typed-Unknown WARN finding, never a panic"
  artifacts:
    - path: "internal/doctor/doctor.go"
      provides: "Pure doctor core: Finding, Report, Deps, Aggregate, reportSchemaVersion"
      contains: "func Aggregate"
      min_lines: 120
    - path: "internal/doctor/doctor_test.go"
      provides: "Off-hardware table-driven core tests with stubbed Deps"
      contains: "func TestOffloadFailDominatesHealth"
  key_links:
    - from: "internal/doctor/doctor.go"
      to: "internal/status (Report.Services[i].Offload)"
      via: "Deps.StatusReport func() status.Report"
      pattern: "StatusReport"
    - from: "internal/doctor/doctor.go"
      to: "internal/orchestrate (Plan.Changed)"
      via: "Deps.DriftPlan func() (orchestrate.Plan, error)"
      pattern: "DriftPlan"
    - from: "internal/doctor/doctor.go"
      to: "internal/inference (IsROCmFamily, Verdict)"
      via: "backend-keyed preflight routing + opaque Verdict consumption"
      pattern: "IsROCmFamily"
---

<objective>
Build the pure `internal/doctor` core that composes the shipped cores into a single
worst-wins health diagnosis, plus the Wave-0 off-hardware test scaffold that freezes
its behavior. This plan creates ALL doctor decision logic; it does NO host I/O (host
seams are injected `func` fields, exercised here only with stubbed `Deps`).

Purpose: DOCTOR-01/02/03 decision logic lives in a pure, off-hardware-testable core
(project convention), mirroring `internal/status/status.go`. Per D-01, this is
composition only — it never re-implements a probe a shipped core already produces.

Output:
- `internal/doctor/doctor.go` — `Finding`, `Report`, `Deps`, `Aggregate`, `reportSchemaVersion`
- `internal/doctor/doctor_test.go` — table-driven core tests with stubbed `Deps`
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-villa-doctor-health-diagnosis/13-CONTEXT.md
@.planning/phases/13-villa-doctor-health-diagnosis/13-RESEARCH.md
@.planning/phases/13-villa-doctor-health-diagnosis/13-PATTERNS.md
@.planning/phases/13-villa-doctor-health-diagnosis/13-VALIDATION.md
@CLAUDE.md
</context>

<artifacts_this_phase_produces>
**Symbols created across Phase 13 (this plan owns the core; Plan 02 owns the cmd tier).**
The plan-review-convergence source-grounding pass MUST treat every symbol below as
newly-created (exclude from drift verification — they do not yet exist on `main`):

- Package `internal/doctor` (NEW) and its file `internal/doctor/doctor.go`
- `doctor.Finding` struct (ID, Name, Tier, Status, Detail, Remediation, Provenance; optional `Raw string json:"-"`)
- `doctor.Report` struct (Findings, Overall, SchemaVersion — SchemaVersion LAST)
- `doctor.Deps` struct (Probe, LoadConfig, StatusReport, DriftPlan, Backend)
- `doctor.Aggregate(Deps) Report`
- `doctor.reportSchemaVersion` const (= 1)
- `internal/doctor/doctor_test.go` and its tests: `TestRemediationPresent`,
  `TestOffloadFailDominatesHealth`, `TestDriftWarn`, plus stubbed-Deps builders
  (`healthyDeps`/equivalent) and report fixtures

Plan 02 (cmd tier) additionally produces: `cmd/villa/doctor.go` (`newDoctor`,
`runDoctor`, `renderDoctor`, `renderDoctorTable`, `liveDoctorDeps`, `unitDirReadOnly`),
`cmd/villa/doctor_test.go` (`TestDoctorExitCodes`, `TestDoctorJSON`),
`cmd/villa/testdata/doctor.json.golden`, and the `newDoctor()` registration line in
`cmd/villa/root.go`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave-0 — doctor core test scaffold (RED)</name>
  <files>internal/doctor/doctor_test.go</files>
  <read_first>
    - .planning/phases/13-villa-doctor-health-diagnosis/13-VALIDATION.md (Wave 0 Requirements + Per-Task Verification Map)
    - .planning/phases/13-villa-doctor-health-diagnosis/13-PATTERNS.md (`internal/doctor/doctor_test.go` section — stubbed-Deps builder mirroring `newStatusDeps`)
    - cmd/villa/status_test.go (the `newStatusDeps` stubbed-Deps builder pattern, ~lines 46-88)
    - cmd/villa/preflight_test.go (the `passResults`/`warnResults`/`blockResults` fixture-builder pattern, ~lines 16-37)
    - internal/status/status.go (ServiceStatus.Offload / OffloadApplies fields, lines 45-63; HealthReady at line 39; Report shape lines 89-129)
    - internal/inference/inference.go (Verdict + Status: StatusFail / StatusWarn / StatusPass)
    - internal/orchestrate/orchestrate.go (Plan type — the Changed field)
  </read_first>
  <behavior>
    - TestRemediationPresent: a Report built from a Deps with a drift Plan AND an offload-FAIL service has every non-PASS Finding carrying non-empty Remediation (DOCTOR-02).
    - TestOffloadFailDominatesHealth: a status.Report whose inference ServiceStatus has OffloadApplies=true and Offload.Status==inference.StatusFail, over Health==HealthReady, yields a BLOCK-class FAIL Finding and Report.Overall=="FAIL" (Pitfall 3 — no false-green over health-200).
    - TestDriftWarn: a Deps.DriftPlan returning a Plan with a non-empty Changed slice (and no offload FAIL) yields a drift WARN Finding and Report.Overall=="WARN" (DOCTOR-03).
    - (degradation) a Deps.DriftPlan returning a read error (absent unit dir) yields a typed-Unknown WARN Finding, never a panic (D-08).
  </behavior>
  <action>
    Create `internal/doctor/doctor_test.go` in `package doctor`. Write a stubbed-`Deps`
    builder (mirror `newStatusDeps`): a healthy-default `doctor.Deps` where `Probe`
    returns a benign `detect.HostProfile`, `LoadConfig` returns a default
    `config.VillaConfig` with `Backend:"vulkan"`, `StatusReport` returns an all-PASS
    `status.Report` (one inference `ServiceStatus` with `OffloadApplies:true`,
    `Offload.Status:inference.StatusPass`, `Health:status.HealthReady`), and `DriftPlan`
    returns an empty `orchestrate.Plan` with nil error. Each test overrides ONE knob.
    Write the four test funcs named above plus report-fixture helpers. Reference the
    not-yet-existing `doctor.Finding`, `doctor.Report`, `doctor.Deps`, `doctor.Aggregate`
    so the package fails to COMPILE (the RED state). Assert against string tier/status
    vocabulary "BLOCK"/"WARN"/"PASS"/"FAIL" and `Report.Overall`. Do NOT type any
    backend marker literal (Vulkan0/ROCm0/image tags) — construct offload Verdicts via
    `inference.Verdict{Status: inference.StatusFail, Remediation: "..."}` only.
  </action>
  <verify>
    <automated>cd /home/oliverh/repos/github/MatrixMagician/VillaStraylight && go vet ./internal/doctor/ 2>&1 | grep -qiE 'undefined|undeclared' && echo RED-OK</automated>
  </verify>
  <acceptance_criteria>
    - internal/doctor/doctor_test.go exists and declares `package doctor`
    - The file references symbols `doctor`-package `Finding`, `Report`, `Deps`, `Aggregate` (or unqualified within-package equivalents) that do not yet exist → `go build ./internal/doctor/` fails with an undefined-symbol error (RED)
    - The file defines test funcs `TestRemediationPresent`, `TestOffloadFailDominatesHealth`, `TestDriftWarn`
    - `grep -v '^//' internal/doctor/doctor_test.go | grep -cE 'Vulkan0|ROCm0|HSA_OVERRIDE'` prints 0 (no leaked marker literals)
  </acceptance_criteria>
  <done>The four core tests + stubbed-Deps builder exist and the package does not yet compile because the core symbols are unimplemented (intended RED).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the pure doctor core — Finding/Report/Deps/Aggregate (GREEN)</name>
  <files>internal/doctor/doctor.go</files>
  <read_first>
    - .planning/phases/13-villa-doctor-health-diagnosis/13-PATTERNS.md (`internal/doctor/doctor.go` section — Deps shape, Finding wrapper, Report SchemaVersion-last, Aggregate worst-wins, offload-FAIL-dominates, backend-keyed routing, drift-via-DriftPlan)
    - internal/status/status.go (Deps struct shape lines 184+; Report lines 89-129; Aggregate worst-wins `bump`-closure ~lines 398-419; reportSchemaVersion=1 line 135; package doc comment lines 1-13)
    - internal/preflight/preflight.go (CheckResult fields + Tier/Status enums + pass/warn/fail helpers — the shape Finding mirrors; the `Run`/`RunROCm` signatures)
    - cmd/villa/preflight.go (the IsROCmFamily routing idiom lines 50-59 — Run vs RunROCm)
    - internal/inference/backend.go (IsROCmFamily at line 47 — single ROCm-family enumeration point)
    - internal/inference/inference.go (Verdict at line 152 — consume Status/Detail/Remediation OPAQUELY)
    - internal/orchestrate/orchestrate.go (Plan type) and internal/orchestrate/reconcile.go (Reconcile signature — for the DriftPlan contract this core consumes)
  </read_first>
  <action>
    Create `internal/doctor/doctor.go` in `package doctor`. Open with a package doc
    comment stating: pure core, never `os.Exit`/print, every host touch is a `Deps`
    seam, owns its OWN golden (do NOT extend `status.Report`).

    Define `Deps` with func-fields: `Probe func() detect.HostProfile`,
    `LoadConfig func() (config.VillaConfig, error)`, `StatusReport func() status.Report`,
    `DriftPlan func() (orchestrate.Plan, error)`, `Backend string`.

    Define `Finding` (json-tagged): `ID`, `Name`, `Tier` ("BLOCK"|"WARN"), `Status`
    ("PASS"|"WARN"|"FAIL"), `Detail`, `Remediation`, `Provenance`; if any raw output is
    carried add `Raw string json:"-"` (mirror CheckResult.Raw / Verdict.Raw — never
    serialized).

    Define `Report` (json-tagged): `Findings []Finding`, `Overall string` (worst-wins
    "PASS"/"WARN"/"FAIL"), `SchemaVersion int` as the LAST tagged field; add
    `const reportSchemaVersion = 1` (D-09).

    Implement `Aggregate(d Deps) Report`:
    1. HOST CONDITIONS: `profile := d.Probe()`; route via `inference.IsROCmFamily(d.Backend)`
       → `preflight.RunROCm(profile)` else `preflight.Run(profile)`; normalize each
       `preflight.CheckResult` into a `Finding` (preflight TierBlock→"BLOCK" tier;
       carry its Status, Detail, Remediation, Provenance).
    2. RUNNING-STACK HEALTH: `report := d.StatusReport()`; for each
       `report.Services[i]`: fold the service health, and when `s.OffloadApplies` is true
       fold `s.Offload` (consume `inference.Verdict` opaquely — read `.Status`/`.Detail`/
       `.Remediation`). A confident `inference.StatusFail` MUST become a BLOCK-class FAIL
       Finding (D-05) that DOMINATES a HealthReady (Pitfall 3). A HealthDown / unevaluable
       signal → typed-Unknown WARN Finding with remediation (D-06/D-08), never a false PASS.
    3. DRIFT: `plan, err := d.DriftPlan()`; on `err` → typed-Unknown WARN Finding
       ("units not yet written / unreadable — run `villa install`"); on
       `len(plan.Changed) > 0` → drift WARN Finding, remediation "re-run `villa install`
       to reconcile config-vs-disk drift" (D-05/D-10). Empty Changed → no finding.
    4. WORST-WINS FOLD (mirror status.Aggregate's bump closure): any BLOCK-class FAIL →
       Overall "FAIL"; else any WARN-class → Overall "WARN"; else "PASS". Set
       `SchemaVersion = reportSchemaVersion`.

    Every non-PASS Finding MUST set a non-empty Remediation (D-11). NEVER call os.Exit
    or print. NEVER type a backend marker literal — route only through
    `inference.IsROCmFamily` and consume Verdicts opaquely (TestSeamGrepGate walks
    internal/).
  </action>
  <verify>
    <automated>cd /home/oliverh/repos/github/MatrixMagician/VillaStraylight && go test ./internal/doctor/ -run 'TestRemediationPresent|TestOffloadFailDominatesHealth|TestDriftWarn'</automated>
  </verify>
  <acceptance_criteria>
    - `internal/doctor/doctor.go` contains `func Aggregate(`
    - `internal/doctor/doctor.go` contains `SchemaVersion` as the last json-tagged field of `Report` and `const reportSchemaVersion = 1`
    - `go test ./internal/doctor/ -run 'TestRemediationPresent|TestOffloadFailDominatesHealth|TestDriftWarn'` exits 0 (GREEN)
    - `grep -v '^//' internal/doctor/doctor.go | grep -cE 'Vulkan0|ROCm0|HSA_OVERRIDE'` prints 0
    - `grep -c 'status.Report' internal/doctor/doctor.go` is 0 modifications to status.Report (doctor only READS it; verify by `git diff --stat internal/status/` shows no change)
    - `go test ./internal/inference/ -run TestSeamGrepGate` exits 0
  </acceptance_criteria>
  <done>The pure doctor core compiles, all three core tests pass GREEN, status.Report is untouched, and the seam grep-gate stays green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config.toml → core | Hand-editable config string (`Backend`) crosses into the core; validated via `inference.IsROCmFamily` routing (any non-ROCm-family name falls through to the standalone preflight; an unresolvable backend surfaces downstream in Plan 02's `BackendFor`). |
| status.Report → core | The core READS the frozen `status.Report` (incl. opaque `inference.Verdict`s); it never mutates or reconstructs marker strings. |
| orchestrate.Plan → core | Drift Plan crosses in; the core reads `Plan.Changed` only, never writes units. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | Information Disclosure | doctor.Report `--json`/output | mitigate | Core surfaces findings + remediation only; any `Raw` field is `json:"-"` (never serialized), mirroring `status.ServiceStatus`/`CheckResult.Raw`. config has no secrets. |
| T-13-02 | Tampering | backend marker leak | mitigate | Consume `inference.Verdict` opaquely; route ROCm via `inference.IsROCmFamily`; never type `Vulkan0`/`ROCm0`/image tags. Asserted by `go test ./internal/inference/ -run TestSeamGrepGate`. |
| T-13-03 | Spoofing (false-green) | offload-vs-health fold | mitigate | A confident `inference.StatusFail` is folded as BLOCK FAIL that dominates `HealthReady` (Pitfall 3); covered by `TestOffloadFailDominatesHealth`. |
| T-13-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase (zero new deps; `go.mod` unchanged) — package legitimacy gate N/A per 13-RESEARCH Package Legitimacy Audit. |

No HIGH-severity threat is left un-mitigated. The core is pure (no host I/O, no
outbound, no mutation) — its net ASVS L1 surface is V5 (input validation via
fail-closed routing) only.
</threat_model>

<verification>
- `go test ./internal/doctor/` green (core aggregation, worst-wins, remediation-present, offload-FAIL-dominates-health, drift-WARN, graceful degradation).
- `go test ./internal/inference/ -run TestSeamGrepGate` green (no leaked backend literals in `internal/doctor`).
- `git diff --stat internal/status/` shows NO change (status.Report untouched, D-02).
- `make check` green at wave merge.
</verification>

<success_criteria>
- DOCTOR-01: worst-wins fold produces a "PASS"/"WARN"/"FAIL" Overall whose class maps cleanly to the 0/exitWarn(2)/exitBlocked(1) contract Plan 02 wires (mirroring preflight constants — D-04/Pitfall 1).
- DOCTOR-02: every non-PASS Finding carries non-empty Remediation; a confident offload FAIL is a BLOCK FAIL that dominates HealthReady (no false-green).
- DOCTOR-03: a non-empty `Plan.Changed` yields a drift WARN Finding.
- The core is pure (no os.Exit, no print, no host I/O, no mutation); status.Report is untouched; the seam grep-gate stays green.
</success_criteria>

<output>
Create `.planning/phases/13-villa-doctor-health-diagnosis/13-01-SUMMARY.md` when done.
</output>
