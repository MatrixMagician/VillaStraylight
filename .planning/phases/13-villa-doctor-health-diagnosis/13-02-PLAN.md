---
phase: 13-villa-doctor-health-diagnosis
plan: 02
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - cmd/villa/doctor.go
  - cmd/villa/doctor_test.go
  - cmd/villa/testdata/doctor.json.golden
  - cmd/villa/root.go
autonomous: true
requirements: [DOCTOR-01, DOCTOR-02, DOCTOR-03]

must_haves:
  truths:
    - "`villa doctor` over a healthy stubbed stack exits 0 (exitPass)"
    - "A confident residency FAIL exits 1 (exitBlocked) — mirroring preflight's authoritative constants"
    - "Config-vs-disk drift / typed-Unknown exits 2 (exitWarn)"
    - "`villa doctor --json` emits a byte-frozen contract with schema_version:1"
    - "`villa doctor` never creates the Quadlet unit dir (read-only resolver, no MkdirAll)"
    - "`newDoctor()` is registered on the root command tree"
  artifacts:
    - path: "cmd/villa/doctor.go"
      provides: "doctor cobra verb, liveDoctorDeps, renderDoctor exit rollup, read-only unit-dir resolver"
      contains: "func renderDoctor"
    - path: "cmd/villa/doctor_test.go"
      provides: "TestDoctorExitCodes (exit table) + TestDoctorJSON (golden)"
      contains: "func TestDoctorExitCodes"
    - path: "cmd/villa/testdata/doctor.json.golden"
      provides: "Byte-frozen doctor --json contract (schema_version:1)"
      contains: "schema_version"
    - path: "cmd/villa/root.go"
      provides: "doctor verb registration"
      contains: "newDoctor()"
  key_links:
    - from: "cmd/villa/doctor.go"
      to: "internal/doctor.Aggregate"
      via: "runDoctor builds liveDoctorDeps and calls doctor.Aggregate"
      pattern: "doctor\\.Aggregate"
    - from: "cmd/villa/doctor.go"
      to: "cmd/villa/preflight.go (exitPass/exitWarn/exitBlocked)"
      via: "renderDoctor reuses the package-level exit constants"
      pattern: "exitBlocked"
    - from: "cmd/villa/doctor.go"
      to: "internal/status (status.Run via liveStatusDeps)"
      via: "Deps.StatusReport wired to status.Run(*liveStatusDeps())"
      pattern: "liveStatusDeps"
    - from: "cmd/villa/root.go"
      to: "cmd/villa/doctor.go (newDoctor)"
      via: "root.AddCommand(newDoctor())"
      pattern: "newDoctor"
---

<objective>
Wire the pure `internal/doctor` core (Plan 01) to the host and the CLI: a thin
`cmd/villa/doctor.go` cobra verb with `liveDoctorDeps`, a read-only unit-dir resolver,
the worst-wins exit-code mapping mirroring `renderPreflight`, doctor's OWN `--json`
golden, and registration on the root command tree.

Purpose: deliver `villa doctor` end-to-end — DOCTOR-01's 0/2/1 exit contract
(mirroring the authoritative preflight constants), DOCTOR-02's remediation-bearing
findings, and DOCTOR-03's drift detection — as a read-only verb that mutates nothing
(D-03), creates no Quadlet dir (Pitfall 2), and runs no generation probe (D-07).

Output:
- `cmd/villa/doctor.go` — `newDoctor`, `runDoctor`, `renderDoctor`, `renderDoctorTable`, `liveDoctorDeps`, `unitDirReadOnly`
- `cmd/villa/doctor_test.go` — `TestDoctorExitCodes`, `TestDoctorJSON`
- `cmd/villa/testdata/doctor.json.golden` — frozen `--json` contract (schema_version:1)
- `cmd/villa/root.go` — `root.AddCommand(newDoctor())`
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-villa-doctor-health-diagnosis/13-CONTEXT.md
@.planning/phases/13-villa-doctor-health-diagnosis/13-RESEARCH.md
@.planning/phases/13-villa-doctor-health-diagnosis/13-PATTERNS.md
@.planning/phases/13-villa-doctor-health-diagnosis/13-VALIDATION.md
@.planning/phases/13-villa-doctor-health-diagnosis/13-01-SUMMARY.md
@CLAUDE.md
</context>

<artifacts_this_phase_produces>
This plan creates (treat as newly-created — not on `main`):
- `cmd/villa/doctor.go` and its symbols: `newDoctor() *cobra.Command`, `runDoctor(cmd, args, deps) int`, `renderDoctor(w, report, asJSON, withProvenance) int`, `renderDoctorTable(w, report, withProvenance)`, `liveDoctorDeps() (doctor.Deps, error)`, `unitDirReadOnly() (string, error)`
- `cmd/villa/doctor_test.go` and its tests: `TestDoctorExitCodes`, `TestDoctorJSON`, plus `doctor.Report` fixture builders (`healthyReport`/`driftReport`/`offloadFailReport` or equivalent)
- `cmd/villa/testdata/doctor.json.golden` (NEW byte-frozen fixture, schema_version:1)
- A one-line `root.AddCommand(newDoctor())` edit in `cmd/villa/root.go`

Consumes from Plan 01 (already created): `doctor.Finding`, `doctor.Report`,
`doctor.Deps`, `doctor.Aggregate`, `doctor.reportSchemaVersion`.
Reuses existing (on `main`): `exitPass`/`exitWarn`/`exitBlocked` (cmd/villa/preflight.go),
`liveStatusDeps`/`status.Run`, `detect.Probe`, `config.LoadVilla`, `inference.BackendFor`,
`orchestrate.Render`/`Reconcile`, `assertGolden`, the shared `update` flag.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: doctor cobra verb + liveDoctorDeps + read-only unit-dir resolver + exit rollup</name>
  <files>cmd/villa/doctor.go, cmd/villa/root.go</files>
  <read_first>
    - .planning/phases/13-villa-doctor-health-diagnosis/13-PATTERNS.md (`cmd/villa/doctor.go` section — cobra verb, renderDoctor exit rollup, liveDoctorDeps wiring, the read-only unit-dir resolver twist)
    - cmd/villa/status.go (newStatus ~lines 41-61; runStatus exit mapping ~line 66; liveStatusDeps ~lines 157-202 — reuse wholesale)
    - cmd/villa/preflight.go (renderPreflight lines 73-113 — the authoritative exit rollup to mirror; exit constants lines 19-23; renderPreflightTable lines 127-145)
    - cmd/villa/lifecycle.go (renderStack ~line 52 — the Render-from-config front half)
    - cmd/villa/backend.go (Render→Reconcile call site ~lines 443-452)
    - cmd/villa/install.go (quadletUnitDir ~lines 749-759 — the MkdirAll write twin doctor must NOT reuse as-is)
    - internal/orchestrate/reconcile.go (Reconcile signature line 26; assertInsideDir line 104)
    - internal/orchestrate/render.go (Render at line 80; RenderInput fields)
    - cmd/villa/root.go (newRoot AddCommand list ~lines 34-35)
    - .planning/phases/13-villa-doctor-health-diagnosis/13-01-SUMMARY.md (the exact doctor.Deps field names + Report shape created in Plan 01)
  </read_first>
  <action>
    Create `cmd/villa/doctor.go` in `package main`.

    `newDoctor() *cobra.Command`: `Use: "doctor"`, short/long describing a read-only,
    one-shot health diagnosis of the RUNNING install (exits 0 healthy / 2 warnings /
    1 blocking fault), `Args: cobra.NoArgs`. Its `RunE` builds `deps, err :=
    liveDoctorDeps()`; on err print to `cmd.ErrOrStderr()` and `os.Exit(exitBlocked)`;
    else `os.Exit(runDoctor(cmd, args, deps))`. No new persistent flags — reuse the
    global `jsonOut`/`verbose`. NO `--force` (read-only; D-03).

    `runDoctor(cmd *cobra.Command, args []string, deps doctor.Deps) int`: call
    `report := doctor.Aggregate(deps)`; return `renderDoctor(cmd.OutOrStdout(), report,
    jsonOut, verbose)`.

    `renderDoctor(w io.Writer, r doctor.Report, asJSON, withProvenance bool) int`: mirror
    `renderPreflight` EXACTLY. If `asJSON`: `enc := json.NewEncoder(w);
    enc.SetIndent("", "  "); _ = enc.Encode(r)` else `renderDoctorTable(w, r,
    withProvenance)`. Then worst-wins over `r.Findings`: collect BLOCK-class FAILs and
    anyWarn; `if len(blockFails) > 0 { return exitBlocked }`; `if anyWarn { return
    exitWarn }`; `return exitPass`. REUSE the package-level constants `exitPass`/
    `exitWarn`/`exitBlocked` from preflight.go — DO NOT redefine them. CRITICAL
    (Pitfall 1): a confident FAIL → `exitBlocked`(=1); a WARN/drift/Unknown →
    `exitWarn`(=2). Do NOT invert.

    `renderDoctorTable(w io.Writer, r doctor.Report, withProvenance bool)`: mirror
    `renderPreflightTable` — `tabwriter.NewWriter(w, 0, 4, 2, ' ', 0)`, one row per
    Finding `ID\tTier\tStatus\tDetail`, appending ` — Remediation` to the detail cell on
    any non-PASS status; optional provenance column.

    `unitDirReadOnly() (string, error)`: clone `quadletUnitDir` MINUS the `os.MkdirAll`
    — `base, err := os.UserConfigDir(); ...; return filepath.Join(base, "containers",
    "systemd"), nil`. NO directory creation (Pitfall 2 / D-03).

    `liveDoctorDeps() (doctor.Deps, error)`: build the live seam. `sd, err :=
    liveStatusDeps()` (reuse wholesale — do NOT re-wire HTTP/journald/GTT probes); wire
    `StatusReport: func() status.Report { return status.Run(*sd) }`; `Probe:
    detect.Probe`; `LoadConfig: config.LoadVilla`; `Backend:` from
    `config.LoadVilla().Backend`; `DriftPlan:` a closure that loads config, resolves
    `inference.BackendFor(cfg.Backend)` (fail-closed), renders units via
    `orchestrate.Render(orchestrate.RenderInput{...})` (copy the backend.go Render input
    idiom), then `orchestrate.Reconcile(units, dir)` where `dir, _ := unitDirReadOnly()`
    — returns the `orchestrate.Plan` and any error to the core; NEVER `WriteUnits`. The
    core maps a Reconcile error to a typed-Unknown WARN (D-08), so DriftPlan returns the
    error verbatim rather than swallowing it.

    Then edit `cmd/villa/root.go`: add `newDoctor()` to the existing
    `root.AddCommand(...)` list alongside `newStatus()`/`newBackend()`.

    Do NOT type any backend marker literal anywhere in this file (TestSeamGrepGate walks
    cmd/villa) — route ROCm only via the core's `inference.IsROCmFamily` handling and
    resolve backends only through `inference.BackendFor`.
  </action>
  <verify>
    <automated>cd /home/oliverh/repos/github/MatrixMagician/VillaStraylight && go build ./cmd/villa/ && go test ./internal/inference/ -run TestSeamGrepGate</automated>
  </verify>
  <acceptance_criteria>
    - `cmd/villa/doctor.go` contains `func renderDoctor`, `func liveDoctorDeps`, `func unitDirReadOnly`, and `doctor.Aggregate`
    - `cmd/villa/doctor.go` does NOT contain the literal `MkdirAll`
    - `grep -c 'newDoctor()' cmd/villa/root.go` is >= 1 (verb registered)
    - `go build ./cmd/villa/` exits 0
    - `go test ./internal/inference/ -run TestSeamGrepGate` exits 0 (no leaked literals)
    - `grep -v '^//' cmd/villa/doctor.go | grep -cE 'Vulkan0|ROCm0|HSA_OVERRIDE'` prints 0
  </acceptance_criteria>
  <done>`villa doctor` is registered and builds; the verb wires the pure core to live host seams via a read-only (no-MkdirAll) unit-dir resolver; the exit rollup reuses the authoritative preflight constants; the seam grep-gate stays green.</done>
</task>

<task type="auto">
  <name>Task 2: Wave-0 cmd tests — TestDoctorExitCodes + TestDoctorJSON golden</name>
  <files>cmd/villa/doctor_test.go, cmd/villa/testdata/doctor.json.golden</files>
  <read_first>
    - .planning/phases/13-villa-doctor-health-diagnosis/13-PATTERNS.md (`cmd/villa/doctor_test.go` + `doctor.json.golden` sections)
    - cmd/villa/preflight_test.go (TestPreflightExitCodes ~lines 59-84; assertGolden ~lines 41-57; the shared `update` flag note ~line 13)
    - cmd/villa/status_test.go (TestStatusJSONGolden ~lines 285-315 — the golden compare/update idiom)
    - cmd/villa/detect_test.go (where the package-shared `update` flag is declared — reference it, never redeclare)
    - .planning/phases/13-villa-doctor-health-diagnosis/13-RESEARCH.md (Pitfall 1 warning signs — exitBlocked for FAIL, exitWarn for drift)
  </read_first>
  <action>
    Create `cmd/villa/doctor_test.go` in `package main`.

    Build `doctor.Report` fixtures directly (no live host): `healthyReport()` (all-PASS,
    Overall "PASS"), `driftReport()` (one drift WARN Finding, Overall "WARN"),
    `offloadFailReport()` (one BLOCK-class residency FAIL Finding, Overall "FAIL").

    `TestDoctorExitCodes`: a table of `{name, report, wantCode, golden}` mirroring
    `TestPreflightExitCodes`. Call `code := renderDoctor(&buf, tc.report, false, false)`
    and assert `code == tc.wantCode`. Rows (CRITICAL — Pitfall 1, do NOT invert):
    `{"healthy", healthyReport(), exitPass, "doctor-pass.golden"}`,
    `{"warn", driftReport(), exitWarn, "doctor-warn.golden"}` (drift → exit 2),
    `{"fail", offloadFailReport(), exitBlocked, ""}` (residency FAIL → exit 1). Where a
    golden name is given, compare the rendered table via the shared `assertGolden`
    helper.

    `TestDoctorJSON`: call `renderDoctor(&buf, healthyReport(), true, false)` (or run
    `runDoctor` in json mode), then compare/update `testdata/doctor.json.golden` using
    the shared `assertGolden` + the package-level `update` flag (declared in
    detect_test.go — reference, do not redeclare). The golden MUST contain
    `"schema_version": 1`.

    Generate the golden: run `go test ./cmd/villa/ -run TestDoctorJSON -update`, then
    re-run WITHOUT `-update` to confirm it is frozen.
  </action>
  <verify>
    <automated>cd /home/oliverh/repos/github/MatrixMagician/VillaStraylight && go test ./cmd/villa/ -run 'TestDoctorExitCodes|TestDoctorJSON'</automated>
  </verify>
  <acceptance_criteria>
    - `cmd/villa/doctor_test.go` contains `func TestDoctorExitCodes` and `func TestDoctorJSON`
    - The exit table asserts `exitBlocked` for the residency-FAIL row and `exitWarn` for the drift row (NOT inverted)
    - `cmd/villa/testdata/doctor.json.golden` exists and contains `schema_version`
    - `go test ./cmd/villa/ -run 'TestDoctorExitCodes|TestDoctorJSON'` exits 0 (golden frozen, no -update)
    - The test does NOT redeclare a package-level `update` flag (`grep -c 'update = flag.Bool' cmd/villa/doctor_test.go` prints 0)
  </acceptance_criteria>
  <done>The exit-code table and the frozen `--json` golden pass without `-update`; the residency-FAIL/drift exit codes match the authoritative preflight constants (1/2, not inverted).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host → cmd tier | journald / sysfs GTT / loopback HTTP / systemctl is-active / config.toml / unit-dir reads cross into `liveDoctorDeps`; all reused from the already-bounded `liveStatusDeps` + `orchestrate.Reconcile` seams. |
| cmd tier → filesystem | The ONLY net-new file access is a READ of the Quadlet unit dir via `Reconcile`; doctor adds a path-only resolver and never writes. |
| cmd tier → user (stdout) | `--json`/table output crosses to the user; findings + remediation only, no secrets. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-04 | Tampering | side-effecting "read-only" command | mitigate | `unitDirReadOnly` drops the `quadletUnitDir` MkdirAll (Pitfall 2/D-03); no `WriteUnits`; no generation probe (D-07). Asserted: `doctor.go` contains no `MkdirAll`. |
| T-13-05 | Information Disclosure | new outbound / telemetry | mitigate | doctor adds NO new outbound — `liveDoctorDeps` reuses only `liveStatusDeps`' loopback-bounded HTTP probes; zero-telemetry invariant preserved. |
| T-13-06 | Tampering | command injection via service/model name | mitigate | Fixed-arg `exec.Command` only (inherited from the reused status/orchestrate seams); model names catalog-resolved, never shell-interpolated. |
| T-13-07 | Tampering | path traversal reading unit files | mitigate | `orchestrate.Reconcile`/`assertInsideDir` traversal guard (reconcile.go:104); doctor reads the unit dir, never writes. |
| T-13-02 | Tampering | backend marker leak in cmd tier | mitigate | No marker literals in `cmd/villa/doctor.go`; ROCm routed via core's `inference.IsROCmFamily`; backends resolved via `inference.BackendFor`. Asserted by `TestSeamGrepGate` (walks cmd/villa). |
| T-13-SC | Tampering | npm/pip/cargo installs | accept | No package installs (zero new deps; `go.mod` unchanged) — N/A per 13-RESEARCH Package Legitimacy Audit. |

No HIGH-severity threat is left un-mitigated. doctor strictly NARROWS risk vs existing
verbs — it mutates nothing, adds no outbound, and reuses already-bounded probes.
</threat_model>

<verification>
- `go build ./cmd/villa/` green; `villa doctor` registered on the root tree.
- `go test ./cmd/villa/ -run 'TestDoctorExitCodes|TestDoctorJSON'` green; golden frozen WITHOUT `-update`.
- `go test ./internal/inference/ -run TestSeamGrepGate` green (no leaked literals in cmd/villa).
- `cmd/villa/doctor.go` contains no `MkdirAll` (read-only invariant).
- `make check` green at phase gate.
- On-hardware UAT (manual, gfx1151 — per 13-VALIDATION Manual-Only Verifications): healthy install → exit 0; induced CPU fallback → exit 1 + residency-FAIL finding + remediation; hand-touched unit → drift WARN exit 2 + reconcile remediation.
</verification>

<success_criteria>
- DOCTOR-01: `villa doctor` exits 0 (healthy) / 2 (warning) / 1 (blocking), mirroring the authoritative preflight exit constants (Pitfall 1).
- DOCTOR-02: every non-healthy finding renders with remediation text; a confident CPU fallback exits 1 (never a false-green over a health-200).
- DOCTOR-03: config-vs-disk drift renders a WARN finding and exits 2.
- doctor mutates nothing: no MkdirAll, no WriteUnits, no generation probe; adds no outbound; its `--json` golden is frozen at schema_version:1; the seam grep-gate stays green.
</success_criteria>

<output>
Create `.planning/phases/13-villa-doctor-health-diagnosis/13-02-SUMMARY.md` when done.
</output>
