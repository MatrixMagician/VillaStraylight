---
phase: 10-backend-tok-s-surfacing
plan: 03
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - internal/dashboard/assets/dashboard.html
  - internal/dashboard/assets/dashboard.css
  - internal/dashboard/assets/dashboard.js
  - internal/dashboard/api_test.go
autonomous: true
requirements: [DASH-06]
must_haves:
  truths:
    - "The control dashboard Health panel shows the active backend + image tag from the /api/status poll (image omitted when unset; backend-absent → gray 'unavailable' badge, never a fabricated default)"
    - "The dashboard Performance panel labels the tok/s row by the active backend (number from /api/metrics, label from /api/status); idle/unavailable keep the existing honest copy with NO backend appended to a fabricated 0"
    - "The dashboard Health panel shows a tri-state ROCm-readiness badge reusing the existing badge-ready/warn/unknown classes and the GPU busy_available gray-badge precedent (unknown off-hardware, never not-ready from an unevaluable signal)"
    - "metricsView (/api/metrics) JSON shape is UNCHANGED — backend identity lives only in /api/status (D-01)"
  artifacts:
    - path: "internal/dashboard/assets/dashboard.js"
      provides: "renderBackend (backend+image rows + readiness badge); lastBackend stash in poll(); (backend) suffix on the generation tok/s row"
      contains: "lastBackend"
    - path: "internal/dashboard/api_test.go"
      provides: "assertions that /api/status carries backend/image/rocm_readiness and metricsView shape is unchanged"
  key_links:
    - from: "dashboard.js poll() .then(report)"
      to: "report.backend / report.image / report.rocm_readiness"
      via: "renderBackend after renderHealth + lastBackend stash (textContent, XSS-safe)"
      pattern: "renderBackend"
    - from: "dashboard.js renderPerformance generation row"
      to: "lastBackend stashed from the status poll"
      via: "(backend) suffix gated so idle/unavailable branches are untouched"
      pattern: "lastBackend"
---

<objective>
Render the three approved UI-SPEC elements in the existing vanilla-JS control dashboard, composing the `Report` fields landed by Plan 10-01: the active backend + image tag (Health panel), the tok/s row labeled by backend (Performance panel, number from `/api/metrics` + label from `/api/status`), and the tri-state ROCm-readiness badge (Health panel). Reuse existing panel/metric-row/badge idioms — no new framework, no new HTTP endpoint, no new CSS token. Assert `/api/status` carries the new fields and `metricsView` is unchanged. (DASH-06)

Purpose: The dashboard consumes the SAME `status.Report` the CLI does — `handleStatus` serves it verbatim, so the new fields arrive on `/api/status` "for free" once Plan 10-01 lands them (depends_on: 10-01). This plan is pure presentation: three append-only UI additions per the approved 6/6 UI-SPEC, all honest-Unknown, all built via `document.createElement` + `textContent` (XSS-safe). It touches NO Go golden (no double-touch ordering risk).

Output: `renderBackend` + the readiness badge + the tok/s `(backend)` suffix in `dashboard.js`; any minimal HTML mount / CSS only if an existing class genuinely cannot be composed (expected: zero structural HTML, zero new CSS); api_test.go assertions for the new `/api/status` fields + the unchanged `metricsView`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-backend-tok-s-surfacing/10-CONTEXT.md
@.planning/phases/10-backend-tok-s-surfacing/10-RESEARCH.md
@.planning/phases/10-backend-tok-s-surfacing/10-PATTERNS.md
@.planning/phases/10-backend-tok-s-surfacing/10-UI-SPEC.md
@.planning/phases/10-backend-tok-s-surfacing/10-VALIDATION.md
@internal/status/status.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: renderBackend (active backend + image rows + ROCm-readiness badge) + lastBackend stash in poll()</name>
  <files>internal/dashboard/assets/dashboard.js, internal/dashboard/assets/dashboard.html, internal/dashboard/assets/dashboard.css</files>
  <read_first>
    - internal/dashboard/assets/dashboard.js — `poll()` `.then(report)` body (:452-456: add `lastBackend = report.backend` + a `renderBackend(...)` call after `renderHealth`); the module-scoped var declarations near :31 (declare `lastBackend` like `switching`); `renderHealth` (:117-141, the `.health-row` build idiom via createElement+textContent); the GPU `busy_available` gray-badge branch (:217-230, the honest-Unknown badge precedent to mirror); `mutedP(text)` helper (:40); `healthClass`/`healthLabel` idiom (:72-89).
    - internal/dashboard/assets/dashboard.css — confirmed already-present: `.badge` + `.badge-ready/warn/down/unknown` (:156-168) backed by `--status-ready/warn/unknown` (:16-19); `.health-row`/`.health-detail` (:126-141); `.metric-value` tabular-nums (:182). Add NO new color/size/weight/spacing token (UI-SPEC locked).
    - internal/dashboard/assets/dashboard.html — `#health-panel`/`#health-rows` (:28-33); rows are JS-appended, so no structural change required (an optional `<div id="backend-rows">` mount is executor discretion).
    - SOURCE OF TRUTH: 10-UI-SPEC.md §"The three NEW visual elements" + §"Readiness badge color mapping" (ready→badge-ready "ROCm ready"; not-ready→badge-warn "ROCm not ready"; unknown→badge-unknown "ROCm readiness unknown") + §Copywriting Contract (image unset → omit row; backend absent → gray "unavailable" badge, never a fabricated default) + §"Forbidden copy" + §"Seam-gate copy rule". 10-CONTEXT.md D-01 (mirror backend show {backend,image}), D-04 (tri-state, unknown never masquerades as not-ready). PATTERNS.md §dashboard.js Analogs B/C/D.
    - internal/status/status.go — the json tags landed by Plan 10-01 (`backend`, `image`, `rocm_readiness`) so the JS keys match exactly.
  </read_first>
  <action>
    Declare a module-scoped `lastBackend` near dashboard.js:31 (like `switching`). In `poll()` `.then(report)` (after `renderHealth(report.services)` at :452-456): set `lastBackend = report.backend;` and call a new `renderBackend(report.backend, report.image, report.rocm_readiness)`. Implement `renderBackend`: build `.health-row` rows via `document.createElement` + `textContent` exactly as `renderHealth` does — a `backend` row (value `report.backend` verbatim; if absent/empty render a gray `badge badge-unknown` reading `unavailable` reusing the `healthLabel` default — NEVER a fabricated default backend name), and an `image` row (value `report.image` in `.health-detail` monospace tabular; OMIT the row entirely when image is unset). Render the readiness badge by copying the GPU `busy_available` branch shape (:217-230, createElement+textContent, XSS-safe), mapping the tri-state to the EXISTING classes per the UI-SPEC table: `ready`→`badge badge-ready` "ROCm ready"; `not-ready`→`badge badge-warn` "ROCm not ready"; `unknown`/absent→`badge badge-unknown` "ROCm readiness unknown" + optional `mutedP` caption "ROCm readiness can't be evaluated on this host." Use ONLY existing CSS classes/tokens — add a CSS rule ONLY if an existing class genuinely cannot be composed (expected: none). Do NOT write any gated marker literal (`ROCm0`, `HSA_OVERRIDE_GFX_VERSION`, `Memory access fault`, `kyuz0`, `docker.io/`, image tags, `--device`, `--group-add`, `keep-groups`) into any asset — the bare word "ROCm" in a badge label is allowed; backend/image arrive as runtime values from `report.backend`/`report.image` (Pitfall 5 / UI-SPEC seam-gate copy rule). Do NOT use `innerHTML` for any server value. Do NOT add a backend field to `metricsView`/`/api/metrics` (D-01 — identity in status only).
  </action>
  <verify>
    <automated>go test ./internal/dashboard/</automated>
    <automated>go test ./internal/inference/ -run TestSeamGrepGate</automated>
    <automated>go build ./...</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: `internal/dashboard/assets/dashboard.js` declares module-scoped `lastBackend`, sets it in `poll()`, and defines `renderBackend` building `.health-row` rows + a tri-state badge via `document.createElement`/`textContent` (no `innerHTML` of server values).
    - SOURCE: readiness badge maps `ready`→`badge-ready`, `not-ready`→`badge-warn`, `unknown`/absent→`badge-unknown` with the UI-SPEC copy strings.
    - GUARD (VALIDATION.md): `go test ./internal/inference/ -run TestSeamGrepGate` green — no gated marker literal in any dashboard asset.
    - GUARD: no `innerHTML` interpolation of `report.backend`/`report.image`; no new CSS color/size/weight/spacing token; no new HTTP endpoint; `git diff go.mod` empty.
    - BEHAVIOR: `go build ./...` succeeds (assets embed via go:embed).
  </acceptance_criteria>
  <done>The Health panel renders the active backend + image (image omitted when unset; gray "unavailable" badge when backend absent) and a tri-state ROCm-readiness badge reusing existing classes; lastBackend is stashed for the Performance renderer; no seam-gate marker leaks; no innerHTML of server values.</done>
</task>

<task type="auto">
  <name>Task 2: Label the tok/s row by backend + assert /api/status carries new fields and metricsView is unchanged</name>
  <files>internal/dashboard/assets/dashboard.js, internal/dashboard/api_test.go</files>
  <read_first>
    - internal/dashboard/assets/dashboard.js — `renderPerformance(m)` generation row (:170, the `metricRow("generation", ...)` to extend); the idle/unavailable branches (:151, :159, :163) which MUST stay untouched; `metricRow(label, value)` helper (:49-61).
    - internal/dashboard/api.go — `handleStatus` (:17-20) serves the `Report` verbatim (new fields arrive for free); `metricsView`/`handleMetrics` (:39-67, :73) — the shape that MUST stay unchanged (do NOT add backend here, D-01).
    - ANALOG (existing api tests) internal/dashboard/api_test.go — the existing `/api/status` and `/api/metrics` assertions to extend.
    - SOURCE OF TRUTH: 10-UI-SPEC.md §Copywriting Contract (tok/s generating → "{N.N} tok/s ({backend})"; idle → keep "Idle — no active generation." with NO backend; unavailable → keep "Unavailable"). 10-CONTEXT.md D-01 (number from metrics, label from status, never duplicate identity into metricsView). PATTERNS.md §dashboard.js Analog A.
  </read_first>
  <action>
    In `renderPerformance(m)` generation row (:170), append the `(backend)` suffix using the stashed `lastBackend`, ONLY on the generating branch: `metricRow("generation", (m.gen_tokens_per_sec || 0).toFixed(1) + " tok/s" + (lastBackend ? " (" + lastBackend + ")" : ""))`. Leave the idle (:159), activity-unknown (:163), and unavailable (:151) branches BYTE-UNCHANGED — never append a backend to a fabricated 0 or to the honest idle/unavailable copy (UI-SPEC element 2 honesty rule). In `internal/dashboard/api_test.go`: extend the `/api/status` test to assert the response carries `backend`, `image`, and `rocm_readiness` (decode the served `Report` JSON and assert the keys/values come through `handleStatus`); add/extend a `/api/metrics` test asserting the `metricsView` JSON shape is UNCHANGED (no `backend`/identity key added). Do NOT modify `internal/dashboard/api.go` (handleStatus serves the new fields automatically; metricsView stays as-is).
  </action>
  <verify>
    <automated>go test ./internal/dashboard/</automated>
    <automated>go test ./internal/inference/ -run TestSeamGrepGate</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: `renderPerformance` generation row appends `(lastBackend)` only on the generating branch; the idle/activity-unknown/unavailable branches are unchanged (no backend appended to a fabricated 0).
    - BEHAVIOR (VALIDATION.md): `go test ./internal/dashboard/` green — `/api/status` carries `backend`/`image`/`rocm_readiness`; `metricsView` JSON shape is asserted UNCHANGED (no identity key added).
    - SOURCE: `internal/dashboard/api.go` is NOT modified (metricsView untouched, D-01).
    - GUARD (VALIDATION.md): `go test ./internal/inference/ -run TestSeamGrepGate` green.
  </acceptance_criteria>
  <done>The Performance tok/s row reads e.g. "12.3 tok/s (vulkan)" when generating, with idle/unavailable copy untouched; api_test asserts /api/status carries the new fields and metricsView is unchanged; metricsView/api.go untouched; seam gate green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| /api/status + /api/metrics → browser DOM | Resolver-sourced backend/image strings + enums + a pinned image tag are rendered into HTML. Must use textContent, never innerHTML. |
| inference seam → dashboard assets | Backend identity arrives as runtime values from report fields, never as gated literals in the JS/HTML. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-08 | Tampering (XSS) | rendering backend/image/readiness strings into the DOM | mitigate | All server values set via `document.createElement` + `textContent` (the established dashboard idiom, dashboard.js:115-133); NO `innerHTML` interpolation of `report.backend`/`report.image`. Values are resolver-sourced enums + a pinned image tag, but the no-innerHTML rule is enforced regardless. |
| T-10-09 | Tampering | backend-marker literal leak into a dashboard asset | mitigate | Bare word "ROCm" only in badge labels; no `ROCm0`/`HSA_OVERRIDE_GFX_VERSION`/`Memory access fault`/image-tag/device literal. Enforced by `TestSeamGrepGate` walking `internal/`. |
| T-10-10 | Information disclosure | duplicating identity into /api/metrics or widening metricsView | mitigate | metricsView untouched; identity lives only in /api/status (D-01); api_test asserts metricsView shape unchanged. No new endpoint, no new field on the metrics surface. |
| T-10-11 | Information disclosure / telemetry | introducing an outbound call | mitigate | No new HTTP endpoint, no external font/CDN/analytics; only the existing same-origin /api/* polls (strictly-local, zero-telemetry). |
| T-10-SC | Tampering | npm/pip/cargo installs | accept | No package installs; no build step; `go.mod` frozen for v1.1. Guarded by `git diff go.mod` empty. |
</threat_model>

<verification>
- `go build ./... && go vet ./... && go test ./...` green (full suite — per-wave merge gate + phase gate).
- `git diff cmd/villa/testdata/detect.golden.json` empty; `git diff cmd/villa/testdata/status.json.golden` + `recommend.golden.json` reviewed as the pure-addition diffs from Plans 01/02 (no further change here).
- `git diff go.mod` empty; no new HTTP endpoint added.
- `go test ./internal/inference/ -run TestSeamGrepGate` green.
</verification>

<success_criteria>
- The dashboard shows the active backend + image (Health), tok/s labeled by backend (Performance, idle/unavailable copy honest), and the tri-state ROCm-readiness badge (Health) — all append-only, honest-Unknown, reusing existing idioms (DASH-06 SC#1).
- `metricsView`/`/api/metrics` JSON shape is unchanged; identity lives only in `/api/status` (D-01).
- No XSS (textContent only), no seam-marker leak, no new dependency/endpoint/telemetry.
</success_criteria>

<output>
Create `.planning/phases/10-backend-tok-s-surfacing/10-03-SUMMARY.md` when done.
</output>
