---
phase: 10-backend-tok-s-surfacing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/status/status.go
  - internal/status/status_test.go
  - cmd/villa/status.go
  - cmd/villa/status_test.go
  - cmd/villa/testdata/status.json.golden
autonomous: true
requirements: [DASH-06]
must_haves:
  truths:
    - "villa status (table + --json) shows the active backend name and image tag, sourced from the resolved backend (not a literal)"
    - "villa status surfaces live token-generation tok/s labeled by the active backend, omitted entirely when idle or when the /metrics scrape is unavailable (never a fabricated 0)"
    - "villa status shows a tri-state ROCm-readiness indicator folded from the detect rocm_readiness sub-tree, where any unevaluable signal yields unknown (never not-ready)"
    - "On a rocm-configured install the residency verdict keys on the resolved backend's ROCm0 markers (SC#1 correctness, exercisable off-hardware via ResidencyProof markers)"
    - "The status --json golden re-freezes exactly once as a reviewed pure-addition diff; detect.golden.json stays byte-identical"
  artifacts:
    - path: "internal/status/status.go"
      provides: "Report tail-appended fields (Backend, Image, GenTokensPerSec, ROCmReadiness, SchemaVersion); foldROCmReadiness helper; new Deps seam members"
      contains: "func foldROCmReadiness"
    - path: "cmd/villa/status.go"
      provides: "tok/s + readiness seam wiring in liveStatusDeps; new table rows"
      contains: "GenTokensPerSec"
    - path: "cmd/villa/testdata/status.json.golden"
      provides: "Re-frozen status --json contract with the appended fields"
  key_links:
    - from: "internal/status/status.go Run"
      to: "inference.BackendFor(cfg.Backend).Name()/.Image()"
      via: "already-resolved backend at status.go:179 (do not re-resolve)"
      pattern: "report\\.Backend = backend\\.Name\\(\\)"
    - from: "cmd/villa/status.go liveStatusDeps"
      to: "metrics.ScrapeMetrics / IsGenerating / ScrapeSlots"
      via: "GenTokensPerSec Deps seam (nil on idle/unavailable)"
      pattern: "metrics\\.ScrapeMetrics"
    - from: "internal/status/status.go foldROCmReadiness"
      to: "detect.ROCmReadiness (consumed, never recomputed)"
      via: "worst-wins fold, Known-first short-circuit to unknown"
      pattern: "detect\\.ROCmReadiness"
---

<objective>
Make `villa status` (CLI table + `--json`) and the shared `internal/status` read-model backend-aware: append the active backend name + image tag, live token-generation tok/s, a tri-state ROCm-readiness indicator, and a schema version to the frozen `Report` struct; wire the tok/s and readiness seams in `liveStatusDeps`; and prove the already-wired SC#1 residency correctness with a rocm-config test. Re-freeze the `status.json.golden` exactly once. (DASH-06)

Purpose: This is one of the two structs that grows in Phase 10. Landing the `Report` fields here surfaces them on BOTH `villa status` AND the dashboard `/api/status` poll (one struct, two consumers) — Plan 03 (dashboard) renders these same fields. The golden re-freezes here exactly once (D-06), so no later plan touches `status.json.golden`.

Output: Tail-appended `Report` fields + `foldROCmReadiness` helper + new `Deps` seam members in `internal/status`; the `metrics.ScrapeMetrics` tok/s wiring + new table rows in `cmd/villa`; a re-frozen `status.json.golden`; new unit tests (residency, tok/s typed-optional, readiness fold).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-backend-tok-s-surfacing/10-CONTEXT.md
@.planning/phases/10-backend-tok-s-surfacing/10-RESEARCH.md
@.planning/phases/10-backend-tok-s-surfacing/10-PATTERNS.md
@.planning/phases/10-backend-tok-s-surfacing/10-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Tail-append Report fields + foldROCmReadiness helper + new Deps seams (internal/status)</name>
  <files>internal/status/status.go, internal/status/status_test.go</files>
  <read_first>
    - internal/status/status.go — the `Report` struct (lines 89-104: append AFTER `Overall` at :98, BEFORE the trailing unexported `err error` at :103); the `Deps` struct (:109-143, mirror the `Props func(endpoint string) *inference.PropsInfo` / `Endpoint func() string` seam shape); `Run` which already resolves `backend, err := inference.BackendFor(cfg.Backend)` at :179 and feeds `Markers: backend.ResidencyProof()` at :239.
    - ANALOG (tail-append discipline) internal/detect/profile.go:53-60 — how Phase 7 appended `ROCmReadiness` then `SchemaVersion` LAST, "nothing above it moved" (PATTERNS.md §status.go, Pattern 1).
    - ANALOG (typed-optional, no fabricated 0) internal/dashboard/api.go:44-47 — `LatencyMS *float64 json:"latency_ms,omitempty"` (RESEARCH.md Pattern 2 / PATTERNS.md).
    - ANALOG (Known-first fold) internal/detect/value.go:63-74 (`Bool{Known,Value}`, `KnownBool`/`UnknownBool`); detect.ROCmReadiness 5-Bool sub-tree at internal/detect/profile.go:70-88 (fields: HSAOverrideViable, FirmwareDateOK, KernelFloorOK, RocminfoGfx1151, ImagePolicyOK).
    - SOURCE OF TRUTH: 10-CONTEXT.md D-01 (backend+image, single authoritative surface, mirror backendShowEntry), D-03 (typed-optional tok/s, never fabricated 0), D-04 (tri-state fold, consume-never-recompute, unknown wins over not-ready), D-07 (additive schema_version int, tail-appended last). RESEARCH.md Pattern 3 (the exact foldROCmReadiness shape).
  </read_first>
  <behavior>
    - foldROCmReadiness(detect.ROCmReadiness) returns ROCmUnknown when ANY of the 5 Bools has Known==false (short-circuit, off-hardware default).
    - Returns ROCmNotReady only when every Bool is Known and at least one Value is false.
    - Returns ROCmReady only when every Bool is Known-true.
    - All-unset fixture (off-hardware) → "unknown"; all-Known-good → "ready"; one-Known-bad (rest Known) → "not-ready"; any-unknown → "unknown".
  </behavior>
  <action>
    Append to `Report` (after `Overall` at status.go:98, before the unexported `err` at :103) these tagged fields IN THIS ORDER: `Backend string json:"backend"`, `Image string json:"image"`, `GenTokensPerSec *float64 json:"gen_tokens_per_sec,omitempty"`, `ROCmReadiness ROCmReadinessIndicator json:"rocm_readiness"`, and finally `SchemaVersion int json:"schema_version"` as the LAST tagged field (D-06/D-07). Define `type ROCmReadinessIndicator string` with consts `ROCmReady="ready"`, `ROCmNotReady="not-ready"`, `ROCmUnknown="unknown"` (Claude's-discretion spellings per D-04, use these). Define a `reportSchemaVersion` int const (start at 1 per RESEARCH A2). Add a pure `foldROCmReadiness(r detect.ROCmReadiness) ROCmReadinessIndicator` exactly per RESEARCH.md Pattern 3 (check `b.Known` FIRST, any !Known → ROCmUnknown). Add two `Deps` seam members mirroring the existing seam shapes: `GenTokensPerSec func(endpoint string) *float64` and `ROCmReadiness func() detect.ROCmReadiness`. In `Run`, AFTER the existing `backend, err := inference.BackendFor(cfg.Backend)` at :179, set `report.Backend = backend.Name()`, `report.Image = backend.Image()`, `report.SchemaVersion = reportSchemaVersion`; populate `report.GenTokensPerSec = d.GenTokensPerSec(endpoint)` (guard nil-seam defensively) using the already-resolved endpoint; populate `report.ROCmReadiness = foldROCmReadiness(d.ROCmReadiness())` (guard nil-seam → leave ROCmUnknown). DO NOT touch the residency-input wiring at :239 — it already keys on the resolved `backend.ResidencyProof()` (Pitfall 1: SC#1 is already wired; no edit there). DO NOT write any gated marker literal (`ROCm0`, `HSA_OVERRIDE_GFX_VERSION`, `Memory access fault`, image tags) — backend identity arrives only via `backend.Name()`/`Image()` values (Pitfall 5). Add the readiness-fold table test + the off-hardware-unknown case to status_test.go.
  </action>
  <verify>
    <automated>go test ./internal/status/ -run Readiness</automated>
    <automated>go test ./internal/status/</automated>
    <automated>go build ./...</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: `internal/status/status.go` `Report` has field `Backend string json:"backend"`, `Image string json:"image"`, `GenTokensPerSec *float64 json:"gen_tokens_per_sec,omitempty"`, `ROCmReadiness ROCmReadinessIndicator json:"rocm_readiness"`, and `SchemaVersion int json:"schema_version"` as the LAST tagged field (before unexported `err`).
    - SOURCE: `func foldROCmReadiness` exists and is pure (no I/O, no detect re-probe — reads the passed struct only).
    - BEHAVIOR: `go test ./internal/status/ -run Readiness` passes with the four fold cases (all-unset→unknown, all-good→ready, one-Known-bad→not-ready, any-unknown→unknown).
    - BEHAVIOR (VALIDATION.md): `go test ./internal/status/` green.
    - GUARD: `go build ./...` succeeds; `go.mod` unchanged (`git diff go.mod` empty).
    - GUARD: no edit to the residency wiring (`status.go:239` still reads `backend.ResidencyProof()`).
  </acceptance_criteria>
  <done>Report carries the five tail-appended fields with the correct json tags + order; foldROCmReadiness folds the detect sub-tree honestly (unknown wins over not-ready); the new Deps seams exist; the fold table test is green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire tok/s + readiness seams in liveStatusDeps; add SC#1 rocm-residency proving test + tok/s Deps stubs</name>
  <files>cmd/villa/status.go, cmd/villa/status_test.go</files>
  <read_first>
    - cmd/villa/status.go — `liveStatusDeps` wiring block (:151-175) and the backend/endpoint resolution (:146-150, the endpoint is already backend-resolved); the `liveProps` HTTP-probe seam (:275-301) for the nil-on-failure discipline to mirror; `renderStatusTable` tabwriter rows (:100, :113, :121) and the `withProvenance` (-v) gate at :122.
    - internal/metrics/llamacpp.go — `ScrapeMetrics` (:106), `ScrapeSlots` (:141), `IsGenerating` (:170), `PerfSnapshot.GenTokensPerSec` (:43). REUSE verbatim; do NOT add fields or query the removed KV-cache gauge.
    - ANALOG (Deps stub) cmd/villa/status_test.go — `newStatusDeps` stub (:49-83): add `GenTokensPerSec`/`ROCmReadiness` knobs the same way `Props`/`GTTUsed`/`DashboardHealth` are stubbed.
    - SOURCE OF TRUTH: 10-CONTEXT.md D-03 (reuse metrics.ScrapeMetrics, typed-optional, idle/unavailable → omit), D-02 + RESEARCH.md Pitfall 1 (SC#1 residency is ALREADY wired — Phase 10 PROVES it via a rocm-config test that asserts the verdict keys on ROCm0 markers from the resolved backend's ResidencyProof, exercisable off-hardware). RESEARCH.md "tok/s seam in liveStatusDeps" code example.
  </read_first>
  <behavior>
    - tok/s seam: scrape fails (404/transport) → nil (omitted); scrape OK but !IsGenerating (idle) → nil (omitted); scrape OK + generating → &snap.GenTokensPerSec.
    - rocm-config residency: with cfg.Backend="rocm", the residency verdict's Markers come from backendROCm.ResidencyProof() (asserts ROCm0), not a Vulkan default — assertable off-hardware via the resolved markers.
  </behavior>
  <action>
    Add `"github.com/MatrixMagician/VillaStraylight/internal/metrics"` to the status.go import block (:14-22). In `liveStatusDeps`, wire `GenTokensPerSec: func(endpoint string) *float64 { ... }` reusing `metrics.ScrapeMetrics`/`metrics.ScrapeSlots`/`metrics.IsGenerating` exactly per RESEARCH.md's code example (return nil on `!ok`, return nil on `!IsGenerating`, else return `&snap.GenTokensPerSec`) — mirror `liveProps`'s nil-on-failure discipline. Wire `ROCmReadiness: func() detect.ROCmReadiness { return detect.Probe().ROCmReadiness }`. In `renderStatusTable`, append rows in the existing `fmt.Fprintf(tw, "label\t%s\n", ...)` idiom: `backend  <r.Backend>` always; `image  <r.Image>` gated behind `withProvenance` (-v) per the compact-table recommendation (RESEARCH Open Q1); the tok/s row ONLY when `r.GenTokensPerSec != nil` (omit on nil — never `0.0`; keep the honest idle copy precedent); `rocm-readiness  <r.ROCmReadiness>`. In status_test.go: add `GenTokensPerSec`/`ROCmReadiness` stub knobs to `newStatusDeps`; add the three tok/s cases (generating→value rendered+labeled, idle→omitted, unavailable→omitted); add the SC#1 rocm-config residency test (`cfg.Backend="rocm"` → assert the verdict keys on ROCm markers via the resolved `ResidencyProof()`, off-hardware). DO NOT write gated marker literals into status.go (Pitfall 5); the test may reference `ROCm0` ONLY if the seam-gate excludes `_test.go` — confirm via seam_test.go before doing so, otherwise assert via the backend's `ResidencyProof()` returned markers.
  </action>
  <verify>
    <automated>go test ./cmd/villa/ -run TestStatus</automated>
    <automated>go test ./internal/status/ -run ROCm</automated>
    <automated>go test ./internal/inference/ -run TestSeamGrepGate</automated>
  </verify>
  <acceptance_criteria>
    - BEHAVIOR (VALIDATION.md): `go test ./cmd/villa/ -run TestStatus` green with the tok/s cases (generating→value+backend label, idle→omitted, unavailable→omitted; never a fabricated 0).
    - BEHAVIOR (VALIDATION.md): `go test ./internal/status/ -run ROCm` green — rocm-config residency verdict keys on the resolved ROCm markers (SC#1 proof), exercisable off-hardware.
    - SOURCE: `cmd/villa/status.go` `liveStatusDeps` references `metrics.ScrapeMetrics` and `metrics.IsGenerating`; returns nil on scrape-fail and on idle.
    - GUARD (VALIDATION.md): `go test ./internal/inference/ -run TestSeamGrepGate` green — no `ROCm0`/`HSA_OVERRIDE_GFX_VERSION`/`Memory access fault`/image-tag literal in status.go.
  </acceptance_criteria>
  <done>liveStatusDeps reuses the metrics collector for tok/s (nil on idle/unavailable) and probes readiness; the table renders the four new rows honestly; the rocm-config residency proving test and the three tok/s stub cases are green; the seam gate stays green.</done>
</task>

<task type="auto">
  <name>Task 3: Re-freeze status.json.golden exactly once as a reviewed pure-addition diff</name>
  <files>cmd/villa/testdata/status.json.golden, cmd/villa/status_test.go</files>
  <read_first>
    - ANALOG (the -update mechanism) cmd/villa/status_test.go `TestStatusJSONGolden` (:214-244, writes the golden when `*update` set); the shared `var update = flag.Bool("update", ...)` defined ONCE in cmd/villa/detect_test.go:13 (PATTERNS.md §Golden tests).
    - SOURCE OF TRUTH: 10-CONTEXT.md D-06 (status.json.golden re-frozen exactly once as a pure-addition diff; detect.golden.json stays byte-identical; no existing field reordered/retagged).
  </read_first>
  <action>
    Confirm `TestStatusJSONGolden` (cmd/villa/status_test.go:214) exercises a `Report` whose new fields are populated (so the golden reflects them — populate backend/image/schema_version unconditionally; tok/s only when the fixture generates, else it stays omitted by `omitempty`; rocm_readiness folds to its fixture value). Re-freeze ONCE with `go test ./cmd/villa/ -run TestStatusJSONGolden -update`. Then run the full `go test ./cmd/villa/` so the golden must match without `-update`. Review `git diff cmd/villa/testdata/status.json.golden` — it MUST be a pure tail-addition (new keys `backend`, `image`, optionally `gen_tokens_per_sec`, `rocm_readiness`, `schema_version`); NO existing key reordered/renamed/retyped. Confirm `git diff cmd/villa/testdata/detect.golden.json` is EMPTY (detect unchanged this phase, Pitfall 6) and `git diff cmd/villa/testdata/recommend.golden.json` is EMPTY (owned by Plan 02).
  </action>
  <verify>
    <automated>go test ./cmd/villa/ -run TestStatusJSONGolden</automated>
    <automated>git diff --quiet cmd/villa/testdata/detect.golden.json && echo DETECT_UNCHANGED</automated>
    <automated>go test ./cmd/villa/</automated>
  </verify>
  <acceptance_criteria>
    - BEHAVIOR (VALIDATION.md): `go test ./cmd/villa/ -run TestStatusJSONGolden` passes WITHOUT `-update` (golden matches the encoder bytes).
    - GUARD (VALIDATION.md): `git diff cmd/villa/testdata/detect.golden.json` empty (detect byte-identical).
    - GUARD: `git diff cmd/villa/testdata/recommend.golden.json` empty (not this plan's golden).
    - CONTRACT: the status golden diff is pure tail-addition — no existing key reordered/renamed/retyped.
    - BEHAVIOR: `go test ./cmd/villa/` green.
  </acceptance_criteria>
  <done>status.json.golden re-frozen exactly once carrying the appended fields as a reviewed pure-addition diff; detect + recommend goldens byte-identical; cmd/villa suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| llama-server `/metrics` → CLI scrape | The tok/s number crosses from the local inference container into the CLI read-model. Already bounded upstream. |
| inference seam → surfacing code | Backend identity/markers must arrive only as seam-returned values, never as literals in the consuming layer. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Tampering | backend-marker literal leak into status.go | mitigate | Use `backend.Name()`/`Image()` values only; no `ROCm0`/`HSA_OVERRIDE_GFX_VERSION`/`Memory access fault`/image-tag literal in surfacing code. Enforced by `TestSeamGrepGate` (seam_test.go:34) walking `cmd/villa`+`internal/`. |
| T-10-02 | Repudiation | fabricated tok/s 0 / false-green readiness undermining the honest-status contract | mitigate | `*float64`+`omitempty` for tok/s (nil on idle/unavailable, never 0); `foldROCmReadiness` short-circuits to `unknown` on any unevaluable signal (D-03/D-04/D-08). Asserted by the typed-optional + fold table tests. |
| T-10-03 | Information disclosure | new perf field leaking prompt/slot internals | mitigate | Reuse the field-narrowed `metrics.ScrapeMetrics`/`PerfSnapshot` (no /slots prompt leakage, T-05-08/T-09-01); surface only the gen tok/s float. metricsView untouched (Plan 03). |
| T-10-04 | Denial of service | CLI tok/s scrape hanging Run | mitigate | `ScrapeMetrics` is bounded (2s timeout + 64 KiB io.LimitReader); CLI reuse inherits the bounds — no new scraper. |
| T-10-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase; `go.mod` frozen for v1.1 (no `go get`). Guarded by `git diff go.mod` empty. |
</threat_model>

<verification>
- `go build ./... && go vet ./... && go test ./...` green (full suite — per-wave merge gate, VALIDATION.md).
- `git diff cmd/villa/testdata/detect.golden.json` empty (detect byte-identical, Pitfall 6).
- `git diff go.mod` empty (no new deps, v1.1 constraint).
- `go test ./internal/inference/ -run TestSeamGrepGate` green (no marker leak).
</verification>

<success_criteria>
- `villa status` (table + `--json`) shows active backend + image, live tok/s labeled by backend (omitted when idle/unavailable), and the tri-state ROCm-readiness indicator (DASH-06 SC#1).
- The rocm-config residency verdict keys on the resolved backend's ROCm0 markers (SC#1 correctness proof, off-hardware).
- The `status.json.golden` re-froze exactly once as a pure-addition diff; `detect.golden.json` byte-identical; `schema_version` appended (DASH-06 SC#3).
</success_criteria>

<output>
Create `.planning/phases/10-backend-tok-s-surfacing/10-01-SUMMARY.md` when done.
</output>
