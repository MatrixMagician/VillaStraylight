---
phase: 20-open-webui-memory-rag-wiring-offline-lockdown
plan: 03
type: execute
wave: 2
depends_on: ["20-01", "20-02"]
files_modified:
  - docs/MEMORY.md
autonomous: false
requirements: [MEM-01, MEM-02, MEM-03, MEM-04, KB-01, KB-02, KB-03, PRIV-05]
user_setup:
  - service: host-egress-block
    why: "The runtime zero-outbound proof (PRIV-05/SC#4) needs the host's outbound to the public internet blocked for the duration of the RAG drive, paired with a negative-control probe that MUST fail. The dev/CI host firewall is not under villa's control."
    dashboard_config:
      - task: "Block host outbound to the public internet (e.g. a deny-all egress firewall rule / offline NIC) for the duration of the `villa verify memory` run on gfx1151"
        location: "Host firewall (firewalld/nftables) on the Fedora gfx1151 dev box"
must_haves:
  truths:
    - "An operator doc (docs/MEMORY.md) documents the v1.3 memory stack: save/view/edit/delete memories, the auto-extraction enable path (and why it is default-off), knowledge upload, and the offline-lockdown env table + the `villa verify memory` run incl. its host-egress precondition"
    - "On the live gfx1151 host, a user-stated fact is remembered across separate chats and injected into a later conversation; the user can explicitly save a message/fact; view/edit/delete works and a deleted memory stops being injected"
    - "Automatic LLM-assisted memory extraction is confirmed OFF by default (no auto-save on a default model), and the user enable path (Native Function Calling per-model / a community memory filter) is documented"
    - "A document uploaded into a local knowledge collection yields a cited answer; chunk/embed/retrieve hit only villa-embed + Qdrant (no cloud API, no runtime model download)"
    - "`villa verify memory` PASSes on the live host: the negative-control external probe FAILS (egress proven blocked) AND the real upload→retrieve completes with the planted fact + citation — a runtime assertion, not install-time green"
    - "The three RESEARCH open questions are resolved on-hardware: admin-token mint path, citation field name in the chat-completions response, and the host-egress negative-control mechanism"
  artifacts:
    - path: "docs/MEMORY.md"
      provides: "operator doc for the v1.3 memory stack: how to save/view/edit/delete memories, where to enable auto-extraction (and why it is default-off), how to upload a knowledge doc, the offline-lockdown env table, and how to run `villa verify memory` (incl. the host-egress precondition)"
      contains: "verify memory"
  key_links:
    - from: "villa verify memory"
      to: "live OWUI + villa-embed + villa-qdrant on gfx1151"
      via: "evalRagSmoke negative-control-first + real REST RAG drive"
      pattern: "verify memory"
---

<objective>
On the live gfx1151 host, confirm the behavioral and runtime-privacy criteria that cannot be proven off-hardware: OWUI native Memory across chats (MEM-01/02/04), auto-extraction default-off + its enable path (MEM-03/SC#2), document upload → cited local answer (KB-01/02/03/SC#3), and the headline runtime firewalled zero-outbound document-upload smoke test with a negative control (PRIV-05/SC#4). Capture the operator-facing memory documentation (off-hardware, automatable) and resolve the three RESEARCH open questions on-hardware.

Purpose: SC#4 explicitly requires a RUNTIME assertion (install-time green is insufficient). These criteria depend on a live model + a populated OWUI DB + host-egress control, so they are verification-wave checkpoints, not unit tests. The Wave-1 code (parameterized env block + `villa verify memory` harness) exists; this plan writes the operator doc, runs the harness for real, and signs off the phase.
Output: A `docs/MEMORY.md` operator doc (verified by an automatable content check) and human-verified sign-off on SC#1–4, with the three open questions resolved.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-open-webui-memory-rag-wiring-offline-lockdown/20-CONTEXT.md
@.planning/phases/20-open-webui-memory-rag-wiring-offline-lockdown/20-RESEARCH.md
@.planning/phases/20-open-webui-memory-rag-wiring-offline-lockdown/20-01-SUMMARY.md
@.planning/phases/20-open-webui-memory-rag-wiring-offline-lockdown/20-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author docs/MEMORY.md — the operator doc for the v1.3 memory stack (off-hardware)</name>
  <read_first>
    - 20-01-SUMMARY.md (the rendered D-09 env block — the exact keys/values to reproduce in the offline-lockdown env table)
    - 20-02-SUMMARY.md (the `villa verify memory` invocation + the ragSmokeInput it expects: owuiAddr/owuiPort/question/wantFact — what the operator runs)
    - 20-RESEARCH.md "Runtime State Inventory" (enabling memory on a previously-booted OWUI starts a fresh Qdrant store; `ENABLE_PERSISTENT_CONFIG=False` makes env win over stale DB rows) and "Automatic memory extraction (D-07/MEM-03)" (Native FC / community filter enable paths)
    - docs/ARCHITECTURE.md + docs/DEVELOPMENT.md (house doc style/location)
  </read_first>
  <files>docs/MEMORY.md</files>
  <action>
    Author `docs/MEMORY.md` (off-hardware — pure documentation, no host interaction) as the operator doc for the v1.3 memory stack, covering: (1) how to save a fact/message to Memory and how cross-chat injection works (MEM-01/02); (2) how to view/edit/delete memories and that a deleted memory stops being injected (MEM-04); (3) an explicit "Enabling automatic memory extraction" section stating that automatic LLM-assisted memory extraction is DEFAULT-OFF and WHY (local-model function-calling reliability caveat — D-07), plus the exact enable path the user flips (Native Function Calling per-model in Workspace → Models → Edit, OR a community Adaptive/Auto Memory filter function) and the caveat that enabling Native FC changes RAG injection (keep it off for deterministic citations — KB-02); (4) how to upload a document into a Knowledge collection and ask a cited question (KB-01/02/03), noting chunk/embed/retrieve run on villa-embed + Qdrant only; (5) an "Offline-lockdown environment" table reproducing the D-09 memory-on env keys (VECTOR_DB, QDRANT_URI, ENABLE_QDRANT_MULTITENANCY_MODE, QDRANT_COLLECTION_PREFIX, RAG_EMBEDDING_ENGINE, RAG_OPENAI_API_BASE_URL, RAG_OPENAI_API_KEY, RAG_EMBEDDING_MODEL, RAG_EMBEDDING_QUERY_PREFIX, RAG_EMBEDDING_CONTENT_PREFIX, RAG_EMBEDDING_MODEL_AUTO_UPDATE, ENABLE_MEMORIES, ENABLE_PERSISTENT_CONFIG=False) and what each enforces; (6) how to run `villa verify memory`, including the host-egress precondition (block host outbound for the duration) and that the negative control MUST fail. Keep it concise and operator-facing; do not duplicate the architecture doc. The doc MUST contain the literal strings `Enabling automatic memory extraction`, `ENABLE_PERSISTENT_CONFIG`, and `verify memory` (the automatable content check below asserts these section markers exist).
  </action>
  <verify>
    <automated>test -f docs/MEMORY.md && grep -q 'Enabling automatic memory extraction' docs/MEMORY.md && grep -q 'ENABLE_PERSISTENT_CONFIG' docs/MEMORY.md && grep -q 'verify memory' docs/MEMORY.md && grep -q 'ENABLE_MEMORIES' docs/MEMORY.md</automated>
  </verify>
  <acceptance_criteria>
    - docs/MEMORY.md exists and contains the auto-extraction enable-path section (`Enabling automatic memory extraction`), the offline-lockdown env table (incl. `ENABLE_PERSISTENT_CONFIG` and `ENABLE_MEMORIES`), and the `villa verify memory` run-with-egress-precondition section.
    - The doc covers MEM-01/02/03/04 operator flows + KB upload (no host needed to verify — content check only).
  </acceptance_criteria>
  <done>docs/MEMORY.md is written and passes the automatable file-content check (save/view/edit/delete, auto-extraction enable path + default-off rationale, knowledge upload, offline-lockdown env table, and the verify-memory run with its egress precondition).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Re-render + reconcile the live stack with memory on (on-hardware)</name>
  <files>(none — on-hardware re-render/reconcile via `villa install`; no first-party file changes)</files>
  <action>Human-verify checkpoint (no code change). On the gfx1151 host: ensure `memory_enabled=true` in config.toml (via `villa recommend --save` / the wizard, or by setting the field), then run `villa install` so the OWUI unit re-renders with the D-09 env block and the stack reconciles + restarts (the install-time memory readiness proof from Phase 19 should still PASS). Confirm the rendered unit carries the D-09 env block per the how-to-verify steps.</action>
  <what-built>Wave-1 (Plan 01) made `buildOpenWebUIView` render the D-09 env block when `memory_enabled=true`; `villa install` re-renders + reconciles the existing Quadlet unit and restarts OWUI. This task brings the live stack up with memory on so Tasks 3–5 can verify behavior.</what-built>
  <how-to-verify>
    On gfx1151:
    1. Ensure `memory_enabled=true` in config.toml (e.g. `villa recommend --save`, the wizard, or set the field) and run `villa install`.
    2. Confirm `villa install` completes with the memory readiness proof PASS (Phase-19 install-time proof).
    3. Confirm the rendered `~/.config/containers/systemd/villa-openwebui.container` contains the D-09 env block including `ENABLE_PERSISTENT_CONFIG=False` and `ENABLE_MEMORIES=True`.
    4. Confirm the OWUI service restarted and is healthy (`villa status`).
  </how-to-verify>
  <verify>
    <human-check>On gfx1151: `villa install` completes with the memory readiness proof PASS; the rendered `villa-openwebui.container` carries the D-09 env block incl. `ENABLE_PERSISTENT_CONFIG=False` and `ENABLE_MEMORIES=True`; OWUI is healthy in `villa status`.</human-check>
  </verify>
  <resume-signal>Type "approved" if `villa install` completed with the memory readiness proof PASS and the rendered unit carries the D-09 env block (incl. ENABLE_PERSISTENT_CONFIG=False), or describe the discrepancy.</resume-signal>
  <done>The live stack runs with memory on; the rendered env block is confirmed on the live host (incl. ENABLE_PERSISTENT_CONFIG=False).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify OWUI Memory across chats + view/edit/delete (MEM-01/02/04) and auto-extraction default-off (MEM-03)</name>
  <files>(none — on-hardware human verification of the live OWUI Memory feature)</files>
  <action>Human-verify checkpoint (no code change). Drive the live OWUI Memory feature per the how-to-verify steps and report the outcome; this confirms MEM-01/02/04 cross-chat memory + view/edit/delete and MEM-03 auto-extraction-default-off.</action>
  <what-built>The D-09 env block sets `ENABLE_MEMORIES=True` (manual store + cross-chat injection) and deliberately does NOT enable Native Function Calling or any memory filter (auto-extraction stays default-off — MEM-03). docs/MEMORY.md documents the enable path.</what-built>
  <how-to-verify>
    On the live gfx1151 OWUI (http://127.0.0.1:3000):
    1. State a memorable fact to the assistant and explicitly save it to Memory (Settings → Personalization → Memory, or the per-message save). [MEM-02]
    2. Open a NEW chat and ask a question whose answer depends on that fact; confirm the assistant injects/uses it. [MEM-01]
    3. In Settings → Personalization → Memory, view the stored memories, edit one, and confirm the edit takes effect; then delete it. [MEM-04]
    4. Open another NEW chat and confirm the DELETED memory is no longer injected. [MEM-04 / SC#1]
    5. Confirm that with the DEFAULT model (no Native Function Calling, no memory filter installed), a normal conversation does NOT auto-create memories — auto-extraction is off by default. [MEM-03 / SC#2]
    6. Follow docs/MEMORY.md to enable the auto-extraction path (Native FC for the model) once and confirm it can be turned on, then turn it back off — documenting that it is opt-in.
  </how-to-verify>
  <verify>
    <human-check>All 6 steps behave as described: cross-chat injection works, view/edit/delete works, a deleted memory stops being injected, and auto-extraction is default-off but toggleable.</human-check>
  </verify>
  <resume-signal>Type "approved" if all 6 steps behave as described (deleted memory stops injecting; auto-extraction is default-off but toggleable), or describe the discrepancy.</resume-signal>
  <done>MEM-01/02/04 confirmed live (cross-chat memory + view/edit/delete; deleted memory stops injecting) and MEM-03 auto-extraction confirmed default-off + toggleable.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Verify document upload → cited local answer (KB-01/02/03) and resolve the citation-field + token-mint open questions</name>
  <files>(none — on-hardware human verification of the live OWUI Knowledge/RAG path)</files>
  <action>Human-verify checkpoint (no code change). Upload a planted document and confirm a cited, fully-local answer per the how-to-verify steps; capture the citation field name + the working admin-token mint path for the Plan-02 liveRagSmoke parse (resolves RESEARCH Open Questions A5 + A6).</action>
  <what-built>`VECTOR_DB=qdrant` + `RAG_EMBEDDING_ENGINE=openai`→villa-embed wires OWUI's native RAG to the local stack; offline lockdown removes the HF runtime-download vector. Native Function Calling stays OFF so knowledge is auto-injected with citations.</what-built>
  <how-to-verify>
    On the live gfx1151 OWUI:
    1. Create a Knowledge collection and upload a document containing a fact NOT in the model's training data (a planted, unique fact). [KB-01]
    2. In a chat, attach the collection and ask a question whose answer is only in the uploaded doc.
    3. Confirm the answer uses the retrieved content AND shows visible citations/sources. [KB-02]
    4. Confirm retrieval is fully local: while the upload + query run, observe that only villa-embed (/v1/embeddings) + villa-qdrant are contacted (e.g. `villa logs` / `podman logs villa-embed` shows embedding activity; no outbound to HF/cloud). [KB-03 / SC#3]
    5. OPEN QUESTION A6: capture the citation/source field name actually returned by `POST /api/chat/completions` for the collection-attached query (e.g. `sources` vs `citations`) and report it so the Plan-02 `liveRagSmoke` parse can be confirmed/adjusted. OPEN QUESTION A5: confirm the admin-token mint path that worked (`/api/v1/auths/signin` headless, or `signup` first-run).
  </how-to-verify>
  <verify>
    <human-check>The uploaded document yields a cited answer; chunk/embed/retrieve hit only villa-embed + Qdrant (no cloud/HF contact); the citation field name and working token-mint path are captured.</human-check>
  </verify>
  <resume-signal>Type "approved" with the citation field name + the working token-mint path, or describe the discrepancy (e.g. citations UI-only, no cloud contact observed).</resume-signal>
  <done>KB-01/02/03 confirmed live (upload → cited local answer; retrieval local-only) and open questions A5 (token mint) + A6 (citation field) resolved.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: Run the runtime firewalled zero-outbound smoke test with negative control (PRIV-05 / SC#4)</name>
  <files>(none — on-hardware run of `villa verify memory` with a host-egress precondition)</files>
  <action>Human-verify checkpoint (no code change). Block host egress, run `villa verify memory`, and confirm PASS-with-egress-blocked + FAIL-with-egress-open per the how-to-verify steps; this is the headline runtime PRIV-05/SC#4 proof. Resolve open question Q3 (the egress mechanism used).</action>
  <what-built>`villa verify memory` (Plan 02): a pure evalRagSmoke core (negative-control-first) + liveRagSmoke that drives the real REST RAG path over loopback and a negative-control external probe via fixed-arg podman/curl over villa.network. A silent skip / unevaluable result is a FAIL.</what-built>
  <how-to-verify>
    On the live gfx1151 host:
    1. PRECONDITION (user_setup): block the host's outbound to the public internet for the duration (deny-all egress firewall rule / offline NIC) — see docs/MEMORY.md and the negative-control mechanism resolved here (OPEN QUESTION 3: prefer the self-contained `podman run --network villa <img> curl https://huggingface.co/` probe that MUST fail; if the container can still reach the internet via the host, additionally apply the host firewall rule).
    2. Run `villa verify memory`.
    3. Confirm the verdict is PASS: the negative-control external probe FAILED (egress proven blocked — NOT a false-green) AND the real upload→embed→retrieve→cite path completed (the planted fact + a citation were returned). [PRIV-05 / SC#4]
    4. Confirm honesty-by-construction: temporarily UN-block egress (or simulate a reachable external host) and re-run — `villa verify memory` MUST now FAIL the negative control (proving the gate is real, not vacuous). Re-block before final sign-off.
    5. Confirm `villa status` loopback/privacy audit stays green — no new published host port (D-11).
  </how-to-verify>
  <verify>
    <human-check>`villa verify memory` PASSes on gfx1151 with egress blocked AND FAILs when egress is open (the negative control is real, not vacuous); `villa status` privacy audit stays green (no new host port).</human-check>
  </verify>
  <resume-signal>Type "approved" if `villa verify memory` PASSes with egress blocked AND FAILs when egress is open (the negative control is real), or describe the discrepancy.</resume-signal>
  <done>PRIV-05/SC#4 confirmed: the runtime firewalled upload→retrieve reaches no external host (negative control passes), the gate fails when egress is open, and no new host port is published. Open question Q3 (egress mechanism) resolved.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (Plan 03)

| Symbol / path | Kind | Notes |
|---------------|------|-------|
| `docs/MEMORY.md` | new operator doc | save/view/edit/delete memories, auto-extraction enable path (default-off), knowledge upload, offline-lockdown env table, `villa verify memory` + egress precondition (Task 1, off-hardware, automatable content check) |
| (verified, no new symbol) live MEM-01/02/03/04 + KB-01/02/03 + PRIV-05 behavior | sign-off | on-hardware human-verify checkpoints (Tasks 2–5) |
| Resolved open questions A5 (token mint), A6 (citation field), Q3 (egress mechanism) | recorded findings | feed back into liveRagSmoke parse confirmation |
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host ↔ public internet | the egress boundary proven blocked by the runtime negative control |
| user-uploaded document → villa-embed → Qdrant | untrusted content chunked/embedded/stored entirely locally |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-12 | Information Disclosure | runtime data exfil / model download during a real upload (the PRIV-05 headline threat) | mitigate | Task 5 runs the real RAG path with host egress blocked + a negative-control external probe that MUST fail; PASS only if egress is proven blocked AND the local path completed. |
| T-20-13 | Repudiation | false-green privacy claim (egress never actually blocked) | mitigate | Task 5 step 4 deliberately un-blocks egress and confirms the gate FAILs — proving the negative control is real, not vacuous. |
| T-20-14 | Tampering | enabling auto-extraction silently changes memory/citation behavior | mitigate | Task 3 confirms auto-extraction is default-off; docs/MEMORY.md (Task 1) documents that Native FC changes RAG injection and must stay off for deterministic citations. |
| T-20-15 | Spoofing/Elevation | new published host port introduced on the live host | accept | Task 5 step 5 confirms `villa status` privacy audit stays green; the verify drive uses the existing loopback PublishPort + a villa.network probe (D-11). |
</threat_model>

<verification>
- docs/MEMORY.md passes the automatable content check (Task 1, off-hardware): `make check` still green (no off-hardware regression).
- `villa install` re-renders the memory-on env block on the live host (Task 2).
- Human-verified MEM-01/02/03/04 + KB-01/02/03 behavior (Tasks 3–4).
- `villa verify memory` PASSes on gfx1151 with egress blocked and FAILs with egress open (Task 5).
</verification>

<success_criteria>
- SC#1 (MEM-01/02/04): cross-chat memory + view/edit/delete; deleted memory stops injecting — verified live.
- SC#2 (MEM-03): auto-extraction default-off, toggleable — verified + documented (docs/MEMORY.md).
- SC#3 (KB-01/02/03): upload → cited local answer; chunk/embed/retrieve local-only — verified live.
- SC#4 (PRIV-05): runtime firewalled zero-outbound smoke test reaches no external host, with a negative control proving egress is actually blocked — verified live.
</success_criteria>

<output>
Create `.planning/phases/20-open-webui-memory-rag-wiring-offline-lockdown/20-03-SUMMARY.md` when done.
</output>
