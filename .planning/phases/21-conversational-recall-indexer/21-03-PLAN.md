---
phase: 21-conversational-recall-indexer
plan: 03
type: execute
wave: 3
depends_on: ["21-01", "21-02"]
files_modified:
  - cmd/villa/recall_live.go
autonomous: false
requirements: [RECALL-01, RECALL-02, RECALL-03]
must_haves:
  truths:
    - "On the live gfx1151 host, `villa recall index` indexes the operator's real chat history into the 'Villa Recall — Past Conversations' Knowledge collection through OWUI→villa-embed→Qdrant, entirely locally (SC#1/RECALL-01)"
    - "In a NEW chat (no per-chat files param, no # reference), the assistant retrieves planted past-chat content BY MEANING (semantic paraphrase, not keywords) with a `sources` citation — proving the server-side model attachment works (SC#2/RECALL-02)"
    - "Incremental control is live-proven: an edited chat re-indexes by clean-replace, a deleted chat is removed from Knowledge, and `villa recall status` reports honest counts before/after — with OWUI stopped, status reports 'Unknown — could not evaluate' (WARN), never stale=0 (SC#3/RECALL-03)"
    - "The state file on disk is mode 0600 under $XDG_DATA_HOME/villa/ and contains NO chat titles/content (ids/timestamps only)"
    - "Assumptions A1–A4 are resolved on-hardware: bulk-index throughput recorded, poll-timeout sizing confirmed/tuned, attach read-merge-write preserves operator-set Model meta, verification completions do not pollute the index"
  artifacts:
    - path: ".planning/phases/21-conversational-recall-indexer/21-03-SUMMARY.md"
      provides: "on-hardware results: throughput numbers, A1–A4 resolutions, SC#1–3 sign-off evidence"
  key_links:
    - from: "villa recall index"
      to: "live OWUI + villa-embed + villa-qdrant on gfx1151"
      via: "real bulk index of the operator's chat history"
      pattern: "recall index"
    - from: "model meta.knowledge attachment"
      to: "new-chat semantic retrieval with sources"
      via: "server-side knowledge injection in process_chat_payload"
      pattern: "sources"
---

<objective>
On the live gfx1151 host (this IS the dev box — on-hardware tasks run for real, Phase-19/20 precedent), prove the three Phase-21 success criteria that cannot be proven off-hardware: a real bulk index of the operator's chat history (SC#1/RECALL-01), semantic retrieval-by-meaning with citations in a genuinely NEW chat (SC#2/RECALL-02), and live incremental/staleness honesty including the OWUI-down typed-Unknown path (SC#3/RECALL-03). Resolve the four RESEARCH assumptions (A1 throughput, A2 timeout sizing, A3 attach meta-merge preservation, A4 completion-drive chat pollution) and tune the size-aware poll timeout if the hardware demands it (the only sanctioned code edit in this plan).

Decision coverage in this plan: D-03/D-09 live confirmation (attachment mechanism + admin identity path work for real), D-04 (clean-replace observed leak-free on the live KB), D-06 (Unknown ≠ current proven by stopping OWUI), D-02 (vectors landed via OWUI's own path — verified by citation-bearing retrieval, not direct Qdrant inspection).

Purpose: the on-hardware wave is the dominant verification path for this milestone (STATE.md); SC#2's "by meaning, not keyword" is a behavioral property only a live embed+retrieve round-trip can prove. Claude automates every CLI/REST step; the human checkpoints verify the semantic-retrieval quality bar and sign off.
Output: A1–A4 resolved + recorded, SC#1–3 evidence captured in the SUMMARY, timeouts tuned if needed.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-conversational-recall-indexer/21-CONTEXT.md
@.planning/phases/21-conversational-recall-indexer/21-RESEARCH.md
@.planning/phases/21-conversational-recall-indexer/21-01-SUMMARY.md
@.planning/phases/21-conversational-recall-indexer/21-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Live bulk index + state-file audit + assumption resolution (A1–A4) on gfx1151</name>
  <read_first>
    - 21-RESEARCH.md "Assumptions Log" (A1 throughput, A2 timeout, A3 meta-merge echo, A4 completions-no-chat-row) and Pitfall 5 (long-transcript timeout symptoms)
    - 21-02-SUMMARY.md (the exact verbs/flags shipped and the size-aware timeout formula to validate)
  </read_first>
  <files>cmd/villa/recall_live.go</files>
  <action>
    On the live box, run the real first index and audit the results. (1) `make build` then confirm the stack is healthy (`./villa status`; memory_enabled=true persisted — it is, post-Phase-20). (2) BASELINE for A3: capture the served model's Model row if one exists (`curl -s -H "Authorization: Bearer $TOK" http://127.0.0.1:3000/api/v1/models/model?id=<served>` via the established token-mint sequence) — save the JSON for the post-attach diff. (3) Run `./villa recall status` (expect: nothing indexed yet, stale = full live count or empty-state messaging, attachment missing — honest pre-state). (4) Run `./villa recall index` against the REAL chat history; capture wall-clock duration, per-chat timings from the run summary, and any poll timeouts (A1/A2). If long transcripts time out, tune the size-aware timeout constant in cmd/villa/recall_live.go (the ONLY sanctioned code edit; keep unit tests green and re-run `go test ./cmd/villa/ -run TestRecall -count=1`). (5) Verify the index landed: `./villa recall status` shows indexed==live count, stale 0, attachment attached, completed stamp set. (6) A3: re-fetch the Model row and diff against the baseline — every pre-existing meta/params key must survive; only meta.knowledge gained the recall item. (7) A4: count chats via the admin list endpoint before and after a `villa verify memory` run (or a direct completions drive) — confirm no new chat rows appeared from completions; if rows DO appear, record it and confirm the service-account exclusion keeps them out of the index. (8) State-file audit: `stat -c '%a' $(ls ~/.local/share/villa/recall-state.json …)` is 600; `grep -ci 'title\|content' recall-state.json` per the ids-only discipline (key names only — D-05); record indexed counts. Record ALL numbers + A1–A4 verdicts for the SUMMARY.
  </action>
  <verify>
    <automated>./villa recall status && test "$(stat -c '%a' "${XDG_DATA_HOME:-$HOME/.local/share}/villa/recall-state.json")" = "600" && go test ./cmd/villa/ -run 'TestRecall' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - A full real index completed; `recall status` reports indexed==live, stale 0 (known), attachment attached, completed stamp set.
    - recall-state.json is 0600 and contains no chat titles/content keys.
    - A1 (throughput numbers), A2 (timeout verdict/tuning), A3 (meta preserved through attach), A4 (no completion pollution) resolved and recorded.
  </acceptance_criteria>
  <done>The operator's real chat history is indexed locally with honest green status; all four assumptions are resolved with recorded evidence.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify semantic retrieval-by-meaning with citations in a NEW chat (SC#2/RECALL-02)</name>
  <files>(none — on-hardware behavioral verification of the live retrieval path)</files>
  <action>Human-verify checkpoint. Claude first automates the REST half: pick a distinctive fact/topic that exists ONLY in an old indexed chat, then drive a plain `POST /api/chat/completions` (NO files param, NO # reference) asking a SEMANTIC PARAPHRASE of it (different words, same meaning) and confirm the response carries top-level `sources` referencing the recall KB and an answer grounded in the past chat. Then the operator confirms the same behavior through the real web UI per the steps below.</action>
  <what-built>Plan 02's attach step wrote the served model's `meta.knowledge` so OWUI's middleware injects the recall KB server-side into every completion — retrieval is native OWUI RAG over the indexed transcripts (768-dim villa-embed + Qdrant), citation-bearing.</what-built>
  <how-to-verify>
    On the live gfx1151 OWUI (http://127.0.0.1:3000):
    1. Pick a fact/topic you discussed in an OLD chat (something distinctive, not in the model's general knowledge).
    2. Open a brand-NEW chat (no knowledge attached manually, no # reference).
    3. Ask about it using DIFFERENT words than the original chat (a paraphrase by meaning — e.g. original "ROCm residency proof on gfx1151" → ask "how did we check the GPU was actually doing the work?").
    4. Confirm the assistant's answer draws on the past-chat content AND shows a citation/source pointing at the recall collection ("Villa Recall — Past Conversations" / villa-recall-<id>.txt).
    5. Negative control: ask about a topic NEVER discussed in any chat — confirm no spurious past-chat citation is fabricated.
    6. Cross-check Claude's automated REST result (step shown in the task output) matches the UI behavior.
  </how-to-verify>
  <verify>
    <human-check>A new chat retrieves planted past-chat content by MEANING (paraphrase, not keywords) with a visible citation from the recall collection, both via REST (`sources` present) and the real UI; the never-discussed control yields no fabricated past-chat citation.</human-check>
  </verify>
  <resume-signal>Type "approved" if retrieval-by-meaning with citations works in a new chat (REST + UI) and the negative control is clean, or describe the discrepancy.</resume-signal>
  <done>SC#2/RECALL-02 confirmed live: semantic (not keyword) retrieval of past-chat content with citations in a new conversation, via the server-side model attachment.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Live incremental + honest-staleness drill (edit/delete/OWUI-down/swap re-assert) (SC#3/RECALL-03)</name>
  <files>(none — on-hardware behavioral verification; Claude automates all CLI/REST steps)</files>
  <action>Human-verify checkpoint. Claude automates every CLI/REST step; the operator performs the two UI actions (edit a chat, delete a chat) and confirms outcomes. Sequence Claude runs: capture `recall status` baseline → after the operator EDITS an old chat (sends one new message), re-run `status` (expect stale=1 changed) → `recall index` (expect exactly 1 update, delete-then-re-add — confirm the OLD file id is gone from the KB via `GET /api/v1/knowledge/{id}` and exactly one file per chat remains, no duplicate growth) → after the operator DELETES a chat in the UI, `status` (stale=1 deleted) → `index` (file removed from KB, state entry dropped) → `systemctl --user stop villa-openwebui` → `recall status` (MUST print "Unknown — could not evaluate" and exit WARN — never stale=0) → restart OWUI → `status` green again. Optionally (if quick): re-run `recall index` after the OWUI restart to confirm idempotent no-op (0 adds/updates/deletes).</action>
  <what-built>Plan 02's diff-driven incremental pipeline (D-05), clean-replace semantics (D-04), and typed-Unknown staleness (D-06) — unit-proven off-hardware, now exercised against the real stack.</what-built>
  <how-to-verify>
    On gfx1151 (Claude drives the commands; you do the UI actions when asked):
    1. Edit: open an OLD chat in the UI and send one new message. Confirm Claude's `recall status` shows stale=1 (changed) and the re-index reports exactly 1 update with no duplicate files in the KB.
    2. Delete: delete a different chat in the UI. Confirm `status` shows stale=1 (deleted) and the re-index removes its file from the Knowledge collection.
    3. Down: with OWUI stopped, confirm `recall status` says "Unknown — could not evaluate" (WARN exit), and recovers to an honest green after restart.
    4. Confirm the final `recall status` is green (stale 0, attached) and a no-op re-index changes nothing.
  </how-to-verify>
  <verify>
    <human-check>Edited chat → honest stale count → clean-replace re-index (no stale/duplicate files); deleted chat → removed from Knowledge; OWUI down → "Unknown — could not evaluate" WARN (never stale=0); recovery green; no-op re-index idempotent.</human-check>
  </verify>
  <resume-signal>Type "approved" if all four drill steps behave honestly as described, or describe the discrepancy.</resume-signal>
  <done>SC#3/RECALL-03 confirmed live: villa-controlled incremental re-index stays current under edit/delete, and staleness reporting is honest including the typed-Unknown OWUI-down path.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (Plan 03)

| Symbol / path | Kind | Notes |
|---------------|------|-------|
| (possible) tuned size-aware timeout constant in `cmd/villa/recall_live.go` | code tuning | only if A2 demands; unit tests stay green |
| A1–A4 resolutions + throughput numbers | recorded evidence | in 21-03-SUMMARY.md |
| Live recall KB + per-chat transcript files + served-model attachment row | runtime state (OWUI DB + Qdrant) | created by the real index run; Phase-23 backup scope |
| SC#1–3 on-hardware sign-off | verification evidence | feeds /gsd-verify-work |
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| villa (host) ↔ live OWUI loopback REST | real admin-scoped chat reads + KB/model writes during the drill |
| operator UI actions ↔ index state | edits/deletes in the UI must be honestly reflected by villa |
| host ↔ public internet | zero-outbound posture must hold during real indexing (Phase-20 proven; no new paths) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-14 | Repudiation | on-hardware false-green (claiming SC#2 without a real semantic round-trip) | mitigate | SC#2 requires a PARAPHRASE (not keywords) + a `sources` citation + a never-discussed negative control, verified via REST AND the real UI (blocking human checkpoint). |
| T-21-15 | Information Disclosure | real chat content handled during the live drill | mitigate | All traffic loopback/container-DNS; state file audited 0600 + content-free on the real artifact (Task 1); no new outbound (Phase-20 runtime proof posture unchanged). |
| T-21-16 | Tampering | timeout tuning regressing the proven seam behavior | mitigate | The only sanctioned edit is the timeout constant; `go test ./cmd/villa/ -run TestRecall` must stay green after any tuning. |
| T-21-17 | Denial of Service | bulk index degrading the live chat model (shared gfx1151 envelope) | accept | Sequential indexing by construction; throughput measured (A1) and recorded; the full residency-under-embed-load gate is Phase 22 scope (CTRL-03), not this phase. |
| T-21-SC | Tampering | npm/pip/cargo installs | mitigate | No packages installed this phase. No `[ASSUMED]`/`[SUS]` checkpoints required. |
</threat_model>

<verification>
- Task 1 automated gate green on the live box (status green + 0600 state file + unit tests).
- Both human checkpoints approved (SC#2 semantic retrieval; SC#3 incremental/staleness drill).
- `make check` green after any timeout tuning.
</verification>

<success_criteria>
- SC#1/RECALL-01: real chat history indexed locally into the recall Knowledge collection via OWUI's own embed path.
- SC#2/RECALL-02: retrieval-by-meaning with citations in a new chat, REST- and UI-confirmed, negative control clean.
- SC#3/RECALL-03: edit/delete/down drill proves villa-controlled incrementality and typed-Unknown honest staleness on the live stack.
- A1–A4 resolved with recorded evidence; timeouts tuned if hardware demanded it.
</success_criteria>

<output>
Create `.planning/phases/21-conversational-recall-indexer/21-03-SUMMARY.md` when done.
</output>
