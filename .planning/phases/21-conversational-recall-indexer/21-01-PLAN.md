---
phase: 21-conversational-recall-indexer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/recall/store.go
  - internal/recall/store_test.go
  - internal/recall/recall.go
  - internal/recall/recall_test.go
  - internal/recall/transcript.go
  - internal/recall/transcript_test.go
  - internal/recall/staleness.go
  - internal/recall/staleness_test.go
autonomous: true
requirements: [RECALL-01, RECALL-03]
must_haves:
  truths:
    - "A pure Plan diff computes adds/updates/deletes from an injected live chat list + persisted state per the D-05 algebra (new = L∖S, changed = updated_at > owui_updated_at, deleted = S∖L) with no I/O"
    - "RenderTranscript reconstructs the canonical thread by walking history.currentId back through parentId with a cycle guard, strips reasoning <details> blocks, role-labels user/assistant turns under a title + ISO-date header, and SKIPS (never silently drops) a chat with no reconstructable messages (D-04)"
    - "Staleness classification treats Unknown as a distinct state — an unevaluable live list yields stale=Unknown (never 0), while indexed/last-indexed still report from villa-side state; a partial run (no completed stamp) is distinguishable from a clean full pass (D-06)"
    - "recall-state.json loads fail-closed (absent/corrupt/future-schema ⇒ empty state, never a fabricated index), writes atomically with 0600/0700 + traversal guard, and contains ids/timestamps/counts ONLY — no chat titles or content (D-05)"
    - "TestSeamGrepGate stays green: internal/recall imports neither os/exec nor any container-image/backend-marker literal (D-08)"
  artifacts:
    - path: "internal/recall/store.go"
      provides: "State/ChatState types, recallSchemaVersion=1 + SchemaVersion(), fail-closed Load, version-stamping Save, RecallStatePath, WriteFileAtomic + traversal guard (clone of internal/usage discipline)"
      contains: "func Load"
    - path: "internal/recall/recall.go"
      provides: "ChatRef type, PlanResult{Adds,Updates,Deletes}, pure Plan(live, state) diff"
      contains: "func Plan"
    - path: "internal/recall/transcript.go"
      provides: "ChatDoc/ChatHistory/ChatMsg JSON-tagged types, RenderTranscript (chain walk + reasoning strip + header), TranscriptFilename"
      contains: "func RenderTranscript"
    - path: "internal/recall/staleness.go"
      provides: "AttachmentState enum (attached/missing/unknown), Report typed staleness verdict with StaleKnown, Classify(live, liveKnown, attachment, state)"
      contains: "func Classify"
    - path: "internal/recall/store_test.go"
      provides: "fail-closed load table, atomic-write/0600, JSON-key content denylist test"
      contains: "func TestStore"
    - path: "internal/recall/staleness_test.go"
      provides: "typed-Unknown + partial-run staleness tables"
      contains: "func TestStaleness"
  key_links:
    - from: "internal/recall/staleness.go"
      to: "internal/recall/store.go State"
      via: "Classify consumes the persisted State alongside the injected live list"
      pattern: "Classify\\("
    - from: "internal/recall/transcript.go"
      to: "history.currentId parent chain"
      via: "linear thread walk mirroring OWUI get_message_list"
      pattern: "ParentID"
---

<objective>
Land the ONE new pure core `internal/recall` (D-08): the diff/plan algebra that decides what to add/update/delete (D-05), the role-labeled per-chat transcript renderer (D-04), the typed-Unknown staleness classification (D-06), and the `recall-state.json` store cloned from `internal/usage`'s atomic XDG discipline (D-05). Everything in this plan is unit-testable off-hardware with injected chat lists/state — no I/O, no curl, no cobra. This is the testable half of RECALL-01 (what to index, how a chat becomes a document) and RECALL-03 (incremental diff + honest staleness).

Decision coverage in this plan: D-04 (per-chat transcript, chain walk, reasoning strip, clean-replace semantics encoded as Updates in the plan diff), D-05 (state schema + diff algebra + flat-JSON XDG store), D-06 (typed-Unknown staleness, partial-run honesty), D-08 (one pure core, no image/backend literals, seam gate green with no allowlist edit).

Purpose: every "hard" sub-problem of this phase already has an owner (OWUI owns chunking/embedding/retrieval); villa's only novel logic is this plan/staleness algebra and the transcript renderer — getting them pure and fully covered off-hardware is what makes the cmd tier (Plan 02) a thin choreography layer and the on-hardware wave (Plan 03) a confirmation, not a debug session.
Output: `internal/recall` package (4 source files + 4 test files), green under `go test ./internal/recall/ -count=1`, with `TestSeamGrepGate` still green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-conversational-recall-indexer/21-CONTEXT.md
@.planning/phases/21-conversational-recall-indexer/21-RESEARCH.md
@.planning/phases/21-conversational-recall-indexer/21-PATTERNS.md
@internal/usage/usage.go
@internal/memory/memory.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Clone the atomic XDG store into internal/recall/store.go (recall-state.json, fail-closed, content-free)</name>
  <read_first>
    - internal/usage/usage.go IN FULL — the store discipline to clone (schema const + SchemaVersion(), 0600/0700 modes, injectable byte-I/O Deps, fail-closed Load at :193-214, version-stamping Save at :176-186, storeRootDir/assertInsideDir/WriteFileAtomic at :222-315; the clone-don't-import rule is stated at :243)
    - internal/usage/usage_test.go — the JSON-key denylist test idea (content discipline asserted by test) and the table-driven Load cases
    - 21-RESEARCH.md "Recommended recall-state.json schema (D-05)" — the exact field set and the counts/ids/timestamps-only rule
  </read_first>
  <files>internal/recall/store.go, internal/recall/store_test.go</files>
  <behavior>
    - Load(Deps) with nil ReadAll seam → error; ReadAll error → wrapped error; empty/absent ([]byte nil or len 0) → empty State, nil error; corrupt JSON → empty State, nil error (fail closed, never panic); SchemaVersion != 1 (future/unknown) → empty State, nil error.
    - Save stamps SchemaVersion=1 before marshal; round-trip Load(Save(s)) == s.
    - WriteFileAtomic: mkdir 0700 → CreateTemp same-dir → chmod 0600 → write → rename; temp removed on every error branch; assertInsideDir rejects a path outside the FIXED store root (traversal guard).
    - Content denylist: marshaling a populated State produces JSON whose keys contain NO "title"/"content"/"message" keys — ids, timestamps, counts, model/dim only (test walks the marshaled key set).
  </behavior>
  <action>
    Create `internal/recall/store.go` cloning `internal/usage`'s store shape (clone, don't import — the established rule). Define `const recallSchemaVersion = 1` + `func SchemaVersion() int` (the Phase-23 manifest reader-of-record). Define the state types per the RESEARCH schema: `State{ SchemaVersion int; KnowledgeID, KnowledgeName, EmbeddingModel string; EmbeddingDim int; LastIndexStartedAt, LastIndexCompletedAt string; Chats map[string]ChatState }` with RFC3339-UTC string timestamps, and `ChatState{ UserID string; OWUIUpdatedAt int64; FileID string; IndexedAt string }` (OWUIUpdatedAt is the epoch-seconds int the list API returns). JSON tags: snake_case matching the RESEARCH schema (`schema_version`, `knowledge_id`, `owui_updated_at`, `file_id`, `indexed_at`, `last_index_started_at`, `last_index_completed_at`, `embedding_model`, `embedding_dim`, `chats`, `user_id`, `knowledge_name`). NO title/content fields — the state file is ids/timestamps only (D-05; security: chat content must not leak into a host-side file). Define the injectable byte-I/O `Deps{ WriteAll func([]byte) error; ReadAll func() ([]byte, error); Now func() time.Time }`, `Load(Deps) (State, error)` with fail-closed semantics copied verbatim from usage.go:193-214 (absent ⇒ empty, corrupt ⇒ empty, unknown schema ⇒ empty — an empty state means "nothing indexed", never a fabricated index), and `Save(Deps, State) error` stamping the version. Clone `storeRootDir()` ($XDG_DATA_HOME/villa → ~/.local/share/villa → /var/tmp/villa), `RecallStatePath() string` returning `<root>/recall-state.json`, `assertInsideDir` (guard against the FIXED root, not the path's own parent) and `WriteFileAtomic` (0700 dir, CreateTemp same-dir, chmod 0600, write, close, rename, temp cleanup on every error branch) from usage.go:222-315. Open the file with a package doc comment stating purity, the seam, and D-05/D-06/D-08/RECALL-03. Write `store_test.go` FIRST (tdd): table-driven `TestStore*` functions covering every behavior above, including the JSON-key denylist test (marshal a populated State, walk all keys, assert none match title/content/message) — each test function opens with a doc comment naming the invariant + decision ID (house convention).
  </action>
  <verify>
    <automated>go test ./internal/recall/ -run 'TestStore' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - Fail-closed Load covered for absent/corrupt/future-schema/ReadAll-error/nil-seam; Save→Load round-trips.
    - WriteFileAtomic + assertInsideDir cloned with temp-cleanup and traversal-guard tests.
    - The content-denylist test proves the marshaled state carries no chat titles/content keys.
    - `SchemaVersion()` exported and returns 1.
  </acceptance_criteria>
  <done>internal/recall/store.go persists recall-state.json with the usage-store atomic/fail-closed/content-free discipline, fully unit-covered off-hardware.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the pure Plan diff (recall.go) and the transcript renderer (transcript.go)</name>
  <read_first>
    - 21-RESEARCH.md "Staleness algebra" (the diff sets: new = L∖S, changed = L.updated_at > S.owui_updated_at, deleted = S∖L) and "Code Examples" → `linearThread` (the currentId → parentId chain walk with visited-set cycle guard — implement this verbatim) and "Chat JSON structure" (history.currentId/messages shape; flat chat.messages is a STALE branch view — never use it)
    - 21-RESEARCH.md Pitfall 5 (strip `<details type="reasoning" …>…</details>` blocks before rendering — indexing chain-of-thought bloats embeddings and pollutes retrieval)
    - internal/usage/usage.go Fold (:126-158) — the pure copy-not-mutate discipline (return new values; never mutate the injected state/map)
    - 21-PATTERNS.md "internal/recall/recall.go" + "internal/recall/transcript.go" sections (typed-Result discipline from modelswap; header format per D-04)
  </read_first>
  <files>internal/recall/recall.go, internal/recall/recall_test.go, internal/recall/transcript.go, internal/recall/transcript_test.go</files>
  <behavior>
    - Plan(live []ChatRef, state State) PlanResult: a chat in live but not state → Adds; in both with live.UpdatedAt > state.OWUIUpdatedAt → Updates; in state but not live → Deletes (chat ids; the cmd tier resolves FileID from state). Equal/older updated_at → untouched. Plan never mutates its inputs; deterministic output order (sort by chat id) so tests and run output are stable.
    - RenderTranscript(ChatDoc) (text string, ok bool): walks history.currentId via parentId with a visited-set cycle guard, reverses to chronological; ok=false (skip) when no user/assistant messages are reconstructable (missing currentId, empty messages map, cycle-only); header = "# <title>" + "# <ISO-8601 of created_at> — Open WebUI chat <chat-id>"; turns rendered as "user: <content>" / "assistant: <content>"; non-user/assistant roles skipped; reasoning <details type="reasoning"…>…</details> blocks stripped from assistant content (including multiple blocks; unclosed block → strip to end).
    - TranscriptFilename(chatID) == "villa-recall-" + chatID + ".txt" (deterministic, D-04).
  </behavior>
  <action>
    Create `internal/recall/recall.go`: define `ChatRef{ ID, UserID string; UpdatedAt int64 }` (epoch seconds as the list API returns), `PlanResult{ Adds []ChatRef; Updates []ChatRef; Deletes []string }`, and the pure `Plan(live []ChatRef, state State) PlanResult` implementing the D-05 diff algebra above with sorted, copy-not-mutate output. Package doc comment opens the file: `// Package recall …` stating purity, the cmd-tier seam, and D-04/D-05/D-06/D-08. Create `internal/recall/transcript.go`: JSON-tagged input types parsed by the caller from the chat GET body — `ChatDoc{ ID string; Title string; CreatedAt int64; History ChatHistory }` (tags matching the OWUI shape: the caller unmarshals the REST body's `id`/`title`/`created_at`/`chat.history` into this), `ChatHistory{ CurrentID string `json:"currentId"`; Messages map[string]ChatMsg }`, `ChatMsg{ ID, ParentID string `json:"parentId"`; Role, Content string; Timestamp int64 }`. Implement `linearThread` exactly per the RESEARCH example (visited-set guard, reverse to chronological), `stripReasoning(s string) string` removing every `<details type="reasoning"` …`</details>` span (string-scan or a single compiled regexp; handle multiple and unclosed blocks), `RenderTranscript(c ChatDoc) (string, bool)` producing the D-04 header + role-labeled turns and returning ok=false for unreconstructable chats (the caller records the skip — never silently dropped), and `TranscriptFilename(chatID string) string`. internal/recall imports ONLY stdlib (encoding/json tags, fmt, sort, strings, time) — no os/exec, no image literals (D-08, TestSeamGrepGate walks this package). Write the tests FIRST (tdd): `recall_test.go` table-driven TestPlan covering add/update/delete/unchanged/empty-state/empty-live/mixed + input-not-mutated + deterministic order; `transcript_test.go` TestRenderTranscript covering chain walk (out-of-order map → correct chronological order), cycle guard, reasoning-strip (single/multiple/unclosed), skip-on-empty, non-user/assistant roles skipped, header format, and TestTranscriptFilename. Doc comments on every test naming the invariant + D-xx.
  </action>
  <verify>
    <automated>go test ./internal/recall/ -run 'TestPlan|TestRenderTranscript|TestTranscriptFilename' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - Plan implements exactly the D-05 algebra with sorted deterministic output and no input mutation.
    - RenderTranscript walks the currentId→parentId chain with a cycle guard (NOT the flat chat.messages list), strips reasoning blocks, and signals skip via ok=false.
    - All table-driven tests pass; `go vet ./internal/recall/` clean.
  </acceptance_criteria>
  <done>The pure diff and transcript renderer are implemented per D-04/D-05 with full off-hardware coverage.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement typed-Unknown staleness classification (staleness.go) and prove the seam gate stays green</name>
  <read_first>
    - internal/memory/memory.go Decide (:36-75) — the typed-verdict + accumulated-reasons shape to mirror (Unknown is a distinct state, never a zero)
    - 21-RESEARCH.md "Staleness algebra" (L Unknown ⇒ stale Unknown WARN while indexed/last_indexed still report from state) and "Recommended recall-state.json schema" partial-run note (last_index_completed_at only stamped on a clean full pass)
    - 21-PATTERNS.md "internal/recall/staleness.go" section (StaleKnown bool pairing; attachment enum attached|missing|unknown)
  </read_first>
  <files>internal/recall/staleness.go, internal/recall/staleness_test.go</files>
  <behavior>
    - Classify(live []ChatRef, liveKnown bool, attachment AttachmentState, state State) Report.
    - liveKnown=true → Report carries New/Changed/Deleted counts from the Plan algebra, Stale = New+Changed+Deleted, StaleKnown=true.
    - liveKnown=false (OWUI unreachable / listing failed) → StaleKnown=false, Stale/New/Changed/Deleted MUST NOT read as 0-and-current (the rendering layer prints "Unknown — could not evaluate"); Indexed and LastIndex* still populated from state (villa-side truths). A Reasons entry names WHY it is unknown.
    - state.LastIndexCompletedAt empty while LastIndexStartedAt set → CompleteRun=false (partial run — remainder must read as stale/unknown, never as complete).
    - AttachmentState is a typed enum: AttachmentAttached / AttachmentMissing / AttachmentUnknown; the Report folds it in verbatim (retrieval honesty is part of staleness — Pitfall 2).
  </behavior>
  <action>
    Create `internal/recall/staleness.go`: define `type AttachmentState string` with consts `AttachmentAttached`, `AttachmentMissing`, `AttachmentUnknown` ("attached"/"missing"/"unknown"), and `Report{ Indexed int; LastIndexStartedAt, LastIndexCompletedAt string; CompleteRun bool; New, Changed, Deleted, Stale int; StaleKnown bool; Attachment AttachmentState; Reasons []string }`. Implement pure `Classify(live []ChatRef, liveKnown bool, attachment AttachmentState, state State) Report` reusing `Plan` for the diff when liveKnown (do not duplicate the algebra), populating Indexed=len(state.Chats) and the LastIndex* stamps from state unconditionally (villa-side truths report even when OWUI is down — D-06), setting StaleKnown=false with an explanatory Reasons entry when liveKnown=false, and CompleteRun per the partial-run rule. Mirror memory.Decide's refuse-with-reason style: every Reasons string names the thing AND why it matters ("could not list chats from Open WebUI — stale count is Unknown, not 0"). Write `staleness_test.go` FIRST (tdd): table-driven TestStaleness covering (a) known-live counts, (b) liveKnown=false ⇒ StaleKnown=false + Indexed/LastIndex* still from state + non-empty reason, (c) partial run (started set, completed empty) ⇒ CompleteRun=false, (d) deleted-chat counting, (e) each attachment state folded through, (f) empty state + known live ⇒ everything stale-as-new. Then run the EXISTING seam gate to prove D-08: `go test ./internal/inference/ -run TestSeamGrepGate -count=1` must pass with NO allowlist edit (internal/recall has no os/exec import and no image/backend literal — if the gate fails, fix internal/recall, never the allowlist).
  </action>
  <verify>
    <automated>go test ./internal/recall/ -run 'TestStaleness' -count=1 && go test ./internal/inference/ -run TestSeamGrepGate -count=1 && go build ./... && go vet ./internal/recall/</automated>
  </verify>
  <acceptance_criteria>
    - Unknown is structurally distinct from zero (StaleKnown=false case covered by test); Indexed/LastIndex* report from state even when live is Unknown.
    - Partial-run (CompleteRun=false) distinguishable from a clean pass.
    - TestSeamGrepGate green with zero allowlist changes; `go build ./...` green.
  </acceptance_criteria>
  <done>Typed-Unknown staleness classification is implemented per D-06 with full coverage, and the seam gate confirms internal/recall is literal-free (D-08).</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (Plan 01)

| Symbol / path | Kind | Notes |
|---------------|------|-------|
| `internal/recall` | new pure package | the ONE new core for this phase (D-08); no os/exec, no image literals |
| `State{ SchemaVersion, KnowledgeID, KnowledgeName, EmbeddingModel, EmbeddingDim, LastIndexStartedAt, LastIndexCompletedAt, Chats }` / `ChatState{ UserID, OWUIUpdatedAt, FileID, IndexedAt }` | new types | recall-state.json schema v1 (D-05); ids/timestamps only |
| `recallSchemaVersion = 1` / `SchemaVersion() int` | const + accessor | Phase-23 manifest reader-of-record |
| `Deps{ WriteAll, ReadAll, Now }` / `Load(Deps) (State, error)` / `Save(Deps, State) error` | store seam | fail-closed load, version-stamping save |
| `RecallStatePath() string` / `WriteFileAtomic` / `assertInsideDir` / `storeRootDir` | store plumbing | clone of internal/usage atomic XDG discipline (0600/0700, traversal guard) |
| `ChatRef{ ID, UserID, UpdatedAt }` / `PlanResult{ Adds, Updates, Deletes }` / `Plan(live, state) PlanResult` | pure diff core | D-05 algebra; sorted, copy-not-mutate |
| `ChatDoc` / `ChatHistory` / `ChatMsg` | JSON-tagged input types | parsed from the OWUI chat GET body by the cmd tier |
| `RenderTranscript(ChatDoc) (string, bool)` / `TranscriptFilename(string) string` / `stripReasoning` | pure renderer | D-04 chain walk + reasoning strip + skip signal |
| `AttachmentState` (`AttachmentAttached`/`AttachmentMissing`/`AttachmentUnknown`) / `Report` / `Classify(live, liveKnown, attachment, state) Report` | staleness core | D-06 typed-Unknown discipline |
| `TestStore*` / `TestPlan` / `TestRenderTranscript` / `TestTranscriptFilename` / `TestStaleness` | new tests | table-driven, off-hardware |
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OWUI chat content → internal/recall inputs | untrusted user/assistant text enters the pure core via parsed JSON (typed structs, no exec) |
| internal/recall → $XDG_DATA_HOME/villa/recall-state.json | villa-side persistence of index metadata on the host filesystem |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-01 | Information Disclosure | chat content leaking into recall-state.json (host-side file) | mitigate | State schema is ids/timestamps/counts ONLY — no title/content fields; enforced by a JSON-key denylist unit test (Task 1). File mode 0600, dir 0700. |
| T-21-02 | Tampering | recall-state.json path traversal / write outside the XDG root | mitigate | Clone `assertInsideDir` guarding against the FIXED store root + atomic temp+rename writer (Task 1, usage.go WR-05 precedent). |
| T-21-03 | Tampering/Repudiation | tampered/corrupt state file fabricating a "current" index | mitigate | Fail-closed `Load` (corrupt/unknown-schema ⇒ empty state = "nothing indexed"); staleness is recomputed against the LIVE list every status call (Plan 02) — state alone can never claim currency. |
| T-21-04 | Repudiation | stale index reported as current when the live list is unevaluable | mitigate | `Classify` makes Unknown structurally distinct (`StaleKnown=false` + reason), unit-tested; partial runs distinguishable via `CompleteRun` (D-06). |
| T-21-05 | Tampering | malicious chat content breaking the transcript renderer (cycles, huge nesting, unclosed tags) | mitigate | Visited-set cycle guard in the chain walk; reasoning-strip handles unclosed blocks; renderer is pure string transformation — no exec, no eval (covered by Task 2 tests). |
| T-21-SC | Tampering | npm/pip/cargo installs | mitigate | No packages installed this phase (21-RESEARCH Package Legitimacy Audit: none). No `[ASSUMED]`/`[SUS]` checkpoints required. |
</threat_model>

<verification>
- `go test ./internal/recall/ -count=1` green (all four cores covered off-hardware).
- `go test ./internal/inference/ -run TestSeamGrepGate -count=1` green with NO allowlist edit.
- `make check` green.
</verification>

<success_criteria>
- RECALL-01 (testable slice): a chat document model exists — Plan decides what to index; RenderTranscript turns a chat into the D-04 role-labeled document deterministically.
- RECALL-03 (testable slice): the incremental diff algebra and honest typed-Unknown staleness classification are pure, covered, and ready for the cmd tier to drive; recall-state.json persistence is atomic, fail-closed, and content-free.
- D-08 holds: one new pure core, seam gate green, zero new dependencies.
</success_criteria>

<output>
Create `.planning/phases/21-conversational-recall-indexer/21-01-SUMMARY.md` when done.
</output>
