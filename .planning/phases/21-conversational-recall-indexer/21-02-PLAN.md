---
phase: 21-conversational-recall-indexer
plan: 02
type: execute
wave: 2
depends_on: ["21-01"]
files_modified:
  - cmd/villa/recall.go
  - cmd/villa/recall_live.go
  - cmd/villa/recall_test.go
  - cmd/villa/root.go
  - cmd/villa/verify_memory.go
  - docs/MEMORY.md
autonomous: true
requirements: [RECALL-01, RECALL-02, RECALL-03]
must_haves:
  truths:
    - "`villa recall index` (incremental by default, `--rebuild` for clean-recreate) and `villa recall status` exist as a new parent verb registered in the root tree, thin cobra callers over an injectable recallDeps seam (D-07)"
    - "With memory disabled (persisted memory_enabled=false), BOTH recall verbs refuse-with-remediation and return exitBlocked — an explicit index request never honestly no-ops (D-07; the deliberate delta from verify memory's exit-0)"
    - "recall index drives the ordered pipeline: gate → OWUI reachability → ensure KB → list users (excluding the villa-verify@localhost service account) → list chats per user via the admin endpoint → Plan diff → per-chat render→remove-old→upload→poll→add with state persisted INCREMENTALLY after each chat → idempotent model-knowledge attach → stamp last_index_completed_at only on a clean full pass (D-01/D-02/D-04/D-06/D-09)"
    - "A changed chat re-indexes by delete-then-re-add (knowledge file/remove?delete_file=true then re-upload); --rebuild uses knowledge/{id}/reset (id-preserving) — NEVER knowledge delete (D-04)"
    - "The attach step is idempotent read-merge-write: GET the Model row for the SERVED model id, merge the recall KB into meta.knowledge preserving other meta keys, update-or-create — making retrieval (RECALL-02) survive villa model swap when re-asserted"
    - "recall status reports indexed count / last-indexed / stale count against the LIVE chat list plus the attachment state (attached/missing/unknown); OWUI unreachable ⇒ 'Unknown — could not evaluate' WARN exit, never stale=0 (D-06)"
    - "All REST traffic is fixed-arg curl over the existing loopback PublishPort (no new host port, no shell interpolation); transcript content travels via stdin multipart, never argv or temp files (D-01)"
  artifacts:
    - path: "cmd/villa/recall.go"
      provides: "newRecall()/newRecallIndex()/newRecallStatus(), recallDeps seam, runRecallIndex/runRecallStatus return-not-Exit bodies, memory.Decide gate with exitBlocked refusal"
      contains: "func newRecall"
    - path: "cmd/villa/recall_live.go"
      provides: "liveRecallDeps() — fixed-arg curl drives for chats list/get, knowledge ensure/file-remove/reset, files upload+poll+add, models get/create/update (attach), users list; reuses mintAdminToken/runLoopbackCurl(Stdin)/discoverChatModel/pollFileProcessed"
      contains: "func liveRecallDeps"
    - path: "cmd/villa/recall_test.go"
      provides: "gate test (memory off ⇒ exitBlocked, no drive runs), ordering/short-circuit spy tests, attach idempotency (read-merge-write) test, incremental-persist test"
      contains: "func TestRecallGate"
    - path: "cmd/villa/root.go"
      provides: "newRecall() registered in the root command tree"
      contains: "newRecall()"
    - path: "docs/MEMORY.md"
      provides: "operator section for villa recall: verbs, retrieval enable-path, post-model-swap re-assert gotcha, widened admin read scope"
      contains: "villa recall"
  key_links:
    - from: "cmd/villa/recall.go"
      to: "internal/recall"
      via: "run bodies call recall.Plan / recall.RenderTranscript / recall.Classify / recall.Load / recall.Save"
      pattern: "recall\\.(Plan|RenderTranscript|Classify|Load|Save)"
    - from: "cmd/villa/recall.go"
      to: "internal/memory.Decide"
      via: "the enablement gate — memory off/invalid ⇒ exitBlocked with remediation"
      pattern: "memory\\.Decide"
    - from: "cmd/villa/recall_live.go"
      to: "runLoopbackCurlStdin"
      via: "all REST drives reuse the Phase-20 fixed-arg curl seam"
      pattern: "runLoopbackCurl"
---

<objective>
Build the `villa recall` command surface: the live REST seam over the Phase-20 loopback primitives (D-01) and the thin cobra verbs `recall index` / `recall status` (D-07) that choreograph the pure `internal/recall` core from Plan 01 — indexing the operator's chats into a villa-managed OWUI Knowledge collection (D-02/D-09), clean-replacing changed chats (D-04), persisting incremental state honestly (D-05/D-06), and idempotently attaching the recall KB to the served model so retrieval-by-meaning works in every new chat (D-03, RECALL-02). Extends docs/MEMORY.md with the operator section.

Decision coverage in this plan: D-01 (REST over loopback, fixed-arg curl, reuse Phase-20 primitives, no new host port), D-02 (villa-managed Knowledge collection via knowledge/create + files/ + file/add; never direct Qdrant writes), D-03 (model meta.knowledge attachment, idempotent attach-on-index), D-04 (delete-then-re-add clean-replace; reset for --rebuild), D-05 (diff-driven incremental against recall-state.json), D-06 (typed-Unknown status; incremental persistence; completed-stamp only on clean pass), D-07 (recall parent verb; memory-off ⇒ exitBlocked refuse-with-remediation), D-09 (admin JWT via the existing mintAdminToken seam; index all human users, excluding only the villa-verify@localhost service account).

Discretion decisions (recorded here per CONTEXT "Claude's Discretion"): `recall status` ships HUMAN-ONLY this phase — no `--json` (avoids freezing a second contract before Phase 23 evolves status.Report; can be added later as its own schema-versioned contract). No `--user` narrowing flag in v1 (the default — all human users on this single-operator box — is the requirement; a flag is additive later). The pollFileProcessed timeout is parameterized size-aware: base 60s + 1s per 2 KiB of transcript (RESEARCH A2), tuned on-hardware in Plan 03. Indexing is strictly sequential (RESEARCH anti-pattern: parallel uploads serialize at villa-embed anyway and contend with the chat model on the shared gfx1151 envelope).

Purpose: this is the RECALL-01/02/03 delivery layer — everything here is REST choreography over proven primitives plus exit-code mapping; the decision logic stays in Plan 01's pure core, so every run body is testable off-hardware with fake Deps.
Output: `villa recall index|status` built, vetted, fully fake-Deps-tested; docs/MEMORY.md extended. The live drive runs for real in Plan 03.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-conversational-recall-indexer/21-CONTEXT.md
@.planning/phases/21-conversational-recall-indexer/21-RESEARCH.md
@.planning/phases/21-conversational-recall-indexer/21-PATTERNS.md
@.planning/phases/21-conversational-recall-indexer/21-01-SUMMARY.md
@cmd/villa/verify_memory.go
@cmd/villa/verify.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement the live REST seam (recall_live.go) — chats/knowledge/files/models drives over the Phase-20 primitives</name>
  <read_first>
    - cmd/villa/verify_memory.go IN FULL — the primitives to REUSE, not copy: `mintAdminToken` (:274), `runLoopbackCurl`/`runLoopbackCurlStdin` (:419/:425), `pollFileProcessed` (:353), `discoverChatModel` (:329), and the knowledge/create + multipart-upload + file/add idiom (:172-218); also `ragSmokeProcessTimeout` (:115) — the constant to parameterize
    - 21-RESEARCH.md "Confirmed API Surface" — every endpoint shape this task drives: `GET /api/v1/users/?page=N`, `GET /api/v1/chats/list/user/{user_id}?page=N` (60/page, loop until short page, include_archived hard-coded), `GET /api/v1/chats/{id}`, `POST /api/v1/knowledge/create`, `GET /api/v1/knowledge/{id}`, `POST /api/v1/files/` + poll + `POST /api/v1/knowledge/{id}/file/add`, `POST /api/v1/knowledge/{id}/file/remove?delete_file=true`, `POST /api/v1/knowledge/{id}/reset`, `GET /api/v1/models/model?id=…` / `POST /api/v1/models/create` / `POST /api/v1/models/model/update`
    - 21-RESEARCH.md "RECALL-02 mechanism" — the exact Model-row JSON body (id == served base model id, base_model_id null, meta.knowledge [{type:"collection",id,name}]) and the create-401-MODEL_ID_TAKEN / update-401-NOT_FOUND idempotency dance
    - 21-PATTERNS.md "cmd/villa/recall_live.go" section — the per-endpoint rules (json.Marshal-only bodies; URLs = base + path + API-returned ids; every error wrapped with the endpoint name; empty id in a 200 body is an ERROR carrying the raw body)
  </read_first>
  <files>cmd/villa/recall_live.go, cmd/villa/verify_memory.go</files>
  <action>
    Create `cmd/villa/recall_live.go` (package main) with the live drive functions the recall verbs need, each a thin fixed-arg curl wrapper following the verify_memory.go idiom (D-01). Open with the house file-level doc comment (role, D-01/D-02/D-03/D-09/RECALL-01..03, honesty invariants). Implement:
    (1) `owuiListUsers(ctx, base, token) ([]owuiUser, error)` — `GET /api/v1/users/?page=N` paginated; `owuiUser{ ID, Email, Role string }`; loop pages until a short/empty page.
    (2) `owuiListUserChats(ctx, base, token, userID) ([]recall.ChatRef, error)` — `GET /api/v1/chats/list/user/{userID}?page=N`, 60/page, loop until short page; map `{id, updated_at}` → `recall.ChatRef{ID, UserID: userID, UpdatedAt}`. This is the admin endpoint that includes archived/folder/pinned chats (Pitfall 1 — do NOT use `GET /api/v1/chats/`).
    (3) `owuiGetChat(ctx, base, token, chatID) (recall.ChatDoc, error)` — `GET /api/v1/chats/{id}`; unmarshal `{id, title, created_at, chat:{history}}` into `recall.ChatDoc` (the flat `chat.messages` list is NEVER read — stale branch view).
    (4) `owuiEnsureKnowledge(ctx, base, token, name, description) (id string, err error)` — list/get existing KBs (`GET /api/v1/knowledge/`) and return the id of the one matching `name` if present, else `POST /api/v1/knowledge/create` (find-or-create keeps re-runs and state-loss recovery idempotent). KB name constant: `recallKnowledgeName = "Villa Recall — Past Conversations"`.
    (5) `owuiUploadTranscript(ctx, base, token, kbID, filename, content string, timeout time.Duration) (fileID string, err error)` — multipart via `runLoopbackCurlStdin(ctx, content, …, "-F", "file=@-;filename="+filename+";type=text/plain")` (content via stdin, never argv/temp file), poll `GET /api/v1/files/{id}/process/status` until completed (timeout is an ERROR, never a skip), then `POST /api/v1/knowledge/{kbID}/file/add {"file_id":…}`.
    (6) `owuiRemoveKnowledgeFile(ctx, base, token, kbID, fileID) error` — `POST /api/v1/knowledge/{kbID}/file/remove?delete_file=true` (confirmed leak-free: vectors deleted by file_id AND hash).
    (7) `owuiResetKnowledge(ctx, base, token, kbID) error` — `POST /api/v1/knowledge/{kbID}/reset` (id-preserving --rebuild primitive; NEVER `DELETE /knowledge/{id}/delete`, which strips the KB from every model's meta.knowledge).
    (8) `owuiAttachKnowledge(ctx, base, token, servedModelID, kbID, kbName) (recall.AttachmentState, error)` — idempotent read-merge-write per RESEARCH: `GET /api/v1/models/model?id=<served>`; if the row exists, unmarshal its FULL body into a generic map, merge the recall `{type:"collection", id, name}` item into `meta.knowledge` (dedupe by id; preserve every other meta/params key the operator may have set — never clobber), `POST /api/v1/models/model/update`; if absent, `POST /api/v1/models/create` with `{id: served, base_model_id: null, name: served, params: {}, is_active: true, meta: {knowledge: […]}}`. Return AttachmentAttached on success.
    (9) `owuiAttachmentState(ctx, base, token, kbID) recall.AttachmentState` — for `recall status`: discover the served model (reuse `discoverChatModel`), GET its Model row, return Attached if the recall KB id is in meta.knowledge, Missing if reachable-but-absent, Unknown on any error (typed-Unknown, never a guess).
    (10) `owuiHealthy(ctx, base) error` — cheap reachability gate (`GET /health` over loopback) run before any mutating step (Pitfall 7: knowledge/create needs villa-embed up; the index run refuses-with-remediation naming the unreachable service rather than failing confusingly mid-create).
    All bodies via `json.Marshal` only; all URLs composed `base + "/api/v1/…" + <API-returned id>`; every error wrapped with the endpoint name (`fmt.Errorf("knowledge/file/remove: %w", err)`). In `cmd/villa/verify_memory.go`, make the ONE sanctioned edit: parameterize the poll timeout — change `pollFileProcessed` to accept a `timeout time.Duration` parameter (callers in verify_memory.go pass the existing `ragSmokeProcessTimeout`; recall passes the size-aware value: 60s base + 1s per 2 KiB of transcript). Pure refactor, `verify memory` behavior unchanged, its tests stay green. NO new image literals anywhere (loopback URLs/endpoint paths are not gate-relevant — verify_memory.go precedent).
  </action>
  <verify>
    <automated>go build ./cmd/villa/... && go vet ./cmd/villa/... && go test ./cmd/villa/ -count=1 && go test ./internal/inference/ -run TestSeamGrepGate -count=1</automated>
  </verify>
  <acceptance_criteria>
    - All ten drive functions build and vet clean; existing verify-memory tests stay green after the pollFileProcessed timeout parameterization.
    - `grep -v '^[[:space:]]*//' cmd/villa/recall_live.go | grep -c 'kyuz0\|docker.io/\|ghcr.io/'` is 0 (no re-typed image literal).
    - Chat listing uses `/api/v1/chats/list/user/{id}` (admin, archived-inclusive), never `/api/v1/chats/` (Pitfall 1).
    - Rebuild uses `knowledge/{id}/reset`; per-chat replace uses `file/remove?delete_file=true`; attach is GET → merge → update-or-create.
  </acceptance_criteria>
  <done>The full live REST drive surface exists as fixed-arg loopback curl wrappers reusing the Phase-20 primitives, with the timeout parameterized and the seam gate green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire `villa recall index|status` — gated verbs, ordered run bodies, fake-Deps tests, root registration</name>
  <read_first>
    - cmd/villa/verify.go IN FULL — the parent-verb + subcommand + Deps + return-not-Exit shape to mirror (newVerify/newVerifyMemory/runVerifyMemory), and the CRITICAL DELTA: recall returns exitBlocked (not exitPass) when memory is off (D-07)
    - cmd/villa/preflight.go:20-22 — the authoritative exit constants (exitPass=0, exitBlocked=1, exitWarn=2)
    - cmd/villa/install_memory.go:124/:138 — `liveLoadedConfig` / `liveLoadedMemoryEnabled` (reuse, do not duplicate)
    - internal/modelswap/modelswap.go Run (:85-142) — the numbered-steps / short-circuit-with-FailedStep ordering discipline for the index run
    - cmd/villa/verify_memory_test.go — the gate-spy and ordering-spy test harness to replicate (cobra.Command + SetContext + SetOut/SetErr buffers, returned-int asserts)
    - cmd/villa/root.go:35-36 — the AddCommand registration site
  </read_first>
  <files>cmd/villa/recall.go, cmd/villa/recall_test.go, cmd/villa/root.go</files>
  <behavior>
    - TestRecallGate: memory off (loadedMemoryEnabled=false) ⇒ runRecallIndex AND runRecallStatus return exitBlocked, stderr carries the remediation ("enable memory_enabled in config.toml and run villa install first"), and a spy proves NO drive function ran (D-07 — the deliberate delta from verify memory's exit-0).
    - TestRecallIndexOrdering (spy-based): reachability failure ⇒ no KB/list/upload calls; a per-chat upload failure mid-run ⇒ state was ALREADY persisted for the chats that completed (incremental persist), last_index_completed_at NOT stamped, exit non-zero with the failed step named; clean full pass ⇒ completed stamp set, attach ran AFTER the per-chat loop.
    - TestRecallCleanReplace: a chat in Updates drives removeKnowledgeFile(old fileID from state) BEFORE uploadTranscript (delete-then-re-add order, D-04); a chat in Deletes drives removeKnowledgeFile and drops the state entry.
    - TestRecallAttach: attach merges into existing meta (other keys preserved — fake returns a row with extra meta keys; assert the update body still carries them) and creates when absent (idempotent create-or-update).
    - TestRecallStatus: liveKnown=false (listChats fake errors) ⇒ output contains "Unknown — could not evaluate", exit exitWarn, never "stale: 0"; attachment missing ⇒ surfaced in output; happy path ⇒ indexed/last-indexed/stale counts printed, exit exitPass.
    - TestRecallRebuild: --rebuild drives resetKnowledge BEFORE the (now full) re-index and clears the prior chats map from state.
  </behavior>
  <action>
    Create `cmd/villa/recall.go` (package main, house doc comment citing D-01..D-09 + RECALL-01..03). Define `recallDeps` with injectable func fields: `loadedMemoryEnabled func() bool`, `loadedConfig func() config.VillaConfig`, `mintToken func(ctx, base string) (string, error)`, `owuiHealthy func(ctx, base string) error`, `listUsers`, `listUserChats`, `getChat`, `ensureKnowledge`, `uploadTranscript`, `removeKnowledgeFile`, `resetKnowledge`, `attachKnowledge`, `attachmentState`, `discoverModel func(ctx, base, token string) (string, error)`, `readState func() (recall.State, error)`, `writeState func(recall.State) error`, `now func() time.Time` — and `liveRecallDeps()` wiring each to the Task-1 drives, the existing `liveLoadedConfig`/`liveLoadedMemoryEnabled`/`mintAdminToken`/`discoverChatModel`, and a state Deps built over `recall.RecallStatePath()` + `recall.WriteFileAtomic`/os.ReadFile. Cobra surface (D-07): `newRecall()` parent ("recall", short: index past conversations into Knowledge for semantic retrieval) adding `newRecallIndex()` (local flag `--rebuild`) + `newRecallStatus()`; RunE bodies `os.Exit(runRecallIndex(cmd, args, liveRecallDeps(), rebuild))` per the verify.go shape; register `newRecall()` in root.go's AddCommand list.
    `runRecallIndex(cmd, args, deps, rebuild bool) int` — the modelswap-style ordered pipeline, each step short-circuiting with a refuse-with-remediation message naming the failed step:
    (1) GATE: `memory.Decide(deps.loadedConfig())` — not enabled OR invalid ⇒ print remediation to stderr ("recall requires the memory stack: set memory_enabled=true in config.toml and run villa install, then re-run") and return exitBlocked (D-07: an explicit index request cannot honestly no-op).
    (2) REACHABILITY: compose base = `http://127.0.0.1:<cfg.ChatPort>` (loopback constant per the verify.go precedent, never `:port`); `owuiHealthy` failure ⇒ exitBlocked naming OWUI + the villa-embed dependency (Pitfall 7).
    (3) TOKEN: mintToken; failure ⇒ exitBlocked.
    (4) STATE+KB: readState (fail-closed empty on first run); if rebuild: resetKnowledge(state.KnowledgeID) when known, clear state.Chats, then proceed as full index. ensureKnowledge → record KnowledgeID/KnowledgeName + EmbeddingModel/EmbeddingDim from config into state (Phase-23 skew guard, D-05); stamp LastIndexStartedAt=now, CLEAR LastIndexCompletedAt, persist state (a crash mid-run must read as a partial run — D-06/Pitfall 8).
    (5) LIST: listUsers; EXCLUDE the `villa-verify@localhost` service account (D-09 — the only identity villa can deterministically exclude; all remaining human users == the operator on this box); listUserChats per user; any listing failure ⇒ exitBlocked (an index run cannot proceed on a partial universe).
    (6) PLAN: `recall.Plan(live, state)`.
    (7) EXECUTE sequentially (RESEARCH anti-pattern: never parallel): Deletes → removeKnowledgeFile(state fileID) + delete state entry + persist; Updates → removeKnowledgeFile(old) THEN getChat → RenderTranscript (ok=false ⇒ record skip in the run summary, drop any old state entry, continue) → uploadTranscript (size-aware timeout: 60s + 1s per 2 KiB) → update state entry {UserID, OWUIUpdatedAt, FileID, IndexedAt=now} + persist; Adds → same as Updates without the remove. State is persisted AFTER EVERY chat (incremental honesty); any step error ⇒ print the failed chat/step, persist, return exitBlocked — completed work is never lost.
    (8) ATTACH: discoverModel → attachKnowledge (idempotent read-merge-write, D-03); failure is a FAILURE (exitBlocked) — an index without retrieval wiring does not satisfy RECALL-02.
    (9) STAMP: LastIndexCompletedAt=now, persist, print the run summary (indexed/updated/deleted/skipped counts + durations) and return exitPass.
    `runRecallStatus(cmd, args, deps) int` — gate identically (exitBlocked when memory off); readState; attempt mintToken+listUsers+listUserChats to build the LIVE list (liveKnown=false on ANY failure — never a partial false-current); `attachmentState` for the attach check; `recall.Classify(...)`; render HUMAN-ONLY output (discretion decision — no --json this phase): indexed count, last index started/completed (or "partial run — remainder stale"), stale breakdown (new/changed/deleted) OR the literal "Unknown — could not evaluate" when StaleKnown=false, attachment state with a re-run hint when missing (Pitfall 2). Exits: exitPass when StaleKnown && stale==0 && attached; exitWarn when stale>0 or StaleKnown=false or attachment missing/unknown (honest WARN, not failure); exitBlocked only for the gate.
    Write `cmd/villa/recall_test.go` FIRST for the behaviors above (tdd) using the verify_memory_test.go harness: fake recallDeps with spy closures, in-memory state via readState/writeState closures over a variable, table-driven where natural. Every test doc-commented with the invariant + decision ID.
  </action>
  <verify>
    <automated>go test ./cmd/villa/ -run 'TestRecall' -count=1 && go build ./cmd/villa/... && go run ./cmd/villa recall --help</automated>
  </verify>
  <acceptance_criteria>
    - `villa recall index --help` and `villa recall status --help` reachable; verb registered in root.
    - Memory-off gate returns exitBlocked with remediation for BOTH verbs; spy proves no drive ran.
    - Ordering tests prove: incremental persist per chat, completed-stamp only on clean pass, delete-before-re-add, reset-before-rebuild, attach-after-index, read-merge-write attach preserving foreign meta keys.
    - status renders Unknown distinctly (never stale=0 on listing failure) and folds attachment state in; exit codes map pass/warn/blocked as specified.
  </acceptance_criteria>
  <done>`villa recall index|status` is wired, gated, fully fake-Deps-tested off-hardware, and registered in the root tree.</done>
</task>

<task type="auto">
  <name>Task 3: Extend docs/MEMORY.md with the recall section + full-suite green</name>
  <read_first>
    - docs/MEMORY.md — existing section structure (verb-titled `##` with `###` sub-steps, e.g. "Proving zero-outbound with `villa verify memory`"); keep the default-off auto-extraction guidance intact
    - 21-RESEARCH.md Pitfall 2 (post-model-swap re-assert gotcha) + "Identity path" (the widened admin read scope to document) + Pitfall 4 (non-admin retrieval silent-skip caveat)
  </read_first>
  <files>docs/MEMORY.md</files>
  <action>
    Append a new `## Indexing past conversations with villa recall` section to docs/MEMORY.md following the existing house style, covering: (1) what `villa recall index` does (chats → a villa-managed Knowledge collection "Villa Recall — Past Conversations"; OWUI's own chunk→embed→Qdrant path; strictly local, no new outbound, no new port); (2) `villa recall index --rebuild` (clean-recreate via knowledge reset, full re-index); (3) `villa recall status` and how to read it (indexed/last-indexed/stale; "Unknown — could not evaluate" means OWUI was unreachable, NOT that the index is current; partial-run meaning); (4) how retrieval works (the recall KB is attached to the SERVED model; new chats retrieve past-chat content by meaning with citations) and the **post-`villa model swap` gotcha**: a swap serves a new model id with no attachment row — run `villa recall index` (or check `recall status`) after a swap to re-assert retrieval (Pitfall 2); (5) the prerequisite (memory_enabled=true + villa install — the verbs refuse otherwise); (6) a privacy note documenting the widened admin read scope: the indexer reads ALL human users' chats via the admin API using the existing villa-verify@localhost service account on this single-operator box, and the villa-side state file stores ids/timestamps only — never chat content; (7) the non-admin caveat (retrieval silently skips the KB for non-admin OWUI users — Pitfall 4). Do NOT touch the existing auto-extraction default-off guidance. Then run the full suite.
  </action>
  <verify>
    <automated>grep -q 'villa recall' docs/MEMORY.md && grep -q 'model swap' docs/MEMORY.md && grep -qi 'Unknown' docs/MEMORY.md && make check</automated>
  </verify>
  <acceptance_criteria>
    - docs/MEMORY.md documents the verbs, --rebuild, status semantics (incl. Unknown), the model-swap re-assert gotcha, the prerequisite, and the widened-admin-read privacy note — with the existing auto-extraction guidance intact.
    - `make check` (vet + full `go test ./...`) green.
  </acceptance_criteria>
  <done>Operator documentation covers the recall feature end-to-end and the whole repo is green.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (Plan 02)

| Symbol / path | Kind | Notes |
|---------------|------|-------|
| `newRecall()` / `newRecallIndex()` / `newRecallStatus()` | new cobra commands | `villa recall index [--rebuild]`, `villa recall status`; registered in root.go |
| `--rebuild` | new CLI flag | local flag on `recall index`; reset-then-full-reindex (id-preserving) |
| `recallDeps` (17 injectable func fields) / `liveRecallDeps()` | new Deps seam | house pattern; every host effect injectable |
| `runRecallIndex(cmd, args, deps, rebuild) int` / `runRecallStatus(cmd, args, deps) int` | run bodies | return-not-Exit; exitPass/exitWarn/exitBlocked mapping |
| `owuiListUsers` / `owuiListUserChats` / `owuiGetChat` / `owuiEnsureKnowledge` / `owuiUploadTranscript` / `owuiRemoveKnowledgeFile` / `owuiResetKnowledge` / `owuiAttachKnowledge` / `owuiAttachmentState` / `owuiHealthy` | new live drives | `cmd/villa/recall_live.go`; fixed-arg curl over loopback |
| `owuiUser{ ID, Email, Role }` | new struct | users-list item |
| `recallKnowledgeName = "Villa Recall — Past Conversations"` | const | the villa-managed KB name (find-or-create key) |
| `pollFileProcessed(…, timeout time.Duration)` | modified signature | parameterized size-aware timeout (verify-memory callers unchanged in behavior) |
| `TestRecallGate` / `TestRecallIndexOrdering` / `TestRecallCleanReplace` / `TestRecallAttach` / `TestRecallStatus` / `TestRecallRebuild` | new tests | fake-Deps spies, off-hardware |
| docs/MEMORY.md `## Indexing past conversations with villa recall` | doc section | operator guide incl. model-swap gotcha + privacy note |
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| villa (host) ↔ OWUI loopback REST API | admin-scoped reads of ALL users' chats + Knowledge/Model writes over the existing 127.0.0.1 PublishPort |
| untrusted chat content → curl/exec layer | user/assistant text flows into multipart uploads and JSON bodies |
| villa ↔ $XDG_DATA_HOME/villa/recall-state.json | host-side index state writes |
| OWUI ↔ villa-embed/Qdrant (villa.network) | embedding/vector traffic stays container-DNS only (owned by Phase 19/20 posture) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-06 | Tampering | shell injection via chat titles/content reaching curl argv | mitigate | All bodies `json.Marshal` only; transcript content via stdin multipart (`-F file=@-`), never argv or temp files; URLs composed from config + API-returned ids only; fixed-arg `exec.CommandContext` (T-20-09 carried). |
| T-21-07 | Elevation of Privilege / Information Disclosure | admin-scoped chat read (villa reads ALL human users' chats via the service account) | accept | Single-operator box; same Phase-20 accepted credential posture (SECURITY 16/16); JWT held in memory only, never persisted; the WIDENED READ SCOPE is explicitly documented in docs/MEMORY.md (Task 3) and recorded for the phase SECURITY review. |
| T-21-08 | Information Disclosure | chat content leaving the box during indexing | mitigate | All REST traffic loopback-only (existing PublishPort, no new host port — D-11 carried); vectors flow OWUI→villa-embed→Qdrant on villa.network; zero-outbound already runtime-proven (Phase 20); no new outbound paths introduced (re-proven on-hardware in Plan 03). |
| T-21-09 | Repudiation | silent staleness / false-current status (OWUI down, partial run, detached model) | mitigate | Typed-Unknown rendering ("Unknown — could not evaluate", exitWarn); incremental per-chat state persist + completed-stamp-only-on-clean-pass; attachment state folded into status (Pitfall 2/8). Unit-tested. |
| T-21-10 | Tampering | attach step clobbering operator-set Model meta (description/capabilities lost) | mitigate | Read-merge-write attach (GET → merge knowledge into existing meta → update); foreign-key preservation unit-tested; A3 verified on-hardware in Plan 03. |
| T-21-11 | Tampering | stale vectors / duplicate growth on re-index | mitigate | Delete-then-re-add via `file/remove?delete_file=true` (vectors cleaned by file_id AND hash — verified against the pinned digest); `--rebuild` uses id-preserving `reset`, never KB delete. |
| T-21-12 | Denial of Service | bulk index contending with the chat model on the shared gfx1151 envelope | mitigate | Strictly sequential indexing (no parallel uploads); size-aware poll timeouts; throughput measured on-hardware (A1/A2, Plan 03). |
| T-21-13 | Spoofing | wrong identity indexed (service-account noise chats / missed operator chats) | mitigate | Admin list-by-user endpoint (archived-inclusive, Pitfall 1); villa-verify@localhost deterministically excluded; per-chat user_id recorded in state (D-09). |
| T-21-SC | Tampering | npm/pip/cargo installs | mitigate | No packages installed this phase (21-RESEARCH Package Legitimacy Audit: none). No `[ASSUMED]`/`[SUS]` checkpoints required. |
</threat_model>

<verification>
- `go test ./cmd/villa/ -run 'TestRecall' -count=1` green; existing verify-memory tests green after the timeout parameterization.
- `villa recall --help` / `recall index --help` / `recall status --help` reachable.
- `go test ./internal/inference/ -run TestSeamGrepGate -count=1` green (no image literals in recall files).
- `make check` green.
</verification>

<success_criteria>
- RECALL-01: `villa recall index` exists and choreographs the full chats→Knowledge pipeline over the loopback REST seam (gate → reachability → KB → list → diff → sequential clean-replace execute → attach → honest stamp), fully fake-Deps-tested.
- RECALL-02: the idempotent model-attachment step wires the recall KB into every new chat server-side; attachment state is part of honest status; the model-swap gotcha is documented.
- RECALL-03: incremental re-index is explicit-villa-controlled (`index` / `--rebuild`); `recall status` reports indexed/last-indexed/stale with typed-Unknown discipline and partial-run honesty.
- D-07 delta enforced: memory off ⇒ exitBlocked refuse-with-remediation on both verbs, never a silent no-op.
</success_criteria>

<output>
Create `.planning/phases/21-conversational-recall-indexer/21-02-SUMMARY.md` when done.
</output>
