---
phase: 18-memory-spine-config-core-embeddings-wiring-research-spike
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/config/villaconfig.go
  - internal/config/villaconfig_test.go
  - .planning/phases/18-memory-spine-config-core-embeddings-wiring-research-spike/18-DECISIONS.md
autonomous: true
requirements: [INFRA-04]

must_haves:
  truths:
    - "A user can set memory_enabled (plus embedding model / dim / Qdrant+embed addrs/ports) in config.toml and it loads into VillaConfig (D-04/D-06, SC#1)."
    - "An existing v1.2 config.toml with NO memory keys loads with memory defaulted-OFF (memory_enabled=false) and coherent inert defaults — no error (D-05, SC#1)."
    - "A v1.2 config with no memory keys round-trips with no save path triggered: a non-opted-in install is never rewritten with memory keys (byte-identical guarantee, SC#1)."
    - "normalizeVilla self-heals zero/absent memory fields from defaultConfig() as the single source, and NEVER widens a bind — empty addrs fill only the container-DNS default (D-05/D-06, PRIV-01)."
    - "The three spike decisions D-07 (dedicated villa-embed), D-08 (nomic-embed-text-v1.5 / 768-dim / ~512 MiB), D-09 (OWUI env keys incl. mandatory ENABLE_PERSISTENT_CONFIG=False) are durably recorded as a canonical decision artifact later phases consume (SC#3)."
  artifacts:
    - path: "internal/config/villaconfig.go"
      provides: "memory_* fields on VillaConfig + single-source defaults + self-heal"
      contains: "MemoryEnabled"
    - path: "internal/config/villaconfig_test.go"
      provides: "default-off load, byte-identical round-trip, self-heal, never-widen-bind tests"
      contains: "Memory"
    - path: ".planning/phases/18-memory-spine-config-core-embeddings-wiring-research-spike/18-DECISIONS.md"
      provides: "Canonical recorded spike decisions D-07/D-08/D-09 with evidence pointers (SC#3)"
      contains: "ENABLE_PERSISTENT_CONFIG"
  key_links:
    - from: "internal/config/villaconfig.go normalizeVilla"
      to: "defaultConfig()"
      via: "d := defaultConfig() derivation (no re-hard-coded literals)"
      pattern: "d := defaultConfig\\(\\)"
    - from: "internal/config/LoadVillaFrom / Parse"
      to: "normalizeVilla"
      via: "existing call sites self-heal memory fields for free"
      pattern: "normalizeVilla\\(cfg\\)"
---

<objective>
Extend `internal/config/villaconfig.go` with default-off, self-healing memory fields
so an existing v1.2 install stays byte-identical until the user opts in (SC#1), and
durably record the three pinned spike decisions (D-07/D-08/D-09) as a canonical
decision artifact later phases freeze against (SC#3).

This plan is the config half of the memory spine. It is Wave 1 with no dependencies:
`internal/memory` (Plan 02) imports the `VillaConfig` memory fields this plan adds, so
this plan must land first.

Purpose: INFRA-04 — the memory stack is config-driven; config is the single source of
truth that render/recommend/preflight read in later phases. No save path for memory is
added (byte-identical guarantee). No image literal, no host I/O.
Output: extended `VillaConfig` + tests + `18-DECISIONS.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md

@.planning/phases/18-memory-spine-config-core-embeddings-wiring-research-spike/18-CONTEXT.md
@.planning/phases/18-memory-spine-config-core-embeddings-wiring-research-spike/18-RESEARCH.md
@.planning/phases/18-memory-spine-config-core-embeddings-wiring-research-spike/18-PATTERNS.md
@.planning/phases/18-memory-spine-config-core-embeddings-wiring-research-spike/18-VALIDATION.md

@internal/config/villaconfig.go
@internal/config/villaconfig_test.go
</context>

<artifacts_this_phase_produces>
NEW symbols introduced by THIS plan (exclude from source-grounding drift checks):
- `VillaConfig` struct fields: `MemoryEnabled bool` (`toml:"memory_enabled"`),
  `EmbeddingModel string` (`toml:"embedding_model"`),
  `EmbeddingDim int` (`toml:"embedding_dim"`),
  `QdrantAddr string` (`toml:"qdrant_addr"`), `QdrantPort int` (`toml:"qdrant_port"`),
  `EmbedAddr string` (`toml:"embed_addr"`), `EmbedPort int` (`toml:"embed_port"`).
  (Exact key spellings are this plan's call per D-04 discretion — flat `memory_*`-style
  keys mirroring the `dashboard_*`/`chat_*` precedent. If you deviate, record the chosen
  spellings in the SUMMARY so Plan 02 and downstream phases bind the same names.)
- New test functions in `internal/config/villaconfig_test.go` (e.g.
  `TestLoadMemoryDefaultsOff`, `TestMemoryByteIdentical`, `TestNormalizeMemorySelfHeal`,
  `TestMemoryNeverWidensBind`).
- New doc artifact `18-DECISIONS.md`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add default-off, self-healing memory_* fields to VillaConfig</name>
  <files>internal/config/villaconfig.go, internal/config/villaconfig_test.go</files>
  <read_first>
    - internal/config/villaconfig.go — `VillaConfig` (lines 31-51), `defaultConfig()`
      (55-62), `normalizeVilla` (80-92), `LoadVilla`/`LoadVillaFrom`/`Parse` (call sites
      that already invoke normalizeVilla). The `dashboard_*`/`chat_*` fields are the EXACT
      precedent (D-04/D-05).
    - internal/config/villaconfig_test.go — `TestLoadMissingReturnsDefaults` (48),
      `TestLoadNormalizesZeroPorts` (76), `TestLoadPreservesExplicitNonZero` (115),
      `TestDefaultConfigDashboardFields` (144) — mirror these idioms for the memory fields.
    - 18-RESEARCH.md → Pattern 1 (single-source defaults + self-heal), Pitfall 1
      (BurntSushi/toml writes type-zero keys), Code Examples (defaultConfig snippet).
    - 18-PATTERNS.md → `internal/config/villaconfig.go (MODIFY)` section.
  </read_first>
  <behavior>
    - Test: A v1.2 config.toml with NO memory keys loads → MemoryEnabled=false plus the
      inert defaults (EmbeddingModel="nomic-embed-text-v1.5", EmbeddingDim=768,
      QdrantAddr="villa-qdrant", QdrantPort=6333, EmbedAddr="villa-embed", EmbedPort=8080).
    - Test: An absent config file → defaultConfig() with memory defaulted-off (mirror
      TestLoadMissingReturnsDefaults; assert got == defaultConfig()).
    - Test: defaultConfig() seeds the memory literals directly (mirror
      TestDefaultConfigDashboardFields) — the ONLY home of those literals.
    - Test: An on-disk config with zeroed/empty memory fields (embedding_dim=0,
      qdrant_addr="", qdrant_port=0, embed_addr="", embed_port=0) self-heals on load to
      the defaults via normalizeVilla (mirror TestLoadNormalizesZeroPorts).
    - Test: explicit non-default memory values round-trip unchanged through load (mirror
      TestLoadPreservesExplicitNonZero) — normalize FILLS unset, never overrides a choice.
  </behavior>
  <action>
    Add the memory fields to `VillaConfig` alongside `ChatPort`, each with a `toml:"..."`
    tag and a doc comment naming its default and (for endpoints) the PRIV-01
    "container-DNS only / never widen a bind" rule per D-06. Use flat keys matching the
    `dashboard_*`/`chat_*` precedent (D-04); exact spellings are your call within D-04/D-05
    discretion — record them in the SUMMARY. Seed every memory default ONLY in
    `defaultConfig()` (the single source of the literals `nomic-embed-text-v1.5`, `768`,
    `villa-qdrant`/`6333`, `villa-embed`/`8080`); `MemoryEnabled` defaults `false`.
    Extend `normalizeVilla` to treat type-zero memory fields as "unset → default", deriving
    EVERY fill from `d := defaultConfig()` — NEVER re-hard-code a literal (a duplicate is a
    drift bug). For the endpoint addr fields, fill ONLY the container-DNS default name on an
    empty value — never substitute a routable/widened bind (mirror the dashboard_addr
    loopback rule, PRIV-01). Do NOT touch `LoadVilla`/`LoadVillaFrom`/`Parse`/`SaveVilla`/
    `assertInsideDir` bodies — they self-heal for free once normalizeVilla is extended, and
    D-05 mandates SaveVilla's XDG/0600/0700/traversal/no-shell-interpolation discipline stays
    unchanged. Add the mirrored tests to `villaconfig_test.go` covering the behaviors above.
    NOTE: `TestSaveLoadRoundTrip` constructs a full `VillaConfig` literal and asserts equality;
    once memory fields exist it must populate them (use the defaults) or it will fail — update
    that fixture in the same change.
  </action>
  <verify>
    <automated>go test ./internal/config/ -run 'TestLoad|TestDefaultConfig|TestNormalize|TestMemory' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - VillaConfig carries the memory fields; defaultConfig() is the sole home of their
      literals; MemoryEnabled defaults false.
    - A v1.2 config with no memory keys loads memory-off with inert defaults; an absent
      file equals defaultConfig().
    - normalizeVilla self-heals zeroed memory fields from defaultConfig() and never widens
      a bind; explicit values round-trip unchanged.
    - `go test ./internal/config/` is green (including the updated TestSaveLoadRoundTrip).
  </acceptance_criteria>
  <done>
    Memory fields exist with single-source defaults + self-heal; all listed config tests
    pass; no save path for memory was added.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove the byte-identical guarantee (SC#1) for a non-opted-in install</name>
  <files>internal/config/villaconfig_test.go</files>
  <read_first>
    - internal/config/villaconfig_test.go — `TestSaveLoadRoundTrip` (12) for the
      round-trip idiom; `TestLoadNormalizesZeroPorts` (76) for the on-disk-bytes idiom.
    - internal/config/villaconfig.go — confirm Phase 18 adds NO memory save path
      (SaveVilla/SaveVillaTo unchanged); LoadVillaFrom returns the in-memory struct only.
    - 18-RESEARCH.md → Pitfall 1 ("BurntSushi/toml writes a key even when its value is the
      type-zero → byte-identical break") and Validation Architecture (TestMemoryByteIdentical).
    - 18-PATTERNS.md → "Byte-identical guarantee (SC#1) — the load-bearing nuance".
  </read_first>
  <behavior>
    - Test: Write a v1.2-style config.toml on disk containing ONLY the v1.2 keys (model,
      quant, ctx, backend, dashboard_addr, dashboard_port, chat_port) and NO memory keys.
    - Test: Read the raw file bytes before load; LoadVillaFrom it (which self-heals the
      in-memory struct to memory-off defaults); assert the on-disk file is NOT mutated by
      the load (re-read bytes == original bytes — load is read-only).
    - Test: assert that a non-opt-in install has no code path in this phase that emits
      memory keys to disk — i.e. the test does NOT call any save; the guarantee is that
      Phase 18 introduces no memory write. (If you choose to also assert the marshal shape,
      document that a future opt-in WILL emit memory keys — that is the intended Phase 20+
      behavior, NOT a violation here.)
  </behavior>
  <action>
    Add `TestMemoryByteIdentical` (and any helper) asserting that loading a v1.2 config
    with no memory keys leaves the on-disk file unchanged (read bytes, LoadVillaFrom,
    re-read bytes, compare). The assertion proves SC#1's load-path half: a non-opted-in
    install is never rewritten with memory keys because Phase 18 adds no memory save path.
    Do NOT add a save path to make this pass — the absence of a save IS the guarantee.
    Keep the test in the existing `package config` test file (no new fixture files; Phase 18
    changes no byte-frozen --json/golden contract).
  </action>
  <verify>
    <automated>go test ./internal/config/ -run 'TestMemoryByteIdentical|TestSaveLoadRoundTrip' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - Loading a memory-key-free v1.2 config does not mutate the on-disk file (byte-equal
      before/after load).
    - No memory save path was added; the test does not call SaveVilla/SaveVillaTo to
      manufacture memory keys.
    - `go test ./internal/config/` stays green.
  </acceptance_criteria>
  <done>
    SC#1 byte-identical guarantee is proven by an automated test; the existing config
    suite remains green.
  </done>
</task>

<task type="auto">
  <name>Task 3: Record the canonical spike decisions (D-07/D-08/D-09) as 18-DECISIONS.md</name>
  <files>.planning/phases/18-memory-spine-config-core-embeddings-wiring-research-spike/18-DECISIONS.md</files>
  <read_first>
    - 18-RESEARCH.md → "Spike Decisions (PINNED with evidence)" (D-07/D-08/D-09) and the
      D-09 env-key table — this is the verified evidence; the artifact distills it into the
      canonical, durable form later phases cite.
    - 18-CONTEXT.md → D-07/D-08/D-09 (locked decision text) and the "Claude's Discretion"
      note allowing a short decisions appendix.
    - .planning/STATE.md → Research Flags + Blockers/Concerns (the NON-NEGOTIABLE threats
      Phases 19/20/22 must honor) so the recorded decisions name their downstream consumers.
  </read_first>
  <action>
    Create `18-DECISIONS.md` as the canonical, durable record of the three spike decisions
    SC#3 requires (this is documentation, NOT runtime code — introduce no image literal, no
    Go code). For each decision give: the pinned value(s), a one-line rationale, the
    downstream phase that freezes it, and a pointer to the cited evidence in 18-RESEARCH.md.
    Cover exactly:
    - D-07 — embeddings runtime: dedicated `villa-embed` llama-server (reusing the pinned
      kyuz0 toolbox image, OpenAI `/v1/embeddings`, `--embeddings --pooling mean|cls`,
      container-DNS only) — NOT OWUI's built-in embedder. Consumer: Phase 19.
    - D-08 — embedding model + footprint: `nomic-embed-text-v1.5`, 768-dim (pinned, no
      Matryoshka truncation), 8192-token ctx, Q8_0 GGUF; ~512 MiB conservative resident
      reservation (flagged for on-hardware refinement in Phase 19). Consumers: Phase 19
      (pre-stage + measure), Phase 22 (footprint reservation), Phase 23 (dimension in
      manifest / swap guard).
    - D-09 — OWUI env contract: the verified key set (`VECTOR_DB=qdrant`, `QDRANT_URI`,
      `QDRANT_API_KEY`, `ENABLE_QDRANT_MULTITENANCY_MODE` [explicit choice required],
      `QDRANT_COLLECTION_PREFIX`, `RAG_EMBEDDING_ENGINE=openai`, `RAG_OPENAI_API_BASE_URL`,
      `RAG_OPENAI_API_KEY`, `RAG_EMBEDDING_MODEL`, `RAG_EMBEDDING_MODEL_AUTO_UPDATE=False`,
      `ENABLE_MEMORIES`, and the load-bearing `ENABLE_PERSISTENT_CONFIG=False` plus the
      offline/telemetry lockdown set). Flag `ENABLE_PERSISTENT_CONFIG=False` as MANDATORY
      (not optional) and note these are RECORDED only — Phase 20 writes the env block.
    After writing the artifact, optionally also persist the three decisions to graphmind
    memory (`graphmind memory add "<decision>" --type decision`) so they survive across
    sessions — the markdown artifact is the authoritative record either way.
  </action>
  <verify>
    <automated>test -f .planning/phases/18-memory-spine-config-core-embeddings-wiring-research-spike/18-DECISIONS.md &amp;&amp; grep -q ENABLE_PERSISTENT_CONFIG .planning/phases/18-memory-spine-config-core-embeddings-wiring-research-spike/18-DECISIONS.md &amp;&amp; grep -q nomic-embed-text-v1.5 .planning/phases/18-memory-spine-config-core-embeddings-wiring-research-spike/18-DECISIONS.md &amp;&amp; grep -q villa-embed .planning/phases/18-memory-spine-config-core-embeddings-wiring-research-spike/18-DECISIONS.md &amp;&amp; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - 18-DECISIONS.md exists and records all three decisions (D-07/D-08/D-09) with pinned
      values, rationale, downstream consumer phase, and an evidence pointer to 18-RESEARCH.md.
    - ENABLE_PERSISTENT_CONFIG=False is flagged MANDATORY; the dimension 768 and model
      nomic-embed-text-v1.5 are recorded; villa-embed runtime choice is recorded.
    - The artifact introduces NO runtime code and NO image literal (it is documentation).
  </acceptance_criteria>
  <done>
    The three spike decisions are durably recorded in a canonical artifact (SC#3); the
    grep gate passes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user → config.toml | A hand-edited (or malicious) config file is untrusted input that crosses into VillaConfig on load. |
| villa control plane → filesystem | Config writes (existing SaveVilla path) cross into `$XDG_CONFIG_HOME/villa` — Phase 18 adds NO new write. |
| (recorded, not built) OWUI → HuggingFace / vector-store telemetry | The runtime-privacy surface the recorded decisions exist to close downstream (Phases 19/20). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-01 | Tampering | config.toml memory fields (hand-edited zeros/empties) | mitigate | `normalizeVilla` treats type-zero memory fields as "unset → default" from the single `defaultConfig()` source; an absent/zeroed file self-heals to a coherent memory-off state rather than a broken bind. Proven by the self-heal + default-off tests. |
| T-18-02 | Information Disclosure | memory endpoint addr fields (Qdrant/embed) | mitigate | Endpoint fields default to container-DNS names on `villa.network`; `normalizeVilla` fills ONLY the container-DNS default on an empty addr and NEVER widens a bind to a routable interface (mirrors the dashboard_addr PRIV-01 rule). Qdrant has no published host port (D-06). Proven by the never-widen-bind test. |
| T-18-03 | Tampering | config write discipline (XDG/0600/0700/traversal/no-shell-interpolation) | accept (reuse unchanged) | D-05 mandates SaveVilla's existing discipline is reused UNCHANGED; this plan adds no write path and does not touch SaveVilla/assertInsideDir. The existing `TestSaveRefusesTraversal` continues to guard it. |
| T-18-DOC | Information Disclosure (recorded, downstream) | OWUI HF lazy-download + ChromaDB telemetry | mitigate (record only) | 18-DECISIONS.md records WHY `ENABLE_PERSISTENT_CONFIG=False` + the offline/telemetry lockdown + the dedicated local `villa-embed` are MANDATORY downstream (the #1 runtime-privacy risk). Phase 18 only records; Phases 19/20 enforce + run the zero-outbound smoke test. |
| T-18-SC | Tampering | npm/pip/cargo/go installs | accept | Phase 18 introduces ZERO new dependencies (go.mod unchanged; no package install task). No supply-chain checkpoint required. |
</threat_model>

<verification>
- `go test ./internal/config/ -count=1` — all config tests green (default-off load,
  byte-identical, self-heal, never-widen-bind, existing round-trip/traversal).
- `make check` (vet + `go test ./...`) — full suite green, including
  `TestSeamGrepGate` (this plan touches no inference/image literal, so it stays green).
- `18-DECISIONS.md` exists with D-07/D-08/D-09 recorded (grep gate in Task 3).
</verification>

<success_criteria>
- SC#1: An existing v1.2 config.toml (no memory keys) loads memory-OFF with coherent
  inert defaults and is not rewritten — byte-identical on load.
- SC#3 (config-side artifact): the three spike decisions are durably recorded in a
  canonical artifact later phases consume.
- `make check` is green; the seam gate stays green.
</success_criteria>

<output>
Create `.planning/phases/18-memory-spine-config-core-embeddings-wiring-research-spike/18-01-SUMMARY.md` when done.
Record in the SUMMARY: the exact memory field names + TOML key spellings chosen (Plan 02
and downstream phases bind these), and confirmation that no memory save path was added.
</output>
