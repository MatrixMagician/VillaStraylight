---
phase: 19-vector-store-local-embeddings-services
plan: 03
type: execute
wave: 3
depends_on: ["19-01", "19-02"]
files_modified:
  - internal/orchestrate/memory.go
  - internal/orchestrate/testdata/villa-qdrant.container.golden
autonomous: false
requirements: [INFRA-01, INFRA-02, PRIV-04]
user_setup:
  - service: "Fedora dev box (AMD Strix Halo gfx1151)"
    why: "On-hardware freeze of the Qdrant digest + /v1/embeddings health + reboot-survival; cannot be done off-host"
    dashboard_config:
      - task: "Resolve the dev-box RepoDigest for qdrant/qdrant:v1.18.2-unprivileged and confirm it is the official image; curl /v1/embeddings against the pinned kyuz0 digest; reboot to prove volume durability"
        location: "rootless Podman + systemctl --user on the Fedora dev box"
must_haves:
  truths:
    - "The pinned qdrantImage digest matches the dev-box-resolved RepoDigest for the official docker.io/qdrant/qdrant:v1.18.2-unprivileged image"
    - "The pinned kyuz0 digest serves a working /v1/embeddings returning a 768-length vector (not regressed per #15406)"
    - "villa install (memory_enabled=true) brings up villa-qdrant + villa-embed on villa.network with no published host port"
    - "Qdrant is writable (a 768-dim probe collection round-trips) and the named volume survives a reboot"
    - "A first /v1/embeddings request succeeds with no internet access (model pre-staged, runtime zero-download)"
  artifacts:
    - path: "internal/orchestrate/memory.go"
      provides: "the FROZEN, dev-box-confirmed qdrantImage digest (placeholder resolved) + the TODO(19-03) marker removed"
      contains: "func QdrantImage"
  key_links:
    - from: "internal/orchestrate/memory.go qdrantImage const"
      to: "the official docker.io/qdrant/qdrant image RepoDigest on the dev box"
      via: "podman image inspect … RepoDigests confirmation"
      pattern: "docker.io/qdrant/qdrant:v1.18.2-unprivileged@sha256:"
---

<objective>
Freeze and prove the two digest-pinned images on the Fedora AMD Strix Halo dev box, and
verify the full memory-stack bring-up the two prior plans render and wire. This is the
on-hardware honesty gate (RESEARCH ## Package Legitimacy Audit caveat + Assumptions A1/A2/A5,
SC#2/SC#3): (1) confirm the Qdrant `qdrantImage` digest equals the dev-box-resolved official
RepoDigest and curl `/v1/embeddings` against the pinned kyuz0 digest before the units are
considered frozen (D-01/D-06, image-supply-chain legitimacy); (2) prove Qdrant writability +
reboot survival (SC#2) and an offline 768-dim first embedding (PRIV-04/SC#3) on the live host.

Purpose: close the two on-hardware verifications (A1 Qdrant write/`:Z` ownership, A2 `/v1/embeddings`
health, A5 manifest-vs-platform digest) that gate freezing the units — neither can ship silently
broken because the Plan-19-02 install proof + these checkpoints catch them.
Output: the resolved/confirmed `qdrantImage` digest in `internal/orchestrate/memory.go` (placeholder
replaced, TODO removed; golden refrozen if the digest text changed), plus a recorded UAT result for the
three live success criteria.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/19-vector-store-local-embeddings-services/19-CONTEXT.md
@.planning/phases/19-vector-store-local-embeddings-services/19-RESEARCH.md
@.planning/phases/19-vector-store-local-embeddings-services/19-01-SUMMARY.md
@.planning/phases/19-vector-store-local-embeddings-services/19-02-SUMMARY.md
@internal/orchestrate/memory.go
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: LEGITIMACY GATE — confirm the Qdrant dev-box RepoDigest + curl /v1/embeddings against the pinned kyuz0 digest, then freeze qdrantImage</name>
  <files>internal/orchestrate/memory.go (confirmation only at this step — the digest is applied in Task 2)</files>
  <what-built>
    Plan 19-01 pinned `qdrantImage = docker.io/qdrant/qdrant:v1.18.2-unprivileged@sha256:b79aaa49…5962e5`
    (the registry-API manifest-list digest, committed as a placeholder with a `TODO(19-03)` marker) and
    `embedImage` == the already-approved kyuz0 `vulkanImage` digest. Per the RESEARCH Package Legitimacy
    Audit caveat and Assumptions A2/A5, the Qdrant digest MUST be confirmed against the dev-box-resolved
    official RepoDigest, and the kyuz0 `/v1/embeddings` endpoint MUST be curl-proven against the pinned
    digest (the #15406 carry-forward, D-06) BEFORE the units are frozen. This is a BLOCKING, non-auto-approvable
    image-supply-chain legitimacy gate (container-image legitimacy is enforced by digest-pinning + manual
    provenance audit in this project — the npm/PyPI/crates seam does not cover container images).
  </what-built>
  <action>
    PAUSE for the operator to run the dev-box legitimacy checks in &lt;how-to-verify&gt; (Qdrant RepoDigest
    confirmation + the /v1/embeddings 768 curl against the pinned kyuz0 digest). This is a blocking-human
    image-supply-chain gate: it is NON-auto-approvable (workflow.auto_advance is ignored). Do NOT proceed to
    Task 2 until the operator reports the confirmed official digest and the 768-length result. Record the
    digest value to freeze (and note whether it equals the placeholder or supersedes it — A5).
  </action>
  <how-to-verify>
    On the Fedora AMD Strix Halo dev box:
    1. Resolve + confirm the Qdrant digest is the OFFICIAL image:
       `podman pull docker.io/qdrant/qdrant:v1.18.2-unprivileged`
       `podman image inspect docker.io/qdrant/qdrant:v1.18.2-unprivileged --format '{{index .RepoDigests 0}}'`
       Confirm the printed `docker.io/qdrant/qdrant@sha256:…` is the official qdrant/qdrant image (cross-check
       on https://hub.docker.com/r/qdrant/qdrant/tags — the `v1.18.2-unprivileged` tag) and note whether it
       matches the placeholder digest already in memory.go. If the dev-box RepoDigest differs from the
       placeholder (manifest-list vs linux/amd64 child — A5), the new value is applied to the qdrantImage const
       in Task 2.
    2. Curl /v1/embeddings against the PINNED kyuz0 digest (D-06, #15406 gate) — run the embed unit (or a
       one-shot `podman run --rm --network villa.network <pinned-kyuz0-digest> llama-server -m /models/nomic-embed-text-v1.5.Q8_0.gguf --embeddings --pooling mean -c 8192 --host 0.0.0.0 --port 8080`) and from inside villa.network:
       `curl -sf http://villa-embed:8080/v1/embeddings -H 'Content-Type: application/json' -d '{"input":"villa memory readiness probe","model":"nomic-embed-text-v1.5","encoding_format":"float"}' | jq '.data[0].embedding | length'`
       Confirm the result is exactly 768 (not 501/400/empty). If the pinned kyuz0 digest is regressed, pin a
       different known-good kyuz0 digest for embedImage (it need not equal the chat digest, but D-04 prefers
       reuse — reuse only if this curl passes) and note it for Task 2.
    3. Confirm both images are digest-pinned in memory.go (no floating tag) and QdrantImage()/EmbedImage()
       return the frozen literals.
  </how-to-verify>
  <verify><human-check>Operator confirms the dev-box RepoDigest is the official docker.io/qdrant/qdrant image and /v1/embeddings returns a 768-length vector against the pinned kyuz0 digest.</human-check></verify>
  <resume-signal>Type "approved: digest &lt;sha256&gt; confirmed, /v1/embeddings returns 768" (note whether the qdrantImage const must change in Task 2), or describe the mismatch/regression to address.</resume-signal>
  <done>The operator has confirmed the official Qdrant RepoDigest (matching or superseding the placeholder) and a working 768-length /v1/embeddings against the pinned kyuz0 digest; the value to freeze is recorded for Task 2.</done>
</task>

<task type="auto">
  <name>Task 2: Apply the confirmed digest + refreeze the Qdrant golden (only if the digest text changed)</name>
  <files>internal/orchestrate/memory.go, internal/orchestrate/testdata/villa-qdrant.container.golden</files>
  <read_first>
    - internal/orchestrate/memory.go (the qdrantImage const + its TODO(19-03) marker)
    - internal/orchestrate/testdata/villa-qdrant.container.golden (the frozen Qdrant container contract — its Image= line embeds the digest)
  </read_first>
  <action>
    Using the dev-box-confirmed RepoDigest from Task 1:
    - If the confirmed digest EQUALS the placeholder already in memory.go: just remove the `TODO(19-03)` marker
      from the const comment and record the dev-box confirmation date. No golden change (the Image= line is
      unchanged) — confirm the golden is byte-identical.
    - If the confirmed digest DIFFERS: update the `qdrantImage` const literal to the dev-box RepoDigest, update
      the resolution-date comment, remove the TODO marker, then refreeze ONLY the Qdrant container golden via
      `go test ./internal/orchestrate/ -run TestRenderQdrant -update`. The villa-embed + villa-qdrant.volume
      goldens and the 5 v1.2 goldens MUST stay unchanged.
    Run the full orchestrate suite + the seam gate to confirm nothing else drifted.
  </action>
  <verify>
    <automated>go test ./internal/orchestrate/ -count=1 && go test ./internal/inference/ -run TestSeamGrepGate -count=1 && grep -q 'docker.io/qdrant/qdrant:v1.18.2-unprivileged@sha256:' internal/orchestrate/memory.go && ! grep -q 'TODO(19-03)' internal/orchestrate/memory.go</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'docker.io/qdrant/qdrant:v1.18.2-unprivileged@sha256:' internal/orchestrate/memory.go` succeeds (digest-pinned, no floating tag).
    - `grep -c 'TODO(19-03)' internal/orchestrate/memory.go` returns 0 (the freeze marker is removed — the digest is confirmed).
    - `go test ./internal/orchestrate/ -count=1` PASSES and `git status --porcelain internal/orchestrate/testdata/` shows ONLY the Qdrant container golden as modified IF the digest changed (otherwise nothing); the villa-embed + qdrant.volume + 5 v1.2 goldens are untouched.
    - `go test ./internal/inference/ -run TestSeamGrepGate -count=1` PASSES.
  </acceptance_criteria>
  <done>The qdrantImage const holds the dev-box-confirmed RepoDigest with the TODO marker removed; the Qdrant container golden is refrozen only if the digest text changed; all other goldens unchanged; orchestrate suite + seam gate green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: On-hardware bring-up — install (memory on), prove Qdrant writable + reboot survival + offline 768-dim first embedding, container-DNS only</name>
  <files>(no source edit — on-hardware UAT; record results in 19-03-SUMMARY.md)</files>
  <what-built>
    `villa install` with `memory_enabled=true` now renders + starts `villa-qdrant` + `villa-embed` on
    `villa.network` (container-DNS only, no host port — Plan 19-01), pre-stages the nomic Q8_0 GGUF into
    `villa-models` and runs the memory-stack readiness proof (offline 768-dim /v1/embeddings + Qdrant writable
    round-trip — Plan 19-02). This checkpoint verifies the live success criteria the off-hardware tests cannot:
    SC#2 (Qdrant writable + survives reboot via the named `:Z` volume + lingering — A1), SC#3/PRIV-04 (offline
    first embedding, zero runtime download), and SC#4 (no published host port — the loopback-only audit stays green).
  </what-built>
  <action>
    PAUSE for the operator to run the live bring-up + UAT steps in &lt;how-to-verify&gt; on the Fedora dev box.
    Do NOT mark the phase complete until the operator confirms: install PASS, Qdrant writable + survived reboot,
    an offline 768-dim first embedding, and no published host port. If a Qdrant write fails pre-reboot, apply the
    `,U` belt-and-suspenders label fix per Pitfall 1 (qdrantVolumeMount → `…:Z,U` in memory.go), re-run, and
    refreeze the qdrant golden. Record the measured embed resident GTT delta for Phase-22 CTRL-01.
  </action>
  <how-to-verify>
    On the Fedora dev box, with `memory_enabled=true` in config.toml:
    1. `villa install` (or the lifecycle bring-up) completes PASS — the memory-stack readiness proof prints
       "memory stack ready (768-dim embeddings + Qdrant writable)". A proof FAILURE must refuse-with-remediation
       (exitBlocked), never a silent green.
    2. SC#2 writable + reboot survival:
       - `systemctl --user is-active villa-qdrant.service villa-embed.service` → both active.
       - From inside villa.network: PUT a 768-dim test collection, insert a point, then `sudo reboot`.
       - After reboot (with lingering enabled): the services come back up and the collection + point are STILL
         present (the named `:Z` volume persisted). If a write failed pre-reboot, apply the `,U` fix (Pitfall 1).
    3. SC#3 / PRIV-04 offline first embedding: with NO network (e.g. `sudo nmcli networking off` or a firewalled run),
       issue the /v1/embeddings probe from inside villa.network and confirm a 768-length vector returns — the GGUF
       was pre-staged at install, so no HuggingFace pull occurs.
    4. SC#4 loopback-only: `villa status` (or the privacy audit) reports loopback-only stays green; confirm neither
       villa-qdrant nor villa-embed published a host port (`podman port villa-qdrant` / `villa-embed` show nothing;
       no `PublishPort=` in `~/.config/containers/systemd/villa-qdrant.container` / `villa-embed.container`).
    Record the measured resident GTT delta when villa-embed is up (refines the ~512 MiB D-08 estimate for Phase-22
    CTRL-01) — informational, not a gate.
  </how-to-verify>
  <verify><human-check>Operator confirms: install PASS with the memory-readiness proof green; villa-qdrant + villa-embed active and container-DNS only (no host port); a 768-dim collection writes and survives a reboot; an offline /v1/embeddings request returns a 768-length vector.</human-check></verify>
  <resume-signal>Type "approved: install PASS, Qdrant writable + survived reboot, offline 768-dim embedding confirmed, no host port" (note the measured embed GTT delta), or describe the failure (and whether the `,U` fix or a digest re-pin was applied).</resume-signal>
  <done>On the live host: install PASSes with the memory-readiness proof green; Qdrant is writable and survives a reboot (SC#2); an offline first /v1/embeddings returns 768 (SC#3/PRIV-04); neither service published a host port (SC#4). The embed GTT delta is recorded for Phase 22.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Docker Hub → dev box (image pull) | The Qdrant image identity must be confirmed as the official publisher's digest before it is frozen as a const. |
| kyuz0 image (llama.cpp master rebuild) → embed unit | The pinned digest must be proven to serve a working /v1/embeddings (the #15406 carry-forward). |
| Qdrant container (UID 1000) → named `:Z` volume | The unprivileged container must be able to write /qdrant/storage on the rootless named volume (A1). |
| no-network host → villa-embed | The first embedding must succeed offline (pre-staged GGUF), proving zero runtime download. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-12 | Spoofing / Tampering | qdrantImage digest provenance | mitigate | Task 1 confirms the dev-box RepoDigest is the official docker.io/qdrant/qdrant image (cross-checked on Docker Hub) before freezing; the const is digest-pinned, not a floating tag (A5). |
| T-19-13 | DoS / integrity | /v1/embeddings regression on the pinned kyuz0 digest (#15406) | mitigate | Task 1 curls /v1/embeddings against the PINNED digest and confirms a 768-length vector before the unit is frozen (D-06); a regressed digest is re-pinned to a known-good kyuz0 digest. |
| T-19-14 | DoS / integrity | Qdrant write failure / non-durable volume → false-green | mitigate | Task 3 actively proves a 768-dim collection writes AND survives a reboot (SC#2/A1); the `,U` label fix is the documented remedy if a write fails. The Plan-19-02 install proof already gates the install-time write. |
| T-19-15 | Information Disclosure | runtime model exfil / host-port widening | mitigate | Task 3 confirms the first embedding succeeds with NO network (PRIV-04) and that neither unit published a host port (SC#4/D-10) — the loopback-only audit stays green. |
| T-19-SC | Tampering | container image installs (qdrant/qdrant, kyuz0) | mitigate | This plan IS the blocking, non-auto-approvable image-legitimacy checkpoint for the digest-pinned managed-service images (Task 1) — confirmed before freeze; both verdicts were OK in the RESEARCH audit. |

ASVS L1, block on high — Task 1 is a blocking-human legitimacy gate (never auto-approved, `workflow.auto_advance`
ignored); Task 3 is a blocking on-hardware verification. No high-severity item ships unverified.
</threat_model>

<verification>
- `go test ./internal/orchestrate/ -count=1` + `go test ./internal/inference/ -run TestSeamGrepGate -count=1` — green after the digest is applied.
- Task 1 (blocking-human): dev-box RepoDigest confirmed official + /v1/embeddings returns 768 against the pinned kyuz0 digest.
- Task 3 (blocking): install PASS, Qdrant writable + survives reboot, offline 768-dim first embedding, no published host port.
- `make check` — full suite green; CGO-free static build intact.
</verification>

<success_criteria>
- INFRA-01: the frozen qdrantImage is the dev-box-confirmed official RepoDigest; villa-qdrant comes up with a writable `:Z` named volume that survives a reboot (SC#2), no published host port (SC#4).
- INFRA-02: the pinned kyuz0 digest serves a working /v1/embeddings (768-length, not #15406-regressed); villa-embed comes up container-DNS only.
- PRIV-04 / SC#3: a first /v1/embeddings request succeeds offline (model pre-staged at install; runtime zero-download).
- The qdrantImage TODO(19-03) freeze marker is removed and the digest is confirmed.
</success_criteria>

<output>
Create `.planning/phases/19-vector-store-local-embeddings-services/19-03-SUMMARY.md` when done (record the confirmed digest, the /v1/embeddings 768 result, the reboot-survival result, and the measured embed GTT delta for Phase 22).
</output>
