---
phase: 31-grounded-fetch-embed-grounding
plan: 04
type: execute
wave: 3
depends_on: ["31-03"]
files_modified:
  - internal/orchestrate/websafe.go
  - internal/orchestrate/openwebui.go
  - internal/orchestrate/testdata
  - Makefile
autonomous: false
requirements: [GROUND-01, GROUND-02, GROUND-03, GUARD-01]
user_setup:
  - service: gfx1151 dev box (Strix Halo, Fedora, rootless Podman)
    why: "Phase 31's load-bearing claims (BYPASS=False grounds; ephemeral-collection isolation; offload-asserted residency under search load; static binary runs on distroless) cannot be proven off-hardware"
must_haves:
  truths:
    - "file ./villa (built CGO_ENABLED=0) reports 'statically linked' so the bind-mounted binary runs on distroless"
    - "The distroless base RepoDigest is resolved on the dev box and pinned in orchestrate/websafe.go (placeholder replaced)"
    - "With web search on + memory on, a real research question returns an answer grounded in fetched pages with inline citations to live URLs (BYPASS=False + retrieval-fix key confirmed at the pinned OWUI digest)"
    - "Qdrant shows the web-search ephemeral collection distinct from the durable open-webui_* collections (GROUND-02 isolation proof)"
    - "The chat model stays GPU-resident under search load (offload-assert PASS; a silent/partial CPU fallback is a FAIL)"
    - "The web-search-ON OWUI golden + the villa-websafe golden are re-frozen with the on-hardware-confirmed values (final freeze)"
  artifacts:
    - path: ".planning/phases/31-grounded-fetch-embed-grounding/31-UAT.md"
      provides: "the on-hardware UAT evidence record (commands, outputs, PASS/FAIL per SC)"
      contains: "GROUND-01"
  key_links:
    - from: "internal/orchestrate/websafe.go websafeImage"
      to: "the dev-box-resolved distroless RepoDigest"
      via: "the RESOLVE_ON_HARDWARE placeholder is replaced with the real @sha256 before the golden freezes"
      pattern: "distroless/static-debian12@sha256:"
    - from: "internal/orchestrate/openwebui.go retrieval-fix key"
      to: "the OWUI config.py at the pinned digest"
      via: "ENABLE_RETRIEVAL_UNSCOPED_COLLECTIONS confirmed present + grounding before the golden freezes"
      pattern: "ENABLE_RETRIEVAL_UNSCOPED_COLLECTIONS"
---

<objective>
Prove on the live gfx1151 dev box the four claims Phase 31 cannot verify off-hardware, then
perform the FINAL golden freezes that Plan 03 deliberately deferred. This plan is the
blocking human-verify gate for the whole phase — its outcome decides whether GROUND-01/02
land as designed or require a CONTEXT escalation.

The load-bearing risk is RESEARCH Assumption A1 / Pitfall 1 (HIGH): BYPASS=False only grounds
if the v0.9.6 retrieval-fix lever (`ENABLE_RETRIEVAL_UNSCOPED_COLLECTIONS=True`) exists and
works at the pinned OWUI digest. This plan CONFIRMS that before freezing the golden, and
carries an explicit escalation path if it does not (NOT a silent fall-back to BYPASS=True).

Purpose: never false-green a grounding/isolation/residency claim; freeze the byte-frozen
contracts only against on-hardware-confirmed reality.
Output: a 31-UAT.md evidence record, the resolved distroless digest, the confirmed retrieval
key, and the final re-frozen goldens (or a PHASE ESCALATION note if A1 fails).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-grounded-fetch-embed-grounding/31-CONTEXT.md
@.planning/phases/31-grounded-fetch-embed-grounding/31-RESEARCH.md
@.planning/phases/31-grounded-fetch-embed-grounding/31-03-SUMMARY.md
</context>

## Artifacts this plan produces

| Symbol / Artifact | File | Role |
|-------------------|------|------|
| resolved distroless `@sha256` digest | `internal/orchestrate/websafe.go` | the real pinned base image (placeholder replaced) |
| confirmed retrieval-fix key | `internal/orchestrate/openwebui.go` | the on-hardware-validated `ENABLE_RETRIEVAL_UNSCOPED_COLLECTIONS` (or its confirmed equivalent) |
| re-frozen goldens | `internal/orchestrate/testdata/villa-websafe.container.golden`, `villa-openwebui.container.websearch.golden` | final freeze post-confirmation |
| `make build` CGO note / static-binary confirmation | `Makefile` (if a flag change is needed) | ensure the bind-mounted binary is statically linked |
| `31-UAT.md` | `.planning/phases/31-…/31-UAT.md` | the evidence record |

<tasks>

<task type="auto">
  <name>Task 1: Static-binary + distroless digest resolution (Pitfall 5) — confirm CGO-free, pin the base RepoDigest</name>
  <files>internal/orchestrate/websafe.go, Makefile</files>
  <read_first>
    - 31-RESEARCH.md §"Base Image for villa-websafe" + "Gotcha (static-binary + minimal base)" + Pitfall 5 (a dynamically-linked binary crashes on distroless; confirm `file ./villa` → "statically linked"; the websafe-serve path never calls detect, so CGO_ENABLED=0 is safe — ghw degrades to typed-Unknown).
    - 31-RESEARCH.md §"Standard Stack" Installation block (the `podman pull gcr.io/distroless/static-debian12:nonroot` + `podman image inspect --format '{{index .RepoDigests 0}}'` procedure — same as searxng/qdrant).
    - The Makefile build vs build-static targets (the default `make build` is currently dynamically linked; `make build-static` uses CGO_ENABLED=0) — the bind-mounted binary MUST be the static one.
    - internal/orchestrate/websafe.go (Plan 03): the `websafeImage` const with the `RESOLVE_ON_HARDWARE` placeholder to replace.
  </read_first>
  <action>
    On the gfx1151 dev box: (1) Build the static binary (`make build-static` or `CGO_ENABLED=0 go build -o ./villa ./cmd/villa`) and run `file ./villa` — confirm it reports "statically linked". If the default `make build` install path is what the bind-mount uses, decide and document whether the install/opt-in path must build CGO-free (the websafe-serve subcommand never calls detect, so CGO_ENABLED=0 is safe); adjust the Makefile/install note so the bind-mounted binary is guaranteed static. If `ghw` forces cgo and a static build is impossible, fall back to an alpine/glibc base image (RESEARCH Pitfall 5 fallback) and document the deviation. (2) Resolve the distroless base RepoDigest: `podman pull gcr.io/distroless/static-debian12:nonroot` then `podman image inspect gcr.io/distroless/static-debian12:nonroot --format '{{index .RepoDigests 0}}'`. (3) Replace the `RESOLVE_ON_HARDWARE` placeholder in `internal/orchestrate/websafe.go` `websafeImage` with the resolved `gcr.io/distroless/static-debian12@sha256:…` (keeping it behind the orchestrate seam). Record the `file ./villa` output and the resolved digest in 31-UAT.md.
  </action>
  <verify>
    <automated>go build ./... && grep -q 'distroless/static-debian12@sha256:' internal/orchestrate/websafe.go && ! grep -q 'RESOLVE_ON_HARDWARE' internal/orchestrate/websafe.go && echo OK</automated>
  </verify>
  <done>`file ./villa` confirms a statically-linked binary (or the alpine fallback is documented); the distroless RepoDigest is resolved on-hardware and pins websafeImage (placeholder gone); the module builds.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: On-hardware UAT — grounded cited answer (A1 confirm), ephemeral isolation, offload-assert, static-binary exec</name>
  <files>.planning/phases/31-grounded-fetch-embed-grounding/31-UAT.md</files>
  <action>BLOCKING HUMAN CHECKPOINT — no autonomous code change. The operator runs the on-hardware UAT steps in &lt;how-to-verify&gt; on the gfx1151 dev box and records every command + output (PASS/FAIL per SC1-SC5) into 31-UAT.md. This gate confirms the A1 retrieval-fix unknown and the four claims that cannot be proven off-hardware BEFORE Task 3 freezes any golden. See &lt;what-built&gt; and &lt;how-to-verify&gt; below for the exact procedure and the escalation trigger.</action>
  <what-built>
    The full villa-websafe fetch path: an SSRF-guarded loader (Plan 01) wired as OWUI's
    WEB_LOADER_ENGINE=external fetch path with BYPASS_WEB_SEARCH_EMBEDDING_AND_RETRIEVAL=False
    + the retrieval-fix key + the 0600 bearer (Plan 03), reserving a web-search ctx budget
    before the chat fit (Plan 02), running in a digest-pinned distroless container bind-mounting
    the static villa binary (Task 1). This checkpoint proves the four on-hardware claims and
    confirms the A1 retrieval-fix unknown BEFORE the goldens are frozen.
  </what-built>
  <how-to-verify>
    On the gfx1151 dev box (record every command + output in 31-UAT.md):

    1. RETRIEVAL-KEY EXISTENCE (A1, the blocking unknown). Confirm the lever exists at the
       pinned OWUI digest:
         podman exec villa-openwebui sh -c 'grep -rn "ENABLE_RETRIEVAL_UNSCOPED_COLLECTIONS\|bypass_embedding_and_retrieval" /app/backend/open_webui/config.py'
       (distroless OWUI is a python image with a shell; if not, inspect via the running config).
       Expected: the key (or its confirmed equivalent) is present at this digest.
       → If ABSENT/renamed: STOP. Do NOT freeze. Escalate per Task 3's fallback (this is a
         CONTEXT change, never a silent BYPASS=True revert).

    2. DEPLOY: enable web search + memory in config, `make build-static`, regenerate units,
       `systemctl --user daemon-reload`, restart villa-openwebui + start villa-websafe.
       Confirm the container execs the static binary:
         podman logs villa-websafe   (no "no such file or directory" / glibc error — Pitfall 5)

    3. GROUND-01 (grounded, cited answer). In OWUI (http://127.0.0.1:3000) toggle Web Search
       ON and ask a current-events/research question whose answer is NOT in base knowledge.
       Expected: the answer is grounded in fetched pages WITH inline citations to LIVE URLs
       (OWUI top-level `sources`). Capture the answer + the citations.

    4. GROUND-02 (ephemeral isolation). List Qdrant collections before/after the query:
         podman exec villa-qdrant sh -c 'curl -s http://127.0.0.1:6333/collections'  (or via villa.network)
       Expected: a web-search-<hash> (ephemeral) collection DISTINCT from the durable
       open-webui_* memory/doc-KB collections. Run a SECOND query and confirm the web-search
       collection is clean-replaced (not accumulating cross-query content).

    5. GROUND-03 (offload-assert under search load). With the search-on query running, confirm
       the chat model stays GPU-resident (the residency proof / `villa status` offload signal
       PASSES — a silent or partial CPU fallback is a FAIL, never a false-green).

    6. SSRF spot-check (GUARD-05, on-hardware sanity): confirm a planted internal-host URL is
       refused by villa-websafe (it never appears in citations); the unit tests are the
       authority, this is a live sanity pass.

    Resume only after recording PASS/FAIL for each of SC1-SC5 in 31-UAT.md.
  </how-to-verify>
  <resume-signal>Type "approved" with the PASS/FAIL per criterion, or describe the failure (esp. if the A1 retrieval key is absent — that triggers the escalation path).</resume-signal>
  <verify><human-check>31-UAT.md records PASS/FAIL for SC1-SC5 with captured evidence (grounded answer + citations, Qdrant collection listing, residency result, file/digest, retrieval-key confirmation).</human-check></verify>
  <done>The operator has run all on-hardware UAT steps and recorded per-SC PASS/FAIL evidence in 31-UAT.md; the A1 retrieval-key existence is confirmed (or the failure is documented and escalation is triggered before any golden freeze).</done>
</task>

<task type="auto">
  <name>Task 3: Final golden re-freeze (post-confirmation) + 31-UAT.md evidence record — OR escalation if A1 failed</name>
  <files>internal/orchestrate/openwebui.go, internal/orchestrate/testdata, .planning/phases/31-grounded-fetch-embed-grounding/31-UAT.md</files>
  <read_first>
    - 31-RESEARCH.md §"Open Questions" Q1 (the escalation recommendation: if no env lever works, escalate — do NOT silently keep BYPASS=True, which defeats GROUND-02) and Assumption A1's mitigation.
    - 31-PATTERNS.md §"Planner escalation flags" (Pitfall 1: if ENABLE_RETRIEVAL_UNSCOPED_COLLECTIONS is absent/renamed, keeping BYPASS=True DESCOPES GROUND-02 — a CONTEXT change to escalate, not decide).
    - The Plan-03 deferred-golden note (the web-search-ON OWUI golden + villa-websafe golden are frozen HERE, after confirmation).
    - 31-RESEARCH.md §"Validation Architecture" → "What genuinely needs on-hardware UAT" (the four items this plan proves).
  </read_first>
  <action>
    IF the checkpoint PASSED (A1 confirmed, all SC PASS): (1) If the on-hardware confirmation
    showed the exact retrieval-fix key differs from `ENABLE_RETRIEVAL_UNSCOPED_COLLECTIONS`,
    correct it in `internal/orchestrate/openwebui.go` to the confirmed key (single source). (2)
    Re-freeze the deferred goldens INTENTIONALLY against the confirmed values:
    `go test ./internal/orchestrate/ -run 'TestRenderOpenWebUI|TestRenderWebsafe' -update` —
    this is the FINAL freeze (the villa-websafe golden now carries the resolved digest; the
    web-search-ON OWUI golden carries BYPASS=False + the confirmed retrieval key + the loader
    keys + the EnvironmentFile). Remove any Plan-03 `t.Skip` on the search-ON OWUI golden. (3)
    Write 31-UAT.md as the evidence record: per-SC PASS/FAIL, the captured grounded answer +
    citations, the Qdrant collection listing (isolation proof), the residency/offload result,
    the `file ./villa` + resolved digest, and the retrieval-key confirmation. (4) Run `make check`
    — full suite green with the final goldens.

    IF the checkpoint FAILED on A1 (retrieval key absent/renamed and no lever grounds): do NOT
    freeze any golden and do NOT revert to BYPASS=True silently. Write 31-UAT.md documenting the
    failure and return a `## PHASE ESCALATION` note to the orchestrator proposing the CONTEXT
    change: either (a) descope GROUND-02 to "ephemeral by construction (never persisted)" with
    BYPASS=True direct-inject retained (the Phase-30 path), or (b) hold Phase 31 for an OWUI
    digest bump that restores retrieval. This is a user/CONTEXT decision, not a silent planner
    call (RESEARCH Open Q1).
  </action>
  <verify>
    <automated>make check</automated>
  </verify>
  <done>On PASS: the confirmed retrieval key is in source, the villa-websafe + web-search-ON OWUI goldens are re-frozen against on-hardware-confirmed values, 31-UAT.md records all evidence, and `make check` is green. On A1 FAIL: no golden frozen, 31-UAT.md documents the failure, and a PHASE ESCALATION note is returned (never a silent BYPASS=True revert).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pinned OWUI digest behavior → frozen golden | freezing a golden against an unconfirmed retrieval lever would bake in a silent grounding failure |
| live fetched page → embedded/cited content | the on-hardware path is the first time real untrusted pages reach the embed/cite pipeline |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-17 | Repudiation / false-green | grounding claim (GROUND-01) | mitigate | the golden is frozen ONLY after a real cited-answer UAT confirms BYPASS=False + the retrieval key grounds; an unconfirmed key triggers escalation, never a frozen false-green |
| T-31-18 | Information Disclosure | ephemeral vs durable collection (GROUND-02) | mitigate | on-hardware Qdrant collection listing proves the web-search collection is distinct from durable open-webui_* and clean-replaced per query |
| T-31-19 | Denial of Service | residency under search load (GROUND-03) | mitigate | offload-assert under real search load; a silent/partial CPU fallback is recorded as a FAIL, never a PASS |
| T-31-20 | Tampering | static-binary trust on distroless (Pitfall 5) | mitigate | `file ./villa` confirms static linking before relying on distroless; alpine fallback documented if cgo is forced |
</threat_model>

<verification>
- `make check` — full suite green with the FINAL re-frozen goldens (on PASS path).
- 31-UAT.md exists and records per-SC PASS/FAIL with captured evidence (answer+citations, Qdrant listing, residency result, file/digest, retrieval-key confirmation).
- `internal/orchestrate/websafe.go` carries a real `@sha256` distroless digest (no placeholder).
- `internal/orchestrate/openwebui.go` carries the on-hardware-confirmed retrieval-fix key.
</verification>

<success_criteria>
- GROUND-01: a real research question returns a grounded answer with inline citations to live URLs (BYPASS=False + retrieval-fix confirmed).
- GROUND-02: the web-search ephemeral collection is proven distinct from durable collections and clean-replaced per query.
- GROUND-03: residency is offload-asserted under search load (no silent CPU fallback).
- GUARD-01 closure: the static binary execs on distroless and villa-websafe is the live sole producer of page_content; the goldens are frozen against confirmed reality (or a CONTEXT escalation is raised, never a silent descope).
</success_criteria>

<output>
Create `.planning/phases/31-grounded-fetch-embed-grounding/31-04-SUMMARY.md` when done.
Also create `.planning/phases/31-grounded-fetch-embed-grounding/31-UAT.md` (the evidence record).
</output>
