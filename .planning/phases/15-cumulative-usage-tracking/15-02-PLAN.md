---
phase: 15-cumulative-usage-tracking
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/metrics/llamacpp.go
  - internal/metrics/llamacpp_test.go
  - internal/metrics/testdata/metrics.txt
autonomous: true
requirements: [USAGE-01]
must_haves:
  truths:
    - "A scrape surfaces llamacpp:prompt_tokens_total and llamacpp:tokens_predicted_total as a typed counter reading"
    - "An absent/unparseable _total counter yields Known=false (typed-Unknown), never a fabricated 0"
    - "The two new counter NAME literals live ONLY in internal/metrics"
    - "The counter read reuses the existing bounded request (2s timeout, 64 KiB LimitReader) — no new HTTP request"
  artifacts:
    - path: "internal/metrics/llamacpp.go"
      provides: "CounterSample struct + a sibling scrape accessor surfacing the two _total counters from the existing bounded scrape, typed-Unknown via the parsePromText presence signal"
      contains: "llamacpp:prompt_tokens_total"
    - path: "internal/metrics/testdata/metrics.txt"
      provides: "fixture lines for the two token _total counters (counter type), beside the existing n_decode_total"
      contains: "llamacpp:tokens_predicted_total"
    - path: "internal/metrics/llamacpp_test.go"
      provides: "TestScrapeCountersTotal asserting both counters read from the fixture + a typed-Unknown (absent) case"
      contains: "func TestScrapeCountersTotal"
  key_links:
    - from: "internal/metrics CounterSample read"
      to: "parsePromText map presence"
      via: "ok=false ⇒ Known=false (no fabricated 0)"
      pattern: "Known"
    - from: "counter read"
      to: "existing bounded scrape"
      via: "reuse maxScrapeBody + scrapeTimeout (no second request)"
      pattern: "LimitReader|scrapeTimeout"
---

<objective>
Extend the existing bounded `internal/metrics` `/metrics` scrape to surface the two monotonic
cumulative counters `llamacpp:prompt_tokens_total` and `llamacpp:tokens_predicted_total` as a
typed-Unknown reading (a sibling `CounterSample` struct), reusing the SAME bounded request and
the SAME `parsePromText` presence discipline as the existing gauges.

Purpose: This is the counter feed for USAGE-01 (D-06). The fold (Plan 01) accumulates from
these `_total` counters, NOT the rate gauges. Per D-06 the two counter NAME literals are
confined to `internal/metrics` (same single-home discipline as the existing gauge names).
Per D-05 an absent counter degrades to `Known=false` — never a fabricated 0.

Output: extended `internal/metrics/llamacpp.go`, `llamacpp_test.go`, and `testdata/metrics.txt`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-cumulative-usage-tracking/15-CONTEXT.md
@.planning/phases/15-cumulative-usage-tracking/15-RESEARCH.md
@.planning/phases/15-cumulative-usage-tracking/15-PATTERNS.md
@./CLAUDE.md
@internal/metrics/llamacpp.go
</context>

<artifacts_this_phase_produces>
This plan creates (treat as newly-created downstream):
- Type: `metrics.CounterSample` (`{PromptTokensTotal uint64; PromptTokensKnown bool;
  PredictedTokensTotal uint64; PredictedTokensKnown bool}` — exact shape Claude's discretion
  per RESEARCH Open Question 2).
- Func: a sibling accessor surfacing the two `_total` counters from the bounded scrape — either
  a new `ScrapeCounters(endpoint string) (CounterSample, bool)` OR an additive return on the
  existing scrape path (Claude's discretion; RESEARCH recommends a sibling that reuses the SAME
  bounded request). Plus an internal counter-extraction helper over the `parsePromText` map.
- Consts: `mPromptTokensTotal = "llamacpp:prompt_tokens_total"`, `mPredictedTokensTotal =
  "llamacpp:tokens_predicted_total"` (the ONLY new literals; confined to this package per D-06).
- Test: `TestScrapeCountersTotal`; extension of the existing gauge-parse test/fixture.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the two _total counters to the fixture + a typed CounterSample read</name>
  <files>internal/metrics/llamacpp.go, internal/metrics/testdata/metrics.txt, internal/metrics/llamacpp_test.go</files>
  <read_first>
    - .planning/phases/15-cumulative-usage-tracking/15-CONTEXT.md (D-05 typed-Unknown, D-06 counter source + single-home literals + reuse the bounded scrape)
    - .planning/phases/15-cumulative-usage-tracking/15-RESEARCH.md (Pattern 2 typed-Unknown read lines 227-245; confirmed counter names lines 370-378; Pitfall 3 float64→uint64 lines 344-354)
    - .planning/phases/15-cumulative-usage-tracking/15-PATTERNS.md (§"internal/metrics/llamacpp.go" lines 133-171; §fixture analog lines 174-187)
    - internal/metrics/llamacpp.go:34-49 (PerfSnapshot doc — counters are a DIFFERENT category, sibling struct preferred), :66-99 (parsePromText label-strip + presence-as-signal), :101-124 (ScrapeMetrics bounded request to reuse), :26-32 (scrapeTimeout + maxScrapeBody bounds)
    - internal/metrics/testdata/metrics.txt:13-15 (existing n_decode_total counter block — analog for the two new blocks)
    - internal/metrics/llamacpp_test.go:15-40 (TestParsePromTextExtractsGauges — add the two keys to want), :74-107 (TestScrapeMetricsFromServer httptest pattern to mirror for TestScrapeCountersTotal)
  </read_first>
  <behavior>
    - Fixture parse: parsePromText over the extended metrics.txt yields entries for both
      "llamacpp:prompt_tokens_total" and "llamacpp:tokens_predicted_total".
    - TestScrapeCountersTotal (present): scraping an httptest server serving the fixture returns a
      CounterSample with PromptTokensKnown=true + the fixture value as uint64, and
      PredictedTokensKnown=true + its fixture value.
    - TestScrapeCountersTotal (absent/typed-Unknown): a body WITHOUT the two _total lines yields
      PromptTokensKnown=false and PredictedTokensKnown=false (no fabricated 0), exactly mirroring
      the existing gauge ok=false discipline; a 404/transport error yields the availability bool
      false (whole-scrape unavailable).
  </behavior>
  <action>
    Append two counter blocks to internal/metrics/testdata/metrics.txt modeled on the existing
    n_decode_total block (metrics.txt:13-15): a "# HELP"/"# TYPE … counter"/value triple for
    llamacpp:prompt_tokens_total and one for llamacpp:tokens_predicted_total, with distinct
    non-zero integer values. In internal/metrics/llamacpp.go declare the two name consts
    (mPromptTokensTotal / mPredictedTokensTotal — RESEARCH lines 370-378; the ONLY new literals,
    confined here per D-06). Add a sibling `CounterSample` struct (per RESEARCH Open Question 2 +
    PATTERNS lines 161-166) and a sibling accessor surfacing both counters from the bounded scrape:
    reuse the SAME http.Client{Timeout: scrapeTimeout} + io.LimitReader(resp.Body, maxScrapeBody)
    + parsePromText pattern as ScrapeMetrics (llamacpp.go:106-124) — do NOT add a second HTTP
    request; either fold the counter read into the existing ScrapeMetrics return or expose a
    sibling that mirrors its bounded shape (discretion). Read each counter via the map presence
    signal: `v, ok := m["llamacpp:prompt_tokens_total"]` ⇒ Known=ok; convert the float64 to uint64
    (safe < 2^53, Pitfall 3). A non-200/transport error returns the availability bool false.
    Extend TestParsePromTextExtractsGauges' want map with the two keys, and add
    TestScrapeCountersTotal (present + absent cases) mirroring TestScrapeMetricsFromServer's
    httptest harness.
  </action>
  <verify>
    <automated>go test ./internal/metrics -run 'TestParsePromText|TestScrapeCounters|TestScrapeMetrics' -x</automated>
  </verify>
  <acceptance_criteria>
    - `internal/metrics/llamacpp.go` contains `llamacpp:prompt_tokens_total` and `llamacpp:tokens_predicted_total`
    - `internal/metrics/llamacpp.go` contains `CounterSample`
    - `internal/metrics/testdata/metrics.txt` contains both `llamacpp:prompt_tokens_total` and `llamacpp:tokens_predicted_total` (counter type)
    - `internal/metrics/llamacpp_test.go` contains `func TestScrapeCountersTotal`
    - `go test ./internal/metrics` exits 0
    - `grep -rn 'llamacpp:prompt_tokens_total\|llamacpp:tokens_predicted_total' internal/usage internal/dashboard internal/status cmd/villa` returns nothing (the two counter literals stay confined to internal/metrics per D-06)
  </acceptance_criteria>
  <done>Both _total counters are read as a typed-Unknown CounterSample from the existing bounded scrape; the counter literals stay confined to internal/metrics; the full metrics suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| llama-server /metrics → process | an untrusted loopback HTTP body is parsed for counter values |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-06 | DoS | /metrics body size | mitigate | reuse the existing io.LimitReader(64 KiB) + 2s timeout (llamacpp.go:28,32,116) — NO new request, no new bound |
| T-15-07 | Spoofing/Tampering | absent/garbled counter line | mitigate | typed-Unknown via parsePromText presence (ok=false ⇒ Known=false), never a fabricated 0 (D-05) |
| T-15-08 | Information Disclosure | new outbound channel | accept | no new network call introduced — same loopback endpoint already scraped for live tok/s (D-12); asserted by the no-new-host UAT in 15-VALIDATION.md |
</threat_model>

<verification>
- `go test ./internal/metrics` green.
- The two counter literals appear ONLY under internal/metrics (grep gate above).
- No second HTTP request added (the counter read shares ScrapeMetrics' bounded request shape).
</verification>

<success_criteria>
- Both `_total` counters surfaced as a typed CounterSample from the bounded scrape (USAGE-01, D-06).
- Absent counter ⇒ Known=false, never a fabricated 0 (D-05).
- Counter NAME literals confined to internal/metrics (D-06 single-home discipline).
</success_criteria>

<output>
Create `.planning/phases/15-cumulative-usage-tracking/15-02-SUMMARY.md` when done
</output>
