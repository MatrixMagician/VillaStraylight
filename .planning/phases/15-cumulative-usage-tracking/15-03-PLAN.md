---
phase: 15-cumulative-usage-tracking
plan: 03
type: execute
wave: 2
depends_on: ["15-01"]
files_modified:
  - internal/status/status.go
  - internal/status/status_test.go
  - cmd/villa/status.go
  - cmd/villa/testdata/status.json.golden
autonomous: true
requirements: [USAGE-02]
must_haves:
  truths:
    - "status.Report carries exactly ONE new append-only usage field, inserted immediately above SchemaVersion"
    - "An absent/empty usage store makes the field omitted (omitempty) — typed-Unknown, never a fabricated 0"
    - "reportSchemaVersion is 2 (was 1)"
    - "The frozen --json golden changes ONLY by the new usage key + schema_version 1→2"
    - "villa status reads usage.json read-only via the usage store seam and NEVER writes it"
  artifacts:
    - path: "internal/status/status.go"
      provides: "ONE *usage.UsageTotals omitempty Report field above SchemaVersion + reportSchemaVersion=2 + a ReadUsage Deps seam + nil-guarded Run population"
      contains: "reportSchemaVersion = 2"
    - path: "cmd/villa/status.go"
      provides: "liveStatusDeps wires the read-only ReadUsage seam (usage.Load over UsagePath); human-table render of cumulative totals"
      contains: "ReadUsage"
    - path: "cmd/villa/testdata/status.json.golden"
      provides: "re-frozen golden with the usage key + schema_version 2"
      contains: "\"schema_version\": 2"
  key_links:
    - from: "internal/status Report.Usage"
      to: "d.ReadUsage()"
      via: "nil-guarded Run population (nil seam ⇒ omitted)"
      pattern: "d\\.ReadUsage"
    - from: "cmd/villa/status.go liveStatusDeps ReadUsage"
      to: "usage.Load"
      via: "read-only load over usage.UsagePath (NEVER a write)"
      pattern: "usage\\.Load"
---

<objective>
Evolve the byte-frozen `status.Report` contract by EXACTLY ONE append-only field: a
`*usage.UsageTotals` (`omitempty`) inserted immediately above `SchemaVersion`, bump
`reportSchemaVersion` 1→2, wire a READ-ONLY `ReadUsage` Deps seam (`villa status` loads
usage.json and never writes it), and re-freeze the single `status.json.golden` once.

Purpose: This is the `villa status` surface for USAGE-02 (D-09). It mirrors the
`GenTokensPerSec` additive-field + Deps-seam precedent exactly, keeps the one-byte-frozen-
contract-at-a-time discipline (only status.Report evolves this phase), and enforces D-07 — the
CLI is one-shot and read-only; the dashboard (Plan 04) is the sole writer.

Output: status.go (+field/+const/+seam), status_test.go (omitted/surfaced tests),
cmd/villa/status.go (read-only wiring + table render), re-frozen status.json.golden.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-cumulative-usage-tracking/15-CONTEXT.md
@.planning/phases/15-cumulative-usage-tracking/15-RESEARCH.md
@.planning/phases/15-cumulative-usage-tracking/15-PATTERNS.md
@.planning/phases/15-cumulative-usage-tracking/15-01-SUMMARY.md
@./CLAUDE.md
@internal/status/status.go
</context>

<artifacts_this_phase_produces>
This plan creates (treat as newly-created downstream):
- `status.Report` field `Usage *usage.UsageTotals` with json tag `usage,omitempty` (exact name
  Claude's discretion; the json key is what the golden + dashboard read).
- `reportSchemaVersion` bumped 1 → 2.
- `status.Deps.ReadUsage func() *usage.UsageTotals` (read-only seam; nil ⇒ field omitted).
- cmd/villa `liveReadUsage` (or inline closure) reading via `usage.Load` over `usage.UsagePath`.
- Tests: `TestUsageOmittedWhenAbsent`, `TestUsageSurfacedWhenPresent` (or equivalent) in
  internal/status/status_test.go.
- Re-frozen `cmd/villa/testdata/status.json.golden` (usage key + schema_version 2).
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Append the ONE usage field to status.Report, bump schema, wire the read-only seam</name>
  <files>internal/status/status.go, internal/status/status_test.go, cmd/villa/status.go</files>
  <read_first>
    - .planning/phases/15-cumulative-usage-tracking/15-CONTEXT.md (D-07 CLI read-only / dashboard sole writer; D-09 ONE append-only field above SchemaVersion + omitempty + bump 1→2; D-10 dashboard reads the SAME field, no new endpoint)
    - .planning/phases/15-cumulative-usage-tracking/15-PATTERNS.md (§"internal/status/status.go" lines 190-224 — exact GenTokensPerSec field/seam/population precedent)
    - .planning/phases/15-cumulative-usage-tracking/15-RESEARCH.md (Pattern 3 lines 247-265; Pitfall 4 surgical re-freeze lines 356-366)
    - .planning/phases/15-cumulative-usage-tracking/15-01-SUMMARY.md (usage.UsageTotals shape, usage.Load/UsagePath signatures)
    - internal/status/status.go:108-135 (GenTokensPerSec *float64 omitempty field placement + SchemaVersion MUST stay last tagged + reportSchemaVersion const), :184-211 (Deps seam block incl. GenTokensPerSec seam doc), :281-305 (Run population: report.SchemaVersion + nil-guarded GenTokensPerSec population)
    - cmd/villa/status.go:157-202 (liveStatusDeps wiring — add ReadUsage alongside GenTokensPerSec at :197), :204-223 (liveGenTokensPerSec nil-on-failure discipline to mirror), :110-116 (human-table render block where the cumulative rows print)
  </read_first>
  <action>
    In internal/status/status.go add ONE field to Report immediately ABOVE `SchemaVersion`
    (status.go:120 — mirror the GenTokensPerSec/ROCmReadiness tail-append at :108-118):
    `Usage *usage.UsageTotals` with json tag `usage,omitempty` and a doc comment citing D-09
    (pointer+omitempty so an absent/empty store is OMITTED = typed-Unknown, never a fabricated 0).
    Import internal/usage. Change `const reportSchemaVersion = 1` (status.go:135) to `= 2` and
    update its doc comment to note the Phase-15 usage field. Add a `ReadUsage func() *usage.UsageTotals`
    seam to Deps (status.go:184-211, beside GenTokensPerSec) with a doc comment: read-only, nil ⇒
    omitted; the dashboard reads the SAME field via handleStatus, no new endpoint (D-10). In Run
    (status.go:294-299), after `report.SchemaVersion = reportSchemaVersion`, add a nil-guarded
    population: `if d.ReadUsage != nil { report.Usage = d.ReadUsage() }`. In cmd/villa/status.go
    liveStatusDeps (status.go:197 area) wire `ReadUsage` to a read-only closure that calls
    usage.Load over a Deps whose ReadAll reads usage.UsagePath() (os.ReadFile; (nil,nil) on
    os.ErrNotExist) and returns a *usage.UsageTotals when non-empty (nil when empty so the field
    omits) — it NEVER writes (D-07). Add a human-table render branch (cmd/villa/status.go:110-116
    style) printing cumulative prompt/generated totals for the configured model when r.Usage is
    non-nil. In internal/status/status_test.go add TestUsageOmittedWhenAbsent (ReadUsage seam nil
    OR returning nil ⇒ Report marshals WITHOUT a "usage" key) and TestUsageSurfacedWhenPresent
    (a stub ReadUsage returning a populated UsageTotals ⇒ the marshaled Report contains the usage
    key + schema_version 2).
  </action>
  <verify>
    <automated>go test ./internal/status -run 'TestUsage' -x && go build ./...</automated>
  </verify>
  <acceptance_criteria>
    - `internal/status/status.go` contains `reportSchemaVersion = 2`
    - `internal/status/status.go` contains a Report field with json tag `usage,omitempty` placed before the `schema_version` field
    - `internal/status/status.go` contains `ReadUsage func() *usage.UsageTotals` and `d.ReadUsage` population in Run
    - `cmd/villa/status.go` contains `usage.Load` and `usage.UsagePath` and NO usage write call (`grep -n 'usage.Save\|WriteFileAtomic\|usage.*Write' cmd/villa/status.go` returns nothing)
    - `internal/status/status_test.go` contains `func TestUsageOmittedWhenAbsent`
    - `go test ./internal/status -run TestUsage` exits 0
    - `go build ./...` exits 0
  </acceptance_criteria>
  <done>status.Report carries exactly one append-only omitempty usage field above SchemaVersion, reportSchemaVersion=2, the CLI reads usage.json read-only via the seam, and the omitted/surfaced unit tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Re-freeze the single status.json golden once and confirm the surgical diff</name>
  <files>cmd/villa/testdata/status.json.golden</files>
  <read_first>
    - .planning/phases/15-cumulative-usage-tracking/15-RESEARCH.md (the single re-freeze command lines 380-385; Pitfall 4 — re-freeze ONLY the one golden, lines 356-366)
    - .planning/phases/15-cumulative-usage-tracking/15-PATTERNS.md (§"cmd/villa/testdata/status.json.golden" lines 227-245 — -update flow + the only two lines that may change)
    - cmd/villa/testdata/status.json.golden (current tail: "rocm_readiness": "unknown" then "schema_version": 1 — the insertion point)
    - cmd/villa/status_test.go (TestStatusJSONGolden + the package-level `var update` `-update` mechanics)
  </read_first>
  <action>
    Run the SINGLE surgical re-freeze (RESEARCH lines 380-385):
    `go test ./cmd/villa -run TestStatusJSONGolden -update`. Then run
    `git diff cmd/villa/testdata/status.json.golden` and CONFIRM the ONLY changes are (a)
    `"schema_version": 1` → `"schema_version": 2` and (b) at most a new `"usage": …` key inserted
    immediately before `"schema_version"` — and that the default golden fixture's status path
    yields an absent store, so per omitempty the `usage` key is OMITTED (i.e. the only diff line
    is schema_version 1→2 unless the golden harness stubs a populated store). Run
    `git status --short cmd/villa/testdata/` and CONFIRM no OTHER golden changed (detect.golden.json,
    doctor.json.golden, bench*.golden, recommend.golden.json MUST be untouched — Pitfall 4). If any
    other golden shows as modified, revert it (`git checkout -- <that golden>`) — it is unrelated
    drift, never re-freeze it here.
  </action>
  <verify>
    <automated>go test ./cmd/villa -run TestStatusJSONGolden && test -z "$(git status --short cmd/villa/testdata/ | grep -v 'status.json.golden')"</automated>
  </verify>
  <acceptance_criteria>
    - `cmd/villa/testdata/status.json.golden` contains `"schema_version": 2`
    - `go test ./cmd/villa -run TestStatusJSONGolden` exits 0 (golden matches re-frozen contract)
    - `git status --short cmd/villa/testdata/` shows ONLY status.json.golden modified (no other golden re-frozen)
    - `git diff cmd/villa/testdata/status.json.golden` shows schema_version 1→2 (and, if the harness stubs a store, only the inserted usage key) — no reordering of existing keys
  </acceptance_criteria>
  <done>The one status.json golden is re-frozen with schema_version 2 (+ usage key iff stubbed), TestStatusJSONGolden is green, and no unrelated golden changed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| usage.json → status read path | the CLI loads an untrusted on-disk store to populate Report |
| Report --json → external consumers | the byte-frozen contract crosses to scripts/dashboard |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-09 | Tampering | CLI status accidentally writing usage.json | mitigate | ReadUsage seam is read-only (usage.Load only); a grep-gate acceptance criterion forbids any write call in cmd/villa/status.go (D-07) |
| T-15-10 | Information Disclosure | a fabricated 0 total implying real usage | mitigate | pointer+omitempty: absent/empty store ⇒ field omitted = typed-Unknown, never a confident 0 (D-09); covered by TestUsageOmittedWhenAbsent |
| T-15-11 | Tampering | unintended --json contract drift via -update | mitigate | surgical single-golden re-freeze + git-status gate confirming ONLY status.json.golden changed (Pitfall 4) |
| T-15-12 | Tampering | corrupt usage.json crashing status | mitigate | usage.Load fails closed to empty (Plan 01) ⇒ field omitted, status never panics |
</threat_model>

<verification>
- `go test ./internal/status` + `go test ./cmd/villa -run TestStatusJSONGolden` green.
- `go build ./...` clean.
- Exactly one golden re-frozen; reportSchemaVersion=2; usage field above SchemaVersion.
- No write path in cmd/villa/status.go (D-07).
</verification>

<success_criteria>
- `villa status` surfaces cumulative totals (read-only) as ONE append-only schema-bumped field (USAGE-02 SC#3, D-09).
- Absent store ⇒ omitted, never a fabricated 0 (D-09).
- The single byte-frozen contract evolves exactly once (D-09, Pitfall 4).
</success_criteria>

<output>
Create `.planning/phases/15-cumulative-usage-tracking/15-03-SUMMARY.md` when done
</output>
