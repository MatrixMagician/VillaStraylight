---
phase: 28-agent-surfacing-contracts
plan: 03
type: execute
wave: 2
depends_on: ["28-02"]
files_modified:
  - internal/status/status.go
  - internal/status/status_test.go
  - cmd/villa/status.go
  - cmd/villa/status_test.go
  - cmd/villa/testdata/status.json.golden
  - cmd/villa/testdata/status-coding.json.golden
  - internal/dashboard/assets/dashboard.html
  - internal/dashboard/assets/dashboard.css
  - internal/dashboard/assets/dashboard.js
autonomous: true
requirements: [SURF-01, USAGE-03, USAGE-04]
must_haves:
  truths:
    - "villa status (human + --json) reports an append-only coding block: enabled, version, pin_match, model, mode, residency"
    - "status.Report schema bumps 3→4 EXACTLY ONCE here, with coding-on AND coding-off golden variants refrozen together"
    - "With the agent disabled, status --json is byte-identical to today EXCEPT schema_version (the coding key is omitted)"
    - "The dashboard renders an Agent panel mirroring the Memory panel, hidden entirely until the coding block exists"
    - "Agent token usage is attributed per-model (the coder model id) via the v1.2 usage core, in status + dashboard"
    - "Cache effectiveness is shown only when cache_n AND prompt_n are Known and prompt_n>0; otherwise a gray Unknown badge — never a fabricated 0%"
  artifacts:
    - path: "internal/status/status.go"
      provides: "Coding *CodingInfo block tail-appended above SchemaVersion; reportSchemaVersion 3→4; codingInfo() populator; AgentEnabled gate"
      contains: "type CodingInfo"
    - path: "cmd/villa/status.go"
      provides: "human-table coding rows + live seam wiring of the coding-block probes (pin/cache)"
      contains: "r.Coding != nil"
    - path: "cmd/villa/testdata/status-coding.json.golden"
      provides: "Coding-on status --json fixture carrying the coding block (mirror status-memory.json.golden)"
    - path: "internal/dashboard/assets/dashboard.js"
      provides: "renderAgent(report) hidden-until-data panel fed by the existing /api/status poll"
      contains: "renderAgent"
  key_links:
    - from: "internal/status/status.go"
      to: "config.AgentEnabled"
      via: "gated population: if cfg.AgentEnabled { report.Coding = codingInfo(...) }"
      pattern: "cfg\\.AgentEnabled"
    - from: "internal/dashboard/assets/dashboard.js"
      to: "/api/status"
      via: "renderAgent(report) beside renderMemory(report) in the existing poll .then (no new fetch)"
      pattern: "renderAgent\\(report\\)"
    - from: "internal/dashboard/assets/dashboard.js"
      to: "report.usage.models[report.coding.model]"
      via: "per-model coder usage selection (USAGE-03)"
      pattern: "report\\.coding\\.model"
---

<objective>
The v1.4 finale: surface the coding agent in `villa status` and the dashboard, landing the SINGLE `status.Report` 3→4 contract bump (D-01..D-04) — the ONLY contract change of the entire phase. Add the append-only `Coding *CodingInfo` block (enabled, version, pin_match, model, mode, residency), bump `reportSchemaVersion` 3→4 ONCE, and re-freeze BOTH coding-on/off goldens together — with the agent off the block is omitted entirely (nil pointer + omitempty) so coding-off output is byte-identical to today except schema_version (D-02 hidden-until-data). Add the dashboard Agent panel mirroring the Memory panel (hidden-until-data, fed by the existing /api/status poll — no new endpoint/fetch/probe). Surface per-model coder token usage via the v1.2 usage core (USAGE-03, D-09) and cache effectiveness as the `cache_n/prompt_n` ratio (USAGE-04 surfacing, D-10) — typed-Unknown, never a fabricated 0%.

Purpose: The operator can SEE and MEASURE the agent. This plan is the contract-bearing finale; the schema bump + golden re-freeze happen exactly once, here, after every consuming surface (doctor, backup, metrics) is in place.
Output: `CodingInfo` block + 3→4 bump + `codingInfo()` populator in `internal/status`; human-table rows + live seam wiring in `cmd/villa/status.go`; BOTH status goldens re-frozen; the `renderAgent` dashboard panel (html/css/js) reusing existing classes/helpers.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-agent-surfacing-contracts/28-CONTEXT.md
@.planning/phases/28-agent-surfacing-contracts/28-PATTERNS.md
@.planning/phases/28-agent-surfacing-contracts/28-UI-SPEC.md

# Cores being surfaced (read the cited line ranges only)
@internal/status/status.go
@cmd/villa/status.go
@internal/dashboard/assets/dashboard.js

# Prior plan dependency: the metrics cache-counter primitive
@.planning/phases/28-agent-surfacing-contracts/28-02-SUMMARY.md
</context>

<artifacts_this_phase_produces>
This plan's contribution to the phase artifact ledger (the phase's ONLY byte-frozen contract change):

- `internal/status/status.go`:
  - `type CodingInfo struct` — snake_case + omitempty tails (LOCKED field spellings, matching the MemoryInfo idiom, D-03/Claude's-discretion):
    - `Enabled bool json:"enabled"`
    - `Version string json:"version,omitempty"`
    - `PinMatch string json:"pin_match"` — tri-state string `"match"` / `"mismatch"` / `"unknown"` (NOT a bare bool — typed-Unknown when the policy/binary comparison cannot be made; mirrors ROCmReadinessIndicator + the Memory skew idiom; the UI-SPEC pin badge maps it directly).
    - `Model string json:"model,omitempty"`
    - `Mode string json:"mode,omitempty"`
    - `Residency string json:"residency,omitempty"` — `swap` / `shared`.
    - `CacheEffectivenessPct *float64 json:"cache_effectiveness_pct,omitempty"` — nil-on-Unknown (mirrors GenTokensPerSec); set ONLY when both cache_n/prompt_n Known and prompt_n>0.
    - `CacheN uint64 json:"cache_n,omitempty"` / `PromptN uint64 json:"prompt_n,omitempty"` — the raw ratio inputs (omitted when Unknown).
  - `Report.Coding *CodingInfo json:"coding,omitempty"` — tail-appended BETWEEN Memory and SchemaVersion (D-01; nothing above moves).
  - `reportSchemaVersion` 3→4 (the SINGLE bump; version-history comment appended with a v4 line).
  - `codingInfo(cfg, pinMatch, cacheSample) *CodingInfo` populator — typed-Unknown discipline (pin unevaluable → `"unknown"`; cache absent → nil pct + omitted counts).
  - `Deps` new nil-safe seams: `AgentPinMatch func() string` (tri-state pin compare) + `AgentCache func() (cacheN uint64, promptN uint64, ok bool)` (reuse the Plan-02 metrics primitive). Nil → typed-Unknown (no fabricated value).
  - Gated population in `Run`: `if cfg.AgentEnabled { report.Coding = codingInfo(...) }`.
- `cmd/villa/status.go`:
  - `renderStatusTable` `if r.Coding != nil { ... }` block: version / pin-match / model / mode / residency rows + per-model coder usage rows (selected from r.Usage.Models[r.Coding.Model]) + a cache-effectiveness row (pct + raw, or "unavailable").
  - `liveStatusDeps` wiring of `AgentPinMatch` (via agent.DetectDrift / hashFileSHA256 vs policy pin → tri-state) + `AgentCache` (via the Plan-02 metrics ScrapeCacheCounters), gated on persisted agent_enabled. The dashboard reuses `*liveStatusDeps()` verbatim — the panel is fed for free.
- `cmd/villa/testdata/status.json.golden` (coding-off; byte-identical EXCEPT `"schema_version": 4`) + `cmd/villa/testdata/status-coding.json.golden` (NEW coding-on fixture) — both re-frozen together with `-update`.
- `internal/dashboard/assets/dashboard.html`: `#agent-panel` shell (hidden), mirroring `#memory-panel`.
- `internal/dashboard/assets/dashboard.css`: NO new tokens — reuse `.panel`/`.panel-heading`/`.metric-row`/`.badge`/`.muted` verbatim.
- `internal/dashboard/assets/dashboard.js`: `renderAgent(report)` (hidden-until-data; version/pin/model/mode/residency rows via metricRow/memoryBadgeRow; per-model coder usage via the renderCumulativeUsage selection logic; cache row pct-or-Unknown per the UI-SPEC percentage rule), wired beside `renderMemory(report)` in the /api/status `.then` (NOT the `.catch`).
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: status.Report Coding block + 3→4 single bump + populator + live seam wiring + golden re-freeze</name>
  <files>internal/status/status.go, internal/status/status_test.go, cmd/villa/status.go, cmd/villa/status_test.go, cmd/villa/testdata/status.json.golden, cmd/villa/testdata/status-coding.json.golden</files>
  <read_first>
    - internal/status/status.go:110-160 (the Memory pointer-block tail-append + SchemaVersion-last discipline; GenTokensPerSec *float64 omitempty precedent at :110-115)
    - internal/status/status.go:162-191 (reportSchemaVersion + version-history comment + MemoryInfo sidecar shape to clone)
    - internal/status/status.go:240-318 (Deps seams + the nil-safe ReadRecallState/ReadUsage precedent)
    - internal/status/status.go:385-411, :526-563 (Run gated population `if cfg.MemoryEnabled` + memoryInfo() typed-Unknown populator to clone)
    - cmd/villa/status.go:84-88 (the --json encoder — surfacing the struct is automatic), :107-169 (renderStatusTable usage-present-only loop at :131-136), :175-234 (liveStatusDeps wiring), :243-265 (liveReadUsage read-only nil-on-absent shape)
    - internal/config/villaconfig.go:114-122 (AgentEnabled gate); cmd/villa/code.go:138-217 + internal/agent/drift.go:80-117 (pin-compare inputs)
    - .planning/phases/28-UI-SPEC.md:154-199 (the binding pin/cache/usage state table + percentage rule)
  </read_first>
  <behavior>
    - CodingInfo serializes with the LOCKED snake_case spellings above; pin_match is tri-state "match"/"mismatch"/"unknown" (never a bare bool; unevaluable → "unknown").
    - codingInfo(): enabled=true always (only built when agent on); version verbatim or omitted; pin_match from the seam (nil seam → "unknown"); model/mode/residency from cfg (coder model id, coding mode, residency); cache pct set ONLY when both Known and prompt_n>0, else nil pct + omitted counts.
    - Run: `report.Coding` populated ONLY when cfg.AgentEnabled (nil otherwise → omitempty omits the key; coding-off output is byte-identical to today EXCEPT schema_version).
    - reportSchemaVersion == 4 (the SINGLE bump).
    - Human table: an `if r.Coding != nil` block renders version/pin/model/mode/residency + the per-model coder usage rows (from r.Usage.Models keyed on r.Coding.Model) + cache-effectiveness (pct + raw, or "unavailable") — never a fabricated value.
    - Goldens: status.json.golden (coding-off) differs from pre-plan ONLY in schema_version; status-coding.json.golden (NEW) carries the full coding block. BOTH refrozen together; nothing above the coding key moved (append-only).
  </behavior>
  <action>Add `type CodingInfo struct` cloning the MemoryInfo sidecar (status.go:183-191) with the LOCKED snake_case field set from the artifact ledger above — pin_match as a tri-state STRING ("match"/"mismatch"/"unknown", the typed-Unknown convention; the UI-SPEC pin badge maps it directly), CacheEffectivenessPct as `*float64` omitempty (nil-on-Unknown, mirroring GenTokensPerSec at status.go:110-115). Add `Coding *CodingInfo json:"coding,omitempty"` BETWEEN the Memory field and SchemaVersion (status.go:149-154 — append-only, nothing above moves). Bump `reportSchemaVersion` 3→4 and append a v4 version-history line (status.go:162-171) — the SINGLE bump of the phase. Clone `memoryInfo()` into `codingInfo(cfg, pinMatch, cacheSample)` honoring the typed-Unknown discipline (nil pin seam → "unknown"; cache absent/Unknown → nil pct + omitted counts; pct computed only when both Known and prompt_n>0). Add nil-safe `AgentPinMatch func() string` + `AgentCache func() (cacheN, promptN uint64, ok bool)` to Deps (mirror ReadUsage/ReadRecallState nil-safe contract). Gate population in Run: `if cfg.AgentEnabled { report.Coding = codingInfo(...) }` (clone the `if cfg.MemoryEnabled` guard at status.go:409). In cmd/villa/status.go add the `if r.Coding != nil` human-table block (version/pin/model/mode/residency + coder per-model usage rows selected from r.Usage.Models[r.Coding.Model] + the cache-effectiveness row, pct+raw or "unavailable") and wire AgentPinMatch (agent.DetectDrift / hashFileSHA256 vs the policy pin → tri-state) + AgentCache (the Plan-02 metrics ScrapeCacheCounters) in liveStatusDeps, gated on persisted agent_enabled (nil seams when off). Refreeze BOTH goldens together with `go test ./cmd/villa/... -run TestStatus -update`, add the coding-on fixture, and CONFIRM the coding-off golden differs from pre-plan ONLY in schema_version (append-only; nothing above the coding key moved). Drive status_test.go: coding-on report carries the block with locked spellings; coding-off report omits the key and bumps only schema_version; pin tri-state truth table; cache pct shown only when both Known + prompt_n>0 (else nil/omitted).</action>
  <verify>
    <automated>go test ./internal/status/... -count=1 && go test ./cmd/villa/... -run 'TestStatus|TestSeamGrepGate' -count=1</automated>
  </verify>
  <done>status.Report has the Coding block; reportSchemaVersion==4 (single bump); coding-off status --json differs from today ONLY in schema_version; the new status-coding.json.golden carries the coding block; pin_match is the tri-state string; cache pct is nil/omitted unless both counts Known and prompt_n>0; TestSeamGrepGate green; both status golden variants refrozen together; tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Dashboard Agent panel (renderAgent, hidden-until-data) mirroring the Memory panel</name>
  <files>internal/dashboard/assets/dashboard.html, internal/dashboard/assets/dashboard.css, internal/dashboard/assets/dashboard.js</files>
  <read_first>
    - .planning/phases/28-UI-SPEC.md (the BINDING contract: shell at :141-147, the field/state table at :154-163, the copywriting contract at :177-199)
    - internal/dashboard/assets/dashboard.js:62 (metricRow), :53 (mutedP), :85 (groupThousands), :274-286 (memoryBadgeRow)
    - internal/dashboard/assets/dashboard.js:288-352 (renderMemory — the hidden-until-data gate + honesty badge mapping to clone)
    - internal/dashboard/assets/dashboard.js:419-462 (renderCumulativeUsage — the per-model usage selection logic to reuse for the coder)
    - internal/dashboard/assets/dashboard.js (the /api/status poll `.then` where renderMemory(report) is called — wire renderAgent beside it; do NOT call it on `.catch`)
    - internal/dashboard/assets/dashboard.html:64-66 (the #memory-panel shell to mirror)
  </read_first>
  <action>Add the `#agent-panel` static shell to dashboard.html AFTER the Memory panel, shipping `hidden`, exactly per the UI-SPEC shell (28-UI-SPEC.md:143-146): `id="agent-panel"` / `agent-heading` "Agent" / `agent-body`. Introduce NO new CSS — reuse `.panel`/`.panel-heading`/`.metric-row`/`.badge`(+`-ready`/`-warn`/`-unknown`)/`.muted` verbatim (touch dashboard.css only if a class genuinely needs no change — prefer leaving it untouched). Add `renderAgent(report)` to dashboard.js cloning renderMemory's hidden-until-data gate (`var ag = report && report.coding; if (!ag) { agentPanel.hidden = true; return; }`), then populate via the existing helpers (all values via textContent — XSS-safe): version (metricRow, OMIT if absent), policy pin (memoryBadgeRow: "match"/"ready", "mismatch"/"warn", "unknown"→"unavailable"/"unknown" + the pin captions from the UI-SPEC copywriting contract), model/mode/residency (metricRow, OMIT if absent), per-model coder usage (reuse the renderCumulativeUsage selection logic keyed on report.coding.model → prompt/generated total rows or `mutedP("No usage recorded yet")` — NOT a fabricated 0), and cache effectiveness (pct + " (" + cache_n + "/" + prompt_n + ")" ONLY when present; else `memoryBadgeRow("cache effectiveness","unavailable","unknown")` + the muted caption — NEVER a fabricated 0%, honoring the percentage rule at 28-UI-SPEC.md:196-199). Wire `renderAgent(report)` beside `renderMemory(report)` in the existing /api/status poll `.then` (NO new fetch/endpoint/probe — the SAME poll feeds it, D-05); do NOT call it on the `.catch` path (last-good content under stale dimming). Verify the embedded assets compile into the binary (go:embed) and the build is green; per the project GOTCHA, note in the SUMMARY that a live box needs `systemctl --user restart villa-dashboard.service` for the dashboard change to take effect.</action>
  <verify>
    <automated>go build ./... && go test ./internal/dashboard/... -count=1 && make check</automated>
  </verify>
  <done>The #agent-panel shell ships hidden; renderAgent unhides only when report.coding is present and re-hides when absent (coding-off dashboard pixel-identical to v1.3); pin badge maps match/mismatch/unknown per the UI-SPEC; coder usage reuses the per-model selection (honest empty state, no fabricated 0); cache row shows pct+raw or the gray Unknown badge (never 0%); renderAgent is wired in the /api/status `.then` only; no new CSS token introduced; `go build ./...`, the dashboard tests, and `make check` pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| /api/status JSON → dashboard renderAgent | server values rendered into the DOM (must be textContent, never HTML interpolation — XSS) |
| agent binary/policy pin → pin_match seam | the pin comparison reads untrusted on-disk binary bytes |
| llama-server /metrics → cache ratio | scrape counts are untrusted (typed-Unknown; ratio only when proven) |
| dashboard bind | loopback-only + same-origin /api guard must stay intact (no new endpoint/fetch added) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-03-01 | Tampering/XSS | renderAgent rendering server values | mitigate | every value set via createElement + textContent (XSS-safe idiom, reused from renderMemory) — never innerHTML/HTML interpolation of server strings |
| T-28-03-02 | Elevation/exposure | new dashboard data path widening the attack surface | mitigate | NO new endpoint/fetch/probe — renderAgent rides the existing /api/status poll behind the loopback bind + same-origin /api guard (unchanged); no new outbound |
| T-28-03-03 | Information disclosure | secrets/content in the coding block or panel | mitigate | counts/identity-only: version/model/mode/residency + token counts + cache ratio ONLY; no prompt/response/secret text; pin_match is a tri-state label, not the hash |
| T-28-03-04 | Repudiation/false-data | fabricated cache 0% / fabricated usage 0 | mitigate | pct shown only when both counts Known and prompt_n>0 (else gray Unknown badge); per-model usage shows the honest empty state, never a fabricated 0 (D-10/USAGE-03) |
| T-28-03-05 | Tampering | append-only contract breach (a field reordered above schema_version) | mitigate | Coding is tail-appended BETWEEN Memory and SchemaVersion; coding-off golden differs ONLY in schema_version (append-only assertion in verification); single 3→4 bump |
| T-28-03-SC | Tampering | npm/pip/cargo installs | accept | no package installs — the dashboard SPA is hand-written, no-build, served via go:embed; no JS toolchain |
</threat_model>

<verification>
- `go test ./internal/status/... -count=1` green (Coding block + populator + typed-Unknown + 3→4 bump).
- `go test ./cmd/villa/... -run 'TestStatus|TestSeamGrepGate' -count=1` green (both golden variants + seam gate).
- `go build ./...` green (embedded assets compile) and `go test ./internal/dashboard/... -count=1` green.
- `make check` green.
- Coding-OFF status `--json` differs from the pre-plan output ONLY in `schema_version: 4` — append-only confirmed (nothing above the coding key moved).
- The coding `--json` block and the dashboard panel carry NO prompt/response/secret content (counts/identity only).
- Cache pct never renders as `0%` when counts are absent/zero — the gray Unknown badge is shown instead.
</verification>

<success_criteria>
- SURF-01 satisfied: `villa status` (human + --json) reports the append-only coding block; status.Report 3→4 in exactly ONE golden re-freeze (both variants); the dashboard renders a matching Agent panel hidden-until-data; coding-off output byte-identical except schema_version.
- USAGE-03 satisfied: agent token usage attributed per-model (the coder model id) via the v1.2 usage core, in status + dashboard.
- USAGE-04 satisfied: cache effectiveness surfaced as the cache_n/prompt_n ratio with typed-Unknown degradation — never a fabricated 0%.
- The phase's ONLY contract change lands here, once; doctor (schema 2) and backup (schema 3) own separate versions.
</success_criteria>

<output>
Create `.planning/phases/28-agent-surfacing-contracts/28-03-SUMMARY.md` when done.
</output>
