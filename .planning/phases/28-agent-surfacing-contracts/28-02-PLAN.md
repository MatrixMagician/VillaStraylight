---
phase: 28-agent-surfacing-contracts
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/backup/manifest.go
  - internal/backup/manifest_test.go
  - internal/backup/backup.go
  - internal/backup/backup_test.go
  - internal/backup/restore.go
  - internal/backup/restore_test.go
  - cmd/villa/backup.go
  - cmd/villa/restore.go
  - internal/metrics/llamacpp.go
  - internal/metrics/llamacpp_test.go
autonomous: true
requirements: [SURF-03, USAGE-04]
must_haves:
  truths:
    - "villa backup includes the rendered agent config (crush.json) INTO the archive when the agent is enabled"
    - "The agent binary is identity-recorded (sha256 + version/pin) in the manifest and EXCLUDED from the archive bytes, exactly like model weights"
    - "villa restore re-stages the agent binary the same way weights are re-pulled, refusing-with-remediation on identity drift"
    - "An agent-off backup archive is byte-/layout-stable vs today (no crush.json entry, no agent identity record)"
    - "internal/metrics reads cache_n/prompt_n as typed-Unknown counters (Known flags) — never a fabricated 0"
  artifacts:
    - path: "internal/backup/manifest.go"
      provides: "ExcludedAgent identity record (sha256+version+pin) + EntryCrushConfig; backupSchemaVersion 2→3 append-only"
      contains: "ExcludedAgent"
    - path: "internal/backup/backup.go"
      provides: "Optional crush.json INTO archive + agent binary identity record, gated agent-on, skip-when-absent"
      contains: "CrushConfigPath"
    - path: "internal/backup/restore.go"
      provides: "Restore reports the excluded agent for re-stage; fail-closed identity verify"
      contains: "ExcludedAgent"
    - path: "internal/metrics/llamacpp.go"
      provides: "cache_n/prompt_n counter names + typed-Unknown reads via counterFromMap (CacheSample)"
      contains: "cache_n"
  key_links:
    - from: "internal/backup/backup.go"
      to: "manifest.ExcludedAgent"
      via: "agent binary sha256+identity recorded, bytes excluded (BAK-01 ExcludedModel clone)"
      pattern: "ExcludedAgent"
    - from: "internal/metrics/llamacpp.go"
      to: "counterFromMap"
      via: "cache_n/prompt_n parsed with presence+finiteness guard (typed-Unknown)"
      pattern: "counterFromMap\\(m, mCache"
---

<objective>
Cover the coding agent in backup/restore (SURF-03, D-08) and add the cache-effectiveness counter primitive to the metrics scrape (USAGE-04 core half, D-10). Backup mirrors the BAK-01 model-weights pattern EXACTLY: the rendered crush.json goes INTO the archive; the agent binary is identity-recorded in the manifest and EXCLUDED from the bytes; restore re-stages it like weights (refuse-with-remediation on identity drift). The metrics core gains a typed-Unknown read of `cache_n`/`prompt_n` from the SAME bounded `/metrics` scrape (no new HTTP request) — the ratio itself is computed in the Plan-03 surfacing layer.

Purpose: The operator can RECOVER the agent config (and re-stage the binary) and the surfacing layer has an honest cache-effectiveness signal to consume — with NO fabricated value.
Output: `ExcludedAgent` identity record + `EntryCrushConfig` entry + `backupSchemaVersion` 2→3 (append-only, NOT golden-frozen) in `internal/backup`; the `cache_n`/`prompt_n` counter read in `internal/metrics` (pure core, no contract). NO `status.Report` schema bump in this plan.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-agent-surfacing-contracts/28-CONTEXT.md
@.planning/phases/28-agent-surfacing-contracts/28-PATTERNS.md

# Cores being surfaced (read the cited line ranges only)
@internal/backup/manifest.go
@internal/backup/backup.go
@internal/metrics/llamacpp.go
</context>

<artifacts_this_phase_produces>
This plan's contribution to the phase artifact ledger:

- `internal/backup/manifest.go`:
  - `EntryCrushConfig = "crush.json"` archive entry constant (alongside EntryConfig/EntryUsage).
  - `type ExcludedAgent struct { SHA256 string; Version string; PinSHA256 string }` — identity-only record of the EXCLUDED agent binary (clone of `ExcludedModel`; identity only, no content).
  - `Manifest.ExcludedAgent *ExcludedAgent json:"excluded_agent,omitempty"` — tail-appended ABOVE SchemaVersion (append-only).
  - `ManifestInput.ExcludedAgent *ExcludedAgent` + threaded through `BuildManifest`.
  - `backupSchemaVersion` 2→3 (append-only; fail-closed gate `m.SchemaVersion <= backupSchemaVersion` unchanged; NOT golden-frozen).
- `internal/backup/backup.go`:
  - `BackupInput.CrushConfigPath string` + `BackupInput.AgentBinarySHA256/.AgentVersion/.AgentPinSHA256` (cmd-tier-supplied, identity only).
  - Optional crush.json entry: read + checksum + add as EntryChecksum, gated agent-on, skip-when-absent via FileMissing (mirror the qdrant/recall optional entries); ExcludedAgent record set agent-on.
- `internal/backup/restore.go`:
  - Restore reports the ExcludedAgent for re-stage (mirroring the ExcludedModels re-pull report); fail-closed identity verify path consumes it.
- `cmd/villa/backup.go` / `restore.go`: gate the new agent fields on `cfg.AgentEnabled`, source crush.json path from `crushConfigPath()` and the binary identity from `agentBinPath()`+`hashFileSHA256` + the policy pin (reuse code.go/install accessors). No new exported CLI/JSON contract beyond the human messages.
- `internal/metrics/llamacpp.go`:
  - `mCacheTokensTotal`/`mPromptCacheTokensTotal` counter NAME constants (the cache_n/prompt_n source lines) — confined to this package per the metric-literal single-home discipline.
  - `type CacheSample struct { CacheN uint64; CacheKnown bool; PromptN uint64; PromptKnown bool }` (clone of CounterSample).
  - `ScrapeCacheCounters(endpoint)` OR extend `ScrapeCounters` to also read the cache pair — REUSING the same bounded request shape (no second HTTP request / endpoint literal). The RATIO is NOT computed here (Plan 03 surfacing layer owns it).
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Agent backup coverage — ExcludedAgent identity record + crush.json entry + restore re-stage</name>
  <files>internal/backup/manifest.go, internal/backup/manifest_test.go, internal/backup/backup.go, internal/backup/backup_test.go, internal/backup/restore.go, internal/backup/restore_test.go, cmd/villa/backup.go, cmd/villa/restore.go</files>
  <read_first>
    - internal/backup/manifest.go:33-68 (backupSchemaVersion + entry constants + ExcludedModel identity-only shape)
    - internal/backup/manifest.go:80-175 (Manifest tail-append discipline + ManifestInput + BuildManifest)
    - internal/backup/backup.go:36-107 (BackupInput optional-entry fields: qdrant/recall gating + FileMissing skip)
    - internal/backup/checksum.go:17-47 (sum / verify / ErrChecksumMismatch fail-closed)
    - cmd/villa/backup.go:150-264, :326 (optional-entry gating on cfg.MemoryEnabled + excludedModelIdentities — the exact wiring shape to mirror for the agent)
    - cmd/villa/code.go:194-217 (crushConfigPath / agentBinPath); internal/agent/install.go:174-177 (sha256Hex)
  </read_first>
  <behavior>
    - Manifest gains ExcludedAgent (*ExcludedAgent, omitempty) tail-appended ABOVE SchemaVersion; backupSchemaVersion 2→3.
    - Agent-ON backup: crush.json read from CrushConfigPath → checksummed → added as EntryCrushConfig EntryChecksum; ExcludedAgent set to the binary's {sha256, version, pin sha256}; the binary BYTES are NEVER archived.
    - Agent-OFF backup (or absent crush.json): zero agent entries, ExcludedAgent nil — archive layout identical to today (mirror the qdrant/recall skip-when-absent behaviour).
    - Restore reports the ExcludedAgent for re-stage; identity verify is fail-closed (ErrChecksumMismatch → BLOCK with remediation) exactly like the ExcludedModels re-pull report.
    - manifest_test: reflect-over-allow-set / JSON-denylist assertion that ExcludedAgent carries identity ONLY (no content) — clone the ExcludedModel narrow-field test.
  </behavior>
  <action>Clone `ExcludedModel` into `ExcludedAgent` (identity-only: sha256 + version + pin sha256) and add `Manifest.ExcludedAgent *ExcludedAgent json:"excluded_agent,omitempty"` tail-appended ABOVE SchemaVersion (append-only — nothing above moves); thread it through ManifestInput + BuildManifest; bump `backupSchemaVersion` 2→3 with a version-history comment line (the `m.SchemaVersion <= backupSchemaVersion` fail-closed-on-newer gate is unchanged). Add `EntryCrushConfig = "crush.json"`. In backup.go add `CrushConfigPath` + the three agent-identity input fields; read+checksum the crush.json as an optional entry gated agent-on and skipped-when-absent via FileMissing (mirror the qdrant/recall optional-entry shape at backup.go:74-106) and set ExcludedAgent agent-on (binary bytes EXCLUDED — never exported). In restore.go report the ExcludedAgent for re-stage and verify identity fail-closed via the existing `verify`/`ErrChecksumMismatch` BLOCK. Wire cmd/villa/backup.go + restore.go: gate the new fields on `cfg.AgentEnabled`, source crush.json from `crushConfigPath()` and the binary identity from `agentBinPath()`+`hashFileSHA256` + the policy pin (reuse code.go/install accessors; the policy pin SHA is the CrushAsset identity). Drive with table-driven tests: agent-on backup adds the crush.json entry + ExcludedAgent; agent-off backup is layout-identical; restore on identity drift fails closed; the narrow-field identity-only assertion on ExcludedAgent.</action>
  <verify>
    <automated>go test ./internal/backup/... -count=1 && go test ./cmd/villa/... -run 'TestBackup|TestRestore' -count=1</automated>
  </verify>
  <done>Agent-on backup archives crush.json + records the EXCLUDED binary identity (bytes not archived); agent-off backup is layout-identical to today; restore re-stage reports the agent and fails closed on identity drift; backupSchemaVersion is 3 (append-only); ExcludedAgent is identity-only (narrow-field test passes); `go test ./internal/backup/...` + the cmd backup/restore tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Cache-effectiveness counter primitive in internal/metrics (typed-Unknown)</name>
  <files>internal/metrics/llamacpp.go, internal/metrics/llamacpp_test.go</files>
  <read_first>
    - internal/metrics/llamacpp.go:52-105 (mPromptTokensTotal/mPredictedTokensTotal + CounterSample + maxCounterValue + counterFromMap typed-Unknown guard)
    - internal/metrics/llamacpp.go:122-155 (parsePromText label-stripping parser)
    - internal/metrics/llamacpp.go:185-220 (ScrapeCounters bounded request shape: scrapeTimeout + maxScrapeBody LimitReader + parsePromText)
  </read_first>
  <behavior>
    - Add the cache_n / prompt_n metric NAME constants (the llama.cpp timings counters) confined to llamacpp.go.
    - CacheSample carries CacheN/CacheKnown + PromptN/PromptKnown, each via counterFromMap (absent/NaN/Inf/negative/over-2^53 → Known=false, never a fabricated 0).
    - The cache pair is read from the SAME bounded scrape (extend ScrapeCounters or add ScrapeCacheCounters reusing the identical request shape) — NO second HTTP request, NO new endpoint/host literal.
    - The RATIO is NOT computed here (the surfacing layer computes pct only when BOTH Known AND prompt_n>0).
  </behavior>
  <action>Add the cache_n/prompt_n metric NAME constants alongside mPromptTokensTotal (llamacpp.go:57-60) — confined to this package (the grep gate keeps metric literals home). Clone CounterSample into CacheSample (CacheN/CacheKnown + PromptN/PromptKnown) and read both via the existing `counterFromMap` presence+finiteness guard (typed-Unknown — never a fabricated count, D-05/D-10). Surface the pair from the SAME bounded `/metrics` scrape: either extend ScrapeCounters to also populate the cache pair or add a sibling `ScrapeCacheCounters` REUSING the identical bounded request shape (scrapeTimeout client + maxScrapeBody+1 LimitReader truncation refusal + parsePromText) — add NO second HTTP request and NO new endpoint literal. Do NOT compute the ratio here (Plan 03 owns the pct, shown only when both Known and prompt_n>0). Drive with table-driven tests over fixed prom-text bodies: both present → Known with exact counts; one absent → that one Known=false; NaN/negative/over-cap → Known=false; a truncated/over-cap body → the whole sample unavailable (mirror the ScrapeCounters truncation test).</action>
  <verify>
    <automated>go test ./internal/metrics/... -run 'TestScrapeCache|TestCounter|TestCacheSample' -count=1 && make check</automated>
  </verify>
  <done>CacheSample + the cache_n/prompt_n typed-Unknown reads exist; an absent/unparseable counter yields Known=false (never a fabricated 0); no second HTTP request or new endpoint literal added; `go test ./internal/metrics/...` and `make check` pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| manifest.json (restore) → parser | a restored manifest is untrusted input (fail-closed parse; fail-closed-on-newer schema gate) |
| agent binary on disk → identity record | the binary hash recorded for re-stage is computed over untrusted on-disk bytes |
| llama-server /metrics body → counter parse | scrape body is untrusted (bounded read + finiteness guard against fabricated counts) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-02-01 | Tampering | restored archive identity / drifted agent binary | mitigate | restore verifies per-entry SHA-256 via `verify`/`ErrChecksumMismatch` fail-closed BLOCK; the EXCLUDED agent identity is re-stage-verified, refuse-with-remediation on drift (D-08) |
| T-28-02-02 | Information disclosure | secrets/content leaking into manifest/archive | mitigate | ExcludedAgent is identity-only (sha256/version/pin) — narrow-field test enforces no content; crush.json is the rendered villa artifact (kill switches + loopback provider, no cloud credentials by construction — AGENT-02); binary BYTES excluded |
| T-28-02-03 | Spoofing/false-data | fabricated cache_n/prompt_n counts | mitigate | counterFromMap rejects absent/NaN/Inf/negative/over-2^53 → typed-Unknown Known=false; truncated body refuses the whole sample (no partial-read fold) |
| T-28-02-04 | DoS | oversized /metrics body | mitigate | bounded scrapeTimeout client + maxScrapeBody+1 LimitReader (reused, no second request) |
| T-28-02-SC | Tampering | npm/pip/cargo installs | accept | no package installs in this plan (Go stdlib + existing internal packages only) |
</threat_model>

<verification>
- `go test ./internal/backup/... -count=1` green (ExcludedAgent identity-only + crush.json entry + restore fail-closed).
- `go test ./internal/metrics/... -count=1` green (CacheSample typed-Unknown reads).
- `go test ./cmd/villa/... -run 'TestBackup|TestRestore' -count=1` green.
- `make check` green.
- Agent-off backup archive layout is identical to today (no crush.json entry, ExcludedAgent nil).
- No `status.Report` schema bump in this plan (isolated to Plan 03). backupSchemaVersion 2→3 is doctor/status-independent and NOT golden-frozen.
</verification>

<success_criteria>
- SURF-03 satisfied: backup/restore cover the rendered agent config; the agent binary is identity-recorded and excluded from the archive bytes, exactly like model weights; restore re-stages with fail-closed identity verify.
- USAGE-04 core half satisfied: cache_n/prompt_n are read as typed-Unknown counters from the existing bounded scrape (ratio computed in Plan 03) — never a fabricated 0.
- Counts/identity-only discipline preserved (no content/secret leakage).
</success_criteria>

<output>
Create `.planning/phases/28-agent-surfacing-contracts/28-02-SUMMARY.md` when done.
</output>
