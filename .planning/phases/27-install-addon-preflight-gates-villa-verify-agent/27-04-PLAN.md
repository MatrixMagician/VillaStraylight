---
phase: 27-install-addon-preflight-gates-villa-verify-agent
plan: 04
type: execute
wave: 3
depends_on: ["27-01", "27-02", "27-03"]
files_modified:
  - cmd/villa/install_agent.go
  - cmd/villa/verify_agent.go
autonomous: false
requirements: [INSTALL-03, PRIV-06]
tags: [on-hardware, acceptance, crush-run, tool-call, egress, llama-down, gfx1151, checkpoint]

must_haves:
  truths:
    - "On the gfx1151 box, `villa install --coding-agent` stages the picked coder GGUF + pinned binary, renders crush.json, and PASSES readiness via a real tool-call round-trip (read→edit→result), not a health-200"
    - "The deterministic crush run payload (planted file + allowed_tools) reliably forces a tool-call edit without a TTY prompt (Open Q1 resolved on-hardware)"
    - "`villa verify agent` ctrl1: an external probe FAILS under the operator-applied host egress block, then the blocked crush-run task COMPLETES"
    - "`villa verify agent` ctrl2: with villa-llama stopped the same task FAILS (no silent cloud fallback); villa-llama is restored afterward"
    - "The host egress-block operator precondition (the same one verify-memory uses) is documented in the acceptance record"
  artifacts:
    - path: ".planning/phases/27-install-addon-preflight-gates-villa-verify-agent/27-04-SUMMARY.md"
      provides: "on-hardware acceptance evidence for INSTALL-03 readiness + PRIV-06 egress/llama-down controls"
      contains: "tool-call round-trip"
  key_links:
    - from: "on-hardware crush run payload"
      to: "evalAgentProof (Plan 01) + agentTask in evalAgentVerify (Plan 03)"
      via: "the confirmed deterministic planted-file read→edit→result payload, shared by both drivers"
      pattern: "crush run"
    - from: "operator egress block"
      to: "runProbeCurl negative control"
      via: "the documented host-egress precondition (verify-memory mechanism)"
      pattern: "egress"
---

<objective>
On-hardware acceptance of the coding-agent addon on the live gfx1151 box (INSTALL-03 readiness + PRIV-06 runtime controls). PRIV-06 is on-hardware by nature, exactly like `villa verify memory`. This plan confirms the deterministic `crush run` tool-call payload (Open Q1), then runs the two real proofs that cannot be faked off-hardware: the install readiness tool-call round-trip, and `villa verify agent`'s egress + llama-down controls under the operator-applied host egress block.

Purpose: Prove the addon comes up READY via a real tool-call round-trip (a health-200 never suffices), and prove zero-outbound + no-cloud-fallback at runtime, negative-control-FIRST — never flag-trusted. No green is fabricated; an unconfirmed payload or an unrestored service is a FAIL.
Output: confirmed payload constants in `install_agent.go`/`verify_agent.go` (if tuning is needed), and `27-04-SUMMARY.md` recording the acceptance evidence + the operator egress-block step.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-install-addon-preflight-gates-villa-verify-agent/27-CONTEXT.md
@.planning/phases/27-install-addon-preflight-gates-villa-verify-agent/27-RESEARCH.md
@.planning/phases/27-install-addon-preflight-gates-villa-verify-agent/27-01-SUMMARY.md
@.planning/phases/27-install-addon-preflight-gates-villa-verify-agent/27-02-SUMMARY.md
@.planning/phases/27-install-addon-preflight-gates-villa-verify-agent/27-03-SUMMARY.md
@.planning/phases/26-agent-delivery-core-lockdown-launcher/26-03-SUMMARY.md
</context>

<artifacts_this_phase_produces>
This plan produces:
- **Confirmed payload constants** (only if on-hardware tuning is needed): the deterministic `crush run` prompt + planted-file token (TOKEN_A→TOKEN_B) + `permissions.allowed_tools` set that forces a read→edit→result tool call without a TTY prompt on the pinned coder model + `--jinja` unit. Shared by the Plan-01 readiness driver and the Plan-03 `agentTask` driver (DRY).
- **`27-04-SUMMARY.md`** — the acceptance record: readiness PASS evidence, the three verify-agent control outcomes, the operator egress-block step, and the restored villa-llama confirmation.
</artifacts_this_phase_produces>

<tasks>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 1: Operator applies the host egress block (verify-memory precondition)</name>
  <files>(none — operator network action; no villa code change)</files>
  <what-built>
    Plans 01-03 landed the addon install (readiness tool-call round-trip), preflight gates, and `villa verify agent` (egress + llama-down controls). The egress negative control requires the host's outbound network to actually be blocked — this is an operator precondition (the SAME mechanism `villa verify memory` uses), not new villa code. D-07 forbids new cap-root tooling.
  </what-built>
  <action>
    PAUSE for the operator to apply the host egress block on the gfx1151 box (the verify-memory precondition). This is an operator network action with NO CLI/API seam in villa — do not automate or fabricate it. After the operator applies the block, verify it is real and that loopback still works per how-to-verify, and capture the EXACT operator command(s) in the SUMMARY (the Phase-20 artifacts did not record one). This is a blocking human-action checkpoint: do not proceed to Tasks 2/3 until the external probe fails and the loopback endpoint still responds.
  </action>
  <how-to-verify>
    On the gfx1151 box, apply the host egress block the same way the Phase-20 / verify-memory acceptance did (the operator-supplied outbound block). Then confirm it is real and that the loopback inference path still works:
    1. Confirm an external host is unreachable from a container on `villa.network`:
       `podman run --rm --network villa --entrypoint curl <helper-image> -sf --max-time 5 https://huggingface.co/` MUST fail (non-zero).
    2. Confirm the loopback inference endpoint is still reachable (the agent's only allowed path):
       `curl -sf --max-time 5 http://127.0.0.1:8080/v1/models` MUST succeed.
    Record the EXACT operator command(s) used to apply the block in the SUMMARY (the Phase-20 artifacts did not record one — capture it now).
  </how-to-verify>
  <verify>
    <human-check>The external container probe to https://huggingface.co/ FAILS under the block; `curl http://127.0.0.1:8080/v1/models` still succeeds; the operator block command is captured in the SUMMARY.</human-check>
  </verify>
  <done>
    The host egress block is applied and proven real (external unreachable, loopback reachable); the exact operator command is recorded.
  </done>
  <resume-signal>Type "egress blocked" once the external probe fails and the loopback endpoint still responds, with the operator command captured; or describe what is blocking.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Confirm the deterministic crush run tool-call payload on the pinned coder + --jinja unit (Open Q1)</name>
  <files>cmd/villa/install_agent.go, cmd/villa/verify_agent.go</files>
  <read_first>
    - .planning/phases/26-agent-delivery-core-lockdown-launcher/26-03-SUMMARY.md (the on-hardware PONG round-trip + crush run non-interactive path + allowed_tools auto-accept; ROCm-live caveat)
    - .planning/phases/27-.../27-RESEARCH.md § Open Questions Q1 + Pitfall 3 (deterministic read→edit→result; permissions.allowed_tools, NOT --yolo)
    - cmd/villa/install_agent.go (the Plan-01 liveAgentToolCallProbe + payload constants)
    - cmd/villa/verify_agent.go (the Plan-03 agentTask driver — must share the same payload)
  </read_first>
  <action>
    On the live box, with the addon enabled and the coding-mode `--jinja` unit serving the picked coder, drive a non-interactive `crush run` against the loopback endpoint to find the payload that DETERMINISTICALLY forces a tool-call edit: plant a temp file containing TOKEN_A, prompt the agent to "read <file>, replace TOKEN_A with TOKEN_B, save", and confirm the file now contains TOKEN_B and `crush run` exits 0 — a real read→edit→result loop (the tool-bearing analog of Phase-26's PONG). Use `permissions.allowed_tools` (e.g. view/edit/write) in the rendered config so no interactive prompt fires; do NOT use `--yolo` (rejected with `run` in v0.76.0). Bound the run with a timeout — a timeout is a FAIL.

    If the placeholder payload constants from Plans 01/03 need tuning to be deterministic on this model, update them in BOTH `install_agent.go` (readiness) and `verify_agent.go` (agentTask) so the single shared driver stays DRY (one payload, two callers). If no change is needed, record that the placeholder payload was confirmed verbatim. Run `make check` after any edit. Capture the exact prompt + token + allowed_tools set in the SUMMARY.
  </action>
  <verify>
    <human-check>On the gfx1151 box: the planted file contains TOKEN_B after the run AND `crush run` exited 0; the run used permissions.allowed_tools (no TTY prompt); the payload is identical in install_agent.go and verify_agent.go.</human-check>
    <automated>make check</automated>
  </verify>
  <done>
    A deterministic crush run payload forces a verifiable read→edit→result edit on the pinned coder + --jinja unit without a prompt; the same payload is shared by the readiness and verify drivers; make check green.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: On-hardware acceptance — install readiness round-trip + villa verify agent egress + llama-down</name>
  <files>(none — acceptance run; evidence recorded in 27-04-SUMMARY.md)</files>
  <what-built>
    The full addon assembly: `villa install --coding-agent` (pre-stage GGUF + pinned binary + render + readiness tool-call round-trip) and `villa verify agent` (egress negative control + llama-down cloud-fallback control), with the confirmed deterministic payload from Task 2 and the operator egress block from Task 1.
  </what-built>
  <action>
    PAUSE for the operator to run the on-hardware acceptance on the gfx1151 box and confirm each outcome per how-to-verify. This is a blocking human-verify checkpoint: the readiness tool-call round-trip and the two `villa verify agent` controls run against the live served coder + real host egress block and cannot be faked off-hardware. Record every outcome (including the exact operator egress-block command and the restored-villa-llama confirmation + the backend the run used) in 27-04-SUMMARY.md. Do NOT fabricate a PASS if any control is inconclusive.
  </action>
  <how-to-verify>
    On the gfx1151 box, with the host egress block applied (Task 1):

    1. INSTALL READINESS (INSTALL-03 / D-05): run `villa install --coding-agent`. Confirm it:
       - stages exactly the picked coder GGUF (presence-skip on a second run — no re-pull) and the pinned crush binary (checksum-verified),
       - renders crush.json (one loopback provider, kill switches, villa- model id),
       - PASSES readiness via a REAL tool-call round-trip (the planted file is edited), and that a health-200 alone would NOT have passed (the proof drives an edit, not just a 200).

    2. VERIFY AGENT ctrl1 (PRIV-06 egress, negative-control-FIRST): run `villa verify agent`. Confirm:
       - the external egress probe FAILS under the block (proving the block is real), THEN
       - the real `crush run` tool-call task COMPLETES while egress is blocked.

    3. VERIFY AGENT ctrl2 (PRIV-06 no cloud fallback): confirm that with `villa-llama` stopped, the SAME agent task FAILS (the agent does NOT answer — no silent cloud fallback), and that `villa-llama` is RESTORED (running) after the verb returns. An agent that answers with inference down is the smoking gun and is a FAIL.

    4. Confirm the overall `villa verify agent` verdict is PASS only when ctrl1 passes AND ctrl2 fails-as-expected, and the exit code matches (0 on PASS).

    Record all outcomes (including the exact operator egress-block command and the restored-service confirmation) in 27-04-SUMMARY.md. Caveat note: the live box runs ROCm 7.2.4 while the coder qualification is build-9496-vulkan-radv-scoped (D-13) — note the backend the acceptance ran on, exactly as 26-03 did. Do NOT fabricate a PASS if any control is inconclusive.
  </how-to-verify>
  <verify>
    <human-check>Install readiness PASSES via a real planted-file edit (not a health-200); verify-agent ctrl1 (external probe FAILS under block → blocked crush-run task COMPLETES) and ctrl2 (llama-down task FAILS, villa-llama restored) both hold; overall verdict PASS exits 0; evidence + backend caveat in 27-04-SUMMARY.md.</human-check>
  </verify>
  <done>
    On-hardware: addon install PASSES readiness via a real tool-call round-trip; `villa verify agent` proves egress blocked (negative-control-first) and no cloud fallback (llama-down FAILS), villa-llama restored; no fabricated PASS.
  </done>
  <resume-signal>Type "accepted" with the three control outcomes + the egress-block command + villa-llama-restored confirmation; or describe the failure.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent → external network (live host) | The on-hardware proof: external must be unreachable under the operator block; the agent works only over loopback |
| operator → host network state | The egress block is applied/removed by the operator; the run must leave the host restored |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-17 | Repudiation (false-green acceptance) | On-hardware readiness | mitigate | readiness drives a verifiable file edit (read→edit→result); a health-200 is explicitly checked as insufficient; an unconfirmed payload is a FAIL, never recorded as PASS |
| T-27-18 | Information Disclosure (live phone-home/fallback) | verify agent on live host | mitigate | ctrl1 proves the external host is unreachable under the real block FIRST; ctrl2 proves no cloud answer with villa-llama down — both run on the live box, not simulated |
| T-27-19 | Availability (service left stopped) | llama-down control | mitigate | villa-llama is restored after the control; the acceptance explicitly confirms the service is running again before sign-off |
| T-27-20 | Tampering (unrecorded precondition) | operator egress block | mitigate | the exact operator block command is captured in the SUMMARY (the Phase-20 gap) so the proof is reproducible |
</threat_model>

<verification>
- Task 1: external probe FAILS under the block; loopback `/v1/models` still responds; operator command captured.
- Task 2: deterministic crush run edits the planted file (TOKEN_B) + exit 0, no TTY prompt; payload shared across the two drivers; `make check` green.
- Task 3: install readiness PASSES via a real tool-call round-trip (not health-200); verify-agent ctrl1 (egress-open FAIL → blocked task COMPLETE) and ctrl2 (llama-down task FAILS, service restored) both as designed; overall verdict PASS with exit 0.
- All evidence + the backend caveat recorded in 27-04-SUMMARY.md; no fabricated PASS.
</verification>

<success_criteria>
- `villa install --coding-agent` stages the picked GGUF + pinned binary, renders crush.json, and PASSES readiness via a real tool-call round-trip on the gfx1151 box.
- `villa verify agent` proves egress blocked (negative-control-first) and no cloud fallback (llama-down FAILS the task), with villa-llama restored.
- The deterministic payload is confirmed and shared by both drivers; the operator egress-block step is documented.
</success_criteria>

<output>
Create `.planning/phases/27-install-addon-preflight-gates-villa-verify-agent/27-04-SUMMARY.md` when done.
</output>
