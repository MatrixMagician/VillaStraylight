---
phase: 27-install-addon-preflight-gates-villa-verify-agent
plan: 03
type: execute
wave: 2
depends_on: ["27-01"]
files_modified:
  - cmd/villa/verify_agent.go
  - cmd/villa/verify_agent_test.go
  - cmd/villa/verify.go
autonomous: true
requirements: [PRIV-06]
tags: [verify, agent, egress, negative-control, cloud-fallback, llama-down, privacy, zero-outbound]

must_haves:
  truths:
    - "`villa verify agent` proves zero outbound at runtime, negative-control-FIRST: an external probe MUST FAIL under the egress block before the agent task is trusted"
    - "Under the egress block, the real agent task (crush run tool-call round-trip over loopback) MUST complete"
    - "With villa-llama stopped, the same agent task MUST FAIL — an answer is a silent cloud-fallback smoking gun"
    - "The final verdict is PASS only when ctrl1 passes AND ctrl2 fails-as-expected; PASS/FAIL only, a timeout/unevaluable is a FAIL"
    - "The verb mirrors verify_memory.go's four-layer seam exactly; the helper image comes from orchestrate.EmbedImage(), never a re-typed literal"
    - "`villa verify agent` registers under the verify parent and exits 0 with a clear message when the addon is off"
  artifacts:
    - path: "cmd/villa/verify_agent.go"
      provides: "evalAgentVerify (pure, negative-control-first, both controls) + live seam + crush-run drivers"
      exports: ["evalAgentVerify"]
    - path: "cmd/villa/verify.go"
      provides: "newVerifyAgent() registered under the verify parent"
      contains: "newVerifyAgent"
  key_links:
    - from: "evalAgentVerify"
      to: "egressBlocked() FIRST, then agentTask(), then llamaDownTask()"
      via: "negative-control-first ordering; verdict = ctrl1.pass && ctrl2.failed-as-expected"
      pattern: "evalAgentVerify"
    - from: "live egress probe"
      to: "runProbeCurl + orchestrate.EmbedImage()"
      via: "fixed-arg podman/curl against egressNegativeControlHost; helper image from the seam accessor"
      pattern: "runProbeCurl|orchestrate\\.EmbedImage"
    - from: "llamaDownTask"
      to: "orchestrate systemd Stop(villa-llama) + restore"
      via: "injected systemd seam; same crush-run task must fail with inference down"
      pattern: "llamaDown|villa-llama"
---

<objective>
Add `villa verify agent` — the runtime strictly-local proof for the coding agent (PRIV-06), mirroring `verify_memory.go`'s four-layer seam EXACTLY. The pure core `evalAgentVerify` asserts the egress negative control FIRST (an external host must be unreachable under the host egress block — proving the block is real), THEN requires the real agent task (a `crush run` tool-call round-trip over the loopback inference endpoint) to complete while egress is blocked, AND folds in a llama-down control (with `villa-llama` stopped, the same task MUST fail — an answer is a silent cloud-fallback smoking gun). One verb proves both PRIV-06 clauses.

Purpose: Zero-outbound is proven, never flag-trusted — absence alone is a false-green, so the negative control runs first. The cloud-fallback control runs routinely (folded into the same verb, not a manual drill). PASS/FAIL only — a timeout/unevaluable is a FAIL, never a silent skip.
Output: `cmd/villa/verify_agent.go` (pure `evalAgentVerify` + live seam + `crush run` drivers) + test; `newVerifyAgent()` registered in `verify.go`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-install-addon-preflight-gates-villa-verify-agent/27-CONTEXT.md
@.planning/phases/27-install-addon-preflight-gates-villa-verify-agent/27-PATTERNS.md
@.planning/phases/27-install-addon-preflight-gates-villa-verify-agent/27-RESEARCH.md
@.planning/phases/27-install-addon-preflight-gates-villa-verify-agent/27-VALIDATION.md
@.planning/phases/27-install-addon-preflight-gates-villa-verify-agent/27-01-SUMMARY.md
</context>

<artifacts_this_phase_produces>
This plan introduces:
- **New file `cmd/villa/verify_agent.go`** with:
  - **Layer 1 (Verdict):** reuse `memoryProof{status preflight.Status; detail string}` (`install_memory.go:187`) — PASS/FAIL only, no WARN (D-06).
  - **Layer 2 (pure core):** `evalAgentVerify(egressBlocked func() (bool, error), agentTask func() (completed bool, err error), llamaDownTask func() (answered bool, err error)) memoryProof` — negative-control-FIRST, folds both controls (clone of `evalRagSmoke`).
  - **Layer 3 (live seam):** `liveAgentVerify(ctx, ...) memoryProof` cloning `liveRagSmoke`: `egressBlocked` via `runProbeCurl(ctx, orchestrate.EmbedImage(), "-sf", "--max-time", "5", egressNegativeControlHost)`; `agentTask`/`llamaDownTask` as host-side `crush run` drivers using `agentBinPath()` fixed-arg; `llamaDownTask` stops `villa-llama.service` via an injected `orchestrate` systemd seam (Stop) then restores.
  - **Layer 4 (fixed-arg exec):** reuse `runProbeCurl` verbatim (no shell, no new cap-root tooling).
  - `verifyAgentDeps` + `liveVerifyAgentDeps()` quartet mirroring `verifyMemoryDeps`/`liveVerifyMemoryDeps`.
- **`verify.go` registration:** `cmd.AddCommand(newVerifyAgent())` next to `newVerifyMemory()` (verify.go:75); `newVerifyAgent()`/`runVerifyAgent()` cobra wiring gated on the persisted `AgentEnabled` (addon-off exits 0 with a clear message, mirror verify.go:113-116).
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: evalAgentVerify pure core (negative-control-first, both controls) + live seam + crush-run drivers</name>
  <files>cmd/villa/verify_agent.go, cmd/villa/verify_agent_test.go</files>
  <read_first>
    - cmd/villa/verify_memory.go (FULL FILE — evalRagSmoke :70-102 negative-control-first; memoryProof reuse; egressNegativeControlHost :110; liveRagSmoke :142-160 egress probe; runLoopbackCurl host-process discipline :422-443)
    - cmd/villa/install_memory.go (memoryProof{status,detail} :187; runProbeCurl :350-369 fixed-arg podman/curl)
    - internal/orchestrate/memory.go:47 (EmbedImage() seam accessor — the ONLY source for the helper image)
    - internal/orchestrate (systemd Stop/Start seam — orchestrate.NewSystemd(); the villa-llama service name accessor)
    - cmd/villa/code.go:217 (agentBinPath — the crush-run driver's fixed-arg binary), :194 (crushConfigPath)
    - cmd/villa/install_agent.go (the Plan-01 liveAgentToolCallProbe / crush-run tool-call driver shape — reuse the same planted-file read→edit→result mechanism for agentTask)
    - internal/preflight/preflight.go (StatusPass/StatusFail)
  </read_first>
  <behavior>
    - evalAgentVerify: egressBlocked err → FAIL ("could not run the egress negative-control probe …"); !blocked → FAIL ("egress is NOT blocked: an external host was reachable …"); these run BEFORE the agent task is trusted.
    - Then agentTask: err || !completed → FAIL ("the agent task did not complete under the egress block …").
    - Then llamaDownTask: answered == true → FAIL ("the agent ANSWERED with villa-llama stopped — silent cloud-model fallback detected; FAILS verification"); an error from llamaDownTask is the EXPECTED inference-down outcome.
    - All four controls passing-as-designed → PASS ("zero-outbound agent task completed; no cloud fallback (llama-down control failed as expected)").
    - PASS/FAIL only; a timeout (context-bounded driver returning err) is a FAIL.
  </behavior>
  <action>
    Create `cmd/villa/verify_agent.go`. Implement the pure `evalAgentVerify` core cloning `evalRagSmoke`'s exact structure: negative control FIRST — return FAIL on `egressBlocked` err (cannot run the probe) and FAIL on `!blocked` (external host reachable) BEFORE trusting the agent task; then FAIL on `agentTask` err/`!completed`; then FAIL if `llamaDownTask` reports `answered == true` (silent cloud fallback). Reuse the `memoryProof` Verdict type — PASS/FAIL only, no WARN (D-06). Every non-pass detail carries actionable remediation.

    Implement `liveAgentVerify` cloning `liveRagSmoke`: `egressBlocked` runs `runProbeCurl(ctx, orchestrate.EmbedImage(), "-sf", "--max-time", "5", egressNegativeControlHost)` and returns `(err != nil, nil)` — reachable (err==nil) ⇒ NOT blocked. The helper image MUST come from `orchestrate.EmbedImage()`, NEVER a re-typed image literal (TestSeamGrepGate walks cmd/villa). `agentTask` is a host-side `crush run` driver using `agentBinPath()` fixed-arg, reusing the SAME planted-file read→edit→result mechanism as the Plan-01 readiness probe (DRY — share the driver) over the loopback `127.0.0.1:8080/v1` endpoint, with `permissions.allowed_tools` pre-approval from the rendered config; bound it with a context timeout (timeout ⇒ err ⇒ FAIL). `llamaDownTask` stops `villa-llama.service` via an injected `orchestrate` systemd Stop seam, runs the SAME crush-run task, then restores the service (Start) in a deferred restore regardless of outcome — and returns whether the agent answered.

    Add `verifyAgentDeps` + `liveVerifyAgentDeps()` mirroring `verifyMemoryDeps`/`liveVerifyMemoryDeps` (the egress probe, the crush-run driver, the systemd stop/start seam, the persisted-gate loader). Drive both controls through `evalAgentVerify`.

    Write `verify_agent_test.go` FIRST (tdd): a table over `evalAgentVerify` mirroring the `evalRagSmoke` tests — (egress-open: blocked=false → FAIL), (egress probe err → FAIL), (blocked + task !completed → FAIL), (blocked + task completed + llama-down answered=true → FAIL), (blocked + completed + llama-down answered=false → PASS). Inject the three func seams directly (no host). Assert verdict status + that the negative control is evaluated FIRST (egress-open fails even when the agent task would have completed).
  </action>
  <verify>
    <automated>go test ./cmd/villa/ -run 'TestEvalAgentVerify' -count=1 && go test ./internal/inference/ -run TestSeamGrepGate -count=1 && go vet ./cmd/villa/</automated>
  </verify>
  <done>
    evalAgentVerify asserts egress-blocked FIRST (egress-open → FAIL even if the task would pass), requires the blocked task to complete, and FAILS on a llama-down answer; PASS only when ctrl1 passes && ctrl2 fails-as-expected; helper image from orchestrate.EmbedImage(); TestSeamGrepGate + vet green.
  </done>
</task>

<task type="auto">
  <name>Task 2: register newVerifyAgent() under the verify parent, gated on the persisted addon</name>
  <files>cmd/villa/verify.go, cmd/villa/verify_agent.go, cmd/villa/verify_agent_test.go</files>
  <read_first>
    - cmd/villa/verify.go (newVerify :67; AddCommand(newVerifyMemory()) :75; newVerifyMemory :83; runVerifyMemory :109; verifyMemoryDeps :42; liveVerifyMemoryDeps :58; addon-off-exit-0 pattern :113-116)
    - cmd/villa/verify_agent.go (the liveAgentVerify + verifyAgentDeps from Task 1)
    - internal/config/villaconfig.go (AgentEnabled from Plan 01 — the persisted gate)
    - cmd/villa/verify_memory_test.go (the cobra-wiring/exit-code test pattern)
  </read_first>
  <behavior>
    - `villa verify agent` is registered under the `verify` parent next to `verify memory`.
    - When the addon is off (persisted AgentEnabled false), the verb exits 0 with a clear message (no false FAIL), mirroring verify memory's off path.
    - When on, it runs `liveAgentVerify` and maps the verdict to an exit code (PASS → 0, FAIL → non-zero), mirroring `runVerifyMemory`.
  </behavior>
  <action>
    Add `cmd.AddCommand(newVerifyAgent())` next to line 75 in `verify.go`. Implement the `newVerifyAgent()`/`runVerifyAgent()` cobra wiring (in `verify_agent.go`, matching the verify_memory split where the thin cobra wiring may live in either file — keep it consistent with `newVerifyMemory`). Gate on the persisted `AgentEnabled`: if the addon is off, print a clear message and exit 0 (mirror verify.go:113-116). When on, call `runVerifyAgent(cmd, args, liveVerifyAgentDeps())`, run `liveAgentVerify`, render the verdict, and exit 0 on PASS / non-zero on FAIL (mirror `runVerifyMemory`'s exit mapping).

    Extend `verify_agent_test.go`: assert (a) the `verify agent` subcommand is registered under `verify`; (b) addon-off → exit 0 with the expected message (inject a deps whose gate loader returns false); (c) a PASS verdict → exit 0 and a FAIL verdict → non-zero (inject a deps whose verify func returns each).
  </action>
  <verify>
    <automated>go test ./cmd/villa/ -run 'TestVerify' -count=1 && go test ./internal/inference/ -run TestSeamGrepGate -count=1 && make check</automated>
  </verify>
  <done>
    `villa verify agent` is registered, exits 0 when the addon is off, and maps PASS→0 / FAIL→non-zero when on; TestSeamGrepGate + make check green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent → external network | The whole proof: an external host must be UNREACHABLE under the egress block; the agent must work only over loopback |
| agent → inference endpoint | With villa-llama down, the agent must NOT resolve to a cloud provider (no silent fallback) |
| verify driver → host services | The llama-down control stops/starts a user systemd service via a fixed-arg seam |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-12 | Information Disclosure (phone-home) | Agent startup outbound | mitigate | negative-control-FIRST egress proof: an external host MUST be unreachable under the block BEFORE the agent task is trusted (D-07); absence alone is rejected as a false-green |
| T-27-13 | Information Disclosure (cloud fallback) | Inference resolution | mitigate | llama-down control (D-08): with villa-llama stopped the same task MUST FAIL; an answer is the smoking gun and FAILS verification — folded into the SAME verb so it runs routinely |
| T-27-SC | Tampering (false-green control) | The verify gate itself | mitigate | the control that doesn't actually block is caught by the negative-control-first ordering — a probe that cannot run or finds the host reachable is a FAIL, not a skip; PASS requires ctrl1.pass && ctrl2.failed-as-expected; a timeout is a FAIL |
| T-27-14 | Tampering (agent outbound tool) | fetch/download/sourcegraph tools | mitigate | the host egress block fails outbound tools by construction (the point of the runtime proof); defense-in-depth: the rendered config omits/disables outbound tools (Plan 01 render; STRIDE Pitfall 5) — a fetch succeeding under the block is itself a ctrl1 FAIL |
| T-27-15 | Tampering (contract) | Seam-marker leak | mitigate | helper image strictly from `orchestrate.EmbedImage()`; no image/device literal in `cmd/villa`; `TestSeamGrepGate` run per task |
| T-27-16 | Availability (service left stopped) | llama-down control | mitigate | `villa-llama` is restored (Start) in a deferred restore regardless of the control outcome; the restore is asserted/inspected on-hardware (Plan 04) |
</threat_model>

<verification>
- `go test ./cmd/villa/ -run 'TestEvalAgentVerify|TestVerify' -count=1` green.
- `go test ./internal/inference/ -run TestSeamGrepGate -count=1` green (helper image from the seam accessor only).
- `evalAgentVerify` table proves: egress-open → FAIL (negative control first), blocked task → PASS, llama-down answer → FAIL.
- `villa verify agent` registered; addon-off exits 0; PASS→0 / FAIL→non-zero.
- `make check` green at wave merge.
- On-hardware egress + llama-down acceptance is Plan 04 (PRIV-06 is on-hardware by nature).
</verification>

<success_criteria>
- `villa verify agent` proves zero outbound negative-control-FIRST and folds the llama-down cloud-fallback control into the same verb.
- The verdict is PASS only when ctrl1 passes AND ctrl2 fails-as-expected; PASS/FAIL only; a timeout is a FAIL.
- The verb mirrors `verify_memory.go`'s four-layer seam; the helper image is `orchestrate.EmbedImage()`.
- TestSeamGrepGate + `make check` green.
</success_criteria>

<output>
Create `.planning/phases/27-install-addon-preflight-gates-villa-verify-agent/27-03-SUMMARY.md` when done.
</output>
