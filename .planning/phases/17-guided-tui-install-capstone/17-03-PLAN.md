---
phase: 17-guided-tui-install-capstone
plan: 03
type: execute
wave: 3
depends_on: ["17-02"]
files_modified:
  - cmd/villa/install_wizard_test.go
  - cmd/villa/install_test.go
autonomous: true
requirements: [INSTALL-01, INSTALL-02]
must_haves:
  truths:
    - "A test proves the wizard seam fires exactly once on a TTY (no --json, no --no-tui)"
    - "A test proves --no-tui, --json, and a non-TTY stdout each bypass the wizard seam (0 invocations) and run the flag path"
    - "A test proves the wizard-path config.toml byte-matches the flag-path config.toml for identical inputs (SC#1/SC#2)"
    - "A test drives a BLOCK-gap + privileged-consent scenario through runInstall on the WIZARD path (live composition) and proves the privileged seam is invoked AT MOST ONCE, the exit code is the preserved 0/2/1 verdict, and that consent-denied invokes the privileged seam 0 times and exits blocked"
    - "A test proves the live huh form (accessible mode) binds the scripted model selection and Install/Cancel choice"
    - "A test proves safeAutoFix returns false for PRE-03 and PRE-05 (D-05 interpretation 1)"
  artifacts:
    - path: "cmd/villa/install_wizard_test.go"
      provides: "wizard-fires/bypass tests, config-match test, single-gate wizard-path test, accessible-mode driver test, safeAutoFix test"
      contains: "TestInstallWizard"
    - path: "cmd/villa/install_test.go"
      provides: "fakeInstallDeps.wizardCalls counter + default wizard/stdoutIsTTY stubs"
      contains: "wizardCalls"
  key_links:
    - from: "cmd/villa/install_wizard_test.go"
      to: "runInstall"
      via: "fake wizard seam + buffered cobra.Command"
      pattern: "runInstall\\(cmd"
    - from: "cmd/villa/install_wizard_test.go"
      to: "gateInstall single privileged execution"
      via: "setsebool/enableLinger call counter asserted ≤ 1"
      pattern: "setseboolCalls|lingerCalls"
    - from: "cmd/villa/install_wizard_test.go"
      to: "huh accessible mode"
      via: "WithInput/WithOutput/WithAccessible"
      pattern: "WithAccessible"
---

<objective>
Add the Wave-0 wizard test scaffold and the D-05 classifier test, proving the phase's
observable signals: the wizard fires on a TTY, the three fallback conditions
(`--no-tui` / `--json` / non-TTY) bypass it, the wizard- and flag-path config.toml are
byte-identical (SC#1/SC#2), a BLOCK-gap + privileged-consent scenario through the LIVE
composition runs the privileged seam at most once with the preserved 0/2/1 verdict (and
zero times when consent is denied), the live huh form drives correctly in accessible mode,
and `safeAutoFix` returns false for both current privileged fixes.

Purpose: INSTALL-01 (wizard composes + computes nothing) and INSTALL-02 (graceful
fallback) verification — the automated half of the test map; the visual/keyboard half is
the phase-gate on-hardware UAT.
Output: `cmd/villa/install_wizard_test.go` (new) + the `wizardCalls` counter and default
wizard/stdoutIsTTY stubs in `cmd/villa/install_test.go`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-guided-tui-install-capstone/17-RESEARCH.md
@.planning/phases/17-guided-tui-install-capstone/17-VALIDATION.md
@.planning/phases/17-guided-tui-install-capstone/17-PATTERNS.md
@cmd/villa/install.go
@cmd/villa/install_wizard.go
@cmd/villa/install_test.go
</context>

<artifacts_this_phase_produces>
This plan creates (phase-17 capstone, Plan 03 slice):
- **New file `cmd/villa/install_wizard_test.go`:** `TestInstallWizardFires`, `TestInstallGateBypassesWizard` (--no-tui/--json/non-TTY), `TestWizardConfigMatchesFlagPath`, `TestInstallWizardPathRunsGateOnce` (BLOCK-gap + privileged consent through the LIVE composition: consent-granted → seam ≤1, preserved verdict; consent-denied → seam 0, exit blocked), `TestInstallWizardAccessibleDriver`, `TestSafeAutoFixReturnsFalseForPrivilegedFixes`.
- **Modified `cmd/villa/install_test.go`:** `fakeInstallDeps.wizardCalls int` counter + a default `wizard` stub returning a canned `wizardResult` and a default `stdoutIsTTY` stub (false), so existing flag-path tests stay deterministic.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: fakeInstallDeps wizard wiring + wizard-fires/bypass + config-match tests</name>
  <files>cmd/villa/install_test.go, cmd/villa/install_wizard_test.go</files>
  <read_first>
    - cmd/villa/install_test.go (lines 27-144 fakeInstallDeps + newFakeInstallDeps; saveConfig→savedCfg capture; the setsebool/enableLinger seam stubs + any existing call counters; installTestCmd; assertion patterns; the existing gap tests `TestInstallBlockWithoutConsentExits1`/`TestInstallConsentYesRunsSeamOncePerGap`/`TestInstallWarnLingerOfferGoesToStdout`/`TestInstallConsentNoBlocksAndNeverRunsSeam` — must stay green)
    - cmd/villa/install.go (installOpts noTUI, installDeps wizard/stdoutIsTTY seams, runInstall post-runChecks wizard branch + threaded consentDecisions, gateInstall/resolveGap/offerNonBlockingGap consents param from Plan 02)
    - cmd/villa/install_wizard.go (wizardInput/wizardResult shapes)
    - .planning/phases/17-guided-tui-install-capstone/17-VALIDATION.md (Per-Task Verification Map + Observable signals 1-3)
    - .planning/phases/17-guided-tui-install-capstone/17-PATTERNS.md (`cmd/villa/install_wizard_test.go` section: fake-deps embedding + counters, drive runInstall + assert exit/counts, the wizard==flag-path config test)
  </read_first>
  <action>
    In `cmd/villa/install_test.go`: update the `fakeInstallDeps.pick` stub to the WIDENED two-arg signature from Plan 02 — `d.pick = func(detect.HostProfile, recommend.Overrides) recommend.Recommendation { ... }` (the existing canned recommendation body is unchanged; the new `recommend.Overrides` param is accepted and, for the fake, may be ignored or used to swap the returned model when a `TestWizardConfigMatchesFlagPath`-style override case needs it). Without this signature update the package will not compile. Add `wizardCalls int` to `fakeInstallDeps`, and in `newFakeInstallDeps` default the two new seams so existing tests are deterministic: `d.wizard = func(context.Context, wizardInput) (wizardResult, error) { f.wizardCalls++; return wizardResult{}, nil }` (canned, no override, nil consent map) and `d.stdoutIsTTY = func() bool { return false }` (default OFF so existing flag-path tests keep taking the flag path and stay green). Verify the existing flag-path tests still pass unchanged (flag path threads a nil consents map into gateInstall, so resolveGap/offerNonBlockingGap still prompt via `d.consent` exactly as today). Create `cmd/villa/install_wizard_test.go`. `TestInstallWizardFires`: build a fake with `interactive`→true and `stdoutIsTTY`→true, run `runInstall(cmd, installOpts{}, f.installDeps)` (no --json/--no-tui), assert exit `exitPass` and `f.wizardCalls == 1`. `TestInstallGateBypassesWizard`: a table of three cases — `installOpts{noTUI:true}`, `installOpts{json:true}`, and `stdoutIsTTY`→false (non-TTY) — each asserting `f.wizardCalls == 0` and that the flag path ran (e.g. `f.saveCalls`/`f.writeCalls` advanced as in the existing happy-path test). `TestWizardConfigMatchesFlagPath`: drive the SAME inputs through both paths (wizard on via a fake `wizard` returning an empty override + nil consent map; and `--no-tui`), capturing `f.savedCfg` each time, and assert the two `config.VillaConfig` values are byte-identical (SC#1/SC#2) — reuse the existing `savedCfg` capture seam. Use the existing `installTestCmd()` + substring/call-count assertion style (there is NO install golden — do not add one).
  </action>
  <verify>
    <automated>go test ./cmd/villa/ -run 'TestInstall|TestWizard'</automated>
  </verify>
  <acceptance_criteria>
    `go test ./cmd/villa/ -run TestInstallWizardFires` passes (wizardCalls==1, exit 0); `go test ./cmd/villa/ -run TestInstallGateBypassesWizard` passes (all three cases wizardCalls==0, flag path ran); `go test ./cmd/villa/ -run TestWizardConfigMatchesFlagPath` passes (savedCfg byte-equal); all pre-existing install tests stay green.
  </acceptance_criteria>
  <done>
    Wizard-fires, three-way bypass, and config-match tests green; existing install tests unregressed.
  </done>
</task>

<task type="auto">
  <name>Task 2: single-gate wizard-path test + huh accessible-mode driver test + safeAutoFix classifier test</name>
  <files>cmd/villa/install_wizard_test.go</files>
  <read_first>
    - cmd/villa/install_test.go (the setsebool/enableLinger seam stubs + how existing tests count privileged-seam invocations, e.g. the call counter `TestInstallConsentYesRunsSeamOncePerGap` asserts on; the preflight CheckResult builder used to inject a BLOCK gap)
    - cmd/villa/install.go (runInstall wizard branch threading res.consentDecisions; gateInstall/resolveGap consuming the consents map; safeAutoFix classifier from Plan 02)
    - cmd/villa/install_wizard.go (buildWizardForm signature + the bound chosen/doInstall vars; wizardResult.consentDecisions shape)
    - .planning/phases/17-guided-tui-install-capstone/17-RESEARCH.md ("Pattern 2: Testable wizard runner seam", "Note on composition", "Pitfall 1: huh writes to STDERR by default", "Pitfall 2: accessible mode ≠ interactive keystrokes", "D-05 Detailed Finding", "Open Questions (RESOLVED)")
    - .planning/phases/17-guided-tui-install-capstone/17-UI-SPEC.md ("Preflight Gap States" — BLOCK-with-privileged-fix row)
    - .planning/phases/17-guided-tui-install-capstone/17-PATTERNS.md (huh accessible-mode driver note)
  </read_first>
  <action>
    In `cmd/villa/install_wizard_test.go` add three tests.

    `TestInstallWizardPathRunsGateOnce` — the single-gate / consent-threading guard (Blocker 3). Drive `runInstall` on the WIZARD path through the LIVE composition (NOT a fake wizard that returns an empty result): build a `fakeInstallDeps` with `interactive`→true, `stdoutIsTTY`→true, and `runChecks` returning a single BLOCK-tier gap that maps to a privileged fix (use the existing PRE-05/setsebool gap the current consent tests use, so `runGapFix`→`d.setsebool`). For the wizard seam, inject a fake `wizard` that simulates the real collector's OUTPUT for this scenario (the live form needs a TTY, so the seam stands in for the huh run while the REST of the live composition — the single gateInstall consuming the threaded map — runs for real): return `wizardResult{consentDecisions: map[string]bool{"PRE-05": true}}` for the consent-granted case. Drive a counter on the `setsebool` seam (reuse/extend the existing privileged-seam call counter). Assert: (a) `setseboolCalls <= 1` AND `== 1` (the privileged seam ran exactly once, via the single gateInstall→resolveGap→runGapFix path — never twice, proving no double-gate and no wizard-side execution); (b) the exit code is the preserved 0/2/1 verdict for a consented-and-applied BLOCK gap (the same code the flag-path `TestInstallConsentYesRunsSeamOncePerGap` asserts — proving D-06's contract is intact on the wizard path). Add a second sub-case (consent DENIED): the fake wizard returns `wizardResult{consentDecisions: map[string]bool{"PRE-05": false}}`; assert `setseboolCalls == 0` (the privileged seam was NOT invoked — D-04: never silently run a privileged command) AND the exit is `exitBlocked` (BLOCK gap unmet, no --force, no mutation). Note: the gate consumes the threaded map WITHOUT calling `d.consent` (huh already consumed stdin) — so set `d.consent` to a fail-the-test stub in this test to prove the threaded path never re-prompts.

    `TestInstallWizardAccessibleDriver` — drive the REAL form (not the fake seam) off-hardware: call `buildWizardForm(...)` with pointers to a `chosen string`, a `consents` map, and a `doInstall bool`, then `.WithInput(strings.NewReader(scriptedAnswers)).WithOutput(io.Discard).WithAccessible(true)` and `form.Run()`; feed line-based accessible-mode answers (option index/value + `\n`, NOT raw ANSI arrows — Pitfall 2) that select a known alternative model, answer any privileged-gap confirm, and answer the Install confirm, then assert `chosen`/`consents`/`doInstall` hold the scripted selections. Use `WithOutput(io.Discard)` because huh renders to STDERR by default (Pitfall 1).

    `TestSafeAutoFixReturnsFalseForPrivilegedFixes` — assert `safeAutoFix("PRE-03") == false` and `safeAutoFix("PRE-05") == false` (D-05 interpretation 1 — both privileged, [ASSUMED]; this test pins the conservative classification so a future reclassification is a deliberate, test-visible change).

    Keep all assertions on bound variables / return values / seam counters — no golden, no live TTY. Note huh accessible-mode drives the wizard's confirm prompts in the accessible-driver test.
  </action>
  <verify>
    <automated>go test ./cmd/villa/ -run 'TestInstallWizardPathRunsGateOnce|TestInstallWizardAccessibleDriver|TestSafeAutoFix' && make check</automated>
  </verify>
  <acceptance_criteria>
    `go test ./cmd/villa/ -run TestInstallWizardPathRunsGateOnce` passes: consent-granted → privileged seam invoked exactly once (`setseboolCalls == 1`, ≤1 proven) + preserved 0/2/1 verdict; consent-denied → `setseboolCalls == 0` + `exitBlocked`; the `d.consent` stub is never called on the threaded path. `go test ./cmd/villa/ -run TestInstallWizardAccessibleDriver` passes (scripted `chosen`/`consents`/`doInstall` bound via accessible mode, no TTY, no hang). `go test ./cmd/villa/ -run TestSafeAutoFix` passes (both PRE-03/PRE-05 return false). `make check` passes; `CGO_ENABLED=0 go build ./cmd/villa` exits 0.
  </acceptance_criteria>
  <done>
    Single-gate wizard-path test proves the privileged seam runs at most once (zero on denial) with the preserved verdict and no stdin re-prompt; accessible-mode driver test and safeAutoFix classifier test green; full suite + static build green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test input → wizard form | Scripted accessible-mode input exercises the form off-hardware (no privilege) |
| threaded consent → gateInstall (test) | The single-gate test drives the live gateInstall consuming a threaded consent map (no live privilege; the privileged seam is a counted stub) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01 | Elevation of Privilege | privileged-fix consent (regression guard) | mitigate | `TestSafeAutoFixReturnsFalseForPrivilegedFixes` + `TestInstallWizardPathRunsGateOnce` (consent-denied → seam 0) + the preserved existing consent tests assert PRE-03/PRE-05 never auto-run and run at most once; this plan's tests are the regression guard for D-04. |
| T-17-04 | Tampering (contract erosion) | seam grep gate | accept | No new literals introduced in test code; `TestSeamGrepGate` already covers `cmd/villa` and is run per-wave. Tests assert behavior, add no backend literal. |
| T-17-06 | Repudiation / silent regression | flag-path behavior change (D-05) | mitigate | Tests assert existing flag-path tests stay green AND the config-match test proves both paths converge — a silent divergence fails the suite. |
| T-17-07 | Repudiation / double-execution | single-gate consent threading | mitigate | `TestInstallWizardPathRunsGateOnce` asserts the privileged seam runs EXACTLY once (≤1) on the wizard path and that `d.consent` is never re-invoked on the threaded path — proving the gate runs once and consent is honored without a second prompt/execution. |
</threat_model>

<verification>
- `go test ./cmd/villa/` passes (all new + existing tests green).
- `make check` passes; `CGO_ENABLED=0 go build ./cmd/villa` exits 0.
- `TestSeamGrepGate` green.
- Observable signals 1-5 from VALIDATION.md are now backed by automated tests (signal 6 — BLOCK exit — covered by the preserved `TestInstallBlockWithoutConsentExits1` AND the consent-denied sub-case of `TestInstallWizardPathRunsGateOnce`).
</verification>

<success_criteria>
- The wizard fires on a TTY and is bypassed by --no-tui/--json/non-TTY (SC#3, automated).
- Wizard- and flag-path config.toml are byte-identical for identical inputs (SC#1/SC#2, automated).
- The wizard-path privileged fix runs AT MOST ONCE via the single gate (zero on denial), with the preserved 0/2/1 verdict and no stdin re-prompt (D-04/D-06, automated).
- The live huh form drives correctly in accessible mode off-hardware.
- safeAutoFix returns false for both current privileged fixes (D-05/D-04).
</success_criteria>

<output>
Create `.planning/phases/17-guided-tui-install-capstone/17-03-SUMMARY.md` when done
</output>
</content>
</invoke>
