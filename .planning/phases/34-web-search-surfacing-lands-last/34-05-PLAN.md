---
phase: 34-web-search-surfacing-lands-last
plan: 05
type: execute
wave: 2
depends_on: ["34-03"]
files_modified:
  - internal/dashboard/assets/dashboard.html
  - internal/dashboard/assets/dashboard.js
  - internal/dashboard/assets/dashboard.css
autonomous: false
requirements: [SURF-05]
must_haves:
  truths:
    - "The dashboard gains a hidden-until-data Web Search panel that reveals only when report.web_search is present"
    - "A web-search-OFF dashboard renders pixel-identical to v1.4 (panel stays hidden; no new fetch/endpoint/probe)"
    - "Every server/web-derived value in the panel renders via textContent — NEVER innerHTML — especially the row-7 query/URL provenance"
    - "The outbound-bounded row is green ONLY for a real recent verify PASS; stale/absent → gray 'unavailable', never green/red"
    - "Absent fields (guard counters, last_query, fetched URLs) OMIT their row — no fabricated zeros"
  artifacts:
    - path: "internal/dashboard/assets/dashboard.html"
      provides: "#web-search-panel section after #agent-panel, ships hidden"
      contains: "web-search-panel"
    - path: "internal/dashboard/assets/dashboard.js"
      provides: "renderWebSearch(report) cloning renderAgent, called after renderAgent in poll()"
      contains: "renderWebSearch"
  key_links:
    - from: "internal/dashboard/assets/dashboard.js"
      to: "internal/status/status.go"
      via: "renderWebSearch reads report.web_search off the existing /api/status poll (no new fetch)"
      pattern: "report.web_search"
---

<objective>
Add the hidden-until-data, XSS-safe Web Search panel to the embedded control dashboard (SURF-05), binding the approved 34-UI-SPEC verbatim. It clones `#agent-panel` / `renderAgent` exactly, reads `report.web_search` off the EXISTING `/api/status` poll (zero new fetch/endpoint/probe — CTRL-02/D-03), and renders EVERY server/web-derived value via `textContent` — the row-7 query/URL provenance is the most attacker-influenceable surface in any panel. Tri-state outbound indicator mirrors the status core: bounded→green, not-bounded→amber, stale/absent→gray "unavailable". Absent fields omit their row.

Purpose: surface the proven web-search state visually with no false-green and no XSS exposure; a web-search-off dashboard stays pixel-identical to v1.4.
Output: one HTML section + renderWebSearch JS + (ideally zero) CSS, behind a human-verify checkpoint (visual + XSS + dashboard-restart).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-web-search-surfacing-lands-last/34-UI-SPEC.md
@.planning/phases/34-web-search-surfacing-lands-last/34-PATTERNS.md
@.planning/phases/34-web-search-surfacing-lands-last/34-03-SUMMARY.md
</context>

<artifacts_produced>
- `#web-search-panel` section in dashboard.html (after #agent-panel, ships `hidden`) with `#web-search-body`
- `var webSearchPanel` / `var webSearchBody` (beside memoryPanel/agentPanel at dashboard.js:21-23)
- `function renderWebSearch(report)` (clones renderAgent shape), called from poll()'s `.then` AFTER `renderAgent(report)` (dashboard.js:848), NOT from `.catch`
- Tri-state outbound mapping + the UI-SPEC copy (captions, row labels, empty state)
- ZERO new CSS classes is the goal (reuse existing tokens/classes); add a class only if unavoidable, within the existing token vocabulary
- ACCEPTED SCOPE LIMIT (state in SUMMARY): guard-counter rows / last_query / fetched-URL rows render ONLY if the status core ships those fields; since the status core (Plan 03) omits them (no host source), these rows never appear — the renderer must OMIT-when-absent, never fabricate.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Add the hidden Web Search panel (HTML) + renderWebSearch (JS), bound to the UI-SPEC verbatim</name>
  <files>internal/dashboard/assets/dashboard.html, internal/dashboard/assets/dashboard.js, internal/dashboard/assets/dashboard.css</files>
  <read_first>
    - internal/dashboard/assets/dashboard.html:64-77 (#memory-panel + #agent-panel chrome — add #web-search-panel AFTER #agent-panel, mirroring the section/h2/body shape, ships hidden)
    - internal/dashboard/assets/dashboard.js:21-23 (memoryPanel/agentPanel var declarations — add webSearchPanel/webSearchBody beside them), 55-87 (mutedP/metricRow/groupThousands helpers to reuse), 276-299 (memoryBadgeRow helper — the tri-state badge row), 301-367 (renderMemory empty-state shape: model-empty-heading + mutedP), 369-438 (renderAgent — THE clone target: hidden/unhidden/textContent="" reset shape), 820-848 (poll(): the `.then` branch where renderAgent(report) is called at 848 — call renderWebSearch right AFTER), 795 (setConnected(false) stale-dim — renderWebSearch is NOT called on a failed poll so last-good stays)
    - .planning/phases/34-web-search-surfacing-lands-last/34-UI-SPEC.md (the binding contract: Static Shell HTML lines 98-116; Panel Content row-by-row 119-138; Copywriting 142-165; Color honesty rules 87-91; Accessibility 169-175)
    - .planning/phases/34-web-search-surfacing-lands-last/34-PATTERNS.md (dashboard section, lines 216-239)
    - .planning/phases/34-web-search-surfacing-lands-last/34-03-SUMMARY.md (the EXACT web_search JSON keys + tri-state mapping to bind)
  </read_first>
  <action>
    In dashboard.html: add ONE `<section class="panel" id="web-search-panel" aria-labelledby="web-search-heading" hidden>` AFTER #agent-panel (the last panel today), with `<h2 class="panel-heading" id="web-search-heading">Web Search</h2>` and `<div id="web-search-body"></div>`, mirroring the agent-panel chrome and the UI-SPEC comment block verbatim. In dashboard.js: declare `var webSearchPanel = document.getElementById("web-search-panel");` and `var webSearchBody = document.getElementById("web-search-body");` beside the memoryPanel/agentPanel vars. Add `function renderWebSearch(report)` cloning renderAgent's shape exactly: guard `if (!webSearchPanel || !webSearchBody) return;`, `var ws = report && report.web_search; if (!ws) { webSearchPanel.hidden = true; return; }`, then `webSearchPanel.hidden = false; webSearchBody.textContent = "";` and build rows IN THE UI-SPEC ORDER (1 villa-searxng health, 2 villa-websafe health, 3 outbound bounded, 4 verify freshness, 5 guard verdicts, 6 last query, 7 outbound visibility) reusing ONLY mutedP/metricRow/memoryBadgeRow/groupThousands/healthClass/healthLabel. Outbound tri-state: `bounded`→memoryBadgeRow("outbound","bounded","ready") + no caption; `not-bounded`→("outbound","not bounded","warn"); else→("outbound","unavailable","unknown") + a mutedP caption from the UI-SPEC ("Outbound bound not verified — run villa verify search..." or the stale variant). OMIT any row whose field is absent (rows 4-7 are absent today per Plan 03 — they simply don't render; never fabricate 0/"never"). Empty state (web_search present but no activity): model-empty-heading "No searches yet" + mutedP, mirroring renderMemory. Call `renderWebSearch(report);` from poll()'s `.then` IMMEDIATELY AFTER `renderAgent(report);` (NOT from `.catch`). XSS: EVERY server/web-derived value via createElement + textContent — NEVER innerHTML, ESPECIALLY row-7 query/URL provenance. CSS: reuse existing tokens/classes; add a class ONLY if a sub-element genuinely cannot be expressed by existing ones, and then within the existing token vocabulary (goal: zero new classes/tokens).
  </action>
  <verify>
    <automated>grep -c 'innerHTML' internal/dashboard/assets/dashboard.js | head; go test ./internal/dashboard/ -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'web-search-panel' internal/dashboard/assets/dashboard.html` matches and the section ships with the `hidden` attribute, placed AFTER `#agent-panel`.
    - `grep -n 'function renderWebSearch' internal/dashboard/assets/dashboard.js` matches; `renderWebSearch(report)` is called in poll()'s `.then` after `renderAgent(report)` (verify by reading the diff).
    - `grep -c 'report.web_search\|report && report.web_search' internal/dashboard/assets/dashboard.js` is >= 1 (reads the existing poll, no new fetch).
    - The renderWebSearch body adds NO `innerHTML` assignment for any server/web-derived value (verify by reading the diff; the panel uses createElement + textContent only).
    - `go test ./internal/dashboard/ -x` passes (the embed + any server tests stay green).
    - `git diff internal/dashboard/assets/dashboard.css` is empty OR adds only classes within the existing token vocabulary (goal: zero new classes).
  </acceptance_criteria>
  <done>The hidden Web Search panel reveals only on report.web_search, renders the UI-SPEC rows via textContent, maps the outbound tri-state honestly, omits absent rows, and adds no new fetch/endpoint — web-off dashboards stay pixel-identical.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human-verify the Web Search panel (visual, XSS, no-false-green, dashboard restart)</name>
  <files>internal/dashboard/assets/dashboard.html, internal/dashboard/assets/dashboard.js</files>
  <what-built>The hidden-until-data Web Search panel in the embedded control dashboard: a #web-search-panel section + renderWebSearch(report) reading report.web_search off the existing /api/status poll, with a tri-state outbound-bounded indicator (green=bounded / amber=not-bounded / gray=unavailable), dedicated villa-searxng/villa-websafe health rows, and omit-when-absent for source-gap rows. All server/web-derived values render via textContent (XSS-safe).</what-built>
  <action>
    Human-verify on the live dashboard (this is a blocking human-verify checkpoint; perform these manual steps, do not auto-approve):
    1. Rebuild and RESTART the dashboard (mandatory — the dashboard service serves embedded assets from the running binary): `make build` then `systemctl --user restart villa-dashboard.service`.
    2. WEB-OFF check: with web search disabled in config.toml, open the dashboard (http://127.0.0.1:8888 by default) and confirm the Web Search panel is ABSENT (hidden) — the page is pixel-identical to before. Also confirm `villa status --json` shows NO `web_search` key (only `schema_version: 5`).
    3. WEB-ON check: enable web search, restart the stack, refresh the dashboard. Confirm the Web Search panel appears with villa-searxng / villa-websafe health rows and an "outbound" row.
    4. OUTBOUND-BOUNDED honesty: with NO recent `villa verify search` PASS, confirm the outbound row shows GRAY "unavailable" + the "run villa verify search" caption — NOT green. Then run `villa verify search` (on-hardware); after a PASS, refresh and confirm the row shows GREEN "bounded" with the egress-checked timestamp. Confirm a stale result (older than the freshness window) shows gray "unavailable" + the stale caption, never green.
    5. OMIT-WHEN-ABSENT: confirm NO guard-counter rows, NO "last query", and NO fetched-URL rows appear (these have no host source by design) — and crucially NO fabricated zeros.
    6. XSS spot-check: confirm (via DevTools) the panel values are set as text nodes, not parsed HTML (no element injection from any server value).
  </action>
  <verify>
    <human-check>Operator confirms: web-off pixel-identical (no panel, no web_search key); web-on panel present with searxng/websafe rows; outbound green ONLY after a real recent verify PASS (gray "unavailable" otherwise, never false-green); no fabricated guard/last-query/fetched rows; all panel values are text nodes (no innerHTML).</human-check>
  </verify>
  <done>The operator has verified the panel on the live dashboard and typed "approved" (or all reported issues are resolved): web-off pixel-identity, web-on rows, honest tri-state with no false-green, omit-when-absent, and textContent-only XSS-safety all confirmed.</done>
  <resume-signal>Type "approved" or describe issues (panel placement, tri-state color, false-green, a fabricated row, or any innerHTML usage).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| web-derived provenance (search queries / fetched URLs) → dashboard DOM | the most attacker-influenceable surface in any panel; a markdown/HTML payload in a fetched URL or query must NOT execute |
| cached verify result → outbound indicator | a stale/absent result must render gray, never a false-green "bounded" security claim |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-16 | Tampering / Elevation (reflected XSS) | row-7 query/URL provenance (and all panel values) | mitigate | createElement + textContent for EVERY server/web-derived value; innerHTML forbidden; the panel surfaces more untrusted data than any existing panel so this is non-negotiable; checkpoint step 6 spot-checks it |
| T-34-17 | Spoofing (of a security property) | outbound-bounded indicator | mitigate | Green ONLY for a real recent verify PASS (mirrors the status core tri-state); stale/absent → gray "unavailable", never green/red; checkpoint step 4 proves the no-false-green path |
| T-34-18 | Information Disclosure (privacy) | fabricated last-query / fetched-URL rows | mitigate | Omit-when-absent (no host query/URL log exists or is built); never render a fabricated value; checkpoint step 5 confirms no such rows |
| T-34-19 | Tampering | the off-render pixel-identity | mitigate | Panel ships hidden; re-hidden when report.web_search disappears; no new fetch/endpoint/probe — web-off dashboard is pixel-identical to v1.4 |
</threat_model>

<verification>
- `go test ./internal/dashboard/ -x` is green; `make check` is green.
- Manual checkpoint (above) passes on the live dashboard: web-off pixel-identical; web-on panel present; outbound tri-state honest; omit-when-absent; textContent-only.
- Reminder: dashboard JS/HTML changes require `systemctl --user restart villa-dashboard.service` after `make build` (CLAUDE.md GOTCHA).
</verification>

<success_criteria>
- Hidden-until-data Web Search panel, pixel-identical when off, no new fetch/endpoint.
- Tri-state outbound indicator honest (green only for real recent verify PASS); source-gap rows omitted; textContent-only XSS-safety.
- Human-verify checkpoint approved.
</success_criteria>

<output>
Create `.planning/phases/34-web-search-surfacing-lands-last/34-05-SUMMARY.md` when done. Record the rendered rows, the tri-state mapping, the omitted rows (accepted scope limits), and confirmation that no new CSS class/token was introduced (or justify any that was).
</output>
