---
phase: 34-web-search-surfacing-lands-last
plan: 04
type: execute
wave: 2
depends_on: ["34-01"]
files_modified:
  - internal/doctor/doctor.go
  - internal/doctor/doctor_test.go
  - cmd/villa/doctor.go
  - cmd/villa/doctor_test.go
  - cmd/villa/testdata/doctor.json.golden
autonomous: true
requirements: [SURF-06]
must_haves:
  truths:
    - "`villa doctor` folds web-search findings on doctor's OWN schema bump (reportSchemaVersion 2→3, independent of status's 4→5)"
    - "Web-search-OFF doctor output is byte-identical EXCEPT the schema bump (nil seams emit no findings)"
    - "The egress-proof finding reads the cached verify result as tri-state (ready / degraded-with-reason / typed-Unknown)"
    - "The residency-under-search-load check is offload-asserting — a confident CPU fallback under load is a FAIL, not-in-flight is typed-Unknown, never an idle-sampled false-green"
    - "Every non-PASS web-search finding carries a Remediation"
  artifacts:
    - path: "internal/doctor/doctor.go"
      provides: "nil-safe web-search fold (searchEgressFinding, searchResidencyFinding, searxng/websafe readiness) + reportSchemaVersion 2→3"
      contains: "reportSchemaVersion = 3"
    - path: "cmd/villa/doctor.go"
      provides: "runSearchResidencyUnderLoad (clone runAgentResidencyUnderLoad) + SearchEgressProof live seam over the cached verify result"
      contains: "runSearchResidencyUnderLoad"
  key_links:
    - from: "cmd/villa/doctor.go"
      to: "internal/verifystate/store.go"
      via: "SearchEgressProof live seam reads the cached verify result (verifystate.Load)"
      pattern: "verifystate"
    - from: "cmd/villa/doctor.go"
      to: "internal/inference/running_offload.go"
      via: "runSearchResidencyUnderLoad samples inference.RunningOffloadVerdict over the liveStatusDeps input set"
      pattern: "RunningOffloadVerdict"
---

<objective>
Fold web-search checks into `villa doctor` on doctor's OWN schema bump (`reportSchemaVersion` 2→3, INDEPENDENT of status's 4→5): searxng/websafe service readiness, egress-proof status (read the cached `villa verify search` result from Plan 01), and an OFFLOAD-ASSERTING chat-model-GPU-resident check UNDER SEARCH LOAD. Tri-state (ready / degraded-with-reason / typed-Unknown) with remediation on EVERY non-PASS. Guard-health is a documented OMISSION (no host source). Web-off doctor output stays byte-identical except the bump (nil seams emit no findings).

Purpose: doctor is the operator's diagnostic; the residency-under-load check is the project's offload-asserting invariant applied to the search path — a silent CPU fallback under search load is a FAIL, never a false-green behind an HTTP-200.
Output: doctor core fold + live residency proof + egress-proof seam + single doctor golden re-freeze.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-web-search-surfacing-lands-last/34-PATTERNS.md
@.planning/phases/34-web-search-surfacing-lands-last/34-RESEARCH.md
@.planning/phases/34-web-search-surfacing-lands-last/34-01-SUMMARY.md
</context>

<artifacts_produced>
- `const reportSchemaVersion = 3` (was 2, internal/doctor/doctor.go — doctor's OWN version, do NOT conflate with status's)
- doctor.Deps additions (nil-safe): `SearchEgressProof func() <tri-state finding input>` (reads cached verify result), `SearchResidencyUnderLoad func() inference.Verdict` (consumed OPAQUELY)
- `searchEgressFinding(...)` + `searchResidencyFinding(inference.Verdict)` finding builders (clone agentResidencyFinding shape)
- searxng/websafe readiness findings (folded from the status read-model rows or preflight-style checks)
- `runSearchResidencyUnderLoad(cfg, sd)` in cmd/villa/doctor.go (clone runAgentResidencyUnderLoad)
- doctor check IDs / finding names: at executor discretion within the PASS/WARN/FAIL grammar; cite SURF-06 in doc comments
- ACCEPTED SCOPE LIMIT (state in SUMMARY): guard health has NO host-side source → OMIT (no finding). Building a guard counter/health pipeline = NEW behavior = OUT OF SCOPE.
- DEFINED SEARCH WORKLOAD (Open Q2 resolution): document the drive chosen (recommend the cheapest honest drive that keeps villa-llama decoding — a bounded chat completion while villa-searxng/villa-websafe are up); precondition gate = villa-llama + villa-searxng + villa-websafe active, else typed-Unknown.
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: doctor core web-search fold + reportSchemaVersion 2→3 (nil-safe, remediation-on-every-non-PASS)</name>
  <files>internal/doctor/doctor.go, internal/doctor/doctor_test.go</files>
  <read_first>
    - internal/doctor/doctor.go:52-61 (`reportSchemaVersion = 2` — doctor's OWN version, distinct from status; bump to 3), 89-102 (Finding.Remediation — D-11, empty on PASS, actionable on non-PASS), 159-181 (the OPAQUE inference.Verdict + nil-safe Deps doc), 304-318 (the agent fold: nil-safe AgentToolCall/AgentResidencyUnderLoad/AgentDrift append pattern), 437-485 (healthFinding / offloadFinding builders — the Status/Detail/Remediation mapping to clone), 411 (SchemaVersion stamp in the Report builder)
    - internal/verifystate/store.go (Plan 01: State{Verdict,CheckedAt} fail-closed Load) — see 34-01-SUMMARY.md
    - internal/status/status.go (the searxng/websafe ServiceStatus rows from Plan 03, if folding readiness from the status read-model) — see 34-03-SUMMARY.md
    - .planning/phases/34-web-search-surfacing-lands-last/34-PATTERNS.md (doctor.go section, lines 157-177)
  </read_first>
  <behavior>
    - reportSchemaVersion is 3 (doctor's own); status's reportSchemaVersion (5) is untouched by this plan.
    - With all web-search seams nil (web off), Aggregate emits NO web-search findings — the report is byte-identical to the v2 output except schema_version.
    - searchEgressFinding maps a cached verify PASS+fresh → PASS (ready), a real recent non-PASS → degraded-with-reason (WARN/FAIL with remediation), stale/absent → typed-Unknown WARN with remediation "run villa verify search".
    - searchResidencyFinding consumes inference.Verdict OPAQUELY (Status/Detail/Remediation only): a CPU-fallback Verdict folds worst-wins and DOMINATES a healthy-looking HTTP-200; an unevaluable/not-in-flight Verdict → typed-Unknown WARN.
    - Every non-PASS web-search finding has a non-empty Remediation.
    - No guard-health finding is emitted (documented omission).
  </behavior>
  <action>
    In internal/doctor/doctor.go: bump `reportSchemaVersion` 2→3 (doctor's OWN const — do NOT touch status's). Add nil-safe web-search seams to doctor.Deps mirroring the agent fold: `SearchEgressProof` (reads the cached verify result → tri-state finding input) and `SearchResidencyUnderLoad func() inference.Verdict`. In Aggregate(), after the existing agent fold (around lines 310-318), append findings guarded by nil checks: `if d.SearchEgressProof != nil { findings = append(findings, searchEgressFinding(d.SearchEgressProof())) }` and `if d.SearchResidencyUnderLoad != nil { findings = append(findings, searchResidencyFinding(d.SearchResidencyUnderLoad())) }`, plus searxng/websafe readiness findings folded from the status read-model rows (clone healthFinding). Implement searchEgressFinding (tri-state per the behavior block; remediation on every non-PASS) and searchResidencyFinding (clone offloadFinding/agentResidencyFinding — read inference.Verdict Status/Detail/Remediation ONLY, seam-clean; worst-wins FAIL dominates). Do NOT emit a guard-health finding (no host source — accepted scope limit; cite the omission in a doc comment with SURF-06). Add subtests to doctor_test.go: `TestAggregateWebSearch` (web-off → no findings, byte-identical except bump; web-on → findings present), `TestSearchResidencyFinding` (CPU-fallback Verdict → FAIL dominating; not-in-flight → Unknown), `TestWebSearchFindingsHaveRemediation` (every non-PASS web-search finding carries a Remediation).
  </action>
  <verify>
    <automated>go test ./internal/doctor/ -run 'TestAggregateWebSearch|TestSearchResidencyFinding|TestWebSearchFindingsHaveRemediation' -x</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/doctor/ -run 'TestAggregateWebSearch|TestSearchResidencyFinding|TestWebSearchFindingsHaveRemediation' -x` passes.
    - `grep -v '^//' internal/doctor/doctor.go | grep -c 'reportSchemaVersion = 3'` returns 1 (and no non-comment `= 2`).
    - `TestWebSearchFindingsHaveRemediation` asserts a non-empty Remediation for every non-PASS web-search finding.
    - `TestSearchResidencyFinding` includes a case proving a confident CPU-fallback Verdict produces a FAIL that is NOT masked by a healthy HTTP-200.
    - No `inference` backend marker literal (`ROCm0`/`Vulkan0`/`HSA_OVERRIDE`) appears in doctor.go (`grep -c 'ROCm0\|Vulkan0\|HSA_OVERRIDE' internal/doctor/doctor.go` returns 0).
  </acceptance_criteria>
  <done>doctor is at schema 3 with nil-safe web-search findings; egress-proof is tri-state from the cached verify; residency is offload-asserting; every non-PASS carries remediation; guard health is the documented omission.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Live residency-under-search-load proof + egress-proof seam + doctor golden re-freeze</name>
  <files>cmd/villa/doctor.go, cmd/villa/doctor_test.go, cmd/villa/testdata/doctor.json.golden</files>
  <read_first>
    - cmd/villa/doctor.go:168-366 (liveDoctorDeps + runResidencyUnderLoad — the drive→settle→sample-if-in-flight→join shape, RunningOffloadVerdict over the liveStatusDeps input set at 489), 557-560 (agentUnevaluable — the typed-Unknown WARN builder for unmet preconditions), 607-718 (runAgentResidencyUnderLoad — THE clone target: precondition gate at 626-652, in-flight sampling discipline at 652+, RunningOffloadVerdict at 694, join at the end, typed-Unknown when no round in flight at 713)
    - internal/inference (BackendFor, RunningOffloadVerdict, RunningOffloadInput fields: JournalText/Props/GTTUsedBytes/WeightBytes/ConfigModel/ConfigContext/Markers, ResidencyProof()) — consume Verdict OPAQUELY; resolve images ONLY via orchestrate.*Image()
    - internal/verifystate/store.go (Plan 01: Load over live read deps → *State) — see 34-01-SUMMARY.md
    - internal/orchestrate/searxng.go:58 + websafe.go:67 (unit-name accessors for the precondition gate / image resolution)
    - cmd/villa/doctor_test.go (the agent-residency drive-seam test to clone; the `-update` doctor golden mechanism)
    - .planning/phases/34-web-search-surfacing-lands-last/34-PATTERNS.md (cmd/villa/doctor.go section, lines 181-193)
  </read_first>
  <behavior>
    - runSearchResidencyUnderLoad gates read-only on preconditions (chat service active + villa-searxng + villa-websafe active; BackendFor + ModelFile resolvable); each unmet precondition → agentUnevaluable typed-Unknown WARN (NOT FAIL), doctor never starts a service.
    - It drives a bounded search-augmented chat workload, samples inference.RunningOffloadVerdict ONLY while a round is verifiably in flight, and JOINs every sampled round.
    - A confident CPU fallback under load → FAIL; no round caught in flight → typed-Unknown WARN (never an idle-sampled verdict).
    - SearchEgressProof live seam reads the cached verify result (verifystate.Load) and maps it to the doctor egress finding input.
    - TestSeamGrepGate stays green: no backend marker/image literal is re-typed in the new seam; images resolved via orchestrate.*Image(), unit names via orchestrate.*ContainerUnitName().
    - The doctor.json.golden is re-frozen ONCE for the 2→3 bump.
  </behavior>
  <action>
    In cmd/villa/doctor.go: clone runAgentResidencyUnderLoad into `runSearchResidencyUnderLoad(cfg, sd)` verbatim in shape, swapping ONLY the drive — pick the cheapest honest drive that keeps villa-llama decoding under search load (a bounded chat completion); precondition gate adds villa-searxng + villa-websafe active (resolved via orchestrate.SearXNGContainerUnitName()/WebsafeContainerUnitName() → unitServiceName, never a literal); each unmet precondition → agentUnevaluable(...). Sample inference.RunningOffloadVerdict over the EXACT liveStatusDeps input set (JournalText/Props/GTTUsedBytes/WeightBytes/ConfigModel/ConfigContext/Markers: backend.ResidencyProof()); consume the Verdict OPAQUELY; resolve any image via orchestrate.*Image(). Wire it into liveDoctorDeps as `SearchResidencyUnderLoad: func() inference.Verdict { return runSearchResidencyUnderLoad(cfg, sd) }`. Add `SearchEgressProof:` wired over a live closure that reads the cached verify result via `verifystate.Load(liveVerifyStateReadDeps())` and maps it to the egress finding input (PASS+fresh / non-PASS / stale-absent — reuse the freshness window value from the status core or a shared constant). Then re-freeze the doctor golden ONCE: `go test ./cmd/villa/ -run TestDoctorJSON -update` (or the exact existing doctor golden test name); verify the diff is the schema_version 2→3 bump (web-off fixture) only, no field reorder. Add a subtest to doctor_test.go cloning the agent-residency drive-seam test for the search drive (CPU-fallback → FAIL; not-in-flight → Unknown; off-hardware → typed-Unknown).
  </action>
  <verify>
    <automated>go test ./cmd/villa/ -run 'TestDoctor|TestSearchResidency' -x && go test ./internal/inference/ -run TestSeamGrepGate -x</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./cmd/villa/ -run 'TestDoctor|TestSearchResidency' -x` passes against the re-frozen doctor golden.
    - `go test ./internal/inference/ -run TestSeamGrepGate -x` passes (no leaked literal in the new seam).
    - `grep -n 'runSearchResidencyUnderLoad' cmd/villa/doctor.go` matches; `grep -c 'ROCm0\|Vulkan0\|HSA_OVERRIDE' cmd/villa/doctor.go` returns 0.
    - `grep -n 'SearXNGContainerUnitName\|WebsafeContainerUnitName' cmd/villa/doctor.go` matches (precondition gate via accessors, not literals).
    - `git diff cmd/villa/testdata/doctor.json.golden` shows ONLY the schema_version 2→3 bump (web-off fixture) — single intentional re-freeze, no field reorder.
  </acceptance_criteria>
  <done>The live search-residency proof is offload-asserting and seam-clean; the egress-proof seam reads the cached verify result; the doctor golden is re-frozen once to schema 3; the seam gate stays green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cached verify file → doctor egress finding | the egress-proof status is a security claim; stale/absent must be typed-Unknown, never a PASS |
| live service under load → residency verdict | a silent CPU fallback under search load must not be masked behind an HTTP-200 health check |
| inference internals → doctor | backend markers/images must not leak into cmd/villa (TestSeamGrepGate) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-12 | Spoofing (of a security property) | doctor egress-proof finding | mitigate | Tri-state from the cached verify result; PASS only for fresh PASS; stale/absent → typed-Unknown WARN with remediation; never a config-bool-derived PASS |
| T-34-13 | Repudiation / false-green | residency-under-search-load | mitigate | Offload-asserting: confident CPU fallback → FAIL dominating HTTP-200; in-flight-only sampling (not-in-flight → typed-Unknown, never an idle-sampled false-green) |
| T-34-14 | Information Disclosure / Elevation | leaked backend marker / image literal in the new seam | mitigate | Consume inference.Verdict opaquely; resolve images via orchestrate.*Image(); unit names via orchestrate.*ContainerUnitName(); TestSeamGrepGate walks cmd/villa |
| T-34-15 | Tampering | doctor's own byte-frozen contract | mitigate | Independent append-only 2→3 bump (not conflated with status's 5); single golden re-freeze; nil seams keep web-off byte-identical except the bump |
</threat_model>

<verification>
- `go test ./internal/doctor/ ./cmd/villa/ ./internal/inference/ -run 'TestAggregateWebSearch|TestSearchResidencyFinding|TestWebSearchFindingsHaveRemediation|TestDoctor|TestSearchResidency|TestSeamGrepGate' -x` is green.
- `make check` is green.
- On the live Strix Halo box, the residency-under-search-load check is exercised (FAIL on confident CPU fallback; PASS when GPU-resident; typed-Unknown when the stack isn't fully up).
</verification>

<success_criteria>
- doctor at schema 3 (independent of status); web-off byte-identical except the bump.
- Egress-proof finding tri-state from the cached verify result; residency offload-asserting; remediation on every non-PASS.
- Seam gate green; single doctor golden re-freeze; guard health is the documented omission.
</success_criteria>

<output>
Create `.planning/phases/34-web-search-surfacing-lands-last/34-04-SUMMARY.md` when done. Record the chosen search-load drive (Open Q2 resolution), the check IDs/finding names, doctor schema 3, and the guard-health omission.
</output>
