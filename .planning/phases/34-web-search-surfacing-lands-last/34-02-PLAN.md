---
phase: 34-web-search-surfacing-lands-last
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/backup/manifest.go
  - internal/backup/backup.go
  - internal/backup/restore.go
  - internal/backup/backup_test.go
  - internal/backup/restore_test.go
  - cmd/villa/backup.go
autonomous: true
requirements: [SURF-07]
must_haves:
  truths:
    - "`villa backup` includes the SearXNG settings.yml provenance entry when web search is on and the file exists"
    - "`villa backup` skips the entry (FileMissing) when web search is off or the file is absent — no error"
    - "`villa restore` re-writes the settings.yml entry preserving 0600 (never widening the mode)"
    - "Fetched ephemeral web content is NOT archived (no query/URL log entry exists)"
  artifacts:
    - path: "internal/backup/manifest.go"
      provides: "EntrySearxngSettings manifest const + backupSchemaVersion 3→4"
      contains: "EntrySearxngSettings"
    - path: "internal/backup/backup.go"
      provides: "SearxngSettingsPath on BackupInput + optional sources entry"
      contains: "SearxngSettingsPath"
    - path: "internal/backup/restore.go"
      provides: "settings.yml restore branch mirroring crush.json, 0600-preserving"
  key_links:
    - from: "cmd/villa/backup.go"
      to: "internal/backup/backup.go"
      via: "cmd tier sets SearxngSettingsPath only when cfg.WebSearchEnabled, resolved from $XDG_CONFIG_HOME/villa/searxng/settings.yml"
      pattern: "SearxngSettingsPath"
---

<objective>
Extend `villa backup`/`restore` to cover the web-search CONFIGURATION — the SearXNG `settings.yml` provenance — as an optional archive entry mirroring the Phase-28 `crush.json` (`EntryCrushConfig`) pattern, on backup's OWN schema bump (`backupSchemaVersion` 3→4). The `WebSearchEnabled` gate is ALREADY captured by `config.toml` (the existing `EntryConfig`), so the NEW entry is the settings.yml provenance only. Fetched ephemeral web content is EXCLUDED by design — add no query/URL log entry.

Purpose: a web-search install must be backup/restore-recoverable consistent with prior backup coverage, without leaking the rendered SEARXNG_SECRET (settings.yml stays 0600 on restore).
Output: one optional backup entry + restore branch + manifest schema bump, all golden-tested.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-web-search-surfacing-lands-last/34-PATTERNS.md
@.planning/phases/34-web-search-surfacing-lands-last/34-RESEARCH.md
</context>

<artifacts_produced>
- `const EntrySearxngSettings = "searxng-settings.yml"` (manifest.go) — the optional entry name
- `const backupSchemaVersion = 4` (was 3, manifest.go)
- `SearxngSettingsPath string` field on `backup.BackupInput`
- `SearxngSettingsDestPath string` (or equivalent) on `restore.RestoreInput` for the restore destination
- Accepted scope limit (state explicitly in SUMMARY): the `WebSearchEnabled` gate is covered by the EXISTING `EntryConfig` (`config.toml` carries `web_search_enabled`); ephemeral fetched content is intentionally NOT archived.
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the optional SearXNG settings.yml backup entry + manifest schema 3→4</name>
  <files>internal/backup/manifest.go, internal/backup/backup.go, internal/backup/backup_test.go</files>
  <read_first>
    - internal/backup/manifest.go:42 (`const backupSchemaVersion = 3` — bump to 4), 58-64 (`EntryCrushConfig = "crush.json"` optional-entry const precedent), 121-225 (manifest stamps its OWN backupSchemaVersion)
    - internal/backup/backup.go:27-80 (BackupInput + CrushConfigPath optional field at 74-80), 214-227 (the `sources` list with the optional `{EntryCrushConfig, in.CrushConfigPath, false}` row), 251/280 (FileMissing-skip for non-required entries)
    - internal/orchestrate/searxng_settings_write.go:40-50 (`searxngSettingsDir()` → $XDG_CONFIG_HOME/villa/searxng — the host dir holding settings.yml), 53-56 (WriteSearxngSettings name+text)
    - .planning/phases/34-web-search-surfacing-lands-last/34-PATTERNS.md (backup section, lines 197-212)
  </read_first>
  <behavior>
    - With SearxngSettingsPath set to an existing file, Backup includes an EntrySearxngSettings entry in the archive + manifest.
    - With SearxngSettingsPath = "" (web off), the entry is absent and Backup returns no error.
    - With SearxngSettingsPath set but the file missing, the entry is FileMissing-skipped (no error), exactly like crush.json.
    - The manifest's SchemaVersion stamps as 4 (the manifest stamps its own version, never a caller-supplied one).
    - No entry is added for fetched/ephemeral web content (negative assertion).
  </behavior>
  <action>
    In internal/backup/manifest.go: add `const EntrySearxngSettings = "searxng-settings.yml"` beside EntryCrushConfig, and bump `backupSchemaVersion` from 3 to 4 (append-only — bump the const once). In internal/backup/backup.go: add a `SearxngSettingsPath string` field to BackupInput (doc-comment it as the OPTIONAL SearXNG settings.yml provenance, web-off passes ""), and append one optional row to the `sources` list mirroring the crush row: `{EntrySearxngSettings, in.SearxngSettingsPath, false}` (required=false → FileMissing-skipped). Do NOT add any entry for fetched/ephemeral web content. In internal/backup/backup_test.go add subtests `TestBackupSearxngSettings` (present-when-set, skipped-when-empty, FileMissing-skipped-when-absent, schema stamps 4) and `TestBackupExcludesEphemeral` (negative: assert no archive entry references a fetched-URL/query log / no ephemeral content key). Every new file/decision carries the SURF-07 ID in its doc comment per the project comment convention.
  </action>
  <verify>
    <automated>go test ./internal/backup/ -run 'TestBackupSearxngSettings|TestBackupExcludesEphemeral' -x</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/backup/ -run 'TestBackupSearxngSettings|TestBackupExcludesEphemeral' -x` passes.
    - `grep -n 'EntrySearxngSettings' internal/backup/manifest.go` matches.
    - `grep -v '^//' internal/backup/manifest.go | grep -c 'backupSchemaVersion = 4'` returns 1 (and `backupSchemaVersion = 3` no longer present in non-comment lines).
    - `grep -n 'SearxngSettingsPath' internal/backup/backup.go` matches.
  </acceptance_criteria>
  <done>Backup adds the optional settings.yml entry when present, skips it when off/absent, stamps manifest schema 4, and archives no ephemeral content.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Restore the settings.yml entry (0600-preserving) + cmd-tier gating</name>
  <files>internal/backup/restore.go, internal/backup/restore_test.go, cmd/villa/backup.go</files>
  <read_first>
    - internal/backup/restore.go:115-148 (CrushConfigDestPath + crushConfig/crushPresent fields), 172-241 (Restore + captureFile of the prior crush.json), 332+ (the crush.json verbatim restore-rows branch, SHA-256-verified, mode-preserving)
    - internal/backup/manifest.go:42 (backupSchemaVersion=4 after Task 1 — restore's <= gate must accept 4)
    - cmd/villa/backup.go:214-223 (how crushConfigPath() is resolved and passed only when present; the cmd-tier gating posture)
    - internal/orchestrate/searxng_settings_write.go:40-50 (searxngSettingsDir() — the host settings.yml dir; the file mode is 0600, searxngSettingsFileMode at line 30-32)
    - .planning/phases/34-web-search-surfacing-lands-last/34-PATTERNS.md (backup section, lines 197-212; restore mirrors crush.json, preserve 0600)
  </read_first>
  <behavior>
    - Restore of an archive containing EntrySearxngSettings re-writes the settings.yml file at its destination with mode 0600 (never widened).
    - Restore of an archive WITHOUT the entry leaves no settings.yml side effect and reports it as not-present (mirror crush absent handling).
    - The restore is SHA-256-verified through the same verification path as crush.json (no unverified write).
    - The cmd tier sets the backup source path (and restore dest path) ONLY when cfg.WebSearchEnabled, resolved from $XDG_CONFIG_HOME/villa/searxng/settings.yml; web-off passes "".
  </behavior>
  <action>
    In internal/backup/restore.go: mirror the crush.json restore branch for EntrySearxngSettings — add a `SearxngSettingsDestPath string` field to RestoreInput, capture the prior file for rollback (captureFile), and on a present entry write it through the SAME SHA-256-verified, mode-preserving path crush uses, forcing 0600 (the entry holds the rendered SEARXNG_SECRET — NEVER widen the mode; mirror searxngSettingsFileMode). Ensure restore's `m.SchemaVersion <= backupSchemaVersion` gate now accepts version 4. In cmd/villa/backup.go: resolve the settings.yml host path from the orchestrate searxng config dir ($XDG_CONFIG_HOME/villa/searxng/settings.yml — add/use an exported accessor if searxngSettingsDir is unexported, mirroring how crushConfigPath() is exposed at the cmd tier) and set `SearxngSettingsPath` (backup) / `SearxngSettingsDestPath` (restore) ONLY when `cfg.WebSearchEnabled`, passing "" otherwise (mirror the crush gating at backup.go:218-223). Add subtests `TestRestoreSearxngSettings` to restore_test.go: present→written 0600, absent→not-present, and a mode-assertion that the restored file is exactly 0600.
  </action>
  <verify>
    <automated>go test ./internal/backup/ -run 'TestRestoreSearxngSettings' -x</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/backup/ -run 'TestRestoreSearxngSettings' -x` passes including the 0600 mode assertion.
    - `grep -n 'SearxngSettingsDestPath' internal/backup/restore.go` matches.
    - `grep -n 'SearxngSettingsPath\|SearxngSettingsDestPath' cmd/villa/backup.go` matches and is gated on cfg.WebSearchEnabled (verify by reading the diff).
    - `go test ./internal/backup/ ./cmd/villa/ -run 'TestBackup|TestRestore' -x` (existing backup/restore tests) stays green.
  </acceptance_criteria>
  <done>Restore re-writes settings.yml 0600-preserving and SHA-256-verified; the cmd tier gates both source and dest paths on WebSearchEnabled; existing backup/restore tests remain green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| backup archive → restored host file | settings.yml holds the rendered SEARXNG_SECRET; restoring it must preserve 0600 and verify integrity |
| operator config → archive | only the web-search CONFIG (settings.yml) crosses; ephemeral fetched content must NOT cross |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-05 | Information Disclosure | settings.yml backup entry (contains SEARXNG_SECRET) | mitigate | Preserve 0600 on archive + restore (crush.json precedent); never widen the mode; the archive itself inherits the existing 0600 backup posture |
| T-34-06 | Information Disclosure (privacy regression) | persisting ephemeral web content / queries | mitigate | Exclude fetched ephemeral content by design (SURF-07); TestBackupExcludesEphemeral asserts no query/URL log entry is added |
| T-34-07 | Tampering | restored settings.yml integrity | mitigate | SHA-256-verified restore through the same path as crush.json (no unverified write); prior-file capture enables verbatim rollback |
</threat_model>

<verification>
- `go test ./internal/backup/ ./cmd/villa/ -run 'TestBackup|TestRestore' -x` is green.
- `make check` is green.
- Manifest schema is 4; restore accepts 4; web-off backup byte-behaviour is unchanged (entry absent).
</verification>

<success_criteria>
- The optional settings.yml entry is present-when-on/absent-when-off, FileMissing-tolerant, schema-4-stamped.
- Restore re-writes it 0600-preserving + integrity-verified.
- No ephemeral fetched content is archived.
</success_criteria>

<output>
Create `.planning/phases/34-web-search-surfacing-lands-last/34-02-SUMMARY.md` when done. Record the EntrySearxngSettings name, the schema-4 bump, and the accepted scope limit (WebSearchEnabled gate covered by EntryConfig; ephemeral content excluded by design).
</output>
