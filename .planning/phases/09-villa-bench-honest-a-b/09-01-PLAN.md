---
phase: 09-villa-bench-honest-a-b
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/llm/openai.go
  - internal/llm/openai_test.go
autonomous: true
requirements: [BENCH-01]
must_haves:
  truths:
    - "A non-streaming /v1/chat/completions request returns the server-computed per-request timings (pp and tg separated)"
    - "Complete reads timings.prompt_per_second and timings.predicted_per_second from the response body (StreamChat discards them)"
    - "Complete enforces fixed-params: max_tokens (n_predict), seed, temperature on the wire request"
  artifacts:
    - path: "internal/llm/openai.go"
      provides: "OpenAIClient.Complete non-streaming method + Timings type"
      contains: "func (c *OpenAIClient) Complete"
    - path: "internal/llm/openai_test.go"
      provides: "httptest fixture asserting Complete parses the timings block"
      contains: "timings"
  key_links:
    - from: "internal/llm/openai.go Complete"
      to: "/v1/chat/completions"
      via: "non-streaming POST (stream:false), parse top-level timings object"
      pattern: "chat/completions"
---

<objective>
Add a non-streaming `Complete` method to `internal/llm.OpenAIClient` — the honest measurement leaf for Phase 9. `StreamChat` forwards content deltas and discards the per-request `timings` block; `Complete` does a `stream:false` POST and returns the server-computed `timings` (prompt-processing and token-generation rates already separated, scoped to exactly this request). This is the measurement source the bench core (Plan 02) and the live wiring (Plan 03) read pp/tg from.

Purpose: Capture the per-request `timings` block as the honest, per-run, pp/tg-separated throughput source — never the `/metrics` last-window averages (which smear warmup).
Output: `internal/llm.OpenAIClient.Complete` + `llm.Timings` type + a fixture test proving `timings` parsing.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-villa-bench-honest-a-b/09-RESEARCH.md
@.planning/phases/09-villa-bench-honest-a-b/09-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add OpenAIClient.Complete non-streaming method + Timings type</name>
  <files>internal/llm/openai.go, internal/llm/openai_test.go</files>
  <read_first>
    - internal/llm/openai.go (the file being modified — clone StreamChat's request-build/auth/bounded-error scaffolding at lines 55-95; reuse wireRequest at lines 40-44)
    - internal/llm/llm.go (ChatRequest/Message/Options types — Complete's input)
    - .planning/phases/09-villa-bench-honest-a-b/09-RESEARCH.md (Pattern 2: the timings field names; Code Examples section)
    - .planning/phases/09-villa-bench-honest-a-b/09-PATTERNS.md (the `internal/llm/openai.go — NEW Complete method` section, lines 161-186)
  </read_first>
  <behavior>
    - Test 1 (TestCompleteParsesTimings): an httptest server returning a JSON body with a top-level `timings` object yields a Timings whose PromptPerSecond and PredictedPerSec equal the fixture values, and PromptN/PredictedN are populated.
    - Test 2 (params on wire): the request body the server receives has stream=false, the supplied max_tokens (n_predict), seed, and temperature.
    - Test 3 (non-200 bounded error): a 500 response returns a wrapped error with a bounded (io.LimitReader) snippet, no panic.
    - Test 4 (no-model error): empty req.Model with no default returns the same "no model specified" error StreamChat returns.
  </behavior>
  <action>
    In `internal/llm/openai.go`, add a `Timings` struct with json-tagged fields `PromptN`(`prompt_n`), `PromptMS`(`prompt_ms`), `PromptPerSecond`(`prompt_per_second`), `PredictedN`(`predicted_n`), `PredictedMS`(`predicted_ms`), `PredictedPerSec`(`predicted_per_second`) — all matching the llama.cpp `/v1` extension field names from RESEARCH Pattern 2.

    Add a non-streaming wire request type (extend the existing `wireRequest` shape or add a sibling) carrying `Model`, `Messages`, `Stream:false`, plus `MaxTokens int` (`max_tokens`), `Seed int` (`seed`), `Temperature float64` (`temperature`). Add a non-streaming response struct with at least a top-level `Timings Timings` (`json:"timings"`) field; other choices fields are not needed by bench.

    Add method `func (c *OpenAIClient) Complete(ctx context.Context, req ChatRequest, nPredict int, seed int, temp float64) (Timings, error)`. Mirror StreamChat's model-fallback + empty-messages guards (openai.go:57-66), build the body with `stream:false` and the fixed params, POST to `c.baseURL+"/chat/completions"`, set `Content-Type: application/json` and the `Authorization: Bearer` header when apiKey is non-empty, set `Accept: application/json` (not text/event-stream). On non-200, return the bounded `io.LimitReader(resp.Body, 2048)` error exactly as StreamChat does (openai.go:89-92). On success, `json.Unmarshal`/`json.NewDecoder` the body and return the parsed `Timings`.

    Keep `Complete` literal-free of backend markers (no device/image/HSA/ROCm0 tokens) — `internal/llm` is inside the `TestSeamGrepGate` internalRoot walk; Complete only knows `/v1`. Per CLAUDE.md: zero new deps — stdlib `encoding/json`/`net/http`/`io` only.
  </action>
  <verify>
    <automated>go test ./internal/llm/ -run TestComplete -count=1 && go vet ./internal/llm/</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/llm/ -run TestComplete -count=1` passes (timings-parse + params-on-wire + bounded-error + no-model cases).
    - `go test ./internal/inference/ -run TestSeamGrepGate -count=1` stays green (Complete introduces no backend-marker literal).
    - `go build ./... && go vet ./...` clean; `go.mod` unchanged (no new dependency).
    - Source assertion: `internal/llm/openai.go` defines `func (c *OpenAIClient) Complete(` and a `Timings` type with the six llama.cpp timing fields.
  </acceptance_criteria>
  <done>Complete drives a non-streaming /v1 completion with fixed params and returns the parsed per-request timings; the fixture test proves pp/tg are read from the response body; the seam gate and full vet stay green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| villa control plane → llama-server /v1 (loopback 127.0.0.1) | bench client issues a non-streaming completion to the already-audited local inference endpoint |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Information disclosure | Complete response parsing | mitigate | Complete unmarshals ONLY the numeric `timings` fields into the typed struct — it never reads/returns the prompt or sampling params into a report (mirrors metrics.Slot field-set discipline). |
| T-09-02 | Denial of service | unbounded body from a wedged server | mitigate | non-200 path is bounded by `io.LimitReader(resp.Body, 2048)` (StreamChat pattern); the per-run timeout that bounds a wedged 200 lives in Plan 03's run context. |
| T-09-SC | Tampering | npm/pip/cargo installs | accept | No package installs — stdlib + existing repo packages only (RESEARCH Package Legitimacy Audit: none). |
</threat_model>

<verification>
- `go test ./internal/llm/ -run TestComplete -count=1` green.
- `go test ./internal/inference/ -run TestSeamGrepGate -count=1` green (no marker leak).
- `go build ./... && go vet ./...` clean; `go.mod` byte-unchanged.
</verification>

<success_criteria>
`OpenAIClient.Complete` returns the server-computed per-request `timings` (pp/tg separated) from a non-streaming /v1 completion with fixed (max_tokens, seed, temperature) params; a fixture test proves the parse; no new dependency; backend-neutrality seam green.
</success_criteria>

<output>
Create `.planning/phases/09-villa-bench-honest-a-b/09-01-SUMMARY.md` when done.
</output>

<artifacts_this_phase_produces>
New symbols/files this plan creates (exclude from drift verification — they do not exist before this phase):

**internal/llm (modified `openai.go`):**
- `func (c *OpenAIClient) Complete(ctx context.Context, req ChatRequest, nPredict int, seed int, temp float64) (Timings, error)` — non-streaming completion returning per-request timings
- `type Timings struct{ PromptN; PromptMS; PromptPerSecond; PredictedN; PredictedMS; PredictedPerSec }` — the llama.cpp `/v1` timings extension
- a non-streaming wire request type carrying `max_tokens`/`seed`/`temperature` + a non-streaming response struct with a top-level `Timings`

**internal/llm (modified `openai_test.go`):**
- `TestCompleteParsesTimings` (+ params-on-wire / bounded-error / no-model cases)
</artifacts_this_phase_produces>
