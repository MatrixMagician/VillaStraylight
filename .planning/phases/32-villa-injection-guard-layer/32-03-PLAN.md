---
phase: 32-villa-injection-guard-layer
plan: 03
type: execute
wave: 2
depends_on: [32-01]
files_modified:
  - internal/websafe/websafe.go
  - internal/websafe/loader.go
  - internal/websafe/websafe_test.go
  - internal/websafe/loader_test.go
autonomous: true
requirements: [GUARD-02, GUARD-03, GUARD-04]
must_haves:
  truths:
    - "fetchOne runs the guard in the load-bearing order sanitize → normalize → classify → fence (sanitize-first, not normalize-first)."
    - "The classifier verdict is USED, not discarded — it is stored on the produced Page."
    - "The Page value carries a Verdict field; fenced text is the Page content regardless of the verdict (flag-not-block)."
    - "The title is routed through the same sanitize+normalize defang as the body."
    - "The /load response metadata carries an additive guard sub-key {detected, rules} without renaming page_content/metadata/source/title."
  artifacts:
    - path: "internal/websafe/websafe.go"
      provides: "rewired fetchOne guard seam + Page.Verdict field"
      contains: "Verdict Verdict"
    - path: "internal/websafe/loader.go"
      provides: "/load metadata.guard threading"
      contains: "\"guard\""
    - path: "internal/websafe/loader_test.go"
      provides: "assertion that metadata.guard carries the verdict and existing keys are unchanged"
      contains: "guard"
  key_links:
    - from: "internal/websafe/websafe.go"
      to: "internal/websafe/classify.go"
      via: "fetchOne assigns verdict := classify(clean) and stores it on Page"
      pattern: "classify\\(clean\\)"
    - from: "internal/websafe/loader.go"
      to: "internal/websafe/websafe.go"
      via: "HandleLoad reads p.Verdict into metadata[guard]"
      pattern: "p.Verdict"
---

<objective>
Rewire the `fetchOne` guard seam in `internal/websafe/websafe.go` to the load-bearing order (sanitize → normalize → classify → fence), USE the classifier verdict (currently discarded via `_ = classify(text)`), add a `Verdict` field to the `Page` struct, route the title through the same defang, and thread the verdict additively into the `/load` response `metadata.guard` sub-key in `loader.go`.

Purpose: This is the integration wave — it connects the four real policy functions (from 32-01) into the live fetch path and surfaces the verdict on the OWUI contract so Phase 34 can later count it. It fixes the two Phase-31 bugs the CONTEXT named: the normalize-first ordering and the discarded verdict.
Output: A rewired `fetchOne` (correct order, verdict used, `extractText` replaced by `sanitize`), `Page.Verdict`, defanged title, additive `metadata.guard`, and updated tests proving the order + the additive metadata.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-villa-injection-guard-layer/32-CONTEXT.md
@.planning/phases/32-villa-injection-guard-layer/32-RESEARCH.md
@.planning/phases/32-villa-injection-guard-layer/32-PATTERNS.md
@internal/websafe/websafe.go
@internal/websafe/loader.go
@internal/websafe/websafe_test.go
</context>

<artifacts_this_phase_produces>
Symbols + struct changes created by THIS plan:

- **Struct field added:** `Page.Verdict Verdict` — `internal/websafe/websafe.go` (the `Page` struct gains the classifier result; flows to `/load` metadata).
- **Rewired function:** `fetchOne` — replaces `extractText(body)` with `sanitize(string(body))`, fixes ordering to sanitize→normalize→classify→fence, assigns `verdict := classify(clean)` (verdict USED, not `_ =`), routes title through `normalize(sanitize(extractTitle(body)))`, returns `Page{Content: fenced, Source, Title, Verdict}`.
- **Metadata contract widening (additive):** `loader.go` `HandleLoad` adds a nested `"guard": map[string]any{"detected": p.Verdict.Detected, "rules": p.Verdict.Rules}` under the existing `metadata` map. NO rename of `page_content`/`metadata`/`source`/`title` (verified OWUI contract).
- **Note on `extractText`:** superseded by `sanitize` — delete it (and `asciiLower` if only `extractTitle` no longer needs it; keep `extractTitle` + `asciiLower`, route the title through sanitize+normalize). Executor's discretion whether to delete dead `extractText` or leave it; preference is delete to avoid two strippers (one is bluemonday-backed, one is the CR-fixed hand-roller).
</artifacts_this_phase_produces>

<tasks>

<!-- planner-discipline-allow: _ = classify -->
<task type="auto" tdd="true">
  <name>Task 1: Rewire fetchOne guard seam (order + use verdict) + add Page.Verdict + defang title</name>
  <files>internal/websafe/websafe.go, internal/websafe/websafe_test.go</files>
  <read_first>
    - internal/websafe/websafe.go (lines 40-44 the Page struct; lines 152-160 the guard-seam call site; lines 163-268 extractText/extractTitle/asciiLower)
    - internal/websafe/verdict.go (the Verdict type from 32-01)
    - internal/websafe/classify.go (the classify signature from 32-01)
    - .planning/phases/32-villa-injection-guard-layer/32-RESEARCH.md (Code Examples → fetchOne rewire; ordering A — classify before fence; Pitfall 5)
    - .planning/phases/32-villa-injection-guard-layer/32-PATTERNS.md (websafe.go section lines 173-198)
  </read_first>
  <behavior>
    - A fetched body containing tags + an injection phrase + invisible-Unicode produces a Page whose Content is FENCED (carries the UNTRUSTED_WEB_CONTENT delimiters) and whose Verdict.Detected is true.
    - The guard order is sanitize-first: a body that is pure markup yields fenced (possibly near-empty) content, never the raw markup.
    - flag-not-block: even when Verdict.Detected is true, Page.Content is the full fenced text (content is NEVER dropped).
    - A body with a malicious `<title>` carrying markup/Unicode yields a Page.Title that is sanitized+normalized (no tags, no invisible runes).
    - The classifier runs on the NORMALIZED text (before fence), so the classifier input contains no fence delimiters (Pitfall 5).
  </behavior>
  <action>
    Add `Verdict Verdict` to the `Page` struct (websafe.go:40-44), with a field doc comment naming GUARD-04. Rewire the guard seam in `fetchOne` (currently websafe.go:152-160) to ordering A from RESEARCH: `clean := sanitize(string(body))` (bluemonday on the RAW HTML — replaces `extractText`), `clean = normalize(clean)` (NFKC + strip invisible/bidi), `verdict := classify(clean)` (verdict USED — fix the `_ = classify(text)` discard), `fenced := fence(clean)` (crypto/rand nonce). Build the title via `title := normalize(sanitize(extractTitle(body)))` so a malicious title is defanged through the same path. Return `Page{Content: fenced, Source: rawURL, Title: title, Verdict: verdict}`. Delete the now-superseded `extractText` (sanitize replaces it); keep `extractTitle` + `asciiLower` (the CR-01-fixed title scanner, now routed through sanitize+normalize). Update the seam doc comment (websafe.go:111-116, 155) to describe the real Phase-32 policy + order, removing the "Phase-31 stubbed" wording.

    Update `websafe_test.go` (or add cases) to assert: the fenced output contains the UNTRUSTED_WEB_CONTENT delimiter; Verdict.Detected is true for an injection body and false for a benign body; content is preserved when detected (flag-not-block); the title is defanged. Add an order assertion (TestGuardSeamOrder) proving sanitize runs before normalize/classify (e.g. a body whose injection phrase only becomes matchable AFTER NFKC fold is still detected — proving normalize precedes classify; and markup is stripped before fence).
  </action>
  <verify>
    <automated>CGO_ENABLED=0 go build ./... && go test ./internal/websafe/ -run 'TestFetch|TestGuardSeam|TestLoad' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'Verdict Verdict' internal/websafe/websafe.go` == 1 (Page gains the field).
    - `grep -c 'verdict := classify(clean)' internal/websafe/websafe.go` == 1 (verdict USED, not discarded).
    - `grep -c '_ = classify' internal/websafe/websafe.go` == 0 (the discard is gone).
    - `grep -c 'sanitize(string(body))' internal/websafe/websafe.go` == 1 (sanitize-first on raw HTML; extractText replaced).
    - `grep -c 'normalize(sanitize(extractTitle(body)))' internal/websafe/websafe.go` == 1 (title defanged through the same path).
    - `grep -c 'func extractText' internal/websafe/websafe.go` == 0 (superseded by sanitize).
    - `go test ./internal/websafe/ -run 'TestGuardSeam|TestFetch' -count=1` passes incl. the flag-not-block content-preserved case and the order assertion.
  </acceptance_criteria>
  <done>
    fetchOne runs sanitize→normalize→classify→fence with the verdict stored on Page.Verdict; extractText is replaced by sanitize; the title is defanged through sanitize+normalize; flag-not-block preserves content; order + detection tests pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Thread the verdict additively into /load metadata.guard + assert contract integrity</name>
  <files>internal/websafe/loader.go, internal/websafe/loader_test.go</files>
  <read_first>
    - internal/websafe/loader.go (lines 53-59 the LoadResponse contract doc; lines 122-137 the HandleLoad metadata map build)
    - internal/websafe/websafe.go (Page.Verdict from Task 1)
    - .planning/phases/32-villa-injection-guard-layer/32-RESEARCH.md (Code Examples → threading the verdict; Assumption A3 — OWUI tolerates extra metadata keys)
    - .planning/phases/32-villa-injection-guard-layer/32-PATTERNS.md (loader.go section lines 201-227)
  </read_first>
  <behavior>
    - HandleLoad on a batch where one page's Verdict.Detected is true returns a 200 JSON array whose element's metadata contains a nested `guard` object `{"detected":true,"rules":[...]}` ALONGSIDE the existing `source` and `title` keys.
    - The existing JSON tags are unchanged: each element still has top-level `page_content` and `metadata`, and metadata still has `source` (the citation URL) and `title`.
    - A benign page yields `metadata.guard.detected:false` (and omitted/empty rules) — the verdict is always present, never absent.
    - The handler still ALWAYS returns 200 with a non-nil (possibly empty) array (per-URL failures omitted, never a non-2xx) — the Phase-31 contract is preserved.
  </behavior>
  <action>
    Extend the metadata map build in `HandleLoad` (loader.go:124-132) to add a nested `"guard": map[string]any{"detected": p.Verdict.Detected, "rules": p.Verdict.Rules}` key ALONGSIDE `source` and `title`. This is the only safe widening — do NOT rename `page_content`/`metadata`/`source`/`title` (the verified OWUI contract, loader.go:53-59). Update the loader.go contract doc comment to note the additive `guard` sub-key (Phase 34 surfaces these counters; OWUI ignores unknown metadata keys, Assumption A3). Do not change the always-200 / non-nil-array / skip-and-continue behavior.

    Add a `loader_test.go` (or extend existing) `TestLoadMetadataGuard`: drive `HandleLoad` (httptest) with a stub Loader/client producing a detected page and a benign page; decode the 200 array; assert each element's `metadata.guard.detected` matches and that `source`/`title`/`page_content` are still present and correctly populated (contract-integrity assertion proving the widening is additive). Carry an invariant doc comment.
  </action>
  <verify>
    <automated>go test ./internal/websafe/ -run 'TestLoad|TestHandleLoad' -count=1 && make check</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '"guard"' internal/websafe/loader.go` == 1 (additive sub-key present).
    - `grep -c 'p.Verdict.Detected' internal/websafe/loader.go` ≥ 1 AND `grep -c 'p.Verdict.Rules' internal/websafe/loader.go` ≥ 1.
    - `grep -c '"page_content"' internal/websafe/loader.go` ≥ 1 AND `grep -c '"source"' internal/websafe/loader.go` ≥ 1 (existing contract tags unchanged).
    - `go test ./internal/websafe/ -run 'TestLoadMetadataGuard' -count=1` passes — proves metadata.guard carries the verdict AND source/title/page_content remain intact.
    - `make check` (vet + full `go test ./...`) exits 0.
    - `go test ./internal/inference/ -run TestSeamGrepGate -count=1` stays green.
  </acceptance_criteria>
  <done>
    /load metadata gains an additive guard sub-key {detected, rules} sourced from Page.Verdict without renaming the verified OWUI contract tags; the contract-integrity test passes; the handler still always returns 200 + non-nil array; make check is green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fetched web page → fetchOne guard seam | Untrusted bytes flow through the ordered guard pipeline (the integration point) |
| websafe `/load` → OWUI | The fenced content + verdict cross to OWUI; metadata widening must not break the contract |

## STRIDE Threat Register (ASVS L1; block on high)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-10 | Tampering (ordering bug) | fetchOne guard order | mitigate | Enforce sanitize→normalize→classify→fence; classify on normalized text (no fence self-match, Pitfall 5); order asserted by TestGuardSeamOrder — wrong order would let obfuscated payloads evade the classifier |
| T-32-11 | Tampering (verdict discard) | fetchOne | mitigate | The verdict is USED (stored on Page.Verdict + surfaced in metadata) — the Phase-31 `_ = classify(text)` discard is removed; grep-gate asserts no `_ = classify` remains |
| T-32-12 | Tampering | malicious `<title>` | mitigate | Title routed through `normalize(sanitize(extractTitle(body)))` — a markup/Unicode-laden title cannot carry markup or invisible runes into metadata.title |
| T-32-13 | DoS / contract break | `/load` metadata widening | mitigate | Additive `guard` sub-key only; verified `page_content`/`metadata`/`source`/`title` tags unchanged; always-200 + non-nil-array behavior preserved; contract-integrity test guards it |
| T-32-14 | Information Disclosure | metadata.guard content | accept | The verdict carries only `{detected, rules[]}` (rule-family NAMES, not page text/secrets) — no sensitive data leaks via the new key |
</threat_model>

<verification>
- `CGO_ENABLED=0 go build ./...` succeeds.
- `go test ./internal/websafe/ -count=1` passes (the full websafe suite incl. the new order, detection, flag-not-block, and metadata-contract tests).
- `grep -c '_ = classify' internal/websafe/websafe.go` == 0 (verdict no longer discarded).
- `go test ./internal/inference/ -run TestSeamGrepGate -count=1` stays green.
- `make check` is green.
</verification>

<success_criteria>
- fetchOne runs sanitize→normalize→classify→fence with the verdict stored on Page.Verdict (GUARD-02 + GUARD-03 + GUARD-04 wired into the live path).
- The title is defanged through sanitize+normalize.
- `/load` metadata carries an additive `guard` sub-key without breaking the verified OWUI contract.
- The discarded-verdict and normalize-first Phase-31 bugs are both fixed.
- `make check` and TestSeamGrepGate are green.
</success_criteria>

<output>
Create `.planning/phases/32-villa-injection-guard-layer/32-03-SUMMARY.md` when done.
</output>
