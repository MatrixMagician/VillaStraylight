---
phase: 23-surfacing-backup-memory-aware-swap
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/recall/skew.go
  - internal/recall/skew_test.go
  - internal/status/status.go
  - internal/status/status_test.go
  - cmd/villa/status.go
  - cmd/villa/status_test.go
  - internal/doctor/doctor.go
  - internal/doctor/doctor_test.go
  - cmd/villa/doctor_test.go
  - cmd/villa/testdata/status.json.golden
  - cmd/villa/testdata/status-memory.json.golden
  - cmd/villa/testdata/doctor-memory.json.golden
  - cmd/villa/testdata/doctor-memory-pass.golden
  - cmd/villa/testdata/doctor-memory-residency-fail.golden
autonomous: true
requirements: [CTRL-02]
must_haves:
  truths:
    - "villa status --json reports schema_version 3; with memory off the output differs from v2 ONLY in the version field"
    - "With memory on, villa-qdrant and villa-embed rows show their OWN per-service health (a stopped villa-embed is never health PASS) and an N/A offload that does not fold into the verdict"
    - "With memory on, the Report carries a memory section: active embedding model + dimension, recall-index summary, and a skew indicator only on confident mismatch"
    - "doctor no longer emits offload:villa-qdrant/villa-embed findings; doctor goldens re-frozen in this same plan"
  artifacts:
    - path: "internal/recall/skew.go"
      provides: "EmbeddingSkew pure helper (single implementation of the D-10 comparison)"
      exports: ["EmbeddingSkew", "SkewState", "SkewUnknown", "SkewMatch", "SkewMismatch"]
    - path: "internal/status/status.go"
      provides: "MemoryInfo struct, Memory *MemoryInfo field above SchemaVersion, reportSchemaVersion = 3, qdrant/embed non-GPU branches"
      contains: "reportSchemaVersion = 3"
    - path: "cmd/villa/status.go"
      provides: "liveQdrantHealth/liveEmbedHealth TTL-cached probes + liveReadRecallState seam + memory service names in liveStatusDeps"
    - path: "cmd/villa/testdata/status-memory.json.golden"
      provides: "byte-frozen memory-on v3 contract fixture"
  key_links:
    - from: "cmd/villa/status.go (liveStatusDeps)"
      to: "internal/status/status.go (Deps.QdrantHealth/EmbedHealth/ReadRecallState)"
      via: "Deps func fields — same seams reach the dashboard verbatim (D-03)"
      pattern: "QdrantHealth|EmbedHealth|ReadRecallState"
    - from: "internal/status/status.go (Run)"
      to: "internal/recall/skew.go (EmbeddingSkew)"
      via: "skew indicator in MemoryInfo"
      pattern: "recall\\.EmbeddingSkew"
---

<objective>
Land the milestone's single byte-frozen contract evolution: `status.Report` reportSchemaVersion 2→3 with the COMPLETE v3 field set (memory service-row classification fix, Memory section, skew indicator), per-service health probes for the container-DNS-only memory services, the doctor ripple, and exactly ONE golden re-freeze. Implements D-01, D-02, D-04, the status half of D-03, and the status-surfaced WARN half of D-10 (CTRL-02).

Purpose: the memory stack becomes observable and the carried per-row health false-green (Phase 22 UAT, status.go:376 probes the chat endpoint for every non-OWUI row) is fixed. Any later plan touching `Report`'s tagged fields is a planning error (Pitfall 1).
Output: v3 status contract (CLI + dashboard read-model), per-service health seams, recall skew helper, re-frozen goldens.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-surfacing-backup-memory-aware-swap/23-CONTEXT.md
@.planning/phases/23-surfacing-backup-memory-aware-swap/23-RESEARCH.md
@.planning/phases/23-surfacing-backup-memory-aware-swap/23-PATTERNS.md
</context>

## Artifacts this phase produces (this plan's share)

- `internal/recall/skew.go`: `type SkewState string`, consts `SkewUnknown`/`SkewMatch`/`SkewMismatch`, `func EmbeddingSkew(st State, cfgModel string, cfgDim int) SkewState` — THE single implementation of the embedding-skew comparison (consumed here for the status field; consumed by Plan 23-04 for the recall-index refusal and install WARN).
- `internal/status/status.go`: `type MemoryInfo struct` with JSON keys `embedding_model`, `embedding_dim`, `recall_state`, `indexed_chats` (omitempty), `last_index_started_at` (omitempty), `last_index_completed_at` (omitempty), `embedding_skew` (omitempty); `Report.Memory *MemoryInfo` tagged `json:"memory,omitempty"` inserted between `Usage` and `SchemaVersion`; `const reportSchemaVersion = 3`; new Deps fields `QdrantService string`, `EmbedService string`, `QdrantHealth func(addr string, port int) HealthState`, `EmbedHealth func(addr string, port int) HealthState`, `ReadRecallState func() *recall.State`; two new non-GPU row branches in the Run service loop.
- `cmd/villa/status.go`: `liveQdrantHealth`, `liveEmbedHealth`, shared 15s TTL probe cache (`memoryHealthTTL`), `liveReadRecallState`, `unitServiceName`-derived memory service names wired into `liveStatusDeps`.
- `internal/doctor/doctor.go`: `memoryOffloadDownRanked` predicate REMOVED (OQ3 decision: delete — it can never fire once memory rows are `OffloadApplies=false`; its own comment names this phase as the fix).
- CLI behavior: `villa status` human table renders the two memory rows with OFFLOAD N/A (existing renderer, no change); `villa status --json` emits the v3 contract.
- Goldens: `status.json.golden` re-frozen (version-only delta), NEW `status-memory.json.golden`, three `doctor-memory*.golden` re-frozen — all in ONE `-update` commit.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure cores — recall skew helper + status Report v3 (classification fix, Memory section, bump)</name>
  <files>internal/recall/skew.go, internal/recall/skew_test.go, internal/status/status.go, internal/status/status_test.go</files>
  <read_first>
    - internal/status/status.go — FULL file: the OWUI branch (:364-374), dashboard self-row branch (:404-424), naOffloadVerdict (:71-77), Aggregate fold (:438-459), Report tail-append layout + SchemaVersion-stays-last comments (:90-156), Deps fields OWUIService/DashboardService/DashboardHealth/ReadUsage (:233-260), the generic non-OWUI branch at :376 (the false-green being fixed)
    - internal/recall/store.go — State struct (:62-76: EmbeddingModel, EmbeddingDim, LastIndexStartedAt, LastIndexCompletedAt, Chats), Load fail-closed semantics (:111-132)
    - internal/recall/staleness.go — the complete-run expression (completed != "" && completed >= started, ~:65); reuse the existing predicate (CompleteRun) rather than re-rolling the comparison
    - internal/status/status_test.go — existing fixture conventions and fake Deps shape
    - .planning/phases/23-surfacing-backup-memory-aware-swap/23-PATTERNS.md — §internal/status/status.go (clone shapes, Pitfall 10 pointer-omitempty rule)
  </read_first>
  <behavior>
    - EmbeddingSkew(State{EmbeddingModel:""}, anything) == SkewUnknown (typed-Unknown: no recorded stamp → no alarm, D-10)
    - EmbeddingSkew(stamp nomic/768, cfg nomic/768) == SkewMatch; differing model OR dim == SkewMismatch
    - Run with memory-on fixture (cfg.MemoryEnabled=true + render output containing villa-qdrant.container/villa-embed.container — Pitfall 8 coherence): qdrant/embed rows have OffloadApplies=false, Offload==naOffloadVerdict(), Health from the new seams (NOT from d.Health(chat endpoint))
    - Run with EmbedHealth seam returning HealthDown: embed row Health=="down" (false-green fixed); nil seams → HealthUnknown
    - Memory-off Run: report.Memory == nil; SchemaVersion == 3
    - Memory-on Run: report.Memory populated — RecallState mapping: nil ReadRecallState return → "unknown"; zero-value State → "empty"; complete run → "indexed" (+ IndexedChats=len(Chats), timestamps verbatim); started-but-not-complete → "incomplete"
    - EmbeddingSkew==SkewMismatch → Memory.EmbeddingSkew=="mismatch"; SkewMatch/SkewUnknown → field empty/omitted (never a green "ok" for an unevaluated comparison — UI-SPEC)
    - Aggregate over a memory-on report with a down embed row: overall verdict degrades via health fold, NOT via offload (no offload fold for OffloadApplies=false rows — existing Aggregate, assert unchanged)
  </behavior>
  <action>
    Create internal/recall/skew.go: SkewState string type with consts SkewUnknown/SkewMatch/SkewMismatch and EmbeddingSkew(st State, cfgModel string, cfgDim int) SkewState — empty st.EmbeddingModel ⇒ SkewUnknown; model AND dim equal ⇒ SkewMatch; else SkewMismatch. Doc comment must name D-10 and that this is the single implementation shared by status (this plan) and the recall-index/install guards (Plan 23-04).

    In internal/status/status.go (D-01, D-02, D-04):
    1. Add Deps fields QdrantService, EmbedService (string — service unit names, mirroring OWUIService doc style), QdrantHealth and EmbedHealth (func(addr string, port int) HealthState), ReadRecallState (func() *recall.State). Nil seam ⇒ HealthUnknown / typed-Unknown memory summary.
    2. In the Run service loop, insert two branches BEFORE the generic branch at :376, cloning the OWUI branch shape exactly: when svc == d.QdrantService set ss.Health = d.QdrantHealth(cfg.QdrantAddr, cfg.QdrantPort) (HealthUnknown when seam nil), ss.Offload = naOffloadVerdict(), OffloadApplies=false, OffloadOK=false; same for d.EmbedService with d.EmbedHealth(cfg.EmbedAddr, cfg.EmbedPort). Never call d.Health(endpoint) for these rows (that IS the false-green bug).
    3. Define MemoryInfo with the exact JSON keys listed in Artifacts. Add Report.Memory *MemoryInfo `json:"memory,omitempty"` between Usage and SchemaVersion (POINTER — Pitfall 10; nothing above moves; SchemaVersion stays LAST). Populate ONLY when cfg.MemoryEnabled: EmbeddingModel/EmbeddingDim from cfg; recall summary from d.ReadRecallState() with the RecallState mapping in the behavior block (reuse the staleness.go complete-run predicate — export a thin wrapper if it is unexported, do not re-roll the comparison); EmbeddingSkew set to "mismatch" only when recall.EmbeddingSkew(state, cfg.EmbeddingModel, cfg.EmbeddingDim) == SkewMismatch, otherwise leave empty. When ReadRecallState is nil or returns nil: RecallState "unknown", count/timestamp fields zero (omitempty drops them).
    4. Bump const reportSchemaVersion to 3 and extend its doc-comment version history (v1 Phase 10, v2 Phase 15, v3 Phase 23: memory rows reclassified non-GPU + Memory section).

    Tests: table-driven skew_test.go for all three states; status_test.go memory-on fixtures MUST pair cfg.MemoryEnabled=true with render output containing the memory unit names (Pitfall 8). Do NOT touch cmd/villa goldens in this task (Task 3 owns the single re-freeze).
  </action>
  <verify>
    <automated>go test ./internal/recall ./internal/status</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "reportSchemaVersion = 3" internal/status/status.go` matches exactly once
    - `grep -n "json:\"memory,omitempty\"" internal/status/status.go` matches; the Memory field is a pointer type declared above SchemaVersion in the Report struct
    - `grep -n "func EmbeddingSkew" internal/recall/skew.go` matches; `go test ./internal/recall -run TestEmbeddingSkew` exits 0
    - `go test ./internal/status` exits 0 with new cases covering: false-green fix (down embed ⇒ down row), nil-seam HealthUnknown, all four RecallState values, mismatch-only skew field, memory-off Memory==nil
    - No edit to cmd/villa/testdata in this task (git status shows no golden changes yet)
  </acceptance_criteria>
  <done>Status core emits the complete v3 shape behind injectable seams; skew comparison has exactly one implementation; all package tests green (cmd/villa golden test EXPECTED red until Task 3 re-freeze).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: cmd-tier live wiring — memory service names, TTL-cached per-service probes, recall-state seam</name>
  <files>cmd/villa/status.go, cmd/villa/status_test.go</files>
  <read_first>
    - cmd/villa/status.go — liveStatusDeps (:168-217), liveHealthProbe 200/503/other mapping (:317-333), liveOpenWebUIHealth typed-Unknown discipline (:341-362), liveReadUsage (:226-248), statusHTTPTimeout
    - cmd/villa/doctor.go — memServices derivation via unitServiceName(orchestrate.QdrantContainerUnitName()) (:214-217) and unitServiceName (:295-301)
    - cmd/villa/install_memory.go — runProbeCurl (:317-341), the qdrant /readyz probe (:289), memoryProofNetwork
    - internal/recall/store.go — RecallStatePath (:152-156), Load
  </read_first>
  <behavior>
    - Probe result mapping (concrete, typed-Unknown doctrine): helper container ran and curl wrote an HTTP code → 200=HealthReady, 503=HealthLoading, any other code=HealthDown; curl-level connect failure inside villa.network (podman run exit code < 125, no HTTP code) → HealthDown; podman-level failure (exit 125/126/127, network/image missing, podman absent) → HealthUnknown — never a fabricated confident state from an unevaluable probe
    - Two consecutive probe calls within the TTL window execute podman exactly once (cache hit asserted via injectable runner fake)
    - liveReadRecallState: absent state file → empty recall.State (status renders "empty"); read error other than NotExist → nil (status renders "unknown"); never a fabricated count
  </behavior>
  <action>
    In cmd/villa/status.go:
    1. Wire liveStatusDeps with QdrantService/EmbedService derived exactly like doctor.go:214-217 — unitServiceName(orchestrate.QdrantContainerUnitName()) and unitServiceName(orchestrate.EmbedContainerUnitName()) (reuse or share the existing unitServiceName helper; no string literals — TestSeamGrepGate walks cmd/villa).
    2. Add liveQdrantHealth(addr string, port int) and liveEmbedHealth(addr string, port int) status.HealthState: build the in-network URL from the args (qdrant path /readyz, embed path /health), probe via the runProbeCurl mechanism with helper image orchestrate.EmbedImage(), curl args -s -o /dev/null -w %{http_code} --max-time bound (mirror statusHTTPTimeout magnitude), context-bounded podman exec. Map results per the behavior block. Route the podman invocation through an injectable seam (package-level func var, runProbeCurl convention) so tests run hermetically.
    3. OQ2 decision (locked here): mitigate the dashboard 2.5s-poll × podman-run churn (Pitfall 2) with a package-level mutex-guarded cache refreshing BOTH services' health together, const memoryHealthTTL = 15 * time.Second. Doc comment: TTL is the bounded-staleness honesty tradeoff; fallback if on-hardware (Plan 23-05) shows churn is to raise to 30s — a one-const change. Cache is keyed only on time (single config per process).
    4. Add liveReadRecallState() *recall.State cloning liveReadUsage's shape: read recall.RecallStatePath() via recall.Load's Deps; os.IsNotExist → empty state (pointer to zero State, NOT nil — "no index yet" is a confident empty, D-02); other read/load error → nil (typed-Unknown). Wire into liveStatusDeps as ReadRecallState.
    Tests in cmd/villa/status_test.go: probe mapping table (fake runner returning code strings / exit classes), TTL single-execution assertion, recall seam absent/corrupt cases. Existing TestStatusJSONGolden will be red until Task 3 — that is expected and must NOT be "fixed" by editing the golden here.
  </action>
  <verify>
    <automated>go test ./cmd/villa -run 'TestLiveQdrantHealth|TestLiveEmbedHealth|TestMemoryHealthTTL|TestLiveReadRecallState' && go vet ./cmd/villa</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "villa-qdrant\|villa-embed" cmd/villa/status.go` returns NO direct service-name literals (names derived via orchestrate accessors only)
    - `grep -n "memoryHealthTTL" cmd/villa/status.go` matches a 15s const with the fallback-to-30s doc comment
    - go test ./internal/inference -run TestSeamGrepGate exits 0 (no leaked literals)
    - New probe/TTL/recall-seam tests pass hermetically (no live podman)
  </acceptance_criteria>
  <done>liveStatusDeps carries all v3 seams; dashboard inherits them verbatim (D-03 — cmd/villa/dashboard.go wires *liveStatusDeps() unchanged); probes are fixed-arg, bounded, TTL-cached, honest.</done>
</task>

<task type="auto">
  <name>Task 3: Doctor ripple + the ONE golden re-freeze (status v3 + memory-on fixture + 3 doctor goldens)</name>
  <files>internal/doctor/doctor.go, internal/doctor/doctor_test.go, cmd/villa/doctor_test.go, cmd/villa/status_test.go, cmd/villa/testdata/status.json.golden, cmd/villa/testdata/status-memory.json.golden, cmd/villa/testdata/doctor-memory.json.golden, cmd/villa/testdata/doctor-memory-pass.golden, cmd/villa/testdata/doctor-memory-residency-fail.golden</files>
  <read_first>
    - internal/doctor/doctor.go — memoryOffloadDownRanked (:375-384), its call site (:390), the deferred-fix comment (:359-364), the offload-finding gate for OffloadApplies rows (~:465)
    - cmd/villa/status_test.go — TestStatusJSONGolden + -update machinery (:284-310), newStatusDeps fixture (:49)
    - cmd/villa/doctor_test.go — the three doctor-memory golden tests and their fixtures
    - .planning/phases/23-surfacing-backup-memory-aware-swap/23-RESEARCH.md — golden inventory table + Pitfall 7
  </read_first>
  <action>
    1. OQ3 decision (locked here): DELETE memoryOffloadDownRanked and its call site (:390) plus its dedicated tests — after Task 1 memory rows are OffloadApplies=false so doctor's offload-finding gate never creates offload:villa-qdrant/villa-embed findings and the predicate is unreachable dead code; its own comment (:359-364) names Phase 23 as the fix. Keep the MemoryEnabled/MemoryServices Deps fields if any OTHER doctor check uses them (verify by grep before deleting fields; delete only what becomes unused). Record the deletion rationale in the commit message.
    2. Update doctor test fixtures so memory-service rows carry the new per-service health/classification shape (fixtures must stay coherent with the v3 core — Pitfall 8).
    3. Add TestStatusJSONGoldenMemoryOn in cmd/villa/status_test.go: a memory-on deps fixture (cfg.MemoryEnabled=true, render output with villa-qdrant.container/villa-embed.container, stubbed QdrantHealth/EmbedHealth, stubbed ReadRecallState returning a complete-run State) frozen against NEW testdata/status-memory.json.golden via the same -update flag.
    4. THE single deliberate re-freeze (D-04): run go test ./cmd/villa -run 'TestStatusJSONGolden|TestStatusJSONGoldenMemoryOn|TestDoctor' -update once; inspect the diff before committing — status.json.golden must change ONLY schema_version 2→3 (memory-off fixture, omitempty-nil Memory); doctor-memory goldens lose their offload:villa-qdrant.service/offload:villa-embed.service WARN findings and track the new health stubs. If any internal/dashboard test pins reportSchemaVersion==2 (api_test.go has a SchemaVersion: 1 fixture at :739 and a forbidden-fields list at :181), update the assertion in the same commit — there are no dashboard golden files.
    5. Run make check — full suite green.
  </action>
  <verify>
    <automated>make check</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "memoryOffloadDownRanked" internal/doctor/doctor.go` returns 0
    - `git diff --stat` for this task shows status.json.golden changed with exactly one content line delta (schema_version), and exactly three doctor-memory*.golden re-freezes plus the new status-memory.json.golden
    - `grep -rn "offload:villa-qdrant\|offload:villa-embed" cmd/villa/testdata/` returns 0 matches
    - `grep -n "schema_version\": 3" cmd/villa/testdata/status.json.golden` matches
    - make check exits 0; this is the LAST commit in the phase that may touch Report tagged fields or status/doctor goldens
  </acceptance_criteria>
  <done>v3 contract frozen once; doctor ripple landed in the same plan (Pitfall 7); full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| status core → host probes | probe results from podman-run helper containers cross into the read-model |
| recall-state.json → status | operator-writable JSON file feeds the Memory section |
| status.Report → CLI/dashboard consumers | byte-frozen contract; consumers trust the verdict |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-01 | Information disclosure (false assurance) | status per-row health (status.go:376 chat-endpoint probe for every non-OWUI row — carried from Phase 22 UAT, STATE.md/graphmind 527de579) | mitigate | Task 1/2: per-service health seams; stopped villa-embed renders down/unknown, never PASS; negative-control test asserted |
| T-23-02 | Tampering (contract integrity) | status.Report goldens | mitigate | Task 3: complete v3 field set defined up front, ONE -update re-freeze, diff inspected; later plans forbidden from touching tagged fields |
| T-23-03 | Spoofing/Tampering (seam leak) | cmd/villa probe wiring | mitigate | fixed-arg runProbeCurl, names via orchestrate accessors, TestSeamGrepGate green in Task 2 acceptance |
| T-23-04 | Denial of service | dashboard 2.5s poll × podman-run probes (Pitfall 2) | mitigate | 15s TTL cache + context/timeout-bounded probes (Task 2); measured on-hardware in Plan 23-05 with 30s fallback |
| T-23-05 | Tampering (input) | recall-state.json read into status | mitigate | recall.Load fail-closed (absent/corrupt/future-schema → empty), typed-Unknown rendering, no counts fabricated |
| T-23-SC | Tampering | package installs | accept | no npm/pip/cargo/Go module installs this phase (zero-new-library milestone); no audit table needed |
</threat_model>

<verification>
- `make check` green; `go test ./internal/inference -run TestSeamGrepGate` green.
- Memory-off `villa status --json` differs from the v2 golden only in schema_version (D-04 proof).
- Exactly one commit re-freezes goldens.
</verification>

<success_criteria>
- SC#1 (ROADMAP Phase 23): status surfaces memory rows + active embedding model, schema 2→3, golden re-frozen once, non-GPU fold without spurious offload — fully satisfied on the CLI/read-model side (dashboard SPA panel = Plan 23-03).
- False-green health fixed and proven by negative-control test.
- D-01, D-02, D-04 implemented exactly; D-03 status-core half done; D-10 status WARN surface (embedding_skew field) landed under the single bump.
</success_criteria>

<output>
Create `.planning/phases/23-surfacing-backup-memory-aware-swap/23-01-SUMMARY.md` when done.
</output>
