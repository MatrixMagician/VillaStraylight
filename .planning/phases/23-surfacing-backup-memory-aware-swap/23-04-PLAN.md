---
phase: 23-surfacing-backup-memory-aware-swap
plan: 04
type: execute
wave: 2
depends_on: [23-01]
files_modified:
  - internal/orchestrate/memory_test.go
  - internal/modelswap/modelswap_test.go
  - cmd/villa/recall.go
  - cmd/villa/recall_test.go
  - cmd/villa/install_memory.go
  - cmd/villa/install_memory_test.go
autonomous: true
requirements: [CTRL-05]
must_haves:
  truths:
    - "A chat-model swap leaves the memory stack intact — proven by tests: memory + OWUI unit texts byte-identical across a Model/Quant change, and modelswap restarts exactly one service (InstallServiceName)"
    - "villa recall index REFUSES (exitBlocked, refuse-with-remediation) on a confident embedding model/dim mismatch against the recorded recall-state stamp; --rebuild is the sanctioned bypass and proceeds"
    - "No recorded stamp (empty state.EmbeddingModel) raises no alarm; no guard ever mutates anything (no auto-reindex anywhere)"
    - "villa install's memory flow WARNs with remediation on the same confident mismatch, read-only"
  artifacts:
    - path: "internal/orchestrate/memory_test.go"
      provides: "D-09 render byte-identity invariant test across a chat-model change"
    - path: "internal/modelswap/modelswap_test.go"
      provides: "D-09 restart-scope invariant test (exactly one Restart: InstallServiceName)"
    - path: "cmd/villa/recall.go"
      provides: "D-10 fail-closed skew refusal between readState and the stamp overwrite"
  key_links:
    - from: "cmd/villa/recall.go (runRecallIndex)"
      to: "internal/recall/skew.go (EmbeddingSkew — Plan 23-01)"
      via: "single-implementation comparison against the recall-state stamp"
      pattern: "recall\\.EmbeddingSkew"
    - from: "cmd/villa/install_memory.go"
      to: "internal/recall (Load + EmbeddingSkew)"
      via: "read-only WARN surface (D-10/D-11)"
      pattern: "EmbeddingSkew"
---

<objective>
Make model swap memory-aware (CTRL-05): prove the D-09 invariant (chat swap never touches the embedding model, memory units, or vector collections) with byte-identity and restart-scope tests, and land the D-10 guards — `villa recall index` refuses fail-closed on a confident embedding model/dimension skew against the recall-state stamp (with `--rebuild` as the sanctioned bypass), and `villa install`'s memory flow WARNs read-only on the same skew. No guard mutates anything (D-11). The status-surfaced skew indicator already landed under the single 2→3 bump in Plan 23-01.

Purpose: changing the embedding model can no longer silently corrupt retrieval (SC#3).
Output: invariant test suite + recall-index refusal + install WARN.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/23-surfacing-backup-memory-aware-swap/23-CONTEXT.md
@.planning/phases/23-surfacing-backup-memory-aware-swap/23-RESEARCH.md
@.planning/phases/23-surfacing-backup-memory-aware-swap/23-PATTERNS.md
@.planning/phases/23-surfacing-backup-memory-aware-swap/23-01-SUMMARY.md
</context>

## Artifacts this phase produces (this plan's share)

- `internal/orchestrate/memory_test.go`: new test (e.g. `TestRenderChatSwapLeavesMemoryUnitsByteIdentical`) — render two memory-on configs differing ONLY in `Model`/`Quant`; assert `villa-qdrant.container`, `villa-qdrant.volume`, `villa-embed.container`, and the `villa-openwebui.*` unit texts are byte-identical; only `villa-llama.container` differs.
- `internal/modelswap/modelswap_test.go`: new/extended test — fakeDeps records every `Restart` call; a successful swap performs exactly ONE Restart, with `InstallServiceName`; the Deps surface contains no other service mutator (making the scope permanent for CLI AND dashboard — `handleSwitch` calls `modelswap.Run` verbatim).
- `cmd/villa/recall.go`: D-10 refusal in `runRecallIndex` — placed AFTER `deps.readState()` (:322) and BEFORE the stamp overwrite (:343-344); refusal text naming both stamped and configured model/dim, the consequence (indexing into a mismatched-dimension collection corrupts retrieval), and the fixes (`--rebuild` to re-index cleanly, or revert the config); returns `exitBlocked`.
- `cmd/villa/install_memory.go`: read-only WARN on confident skew during the memory readiness flow, remediation text mirroring the refusal (run `villa recall index --rebuild`, or revert `embedding_model`/`embedding_dim` in config.toml); never blocks, never mutates.
- CLI behavior: OQ4 decision locked — `--rebuild` bypasses the refusal because the rebuild path id-preservingly resets the KB and clean-replaces collection content (:327-335), and the fresh stamp then records the new model; the on-hardware drill (Plan 23-05) proves the rebuild path end-to-end.

<tasks>

<task type="auto">
  <name>Task 1: D-09 invariant proofs — render byte-identity + restart scope</name>
  <files>internal/orchestrate/memory_test.go, internal/modelswap/modelswap_test.go</files>
  <read_first>
    - internal/modelswap/modelswap.go — Run (:85-142): mutates only cfg.Model/cfg.Quant (:117-121), ReconcileAndWrite, single Restart(d.InstallServiceName) (:137); Deps surface (:25-47)
    - internal/modelswap/modelswap_test.go — fakeDeps conventions, TestSwapSaveBeforeReconcileAndInferenceOnlyRestart (:119) as the extension point
    - internal/orchestrate/memory.go — buildQdrantView/buildEmbedView (:153-205) read only memory config fields, never cfg.Model
    - internal/orchestrate/memory_test.go + render_test.go — existing render fixture conventions (memory-on config construction)
  </read_first>
  <action>
    Per D-09 (invariant proof, not new behavior):
    1. orchestrate test: construct two valid memory-on configs identical except Model and Quant; Render both; build a map unitName→text per render; assert byte-equality (==, not contains) for villa-qdrant.container, villa-qdrant.volume, villa-embed.container, and every villa-openwebui.* unit; assert villa-llama.container DOES differ (sanity that the swap is real). Doc comment cites D-09 and SC#3 ("a chat-model swap leaves the embedding model and vector collections intact").
    2. modelswap test: fakeDeps appends every Restart target to a slice; drive a successful Run; assert len==1 and the single element equals the fixture InstallServiceName; assert no Stop/Start-style mutator exists on the Deps struct beyond Restart (compile-time truth — document in the test comment that the Deps surface itself is the guarantee covering both the CLI and dashboard handleSwitch). If the existing :119 test already records restarts, extend it rather than duplicating.
    No production code changes in this task.
  </action>
  <verify>
    <automated>go test ./internal/orchestrate -run TestRender && go test ./internal/modelswap</automated>
  </verify>
  <acceptance_criteria>
    - New orchestrate test fails if any memory or OWUI unit text changes across a Model/Quant-only config delta (verified by temporarily perturbing — then restoring — nothing: the byte-equality assertion is structural)
    - Modelswap test asserts exactly one recorded Restart with InstallServiceName
    - git diff touches ONLY test files in this task
  </acceptance_criteria>
  <done>D-09 is pinned by tests on both the render scope and the restart scope; any future regression breaks the build.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: D-10 guards — recall index fail-closed refusal + install read-only WARN</name>
  <files>cmd/villa/recall.go, cmd/villa/recall_test.go, cmd/villa/install_memory.go, cmd/villa/install_memory_test.go</files>
  <read_first>
    - cmd/villa/recall.go — runRecallIndex step 4 (:319-353): readState (:322), --rebuild KB reset (:327-335), the stamp overwrite (:343-344, comment "Phase-23 skew guards (D-05)"), persist (:350); the WR-05 refusal print register (:371-374); exitBlocked convention
    - internal/recall/skew.go (from Plan 23-01) — EmbeddingSkew/SkewState contract
    - cmd/villa/install_memory.go — the memory readiness flow structure and its WARN/print conventions; where cfg is in scope
    - cmd/villa/recall_test.go — recallDeps fake conventions (exit-code-returning tests)
    - 23-RESEARCH.md Pitfall 6 (guard placement) + §CTRL-05 map
  </read_first>
  <behavior>
    - Stamp nomic/768, cfg other-model/512, no --rebuild ⇒ exitBlocked; stderr contains both model ids, both dims, "corrupts retrieval", and "--rebuild"; state file NOT rewritten (stamp preserved — Pitfall 6)
    - Same mismatch WITH --rebuild ⇒ run proceeds; on completion the fresh stamp records the NEW model/dim
    - Empty stamp (state.EmbeddingModel == "") ⇒ no refusal, run proceeds (typed-Unknown, no false alarm)
    - Pitfall 6 regression test: two consecutive runs with a changed cfg — the SECOND must still refuse without --rebuild (proving the first refusal did not overwrite the stamp)
    - install memory flow with confident mismatch ⇒ WARN line printed with remediation, exit/flow otherwise unchanged, zero state/file mutation (D-11)
  </behavior>
  <action>
    Per D-10/D-11:
    1. cmd/villa/recall.go: immediately after deps.readState() succeeds and BEFORE any state mutation, when the rebuild flag is false and recall.EmbeddingSkew(state, cfg.EmbeddingModel, cfg.EmbeddingDim) == recall.SkewMismatch, print to errOut the refusal (register mirroring the WR-05 print): "recall index: REFUSING — the index was built with <stamped model> (dim <stamped dim>) but config now says <cfg model> (dim <cfg dim>); indexing into a mismatched-dimension collection corrupts retrieval. Re-run with --rebuild to re-index cleanly, or revert the config." and return exitBlocked. --rebuild proceeds (OQ4: rebuild IS the sanctioned re-index — it clean-replaces KB content and the fresh stamp records the new model). Do not move or alter the :343-344 stamp lines.
    2. cmd/villa/install_memory.go: in the memory readiness flow (memory-on path only), read the recall state read-only (recall.Load over the state path, absent ⇒ empty ⇒ SkewUnknown ⇒ silent) and on SkewMismatch print a WARN naming the stamped vs configured model/dim and the remediation "run villa recall index --rebuild to re-index, or revert embedding_model/embedding_dim in config.toml". Strictly read-only: no exit-code change, no state write, no service mutation (D-11; mirrors the Phase 22 diagnose-don't-mutate posture). Route the file read through the existing injectable seam conventions so the test stays hermetic.
    3. Tests per the behavior block, including the consecutive-runs Pitfall 6 regression and an install-flow WARN assertion (stdout/stderr capture, zero-mutation assertion via fake seams).
  </action>
  <verify>
    <automated>go test ./cmd/villa -run 'TestRecallIndex|TestInstallMemory' && make check</automated>
  </verify>
  <acceptance_criteria>
    - In cmd/villa/recall.go the skew check appears textually between the readState call and the state.EmbeddingModel stamp assignment (verify by reading the diff; the stamp lines :343-344 are unmoved)
    - `grep -n "SkewMismatch" cmd/villa/recall.go cmd/villa/install_memory.go` shows both consumers using the Plan 23-01 helper (no re-rolled comparison; `grep -rn "state.EmbeddingModel !=" cmd/villa` returns 0 matches)
    - Refusal test asserts exitBlocked + remediation text containing "--rebuild"; consecutive-runs test green; empty-stamp test green; --rebuild path test green
    - Install WARN test asserts read-only behavior (no writes recorded by fakes)
    - make check exits 0
  </acceptance_criteria>
  <done>CTRL-05 guards landed: fail-closed refusal at the one verb that mutates the index, read-only WARNs at install + status (status half shipped in 23-01); no auto-reindex anywhere.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config.toml (operator-edited) → recall index | a changed embedding_model/dim crosses into an existing vector collection |
| recall-state.json stamp → guards | the recorded truth the comparison trusts |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-15 | Tampering (integrity) | recall index into a mismatched-dimension collection | mitigate | Task 2: fail-closed exitBlocked refusal using EmbeddingSkew; --rebuild is the only sanctioned path (D-10) |
| T-23-16 | Repudiation (recorded-truth destruction) | stamp overwrite before comparison (Pitfall 6) | mitigate | Task 2: guard placed between readState and the stamp; consecutive-runs regression test makes placement permanent |
| T-23-17 | Tampering (collateral mutation) | chat swap touching memory units/collections | mitigate | Task 1: byte-identity + restart-scope invariant tests (D-09); covers dashboard via shared modelswap.Run |
| T-23-18 | Elevation/unsafe automation | auto-reindex on mismatch | mitigate | none exists and none added — guards report/refuse only (D-11); install WARN proven read-only by test |
| T-23-SC | Tampering | package installs | accept | no package-manager installs this phase |
</threat_model>

<verification>
- make check green; TestSeamGrepGate green (no new literals in cmd/villa beyond accessor-derived names).
- Refusal/--rebuild/empty-stamp/consecutive-runs matrix green; invariance tests green.
</verification>

<success_criteria>
- SC#3 (ROADMAP Phase 23) fully satisfied off-hardware: swap warns/guards on embedding change (no auto-reindex), chat swap proven to leave the memory stack intact.
- D-09, D-10, D-11 implemented exactly; OQ4 resolved (--rebuild bypass, proven live in Plan 23-05).
</success_criteria>

<output>
Create `.planning/phases/23-surfacing-backup-memory-aware-swap/23-04-SUMMARY.md` when done.
</output>
