---
phase: 23-surfacing-backup-memory-aware-swap
plan: 03
type: execute
wave: 2
depends_on: [23-01]
files_modified:
  - internal/dashboard/assets/dashboard.html
  - internal/dashboard/assets/dashboard.js
  - internal/dashboard/assets/dashboard.css
  - internal/dashboard/api_test.go
autonomous: true
requirements: [CTRL-02]
must_haves:
  truths:
    - "With memory on, the dashboard shows villa-qdrant/villa-embed health rows (auto-rendered) and a Memory panel with embedding model, dimension, indexed chats, last indexed, and a recall-index state badge"
    - "With memory off, the dashboard renders pixel-identical to v1.2 (the Memory panel ships hidden and stays hidden)"
    - "Typed-Unknown states render gray 'unavailable' badges — never green, never red; skew renders an amber 'mismatch' badge with remediation copy only on confident mismatch"
    - "The dashboard adds no new fetch, endpoint, probe, or external UI dependency — it reads report.memory from the existing /api/status poll (D-03)"
  artifacts:
    - path: "internal/dashboard/assets/dashboard.html"
      provides: "Memory panel section markup (hidden by default), id memory-panel"
      contains: "memory-panel"
    - path: "internal/dashboard/assets/dashboard.js"
      provides: "renderMemory reading report.memory; show/hide on presence; XSS-safe DOM construction"
      contains: "renderMemory"
  key_links:
    - from: "internal/dashboard/assets/dashboard.js"
      to: "/api/status (existing poll)"
      via: "report.memory field from the v3 contract (Plan 23-01)"
      pattern: "report\\.memory|\\.memory"
---

<objective>
Render the Phase 23 dashboard surfacing per the approved 23-UI-SPEC: the two memory health rows arrive free via the existing renderHealth over the v3 report.services (zero JS change for rows), and a new Memory panel reads report.memory from the same /api/status poll — no new probes, fetches, or endpoints (D-03). Implements the SPA half of CTRL-02.

Purpose: the dashboard shows the same memory truths the CLI shows, composed from the shared status core.
Output: Memory panel markup + renderMemory + Go-side passthrough test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/23-surfacing-backup-memory-aware-swap/23-UI-SPEC.md
@.planning/phases/23-surfacing-backup-memory-aware-swap/23-CONTEXT.md
@.planning/phases/23-surfacing-backup-memory-aware-swap/23-PATTERNS.md
@.planning/phases/23-surfacing-backup-memory-aware-swap/23-01-SUMMARY.md
</context>

## Artifacts this phase produces (this plan's share)

- `dashboard.html`: `<section class="panel" id="memory-panel" aria-labelledby="memory-heading" hidden>` appended after the Models panel; heading `<h2 class="panel-heading" id="memory-heading">Memory</h2>`.
- `dashboard.js`: `renderMemory(report)` — unhides the panel only when `report.memory` is present (re-hides if it disappears); rows built with `createElement` + `textContent` only; badge mapping per the UI-SPEC state table; called from the existing poll success handler alongside renderHealth/renderBackend.
- `dashboard.css`: at most reuse of existing classes (`.metric-row`, `.badge`, `.badge-ready/.badge-warn/.badge-unknown`, `.muted`); new rules only if a required class is missing — no new tokens, no accent use.
- Go test: `internal/dashboard/api_test.go` case asserting `/api/status` passes the `memory` field through when the status deps supply it (and omits it when not).

<tasks>

<task type="auto">
  <name>Task 1: Memory panel — markup + renderMemory per UI-SPEC</name>
  <files>internal/dashboard/assets/dashboard.html, internal/dashboard/assets/dashboard.js, internal/dashboard/assets/dashboard.css</files>
  <read_first>
    - .planning/phases/23-surfacing-backup-memory-aware-swap/23-UI-SPEC.md — FULL (binding contract: placement, row labels, badge/state table, copywriting, color/accent rules)
    - internal/dashboard/assets/dashboard.js — FULL: poll loop (:2 cadence), renderHealth XSS-safe idiom (:146-178), readinessClass gray-badge convention (:185-200), renderBackend panel shape (:210+), groupThousands, setConnected/stale handling
    - internal/dashboard/assets/dashboard.html — panel/section structure, connection-banner[hidden] precedent
    - internal/dashboard/assets/dashboard.css — existing .panel/.metric-row/.badge/.muted classes and tokens (verify which badge classes already exist before adding CSS)
    - .planning/phases/23-surfacing-backup-memory-aware-swap/23-PATTERNS.md — §dashboard.js (render-function shape, XSS-safe idiom, badge-class vocabulary)
  </read_first>
  <action>
    Implement the UI-SPEC Component Contract exactly (D-03 — the panel reads report.memory from the v3 contract; JSON keys per Plan 23-01: embedding_model, embedding_dim, recall_state with values indexed/incomplete/empty/unknown, indexed_chats, last_index_started_at, last_index_completed_at, embedding_skew):
    1. dashboard.html: add the hidden memory-panel section after the Models panel with the exact attributes in Artifacts.
    2. dashboard.js: add renderMemory(report) invoked from the same poll handler that calls renderHealth. Behavior: report.memory absent/null ⇒ set hidden=true and return; present ⇒ remove hidden and rebuild the panel body. Rows (labels verbatim from UI-SPEC): "embedding model" (mono, verbatim), "dimension" (mono), "indexed chats" (mono, groupThousands — render the row ONLY for recall_state indexed/incomplete, omit for empty/unknown, never zero-fill), "last indexed" (mono, verbatim RFC3339 from last_index_completed_at — same omission rule), "recall index" badge row mapped: indexed ⇒ badge "indexed" badge-ready; incomplete ⇒ badge "incomplete" badge-warn + muted line "Last index run did not complete — re-run villa recall index."; empty ⇒ no badge, empty-state heading "No recall index yet" + muted body "Build it with villa recall index — indexed chats appear here once a run completes."; unknown ⇒ badge "unavailable" badge-unknown + muted "Recall index state unavailable.". Skew: when embedding_skew === "mismatch" add a "embedding config" row with badge "mismatch" badge-warn (amber, NOT red) + muted "Configured embedding model differs from the indexed vectors — re-index with villa recall index --rebuild, or revert config.toml."; otherwise omit the row entirely (never a green ok). All DOM via createElement + textContent (never innerHTML with server values). Stale/poll-failure behavior: rely on the existing setConnected dimming — the panel keeps last-good content, no spinner, no new animation.
    3. dashboard.css: reuse existing classes; if badge-ready/badge-warn/badge-unknown or the empty-state heading class do not exist under those names, reuse whatever the existing badge vocabulary is (readinessClass classes) rather than inventing parallel classes; add a rule ONLY if no existing class covers the need, using existing tokens (--space-*, --status-*); zero --accent usage (UI-SPEC reserved list).
  </action>
  <verify>
    <automated>go build ./... && go vet ./internal/dashboard && node --check internal/dashboard/assets/dashboard.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "innerHTML" internal/dashboard/assets/dashboard.js` count is unchanged from the pre-task value (no new innerHTML use; verify via git diff)
    - `grep -n "memory-panel" internal/dashboard/assets/dashboard.html` matches a section with hidden attribute and aria-labelledby="memory-heading"
    - `grep -n "renderMemory" internal/dashboard/assets/dashboard.js` shows definition + one call site in the poll handler
    - `grep -n "accent" internal/dashboard/assets/dashboard.css` shows no NEW --accent consumers (git diff of css introduces none)
    - All five UI-SPEC copy strings present verbatim in dashboard.js (grep for "No recall index yet", "Recall index state unavailable.", "Last index run did not complete", "Configured embedding model differs")
  </acceptance_criteria>
  <done>Memory panel renders per the approved UI contract from the existing poll; memory-off DOM untouched (panel stays hidden); no new endpoints/probes/dependencies.</done>
</task>

<task type="auto">
  <name>Task 2: Go-side passthrough test + suite green</name>
  <files>internal/dashboard/api_test.go</files>
  <read_first>
    - internal/dashboard/api_test.go — existing handleStatus test harness and statusDeps stubbing pattern; the forbidden-fields assertion at :181 and the SchemaVersion fixture at :739 (already updated to v3 by Plan 23-01 Task 3 — confirm only, do NOT re-fix)
    - internal/dashboard/api.go — handleStatus (:19-22, serializes status.Run verbatim)
  </read_first>
  <action>
    Add a test case: with status deps stubbed memory-on (cfg.MemoryEnabled=true, memory units in render output, stubbed ReadRecallState — mirror the Plan 23-01 memory-on fixture for coherence, Pitfall 8), GET /api/status body contains a "memory" object with embedding_model/embedding_dim/recall_state keys; with the default memory-off deps the body contains NO "memory" key (omitempty-nil) and schema_version is 3. The schema_version pin updates in api_test.go already landed in Plan 23-01 Task 3 — do not duplicate them here; this task only ADDS the memory-passthrough cases. Run the full dashboard package suite plus make check.
  </action>
  <verify>
    <automated>go test ./internal/dashboard && make check</automated>
  </verify>
  <acceptance_criteria>
    - New test asserts presence of the memory object (memory-on) AND absence of the memory key (memory-off) in the served JSON
    - make check exits 0
    - git diff shows no changes to internal/status or cmd/villa (this plan must not touch the frozen contract — Pitfall 1)
  </acceptance_criteria>
  <done>Dashboard serves the v3 read-model with the memory field passing through untouched; suite green. On-box visual verification (restart villa-dashboard.service after make build) lands in Plan 23-05.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| /api/status JSON → SPA DOM | server-derived strings (model ids, timestamps) cross into the rendered page |
| browser → dashboard server | loopback-only + same-origin guard (unchanged this phase) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-12 | Tampering (XSS) | renderMemory DOM construction | mitigate | createElement + textContent only (existing renderHealth idiom); acceptance gate greps for new innerHTML use |
| T-23-13 | Information disclosure (false assurance) | badge/state rendering | mitigate | typed-Unknown ⇒ gray "unavailable", never green/red; skew row omitted unless confident mismatch (UI-SPEC honesty mapping) |
| T-23-14 | Spoofing / network exposure | dashboard bind surface | accept | unchanged: loopback-only via net.JoinHostPort + requireSameOrigin (PRIV-01); this plan adds no endpoint, port, or fetch |
| T-23-SC | Tampering | package installs | accept | no UI dependencies of any kind may be introduced (go:embed static assets, zero toolchain) |
</threat_model>

<verification>
- make check green; node --check passes on dashboard.js.
- Memory-off DOM unchanged (panel hidden in static shell).
</verification>

<success_criteria>
- Dashboard half of SC#1 satisfied off-hardware: same read-model, memory rows auto-rendered, Memory panel per approved UI-SPEC (D-03).
- Live visual check deferred to Plan 23-05 (dashboard-restart gotcha applies there).
</success_criteria>

<output>
Create `.planning/phases/23-surfacing-backup-memory-aware-swap/23-03-SUMMARY.md` when done.
</output>
