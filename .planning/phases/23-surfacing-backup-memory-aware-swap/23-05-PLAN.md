---
phase: 23-surfacing-backup-memory-aware-swap
plan: 05
type: execute
wave: 3
depends_on: [23-01, 23-02, 23-03, 23-04]
files_modified: []
autonomous: false
requirements: [CTRL-02, CTRL-04, CTRL-05]
must_haves:
  truths:
    - "On the live gfx1151 box: villa status --json shows schema 3 with real per-service memory health (a deliberately stopped villa-embed is NOT health PASS) and the dashboard renders the Memory panel"
    - "A real backup of the populated post-Phase-21 Qdrant volume restores through clean-recreate and retrieval still works; the dimension-skew WARN+confirm fires when provoked"
    - "The live swap drill proves: chat swap leaves memory intact; an embedding config change makes recall index refuse; --rebuild proceeds (OQ4 proof)"
    - "The box is returned to its prior state (backend choice, real data restored, no leftover drill artifacts)"
  artifacts: []
  key_links: []
---

<objective>
On-hardware verification of all three success criteria on the live Strix Halo box (standing v1.x discipline: this IS the dev/test host; real data, real services). Claude drives every CLI step itself; the human verifies the dashboard visuals and signs off. Verifies CTRL-02 (D-01..D-04), CTRL-04 (D-05..D-08), CTRL-05 (D-09..D-11) end to end, including the OQ2 probe-cost measurement and the OQ4 --rebuild proof.

Purpose: off-hardware tests cannot prove live probe behavior, real volume export/import integrity, or the OWUI KB rebuild path.
Output: drill evidence recorded in the SUMMARY; box restored to its pre-drill state.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/23-surfacing-backup-memory-aware-swap/23-CONTEXT.md
@.planning/phases/23-surfacing-backup-memory-aware-swap/23-01-SUMMARY.md
@.planning/phases/23-surfacing-backup-memory-aware-swap/23-02-SUMMARY.md
@.planning/phases/23-surfacing-backup-memory-aware-swap/23-03-SUMMARY.md
@.planning/phases/23-surfacing-backup-memory-aware-swap/23-04-SUMMARY.md
</context>

## Artifacts this phase produces (this plan's share)

No new code. This plan produces verification evidence (recorded in the SUMMARY) and confirms the OQ2 TTL choice on real hardware. Any failure found here routes back through gap-closure planning, not ad-hoc fixes.

<tasks>

<task type="auto">
  <name>Task 1: Live drill — status/dashboard, backup/restore, swap guards (Claude-driven, safety-first ordering)</name>
  <files>(none — verification only)</files>
  <read_first>
    - .planning/phases/23-surfacing-backup-memory-aware-swap/23-RESEARCH.md — Pitfall 9 (drill side effects: install reverts ROCm to Vulkan; dashboard restart gotcha; real-data hazard) and Pitfall 2 (probe-cost measurement)
    - .planning/phases/23-surfacing-backup-memory-aware-swap/23-UI-SPEC.md — Memory panel contract for the human-verify checklist
  </read_first>
  <action>
    Execute in THIS order (Pitfall 9: safety backup FIRST, restore real state LAST; record actual command output for the SUMMARY at each step):

    SAFETY: record current backend (villa status), current config.toml values; run a fresh `villa backup` to a safety archive OUTSIDE the drill artifacts; verify the safety archive's manifest contains qdrant-volume.tar + embedding_model/embedding_dim (first CTRL-04 evidence for free).

    CTRL-02 drill: `make build`; `systemctl --user restart villa-dashboard.service` (binary-trap gotcha). `./villa status --json` → assert schema_version 3, villa-qdrant/villa-embed rows with OFFLOAD N/A and real health, memory section showing nomic-embed-text-v1.5 / 768 + recall summary matching the live recall-state.json. Negative control (T-23-01 closure): `systemctl --user stop villa-embed.service`; within/after the 15s TTL window re-run status → embed row must be down/unknown, NEVER ready; restart the service; confirm recovery. OQ2 measurement: time `./villa status` cold vs TTL-warm; watch `podman ps -a` while the dashboard polls for ~1 min — no runaway helper-container churn; if churn/latency is unacceptable, raise memoryHealthTTL to 30s (the documented one-const fallback) and re-measure before proceeding.

    CTRL-04 drill: run `villa backup` of the REAL populated volume (observe villa-qdrant.service quiesce in journalctl); `villa restore` the drill archive → observe clean-recreate ordering and the honest report (memory posture line); confirm retrieval still works post-restore (OWUI semantic recall spot-check or `villa recall status`). Skew path: temporarily edit config.toml embedding_model to a fake id → `villa restore` again → assert the WARN+confirm fires with the remediation text; decline; revert config.

    CTRL-05 drill: chat-model swap (`villa model swap` to another catalog model and back) → assert villa-qdrant/villa-embed units byte-unchanged on disk (sha256sum before/after), services not restarted (journalctl), retrieval intact. D-10/OQ4: edit config.toml embedding_model → `villa recall index` → assert REFUSING + exitBlocked; revert config OR prove the --rebuild path: with the changed config, `villa recall index --rebuild` → must complete, re-stamp the new model, and OWUI KB must be clean-replaced (this is the OQ4 proof — record the result either way; if --rebuild misbehaves live, that is a gap, file it, do not patch ad hoc). Restore config.toml to the real embedding model and `villa recall index --rebuild` back to a healthy real index if the drill mutated it.

    RESTORE BOX STATE: if the box was on ROCm and any step re-ran install/recommend, re-run `villa backend set rocm`; restore the safety backup if the drill corrupted real data; remove drill archives; `villa status` + `villa doctor` final green; leave the dashboard service running the new binary.
  </action>
  <verify>
    <automated>./villa status --json | grep -c '"schema_version": 3' && make check</automated>
  </verify>
  <acceptance_criteria>
    - status --json on the live box: schema 3, both memory rows present with real health, memory section populated
    - Stopped-embed negative control recorded: health != ready (false-green closed on hardware)
    - Backup manifest from the live box contains qdrant-volume.tar entry + embedding_model "nomic-embed-text-v1.5" + embedding_dim 768 + recall_schema_version
    - Restore drill: clean-recreate observed, retrieval works post-restore; skew WARN+confirm transcript captured
    - Swap drill: memory unit files sha256-identical across the chat swap; recall index refusal + --rebuild outcome transcripts captured
    - OQ2 measurement recorded with the chosen TTL value
    - Final `villa doctor` and `make check` green; backend restored if applicable
  </acceptance_criteria>
  <done>All three SCs proven on gfx1151 with transcripts; box restored; any live failure filed as a gap (no ad-hoc fixes).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Operator sign-off — dashboard visuals + drill evidence</name>
  <files>(none — human verification)</files>
  <action>Present the Task 1 drill transcripts and the dashboard URL to the operator; pause for sign-off. Do not proceed past this gate without explicit approval (legitimacy of the phase close depends on the visual check Claude cannot perform).</action>
  <what-built>Phase 23 complete on-hardware: v3 status contract live, dashboard Memory panel rendering, Qdrant backup/restore proven on real data, swap guards proven (refusal + --rebuild), box restored.</what-built>
  <how-to-verify>
    1. Open http://127.0.0.1:8888 — Health panel shows villa-qdrant and villa-embed rows with sensible badges; Memory panel (below Models) shows: embedding model nomic-embed-text-v1.5, dimension 768, indexed chats count, last indexed timestamp, recall index badge "indexed" (green). No accent-blue anywhere in the new panel; typed-Unknown states (if any) are gray.
    2. Confirm memory-off honesty if desired: not required live (covered by golden — memory-off output differs only in schema_version).
    3. Review the Task 1 transcripts in this session: stopped-embed negative control, restore drill + skew WARN, swap refusal + --rebuild outcome, OQ2 timing, box-state restoration (backend setting correct, real index healthy).
    4. Spot-check OWUI recall still answers a semantic query against your real indexed chats.
  </how-to-verify>
  <resume-signal>Type "approved" to close Phase 23 verification, or describe any issue (it becomes a gap-closure item)</resume-signal>
  <verify>
    <automated>MISSING — human visual verification by design (dashboard rendering cannot be asserted off-screen); all automatable assertions already ran in Task 1</automated>
    <human-check>Memory panel renders per 23-UI-SPEC; drill transcripts reviewed; recall spot-check passes</human-check>
  </verify>
  <done>Operator typed "approved"; any reported issue captured verbatim for gap-closure planning.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| drill ↔ real operator data | the drill exports/imports the REAL Qdrant volume and OWUI data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-19 | Tampering (real-data loss) | restore/swap drill on the live box | mitigate | safety backup FIRST, restore LAST, config reverts scripted into the drill order (Pitfall 9) |
| T-23-20 | Denial of service | dashboard poll probe churn on real hardware | mitigate | OQ2 measurement step with the documented 30s TTL fallback |
| T-23-01 (closure) | Information disclosure (false assurance) | per-row health false-green | mitigate | stopped-embed negative control proves closure on hardware — record in SECURITY/threat register as closed |
| T-23-SC | Tampering | package installs | accept | none this phase |
</threat_model>

<verification>
- Drill transcripts captured in SUMMARY; operator approval recorded.
- Final make check + villa doctor green on the box.
</verification>

<success_criteria>
- ROADMAP Phase 23 SC#1/SC#2/SC#3 all proven live; carried Phase-22 false-green threat closed with an assigned ID (T-23-01); milestone's single contract evolution confirmed frozen exactly once.
</success_criteria>

<output>
Create `.planning/phases/23-surfacing-backup-memory-aware-swap/23-05-SUMMARY.md` when done.
</output>
