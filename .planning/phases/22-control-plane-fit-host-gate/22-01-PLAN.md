---
phase: 22-control-plane-fit-host-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/memory/footprint.go
  - internal/memory/footprint_test.go
  - internal/recommend/recommend.go
  - internal/recommend/recommend_test.go
  - cmd/villa/recommend.go
  - cmd/villa/install.go
  - cmd/villa/status.go
  - cmd/villa/model.go
  - cmd/villa/inference.go
  - cmd/villa/backend.go
  - cmd/villa/dashboard.go
  - cmd/villa/testdata/recommend.golden.json
autonomous: true
requirements: [CTRL-01]
must_haves:
  truths:
    - "With memory_enabled=true, villa recommend sizes the chat model against an envelope already shrunk by the embedding footprint (D-01) — the fit verdict, headroom, OOM guard and UsableEnvelopeBytes all see the shrunken value"
    - "With memory_enabled=false (or no config), recommend math, table output and exit behavior are byte-identical to v1.2 except for the two new JSON keys + schema_version 2"
    - "An unrecognized embedding model id reserves the 512 MiB conservative default and appends an honest note naming the model — never a silent 0 reservation (D-02)"
    - "villa install's pick is memory-aware: an opted-in install recommends against the shrunken envelope and its MinMemBytes gate includes the reservation"
  artifacts:
    - path: "internal/memory/footprint.go"
      provides: "Exported ConservativeFootprintBytes() — single-source 512 MiB conservative default (D-02)"
      contains: "func ConservativeFootprintBytes() uint64"
    - path: "internal/recommend/recommend.go"
      provides: "MemoryInputs struct, envelope-shrink reservation in Pick, EmbeddingReservationBytes + MemoryConsidered fields, recommendSchemaVersion = 2"
      contains: "recommendSchemaVersion = 2"
    - path: "cmd/villa/testdata/recommend.golden.json"
      provides: "Re-frozen recommend --json golden at schema 2 (the ONLY golden that changes this phase besides intentionally NEW fixtures)"
      contains: "\"schema_version\": 2"
  key_links:
    - from: "internal/recommend/recommend.go"
      to: "internal/memory/footprint.go"
      via: "memory.Footprint(in.EmbeddingModel) + memory.ConservativeFootprintBytes()"
      pattern: "memory\\.(Footprint|ConservativeFootprintBytes)"
    - from: "cmd/villa/install.go"
      to: "internal/recommend"
      via: "pick seam threads persisted memory inputs (Pitfall 3: memory-blind install defeats CTRL-01)"
      pattern: "recommend\\.MemoryInputs"
---

<objective>
CTRL-01 / SC#1: `villa recommend` reserves the embedding-model footprint in the unified-memory fit math BEFORE the chat-model fit — the resolved envelope shrinks first (D-01), typed-Unknown footprints reserve the conservative 512 MiB default with an honest note (D-02), and the reservation is surfaced as append-only `Recommendation` fields with `recommendSchemaVersion` bumped 1→2 and the recommend golden re-frozen exactly once (D-03). All 7 production `Pick` call sites thread explicit memory inputs (pure-core rule: no config loads inside the core). D-04 constraint honored: NO change to the villa-embed unit (CPU-only posture stays; the reservation still subtracts from the unified envelope).

Purpose: closes the STATE.md NON-NEGOTIABLE Phase-22 threat — omitting the embed footprint from `recommend.Pick()` means OOM or silent CPU fallback of the chat model under import load.
Output: memory-aware fit math, schema-2 recommend contract, one intentional golden re-freeze.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-control-plane-fit-host-gate/22-RESEARCH.md
@.planning/phases/22-control-plane-fit-host-gate/22-PATTERNS.md
</context>

## Artifacts this phase produces (Plan 01 share)

- `internal/memory.ConservativeFootprintBytes() uint64` — NEW exported accessor (single-source 512 MiB conservative default, D-02; mirrors the `orchestrate.EmbedImage()` accessor-export shape).
- `internal/recommend.MemoryInputs` — NEW exported value struct `{Enabled bool; EmbeddingModel string}`; zero value = memory off = byte-identical math.
- `recommend.Pick(p, c, ov, mem MemoryInputs)` — signature grows a 4th parameter (D-01).
- `Recommendation.EmbeddingReservationBytes uint64` (JSON `embedding_reservation_bytes`) and `Recommendation.MemoryConsidered bool` (JSON `memory_considered`) — NEW append-only fields ABOVE `SchemaVersion` (D-03).
- `recommendSchemaVersion = 2` (was 1).
- Gated "embed reservation" row in the recommend table render (printed only when reservation > 0).
- `cmd/villa/testdata/recommend.golden.json` re-frozen at schema 2.
- Install resource gate: `MinMemBytes` now adds `rec.EmbeddingReservationBytes` (Research Open Question 2 resolved YES — one line, more honest; the value flows from the pick so `memory.Footprint` remains the single source).

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Memory-aware fit math in recommend.Pick + schema-2 contract (D-01/D-02/D-03)</name>
  <files>internal/memory/footprint.go, internal/memory/footprint_test.go, internal/recommend/recommend.go, internal/recommend/recommend_test.go</files>
  <read_first>
    - internal/recommend/recommend.go (entire file — Pick :123-147, degraded note :136-139, Recommendation struct :67-103, SchemaVersion last-field rule :100-102, finalizeRecommendation :154-162, pickBest OOM guard :232, no-fit refusal :244-252)
    - internal/recommend/envelope.go (resolveEnvelope :45, conservativeFloor zero-check discipline :28-37)
    - internal/memory/footprint.go (Footprint :38-46, embedFootprints :29-31)
    - internal/orchestrate/memory.go :45-47 (EmbedImage accessor-export doc-comment shape to copy for ConservativeFootprintBytes)
    - .planning/phases/22-control-plane-fit-host-gate/22-PATTERNS.md (section "internal/recommend/recommend.go" — exact insertion point and note template)
  </read_first>
  <behavior>
    - Memory off (zero-value MemoryInputs): Pick output identical to today's for every existing table-test case; EmbeddingReservationBytes==0, MemoryConsidered==false, SchemaVersion==2.
    - Memory on + pinned model ("nomic-embed-text-v1.5"): UsableEnvelopeBytes == previous envelope − 512 MiB; fit verdict computed against the shrunken value; EmbeddingReservationBytes==536870912, MemoryConsidered==true, no D-02 note.
    - Memory on + unrecognized model id: reservation == ConservativeFootprintBytes() (512 MiB) AND Notes contains a "RESERVED CONSERVATIVELY" note naming the model id and citing D-02.
    - Memory on + reservation >= envelope: envelope becomes 0 and the existing no-fit refusal path triggers honestly (no uint64 wraparound).
    - WeightBytes reservation-invariance: for an explicit --model override, WeightBytes/KVCacheBytes are identical with and without memory inputs (envelope-independence guard for the frozen status path, Pitfall 3).
    - finalizeRecommendation stamps the new fields on ALL return paths including the no-envelope refusal (zero/false when off).
  </behavior>
  <action>
    In internal/memory/footprint.go: add exported `ConservativeFootprintBytes() uint64` returning the same 512 MiB value embedFootprints pins for nomic-embed-text-v1.5 — unexported const + one-line exported func with a "so downstream readers never re-type the literal" doc comment citing D-02 (copy the orchestrate.EmbedImage accessor shape). Add a test asserting it equals the pinned nomic-embed-text-v1.5 footprint (single-source coherence).

    In internal/recommend/recommend.go (implements D-01, D-02, D-03):
    1. Add exported `MemoryInputs` struct: `Enabled bool`, `EmbeddingModel string`, with doc comments citing D-01 (zero value = memory off = byte-identical math).
    2. Grow `Pick(p detect.HostProfile, c catalog.Catalog, ov Overrides, mem MemoryInputs)`. Insert the reservation between the `if !ok` refusal and the degraded-note append: when `mem.Enabled`, call `memory.Footprint(mem.EmbeddingModel)`; `.Known` → reservation = value; else reservation = `memory.ConservativeFootprintBytes()` plus a note copying the degraded-note template (ALL-CAPS prefix "RESERVED CONSERVATIVELY:", the unrecognized model id, the value via humanGiB, "(D-02)" citation). Guard `if reservation >= envelope { envelope = 0 } else { envelope -= reservation }` (never wrap a uint64; envelope 0 falls into pickBest's existing no-fit refusal). Subtract BEFORE the degraded note and BEFORE pickOverride/pickBest so headroomBytes, the OOM guard, and UsableEnvelopeBytes all see the shrunken value (SC#1 "envelope shrinks first").
    3. Append `EmbeddingReservationBytes uint64 \`json:"embedding_reservation_bytes"\`` and `MemoryConsidered bool \`json:"memory_considered"\`` to Recommendation ABOVE SchemaVersion (last-field rule), each with a doc comment citing D-03. Bump `recommendSchemaVersion` 1→2 and update its doc comment. Thread the reservation + considered flag through finalizeRecommendation (or set on the Recommendation before finalize) so EVERY return path — including the no-envelope refusal — stamps them (zero/false when off, reservation+true when on; the refusal path with memory on still reports MemoryConsidered=true and the reservation it would have applied OR zero — pick one and assert it in a test; recommended: reservation as computed, honest surface).
    4. Update all in-package test call sites to pass `MemoryInputs{}`; add table-test cases for every behavior listed above.
  </action>
  <verify>
    <automated>go test ./internal/recommend/ ./internal/memory/ -count=1</automated>
  </verify>
  <done>Pick reserves the footprint before fit when enabled; zero-value inputs are provably byte-identical math; schema const is 2; new fields sit above SchemaVersion; all listed behaviors covered by table tests; `go vet ./...` clean.</done>
  <acceptance_criteria>
    - Source: `grep -n "func ConservativeFootprintBytes" internal/memory/footprint.go` returns exactly one match.
    - Source: `grep -n "recommendSchemaVersion = 2" internal/recommend/recommend.go` returns one match; `grep -v '^\s*//' internal/recommend/recommend.go | grep -c '512'` returns 0 (no re-typed 512-literal in recommend CODE — comments excluded per grep-gate hygiene; the value flows only from internal/memory).
    - Source: in the Recommendation struct, `embedding_reservation_bytes` and `memory_considered` JSON tags appear ABOVE the `schema_version` tag.
    - Test: `go test ./internal/recommend/ -count=1` passes with new cases covering: off-path identity, pinned-model shrink (envelope reduced by exactly 536870912 bytes), unknown-model conservative default + note containing "RESERVED CONSERVATIVELY" and the model id, reservation>=envelope refusal, override WeightBytes invariance.
    - Test: `go test ./internal/memory/ -count=1` passes including the accessor-equals-pinned-footprint case.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Thread the 7 Pick call sites, gated reservation render, single golden re-freeze</name>
  <files>cmd/villa/recommend.go, cmd/villa/install.go, cmd/villa/status.go, cmd/villa/model.go, cmd/villa/inference.go, cmd/villa/backend.go, cmd/villa/dashboard.go, cmd/villa/testdata/recommend.golden.json</files>
  <read_first>
    - cmd/villa/recommend.go (verb RunE :41-76, fail-soft config load for catalog path :48-53, renderRecommendTable fit-math block :149-157, gated ROCmAdvice block :173-178)
    - cmd/villa/install_memory.go :117-144 (liveLoadedMemoryEnabled / fail-soft load shape to copy at every site)
    - cmd/villa/install.go :267-273 (ResourceReq build) and :980-986 (pick seam)
    - cmd/villa/status.go :400-407 (liveWeightBytes — the frozen-path site that MUST get zero-value inputs)
    - cmd/villa/model.go :331, cmd/villa/inference.go :144, cmd/villa/backend.go :392, cmd/villa/dashboard.go :268 (the remaining one-line ripple sites)
    - cmd/villa/detect_test.go :13 (package-level -update flag convention)
    - .planning/phases/22-control-plane-fit-host-gate/22-PATTERNS.md (section "Pick call-site ripple" — the per-site decision table)
  </read_first>
  <action>
    Thread `recommend.MemoryInputs` per the RESEARCH decision table (implements D-01 cmd-tier half; Pitfalls 2/3/4):
    - cmd/villa/recommend.go: extend the existing fail-soft `config.LoadVilla()` to also source `MemoryEnabled` + `EmbeddingModel` (load error → zero-value inputs, never an error-path change); pass to Pick. In renderRecommendTable add a reservation row gated on `rec.EmbeddingReservationBytes > 0` (copy the ROCmAdvice gated-line pattern) so memory-off table output stays byte-identical; insert it in the fit-math tabwriter block (e.g. a "− embed reservation" row before the usable-envelope line). JSON render needs no change — struct growth flows through.
    - cmd/villa/install.go pick seam (:980-986): load persisted memory inputs fail-soft (reuse/parallel the loadedMemoryEnabled seam) and pass them — install MUST be memory-aware (Pitfall 3). In the ResourceReq build (:267-273) add `rec.EmbeddingReservationBytes` to MinMemBytes with a comment citing Research Open Question 2 resolved YES — value comes from the pick, zero when memory off, so the off-path gate is unchanged.
    - cmd/villa/status.go liveWeightBytes (:405): pass `recommend.MemoryInputs{}` (zero value) with a comment stating WHY: provably keeps status.json.golden byte-identical; WeightBytes is envelope-independent for overrides (guarded by the Task-1 invariance test).
    - cmd/villa/model.go :331, cmd/villa/inference.go :144, cmd/villa/backend.go :392, cmd/villa/dashboard.go :268: thread persisted config fail-soft (load error → zero value) so swap/bench/dashboard fit re-validation sees the same shrunken envelope the user was recommended.
    - Fix any cmd-tier test call sites to compile (pass zero-value inputs unless the test exercises memory).
    - Re-freeze the recommend golden ONCE with the targeted filter: `go test ./cmd/villa -run TestRecommend -update` (Pitfall 2 — never package-wide). Then audit: `git status --porcelain cmd/villa/testdata/` must list ONLY recommend.golden.json.
  </action>
  <verify>
    <automated>go test ./cmd/villa/ -count=1 && make check && test "$(git status --porcelain cmd/villa/testdata/ | grep -v '^??' | wc -l)" = "1"</automated>
  </verify>
  <done>All 7 production call sites compile with explicit memory inputs; install pick + MinMemBytes are memory-aware; status path provably untouched; recommend golden re-frozen at schema 2 and it is the only modified golden; `make check` green.</done>
  <acceptance_criteria>
    - Behavior: `git status --porcelain cmd/villa/testdata/` shows exactly one MODIFIED file: `cmd/villa/testdata/recommend.golden.json` (new fixtures, if any, are additions only).
    - Source: recommend.golden.json contains `"schema_version": 2`, `"embedding_reservation_bytes"` and `"memory_considered"` keys.
    - Source: `grep -n "MemoryInputs{}" cmd/villa/status.go` shows the zero-value pass in liveWeightBytes with an adjacent comment naming status.json.golden.
    - Source: cmd/villa/install.go MinMemBytes expression includes `EmbeddingReservationBytes`.
    - Test: `make check` passes (go vet + full go test ./... — proves status/preflight/doctor/orchestrate goldens unchanged).
    - CLI: with no config file present, `./villa recommend` table output contains no "embed reservation" row (gated on >0).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config.toml → recommend core | hand-edited untrusted TOML flows into fit math via cmd-tier load |
| recommend output → install resource gate | a wrong reservation propagates into the install BLOCK gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-01 | DoS | recommend.Pick reservation | mitigate | under-reserving → OOM under embedding load; D-02 conservative default on typed-Unknown, never reserve 0; reservation >= envelope guard prevents uint64 wrap |
| T-22-02 | Tampering | EmbeddingModel config string | mitigate | value flows only into a pure map lookup + fmt note text; no exec, no shell, no path use |
| T-22-03 | Repudiation | install pick seam | mitigate | memory-blind install would silently skip the reservation (false-green install); persisted inputs threaded + MinMemBytes includes reservation; off-path identity guarded by existing goldens |
| T-22-04 | Info integrity | recommend --json contract | mitigate | append-only fields + schema bump 1→2; single targeted golden re-freeze; `make check` proves no other golden moved |
| T-22-SC | Tampering | package installs | accept | N/A — zero new packages this phase (RESEARCH Package Legitimacy Audit: none) |
</threat_model>

<verification>
- `go test ./internal/recommend/ ./internal/memory/ -count=1` green.
- `make check` green; `git status --porcelain cmd/villa/testdata/` shows only recommend.golden.json modified.
- `go test ./internal/inference/ -run TestSeamGrepGate -count=1` green (no leaked literal).
</verification>

<success_criteria>
SC#1 satisfied in code: envelope shrinks before fit when memory_enabled=true; conservative default + note on Unknown; schema 2 append-only contract; exactly one golden re-frozen; memory-off paths byte-identical.
</success_criteria>

<output>
Create `.planning/phases/22-control-plane-fit-host-gate/22-01-SUMMARY.md` when done.
</output>
