---
phase: 22-control-plane-fit-host-gate
plan: 03
type: execute
wave: 3
depends_on: [22-01, 22-02]
files_modified:
  - internal/doctor/doctor.go
  - internal/doctor/doctor_test.go
  - cmd/villa/doctor.go
  - cmd/villa/doctor_test.go
autonomous: true
requirements: [CTRL-03]
must_haves:
  truths:
    - "With memory_enabled=true, villa doctor folds memory-stack findings into its existing PASS/WARN/FAIL exit contract: vector-disk/headroom checks (reused from preflight.RunMemory — no duplicate logic, D-08) and a chat-model residency-under-embedding-load finding (D-09)"
    - "Doctor PASS is REACHABLE on a healthy memory-on stack: the typed-Unknown offload WARN findings for villa-qdrant/villa-embed are down-ranked-but-visible (non-rank-raising), while a confident offload FAIL on those services is NEVER suppressed (Pitfall 1 / no-false-green)"
    - "The residency proof drives a REAL /v1/embeddings workload and samples the chat model's GTT/journal residency DURING the drive: confident CPU fallback = BLOCK FAIL; unevaluable (stack down, scrape failed) = typed-Unknown WARN — never a false-green PASS (D-09)"
    - "Doctor never mutates state: services down → the proof degrades to WARN; only transient podman run --rm probe containers are used (D-10)"
    - "With memory_enabled=false, doctor output is byte-identical — all new Deps fields are nil/zero-safe (mirror D-06)"
  artifacts:
    - path: "internal/doctor/doctor.go"
      provides: "Deps growth (MemoryEnabled, MemoryServices, RunMemoryChecks, ResidencyUnderLoad), memory check fold via findingFromCheck, MEM-DOC-residency finding mapping, non-GPU offload down-rank predicate"
      contains: "ResidencyUnderLoad"
    - path: "cmd/villa/doctor.go"
      provides: "liveDoctorDeps growth: memory service names from orchestrate accessors, RunMemory binding, live under-load proof (goroutine embed drive + mid-load RunningOffloadVerdict sample)"
      contains: "runProbeCurl"
  key_links:
    - from: "internal/doctor/doctor.go"
      to: "internal/preflight/checks_memory.go"
      via: "Deps.RunMemoryChecks func(detect.HostProfile) []preflight.CheckResult bound to preflight.RunMemory in cmd tier (D-08, composition over re-implementation)"
      pattern: "RunMemoryChecks"
    - from: "cmd/villa/doctor.go"
      to: "cmd/villa/install_memory.go"
      via: "embed-load drive reuses runProbeCurl over villa.network with orchestrate.EmbedImage() (villa-embed has no host port)"
      pattern: "runProbeCurl"
    - from: "cmd/villa/doctor.go"
      to: "internal/inference/running_offload.go"
      via: "RunningOffloadVerdict with mid-drive GTTUsedBytes sample + villa-llama ResidencyJournal + liveWeightBytes + BackendFor(cfg.Backend).ResidencyProof()"
      pattern: "RunningOffloadVerdict"
---

<objective>
CTRL-03 / SC#3: `villa doctor` folds memory-stack health into its existing PASS/WARN/FAIL exit contract (D-08), adds the offload-asserting residency proof that the chat model survives a REAL embedding workload (D-09, read-only per D-10), and resolves the live-verified Pitfall 1 — doctor today exits WARN on a perfectly healthy memory-on stack because villa-qdrant/villa-embed status rows carry typed-Unknown offload WARNs. Resolution chosen (Research Open Question 3): **down-rank-but-visible**, mirroring the ROCm supersession precedent at doctor.go:284-295 — the honest "visible but non-rank-raising" pattern; a confident FAIL is never suppressed. The status-side fix is Phase 23: `status.Report`, `internal/status`, and `status.json.golden` are NOT touched.

Purpose: makes doctor PASS reachable and meaningful on memory-on installs, and proves the chat model is not silently evicted to CPU under embedding/import load — the no-false-green bar.
Output: doctor Deps growth + memory findings + down-rank handling (pure core), live under-load proof seam (cmd tier), additive doctor fixtures/goldens.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-control-plane-fit-host-gate/22-RESEARCH.md
@.planning/phases/22-control-plane-fit-host-gate/22-PATTERNS.md
</context>

## Artifacts this phase produces (Plan 03 share)

- `doctor.Deps` NEW nil/zero-safe fields: `MemoryEnabled bool`, `MemoryServices []string` (the `.service` names for villa-qdrant/villa-embed, supplied by the cmd tier), `RunMemoryChecks func(detect.HostProfile) []preflight.CheckResult`, `ResidencyUnderLoad func() inference.Verdict`.
- NEW doctor finding ID `MEM-DOC-residency`, Name "Chat-model residency under embedding load" (Verdict→Finding mapping per the offloadFinding precedent).
- Memory disk/headroom findings folded via the existing `findingFromCheck` (IDs MEM-PRE-disk / MEM-PRE-headroom flow through unchanged).
- Non-GPU offload down-rank predicate in `Aggregate`: `offload:`-prefixed findings whose service is in `MemoryServices` AND `Status == WARN` are visible but non-rank-raising (FAIL never suppressed).
- `liveResidencyUnderLoad` (or equivalently named) live seam in cmd/villa/doctor.go: D-10 precondition gate + goroutine embed drive (N=12 requests, ~4 KiB payload each, per-request 10 s timeout, 60 s overall budget) + mid-drive `RunningOffloadVerdict` sample.
- NEW additive doctor render fixtures/goldens for memory-on output (existing doctor goldens unchanged; `reportSchemaVersion` stays 1 — findings are data, not schema).

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: doctor core — Deps growth, memory fold, MEM-DOC-residency mapping, offload down-rank (D-08/D-09 pure half)</name>
  <files>internal/doctor/doctor.go, internal/doctor/doctor_test.go</files>
  <read_first>
    - internal/doctor/doctor.go (entire file — Deps :117-143, RunROCmImage nil-safe seam doc :134-142, Aggregate :171, status-row offload fold :214-222, ROCm supersession down-rank :284-295 + DOCTOR-02 no-false-green comment :276-283, findingFromCheck :313-324, offloadFinding :361-384, nonEmpty :388, reportSchemaVersion :54)
    - internal/doctor/doctor_test.go (fake-Deps double shape, existing fixture style)
    - internal/preflight/checks_memory.go (the CheckResult shape RunMemoryChecks folds — created by Plan 22-02)
    - internal/inference/running_offload.go (Verdict / StatusPass / StatusFail / StatusWarn consumed opaquely — read Status/Detail/Remediation ONLY, seam-clean)
    - .planning/phases/22-control-plane-fit-host-gate/22-PATTERNS.md (section "internal/doctor/doctor.go" — exact precedent excerpts)
  </read_first>
  <behavior>
    - Memory off (all new Deps fields nil/zero): Aggregate output byte-identical to today for every existing test fixture.
    - RunMemoryChecks non-nil: its CheckResults appear as findings via findingFromCheck, ranked worst-wins like every other check (FAIL raises overall to FAIL).
    - ResidencyUnderLoad non-nil: Verdict StatusFail → finding MEM-DOC-residency Tier BLOCK Status FAIL with non-empty remediation; StatusWarn → Tier WARN Status WARN ("could not evaluate" detail preserved); StatusPass → PASS. Never PASS-by-default when the func is nil (nil → no finding emitted at all).
    - Down-rank: a finding with ID "offload:<svc>" where <svc> ∈ MemoryServices and Status==WARN does not raise the overall rank but REMAINS in the findings list (visible); the same finding with Status==FAIL raises the rank to FAIL (suppression conjunction preserved).
    - Healthy-memory-on scenario: status fold yields offload WARNs for both memory services + everything else PASS + proof PASS + checks PASS → overall PASS (Pitfall 1 resolved).
  </behavior>
  <action>
    In internal/doctor/doctor.go (implements D-08/D-09/D-10 core half; resolves Research Open Question 3 as down-rank-but-visible):
    1. Add the four Deps fields listed in "Artifacts" above, each with a RunROCmImage-style doc comment stating nil/zero-safety and the decision ID (D-08/D-09). `MemoryServices` doc must state the values are systemd `.service` names supplied by the cmd tier from orchestrate accessors — never typed literals here (ID-string-not-marker precedent at doctor.go:56-66).
    2. In Aggregate: when `RunMemoryChecks != nil`, fold `for _, c := range d.RunMemoryChecks(profile) { findings = append(findings, findingFromCheck(c)) }` — no new aggregation logic.
    3. Add `residencyUnderLoadFinding(v inference.Verdict) Finding` copying offloadFinding's opaque-Verdict switch: ID `MEM-DOC-residency`, Name "Chat-model residency under embedding load", Provenance naming the mechanism ("embed-load drive + inference.RunningOffloadVerdict"); StatusFail → tierBlock/statusFail with `nonEmpty(v.Remediation, ...)` fallback text pointing at `villa backend set` + `villa logs`; default/StatusWarn → tierWarn/statusWarn with a "could not evaluate residency under embedding load — ensure the stack is running, then re-run villa doctor" fallback. Emit only when `ResidencyUnderLoad != nil`.
    4. Extend the worst-wins loop with a memory-offload predicate alongside `superseded`: skip rank contribution for findings whose ID has the "offload:" prefix, whose service suffix matches an entry in `MemoryServices`, AND whose `Status == statusWarn` — visible but non-rank-raising, exactly the supersession shape. CRITICAL: keep the Status==statusWarn conjunction so a confident FAIL is never suppressed (DOCTOR-02). Do NOT remove or duplicate the existing health rows for the memory services (they already arrive via the status fold — RESEARCH "Services-up rows" note).
    5. `reportSchemaVersion` stays 1 — no Report/Finding struct fields are added (findings are data, not schema).
    Add table tests with fake Deps covering every behavior above, including the healthy-memory-on → overall PASS scenario and the FAIL-not-suppressed scenario.
  </action>
  <verify>
    <automated>go test ./internal/doctor/ -count=1</automated>
  </verify>
  <done>All four Deps fields nil/zero-safe; memory checks folded; MEM-DOC-residency mapping matches D-09 semantics; down-rank visible-but-non-rank-raising with FAIL never suppressed; memory-off fixtures byte-identical; reportSchemaVersion unchanged at 1.</done>
  <acceptance_criteria>
    - Source: `grep -n "MemoryServices\|RunMemoryChecks\|ResidencyUnderLoad\|MemoryEnabled" internal/doctor/doctor.go` shows all four Deps fields with doc comments citing D-08/D-09.
    - Source: `grep -n "reportSchemaVersion = 1" internal/doctor/doctor.go` still matches (no bump).
    - Source: the down-rank predicate contains an explicit `statusWarn` (or equivalent) equality conjunction — verifiable by reading the worst-wins loop.
    - Test: `go test ./internal/doctor/ -count=1` passes with new cases: memory-off identity, checks folded + FAIL raises overall, proof FAIL→BLOCK FAIL, proof WARN→WARN, proof PASS, offload WARN for a MemoryServices entry down-ranked (overall PASS in the healthy scenario), offload FAIL for the same service NOT suppressed (overall FAIL).
    - Test: `go test ./internal/status/ -count=1` passes untouched (no status-side change; Phase 23 boundary respected).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: live wiring — liveDoctorDeps growth + under-load proof seam (D-09/D-10 live half)</name>
  <files>cmd/villa/doctor.go, cmd/villa/doctor_test.go</files>
  <read_first>
    - cmd/villa/doctor.go (liveDoctorDeps :156-236, rocmImageGate conditional-closure construction :172-187, renderDoctor + exit mapping :81-111)
    - cmd/villa/install_memory.go (runProbeCurl :322-341 — fixed-arg podman run --rm --network villa --entrypoint curl; liveMemoryProof body marshaling :227-273; liveLoadedMemoryEnabled :132-144; evalMemoryProof precondition→verdict shape :180-208)
    - cmd/villa/status.go :168-196 + :400-407 (liveStatusDeps residency wiring — ResidencyJournal, detect.GTTUsedBytes, liveWeightBytes, BackendFor(cfg.Backend).ResidencyProof() — the exact inputs to reuse)
    - internal/orchestrate/memory.go :47, :106-117 (EmbedImage(), QdrantContainerUnitName(), EmbedContainerUnitName() accessors — never typed literals)
    - internal/inference/running_offload.go (RunningOffloadInput :40, RunningOffloadVerdict :327-331, gttFloor :234-263 — the dynamic eviction signal)
    - cmd/villa/doctor_test.go (render-golden fixture style — goldens are driven by injected fixture Reports)
  </read_first>
  <action>
    In cmd/villa/doctor.go (implements D-09/D-10 live half; Pitfalls 6/7):
    1. liveDoctorDeps: load MemoryEnabled/EmbeddingModel from the already-loaded cfg; when enabled, bind `RunMemoryChecks` to a closure over `preflight.RunMemory(p, preflight.MemoryGateInput{EmbeddingModel: cfg.EmbeddingModel})`, set `MemoryServices` from `orchestrate.QdrantContainerUnitName()`/`EmbedContainerUnitName()` converted to their generated systemd `.service` names (the same name derivation the status fold uses — never a typed "villa-qdrant.service" literal), set `MemoryEnabled`, and bind `ResidencyUnderLoad` to the new live seam. When disabled leave all four zero/nil (memory-off byte-identical; mirror the rocmImageGate conditional shape).
    2. New live seam (e.g. `liveResidencyUnderLoad(cfg config.VillaConfig, sd *statusDeps) func() inference.Verdict` — exact wiring shape is the executor's call within these constraints):
       a. D-10 precondition gate FIRST: memory enabled + `memory.Decide(cfg)` valid; villa-llama active; villa-qdrant + villa-embed active (reuse the existing systemd is-active seam the status/doctor path already uses). Any unmet precondition → return a StatusWarn Verdict with Detail "could not evaluate residency under embedding load — <which precondition>" and remediation naming the service (`systemctl --user status <unit>` style). NEVER start a service.
       b. Drive: launch a goroutine sending N=12 sequential POSTs to `http://<cfg.EmbedAddr>:<cfg.EmbedPort>/v1/embeddings` via `runProbeCurl(ctx, orchestrate.EmbedImage(), "-sf", "-X", "POST", url, "-H", "Content-Type: application/json", "-d", string(body))` — body `json.Marshal(map[string]any{"input": <~4 KiB repeated text>, "model": cfg.EmbeddingModel, "encoding_format": "float"})`; model id JSON-marshaled, never interpolated (T-19-10). Per-request 10 s context timeout; overall proof budget 60 s via one parent `context.WithTimeout`; containers are `--rm` (Pitfall 7). Signal request completions over a channel.
       c. Sample DURING load (Pitfall 6): after at least 2 requests have completed and while the drive is still in flight, evaluate `inference.RunningOffloadVerdict(inference.RunningOffloadInput{JournalText: <sys.ResidencyJournal for villa-llama>, GTTUsedBytes: detect.GTTUsedBytes(), WeightBytes: liveWeightBytes(cfg), Markers: <inference.BackendFor(cfg.Backend).ResidencyProof()>, ConfigModel/ConfigContext from cfg})` — exactly the liveStatusDeps input set, sampled mid-drive. Drive errors alone (curl non-zero) degrade to StatusWarn ("embed drive could not complete"), not FAIL — the FAIL signal is the chat model's residency, not the drive's success.
       d. Return the Verdict; wait for the drive goroutine to finish (bounded by the parent context) before returning so no probe container outlives the call.
    3. Add cmd-tier render coverage: NEW additive fixture Report(s) containing the memory findings (MEM-PRE-disk/MEM-PRE-headroom/MEM-DOC-residency, plus a down-ranked offload WARN) with NEW golden file(s); existing doctor goldens must remain byte-identical. If goldens are (re)generated, use a targeted `-run TestDoctor... -update` filter and audit `git status --porcelain cmd/villa/testdata/` shows only NEW files.
  </action>
  <verify>
    <automated>go test ./cmd/villa/ -count=1 && go test ./internal/inference/ -run TestSeamGrepGate -count=1 && make check</automated>
  </verify>
  <done>liveDoctorDeps binds all four memory seams only when enabled; the proof seam is precondition-gated, fixed-arg, bounded, samples mid-drive, and maps to D-09 semantics; new render fixtures additive; seam gate green; make check green.</done>
  <acceptance_criteria>
    - Source: `grep -n "EmbedImage()\|QdrantContainerUnitName()\|EmbedContainerUnitName()" cmd/villa/doctor.go` shows accessor use; `grep -n '"villa-qdrant\|"villa-embed' cmd/villa/doctor.go` shows NO typed service/image literals.
    - Source: the drive body is built with `json.Marshal` and passed as a discrete `-d` arg (no fmt-into-command-string of config values other than the URL host:port composition mirroring liveMemoryProof).
    - Source: a parent `context.WithTimeout` bounds the whole proof at 60 s and each request at 10 s.
    - Test: `go test ./internal/inference/ -run TestSeamGrepGate -count=1` passes with zero allowlist changes.
    - Test: `make check` green; `git status --porcelain cmd/villa/testdata/` shows only NEW (untracked) fixture/golden files, no modifications to existing ones.
    - Behavior: with memory_enabled=false the doctor output for the existing fixtures is byte-identical (existing doctor goldens pass unmodified).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config.toml → embed-load drive | config-sourced embed model/addr/port flow into a podman/curl exec |
| container journal/sysfs → residency verdict | untrusted service output feeds the PASS/WARN/FAIL decision |
| doctor (read-only diagnosis) → host state | a diagnostic that mutates services would violate its contract |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-09 | Tampering | embed-load drive args | mitigate | fixed-arg exec.CommandContext via runProbeCurl; model id into a json.Marshal body, never a command string (T-19-10 precedent) |
| T-22-10 | Repudiation | false-green: chat model silently CPU-falls-back under load | mitigate | offload-asserting D-09 mapping — confident fallback = BLOCK FAIL, unevaluable = WARN, drive errors degrade to WARN (never PASS-by-default); down-rank predicate suppresses ONLY Status==WARN, never FAIL |
| T-22-11 | DoS | load proof hammers villa-embed / hangs / leaks containers | mitigate | N=12 bounded requests, 10 s per-request + 60 s overall context, --rm transient containers, drive goroutine joined before return |
| T-22-12 | Tampering | "read-only" doctor mutating state | mitigate | D-10: precondition gate degrades to WARN; no systemctl start anywhere; only transient podman run --rm probes (established install-proof precedent) |
| T-22-13 | Info Disclosure | new probe traffic leaving the box | mitigate | all drive traffic stays on villa.network (container DNS, no host port); zero new outbound; PRIV-01..03 posture unchanged |
| T-22-SC | Tampering | package installs | accept | N/A — zero new packages this phase |
</threat_model>

<verification>
- `go test ./internal/doctor/ ./cmd/villa/ -count=1` green; `make check` green.
- `go test ./internal/inference/ -run TestSeamGrepGate -count=1` green with zero allowlist changes.
- No modification to internal/status/, status.json.golden, or any existing doctor golden (Phase 23 boundary).
</verification>

<success_criteria>
SC#3 satisfied in code: doctor folds memory checks + the under-load residency finding into its exit contract; PASS reachable on a healthy memory-on stack (down-rank-but-visible); confident CPU fallback = FAIL, unevaluable = typed-Unknown WARN; doctor never mutates state. On-hardware proof runs in Plan 22-04.
</success_criteria>

<output>
Create `.planning/phases/22-control-plane-fit-host-gate/22-03-SUMMARY.md` when done.
</output>
