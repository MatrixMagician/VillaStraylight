---
phase: 22-control-plane-fit-host-gate
plan: 04
type: execute
wave: 4
depends_on: [22-01, 22-02, 22-03]
# Normally zero edits. CONTINGENT (D-05 sanctioned exception, triggered ONLY if the
# step-7 measurement shows MemoryPeak > 512 MiB under sustained drive):
#   - internal/memory/footprint.go (raise pinned value + conservative default)
#   - internal/memory/footprint_test.go (accessor-equals-pinned coherence test follows)
files_modified: []
autonomous: false
requirements: [CTRL-01, CTRL-03, CTRL-06]
must_haves:
  truths:
    - "On the live gfx1151 box with memory_enabled=true: villa recommend shows the embed reservation in the fit math and --json reports schema_version 2 with embedding_reservation_bytes > 0 (SC#1)"
    - "villa preflight emits MEM-PRE-disk + MEM-PRE-headroom rows against the REAL rootless podman volume root; with memory_enabled=false the rows are absent (SC#2)"
    - "villa doctor reaches overall PASS on the healthy memory-on stack, and the MEM-DOC-residency finding is PASS while a real /v1/embeddings drive is in flight; with the stack down it degrades to typed-Unknown WARN, never PASS (SC#3)"
    - "D-05 measurement recorded: villa-embed MemoryCurrent/MemoryPeak under a sustained embed drive, compared against the 512 MiB reservation — raise the constant ONLY if it under-reserves"
    - "The operator's ROCm backend survives verification: backend noted before, restored after (Pitfall 8 — install/recommend --save reverts to vulkan)"
  artifacts:
    - path: ".planning/phases/22-control-plane-fit-host-gate/22-04-SUMMARY.md"
      provides: "On-hardware verification record incl. the D-05 footprint measurement and command outputs"
  key_links:
    - from: "villa doctor (live)"
      to: "villa-embed /v1/embeddings + villa-llama GTT residency"
      via: "real embed drive over villa.network while gttFloor samples mem_info_gtt_used"
      pattern: "MEM-DOC-residency"
---

<objective>
On-hardware verification of all three success criteria on the live Strix Halo (gfx1151) dev box — the doctor residency proof, footprint measurement (D-05), and live preflight checks are manual-only verifications per 22-VALIDATION.md, and this machine IS the target host. Records the D-05 measurement in the phase summary (a code change to the 512 MiB constant happens ONLY if measurement shows under-reservation — that would be a finding, not silent acceptance).

Purpose: SC#1-3 are claims about real unified-memory behavior under real embedding load; only this box can prove them.
Output: verified phase, recorded measurement, operator sign-off.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/22-control-plane-fit-host-gate/22-VALIDATION.md
@.planning/phases/22-control-plane-fit-host-gate/22-RESEARCH.md
</context>

## Artifacts this phase produces (Plan 04 share)

No new code symbols. Produces the on-hardware verification record and the D-05 measurement observation (consumed by Phase 23 per 22-CONTEXT.md Integration Points). If — and only if — the D-05 measurement shows MemoryPeak > 512 MiB under sustained drive, a follow-up constant raise in `internal/memory/footprint.go` (with its coherence test in `internal/memory/footprint_test.go`) is performed in this plan as a recorded finding (D-05's sanctioned exception). These two contingent files are declared in the frontmatter comment above so a triggered change is never an undeclared edit.

<tasks>

<task type="auto">
  <name>Task 1: Build, on-box verification runs, D-05 measurement (backend-safe per Pitfall 8)</name>
  <files>(none normally — verification only; ./villa binary rebuilt. CONTINGENT per D-05: internal/memory/footprint.go + internal/memory/footprint_test.go ONLY if step 7 measures MemoryPeak > 512 MiB)</files>
  <read_first>
    - .planning/phases/22-control-plane-fit-host-gate/22-VALIDATION.md (Manual-Only Verifications table — the three checks this task executes)
    - .planning/phases/22-control-plane-fit-host-gate/22-RESEARCH.md (Pitfall 8 — install/recommend --save reverts ROCm to vulkan; the box is on ROCm 7.2.4)
    - .planning/phases/19-vector-store-local-embeddings-services/19-03-SUMMARY.md (the prior on-hardware checklist precedent, incl. backend restore)
  </read_first>
  <action>
    Execute in order, capturing each command's output verbatim for the summary:
    1. Record current state FIRST: `./villa backend` (or `villa config` equivalent) — note the active backend (expected rocm); `cat $XDG_CONFIG_HOME/villa/config.toml` memory fields; `systemctl --user list-units 'villa-*'` (all expected active).
    2. `make check` (full suite green) then `make build`; restart the long-lived dashboard so it picks up the new Pick signature behavior: `systemctl --user restart villa-dashboard.service` (dashboard binary trap).
    3. SC#1 (CTRL-01): `./villa recommend` — table shows the embed reservation row and a fit verdict against the shrunken envelope; `./villa recommend --json` — assert `schema_version == 2`, `memory_considered == true`, `embedding_reservation_bytes == 536870912`. Do NOT pass `--save` (Pitfall 8).
    4. SC#2 (CTRL-06): `./villa preflight` — MEM-PRE-disk row names the real volume root (~/.local/share/containers/storage/volumes) and PASSes (~469 GiB free); MEM-PRE-headroom PASSes (~82 GiB available vs 512 MiB floor). Negative control: temporarily set `memory_enabled = false` in config.toml, run `./villa preflight` — no "MEM-PRE" substring in output; restore `memory_enabled = true` immediately.
    5. SC#3 (CTRL-03), healthy path: `./villa doctor` and `./villa doctor --json` with the full stack up — assert overall PASS (Pitfall 1 resolved: offload WARNs for villa-qdrant/villa-embed visible but non-rank-raising), MEM-DOC-residency PASS, MEM-PRE-* findings present and PASS. Wall time bounded (< ~90 s including the drive).
    6. SC#3 negative control (no false-green): `systemctl --user stop villa-embed.service`; `./villa doctor --json` — MEM-DOC-residency must be WARN ("could not evaluate"), NEVER PASS; doctor must NOT have started the service (`systemctl --user is-active villa-embed.service` still inactive). Restart: `systemctl --user start villa-embed.service` and confirm active.
    7. D-05 measurement: reset peak visibility by restarting villa-embed (already done in step 6 restart), then drive sustained load — run `./villa doctor` (its 12-request drive) and/or a manual loop of the same probe — and record `systemctl --user show villa-embed.service -p MemoryCurrent -p MemoryPeak`. Compare MemoryPeak against 536870912. If MemoryPeak <= 512 MiB: record the figure, constant stands. If > 512 MiB: raise the pinned value + conservative default in internal/memory/footprint.go (single source; update the accessor-equals-pinned test in internal/memory/footprint_test.go — the two contingent files declared in this plan's frontmatter), re-run `go test ./internal/memory/ ./internal/recommend/ -count=1`, and record the finding.
    8. Restore/verify operator state: confirm the active backend is unchanged from step 1 (if any step reverted it, `./villa backend set rocm` per Pitfall 8); confirm all villa-* units active; `git status` clean of unintended changes.
  </action>
  <verify>
    <automated>make check</automated>
  </verify>
  <done>All eight steps executed with outputs captured; doctor overall PASS on healthy stack and WARN-not-PASS with villa-embed down; D-05 figures recorded; backend and services restored to their step-1 state.</done>
  <acceptance_criteria>
    - CLI: `./villa recommend --json` output contains `"schema_version": 2` and `"embedding_reservation_bytes": 536870912`.
    - CLI: memory-on `./villa preflight` output contains both `MEM-PRE-disk` and `MEM-PRE-headroom`; memory-off run contains neither.
    - CLI: healthy-stack `./villa doctor --json` reports overall PASS with `MEM-DOC-residency` PASS; villa-embed-down run reports `MEM-DOC-residency` WARN and exit code 2 semantics per the existing contract (never PASS/0 with the proof unevaluable... overall WARN exit per existing mapping).
    - Behavior: `systemctl --user is-active villa-embed.service` returns "inactive" immediately after the doctor negative-control run (doctor did not mutate state, D-10).
    - Behavior: D-05 numbers (MemoryCurrent/MemoryPeak bytes) captured for the summary; decision (constant stands / raised) recorded explicitly.
    - Behavior: active backend after step 8 equals the backend recorded in step 1.
    - Behavior: if the D-05 contingency triggered, the ONLY code files changed are internal/memory/footprint.go + internal/memory/footprint_test.go (the declared contingent set); otherwise `git status` shows zero code changes from this plan.
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Operator sign-off on the on-hardware results</name>
  <what-built>Phase 22 control-plane fit + host gate, verified live in Task 1: memory-aware recommend (schema 2), preflight memory gates against the real volume root, doctor memory fold with the residency-under-embedding-load proof (PASS reachable, no false-green), D-05 footprint measurement recorded.</what-built>
  <how-to-verify>
    Review the Task 1 captured outputs (presented in-session), then optionally spot-check yourself:
    1. `./villa recommend` — embed reservation visible in the fit math; `--json` shows schema_version 2.
    2. `./villa preflight` — MEM-PRE rows present (memory on) with sane values for this box.
    3. `./villa doctor` — overall PASS on the healthy stack; re-run with `systemctl --user stop villa-embed.service` to see the honest WARN (then restart the unit).
    4. Confirm the D-05 MemoryPeak figure vs the 512 MiB reservation and the recorded decision.
    5. Confirm the box state: backend is rocm, all villa-* units active, dashboard restarted.
  </how-to-verify>
  <resume-signal>Type "approved" to close Phase 22 verification, or describe any failing observation</resume-signal>
  <files>(none — human verification gate)</files>
  <action>Present the Task 1 captured outputs to the operator (recommend schema-2 JSON, preflight MEM-PRE rows, doctor PASS + negative-control WARN, D-05 MemoryPeak figure and decision, restored box state) and pause for the blocking sign-off described in how-to-verify.</action>
  <verify>
    <human-check>Operator reviewed the five how-to-verify items and responded "approved"</human-check>
  </verify>
  <done>Operator typed "approved"; any described issue is captured as a gap for follow-up instead of silently closing the phase.</done>
  <acceptance_criteria>
    - Behavior: an explicit operator "approved" (or an itemized issue list) is recorded in the session before the phase summary is written.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| verification commands → live operator box | UAT runs against the operator's real running stack and config |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-14 | Tampering | operator state (backend, units, config) | mitigate | record-first/restore-last discipline (Pitfall 8): backend noted in step 1 and re-asserted in step 8; memory_enabled toggles restored immediately; no `--save` on recommend |
| T-22-15 | Repudiation | verification recorded as greener than observed | mitigate | verbatim command outputs captured in the summary; negative controls (embed-down WARN, memory-off absence) are mandatory steps, not optional |
| T-22-16 | DoS | sustained drive destabilizing the live box | accept | bounded 12-request/60 s drive against a 125 GiB host with ~82 GiB available; transient --rm containers only |
| T-22-SC | Tampering | package installs | accept | N/A — zero new packages this phase |
</threat_model>

<verification>
- All three 22-VALIDATION.md Manual-Only Verifications executed and recorded.
- `make check` green on the final tree; `git status` clean of unintended modifications (only the declared D-05 contingent files may differ, and only if the contingency triggered).
</verification>

<success_criteria>
SC#1-3 proven on the live gfx1151 host; D-05 measurement recorded with an explicit constant decision; operator approved; box restored to its pre-verification operational state.
</success_criteria>

<output>
Create `.planning/phases/22-control-plane-fit-host-gate/22-04-SUMMARY.md` when done (must include the D-05 measurement table and the verbatim doctor outputs — Phase 23 consumes the measured-footprint observation).
</output>
