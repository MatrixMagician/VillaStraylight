---
milestone: v1.4
name: Coding Agent
audited: 2026-06-15
status: tech_debt
scores:
  requirements: 17/17
  phases: 5/5
  integration: 14/14
  flows: 3/3
gaps:
  requirements: []   # none — all 17 satisfied (3-source cross-referenced)
  integration: []    # none — 14/14 wired, no broken flows
  flows: []          # none — 3/3 E2E flows complete
nyquist:
  compliant_phases: [24, 26, 27]
  partial_phases: [25]      # 25-VALIDATION nyquist_compliant:false, wave_0_complete:false
  missing_phases: [28]      # no 28-VALIDATION.md
  overall: partial
tech_debt:
  - phase: 26-agent-delivery-core-lockdown-launcher
    items:
      - "[RESOLVED 2026-06-15, quick task 260615-ipn, commit 6eda867] internal/agent/render.go:32 — crush.json providerBaseURL re-types the inference port (http://127.0.0.1:8080/v1) as a literal instead of composing it from inference.ServerPort(). The naive fix (call inference.ServerPort() in render.go) was REJECTED: it would force internal/agent to import internal/inference, violating the documented seam invariant (agent.go:17-24 — the loopback literal is sanctioned by design). Closed instead by a cross-seam drift-guard test in cmd/villa (TestCrushProviderPortMatchesInferenceServerPort) that asserts the rendered crush.json provider port == inference.ServerPort() — a future serverPort change now FAILS CI; the agent core still imports no inference. Touches AGENT-02. (was: Integration WARNING)"
      - "On-hardware acceptance (26-03) ran on ROCm 7.2.4, not the Vulkan RADV default; the Crush TUI was proven via non-interactive `crush run` round-trip, not the interactive TUI."
  - phase: 25-coding-mode-render-transactional-swap-verb
    items:
      - "25-VALIDATION.md: nyquist_compliant:false, wave_0_complete:false (status:bound). Nyquist validation not finalized — discovery only; `/gsd-validate-phase 25` to close."
  - phase: 28-agent-surfacing-contracts
    items:
      - "No 28-VALIDATION.md (Nyquist MISSING). `/gsd-validate-phase 28` to close."
      - "28-01/02/03 (and gap-closure 27-05/06) SUMMARY.md frontmatter lacks the `requirements-completed` field — requirements are encoded in `tags`/`provides` instead. Cross-reference hygiene only; VERIFICATION + traceability confirm SURF-01/02/03 + USAGE-03/04 satisfied."
deferred_to_v2:
  - "CODER-V2-01: co-resident villa-coder Quadlet unit (128 GB fit-gated)"
  - "CODER-V2-02: Qdrant/villa-embed-backed MCP semantic code search (behind a pre-declared eval it must WIN)"
  - "CODER-V2-03: sandboxed/containerized agent profile for untrusted repos"
---

# Milestone v1.4 "Coding Agent" — Audit Report

**Audited:** 2026-06-15
**Status:** `tech_debt` — all requirements satisfied, all integration wired, all flows complete; **no blockers**. Accumulated non-critical debt (1 latent seam WARNING + 2 Nyquist-validation gaps + caveated on-hardware coverage) is documented below for explicit acknowledgment at close.

## Scope

5 phases (24–28), 18 plans, all complete on disk (`progress_percent: 100`). Every phase VERIFICATION.md is `status: passed`:

| Phase | Name | Verification | Score |
|-------|------|--------------|-------|
| 24 | Coder Fit Math, Catalog & On-Hardware Model Qualification | passed | 14/14 |
| 25 | Coding-Mode Render & Transactional Swap Verb | passed | 9/9 |
| 26 | Agent Delivery Core & Lockdown Launcher | passed | 4/4 |
| 27 | Install Addon, Preflight Gates & `villa verify agent` | passed | 5/5 (was 2/5 → gap-closure 27-05/06) |
| 28 | Agent Surfacing & Contracts | passed | 5/5 (on-hardware UAT resolved 2026-06-15) |

## Requirements Coverage — 17/17 satisfied (3-source cross-reference)

Each requirement cross-referenced across (a) REQUIREMENTS.md traceability, (b) phase VERIFICATION.md, (c) SUMMARY frontmatter / integration touchpoint.

| REQ-ID | Phase | VERIFICATION | SUMMARY | Traceability | Final |
|--------|-------|--------------|---------|--------------|-------|
| CODER-01 | 24 | passed | listed (24-01/04) | [x] | **satisfied** |
| CODER-02 | 24 | passed | listed (24-02) | [x] | **satisfied** |
| CODER-03 | 24 | passed | listed (24-03/04) | [x] | **satisfied** |
| CMODE-01 | 25 | passed | listed (25-01) | [x] | **satisfied** |
| CMODE-02 | 25 | passed | listed (25-02, closed on-hardware 2026-06-13) | [x] | **satisfied** |
| AGENT-01 | 26 | passed | listed (26-01/02/03) | [x] | **satisfied** |
| AGENT-02 | 26 | passed | listed (26-01) | [x] | **satisfied** (port-literal WARNING — see tech debt) |
| AGENT-03 | 26 | passed | listed (26-02) | [x] | **satisfied** |
| AGENT-04 | 26 | passed | listed (26-01/02/03) | [x] | **satisfied** |
| INSTALL-03 | 27 | passed | listed (27-01/04) | [x] | **satisfied** |
| INSTALL-04 | 27 | passed | listed (27-02) | [x] | **satisfied** |
| PRIV-06 | 27 | passed | listed (27-03/04) | [x] | **satisfied** |
| SURF-01 | 28 | passed | tags (28-03) | [x] | **satisfied** |
| SURF-02 | 28 | passed | tags (28-01) | [x] | **satisfied** |
| SURF-03 | 28 | passed | tags (28-02) | [x] | **satisfied** |
| USAGE-03 | 28 | passed | tags (28-03) | [x] | **satisfied** |
| USAGE-04 | 28 | passed | tags (28-02/03) | [x] | **satisfied** |

No unsatisfied requirements. No orphans (every REQ appears in a phase VERIFICATION). Phase 28 / 27-05-06 SUMMARYs encode requirements in `tags`/`provides` rather than the `requirements-completed` field — flagged as cross-reference hygiene, not a coverage gap (verified manually).

## Cross-Phase Integration — 14/14 wired, 3/3 flows complete

Integration checker (adversarial, source-traced) verdict: **WIRED**, no BLOCKERs, no BROKEN-FLOWs.

1. **Install addon → render → readiness (P24→P25→P27)** — WIRED. `runInstall` threads `rec.Coder` (P24 `CoderFit`) into `cfg.CoderModel/Quant/AgentCtx` (`install.go:470-474`), resolves the served model via P25 helpers into `renderIn.CodingMode`/`CoderAgentCtx` (`install.go:501-515`), readiness proof runs AFTER render serving the staged coder (`install.go:774`). The CR-01 "claimed but not wired" risk is genuinely wired. Staged GGUF resolved from the same catalog entry via `coderShardFor` — no hard-coded shard.
2. **Agent core → install/launch (P26→P27)** — WIRED. `agent.Install` checksum-before-extract (`install_agent.go:120`), `agent.Render` → `crushConfigPath()` (`install_agent.go:134`), `villa code` execs the same `agentBinPath()` (`code.go:182-185`).
3. **Endpoint single-sourcing** — WIRED + WARNING. In-network endpoint single-sourced via `orchestrate.LlamaInNetworkEndpoint()` (`endpoint.go:29`) = `http://villa-llama:<inference.ServerPort()>/v1`, consumed by verify + status. WARNING: `agent.Render` re-types the host-loopback provider port `8080` as a literal (`render.go:32`) instead of composing `inference.ServerPort()` — correct today, latent drift risk (see tech debt).
4. **Surfacing reads finished state (P24–27→P28)** — WIRED. `status.codingInfo` gates on `cfg.AgentEnabled`, reads `agent.LoadCrushPolicy().Version`, recomputes residency via `recommend.Pick`; doctor REUSES `liveAgentToolCallProbe` (not a fork); backup covers crush.json + identity-only `ExcludedAgent`.
5. **Single contract-bump discipline** — WIRED. `status.reportSchemaVersion = 4` (bumped once 3→4; agent-off golden byte-identical); doctor owns schema 2, backup owns schema 3 — no second status re-freeze. Golden tests pass.
6. **E2E flows — 3/3 complete:** install→stage→render→readiness→`villa code`→`villa status`; `villa verify agent` negative-control-first (egress-open FAIL, egress-blocked completes, llama-down proves no cloud fallback); operate/observe (doctor fold, backup round-trip, uninstall removes binary+config).
7. **Off-path regression** — WIRED. Addon OFF → status/chat-render goldens byte-identical to v1.3; no coding leakage; `TestSeamGrepGate` green.

## Nyquist Compliance (discovery only — non-blocking)

| Phase | VALIDATION.md | Compliant | Action |
|-------|---------------|-----------|--------|
| 24 | exists | true | — |
| 25 | exists | **false** (wave_0 incomplete) | `/gsd-validate-phase 25` |
| 26 | exists | true | — |
| 27 | exists | true (strategy compliant; wave_0 folded into tdd tasks) | — |
| 28 | **missing** | n/a | `/gsd-validate-phase 28` |

## Conclusion

v1.4 delivered a complete, integrated, strictly-local coding-agent addon: qualified coder catalog + fit math (P24), transactional coding-mode swap (P25), pinned checksum-verified Crush install + `villa code` lockdown (P26), the install addon + honest preflight + negative-control-first `villa verify agent` (P27), and full surfacing/doctor/backup/usage (P28) — `status.Report` evolved exactly once (3→4). All 17 requirements satisfied, 14/14 cross-phase connections wired, 3/3 E2E flows complete.

**Recommendation:** safe to complete. The tech debt (1 latent port-literal seam fragility, 2 Nyquist-validation gaps, ROCm-only / non-interactive-TUI on-hardware caveats) is non-blocking and can be accepted into the backlog or closed via a short cleanup pass before close.
