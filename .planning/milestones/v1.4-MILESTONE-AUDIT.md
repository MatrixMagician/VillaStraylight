---
milestone: v1.4
name: Coding Agent
audited: 2026-06-15
status: tech_debt
scores:
  requirements: 17/17
  phases: 5/5
  integration: PASS (7/7 seam groups wired, 0 blockers)
  flows: 6/6
gaps:
  requirements: []   # none — all 17 satisfied (3-source cross-referenced)
  integration: []    # none — 0 blockers, 0 broken flows
  flows: []          # none — 6/6 E2E flows complete
nyquist:
  compliant_phases: [24, 25, 26, 27]   # all VALIDATION.md nyquist_compliant:true, wave_0_complete:true
  partial_phases: []
  missing_phases: [28]                  # no 28-VALIDATION.md
  overall: partial
tech_debt:
  - phase: 27-install-addon-preflight-gates-villa-verify-agent
    items:
      - "BUG (real, follow-up filed in 27-04): `villa install` reverts a persisted `backend=rocm` to Vulkan — install.go `cfg.Backend = rec.Backend` + recommend.Pick always defaults Vulkan (ROCm strictly opt-in). A re-install silently discards a `villa backend set rocm` opt-in (config-is-source-of-truth violation). On the gfx1151 box the runtime stayed ROCm only because `systemctl start` no-op'd the already-active service. Reversible via `villa backend set rocm`. Out of v1.4 scope; track as a maintenance fix."
      - "Advisory (WR-02, fail-safe): external egress probe exit 22 (reachable-but-HTTP-error) classifies as infra-FAIL, not 'not blocked' — can never false-green PRIV-06; only the FAIL message is less precise."
      - "Advisory (WR-01 note): `runProbeCurlCode` live exit-code extraction (errors.As) is convention-anchored, untested with a real-exec case. Synthetic classifier fully tested. Optional follow-up."
      - "Advisory (WR-03): `--coding-agent` with only a shared-residency coder fit blocks with a 'no coder fits' message — misleading copy, not a false-green. v1.4 swap-only limitation."
  - phase: 28-agent-surfacing-contracts
    items:
      - "Nyquist: no *-VALIDATION.md (only v1.4 phase without one). Coverage is real — 28-VERIFICATION passed 5/5 with a per-SC evidence table and green internal/status + doctor + metrics + golden suites — but the Nyquist validation contract was never written. `/gsd-validate-phase 28` to close (likely a State-B reconstruct, no new tests expected)."
      - "Doc hygiene: 28-01/02/03 (and gap-closure 27-05/06) SUMMARY frontmatter lacks `requirements-completed` (REQs encoded in tags/provides + the VERIFICATION SC table). Cross-reference hygiene only; coverage confirmed."
  - phase: 26-agent-delivery-core-lockdown-launcher
    items:
      - "[RESOLVED 2026-06-15, quick task 260615-ipn, commit 6eda867] render.go crush.json provider port re-typed the inference port (127.0.0.1:8080) as a literal. Naive fix REJECTED (would force internal/agent→internal/inference import, violating the seam invariant). Closed by a cross-seam drift-guard test `TestCrushProviderPortMatchesInferenceServerPort` (cmd/villa) asserting rendered provider port == `inference.ServerPort()` — confirmed present + green by the integration checker. Touches AGENT-02."
      - "On-hardware acceptance (26-03) ran on ROCm 7.2.4, not the Vulkan RADV default; the Crush TUI was proven via a non-interactive `crush run` round-trip, not the interactive TUI."
  - phase: integration
    items:
      - "W-1 (by-design, cosmetic): `villa uninstall` leaves config.toml untouched (D-10), so `cfg.AgentEnabled` stays true after binary+crush.json removal. Post-uninstall `villa status`/`doctor` enter the agent branch and surface an honest typed-Unknown (PinUnknown WARN), never a crash or fabricated value. Acceptable per D-10; flagged for awareness only."
deferred_to_v2:
  - "CODER-V2-01: co-resident villa-coder Quadlet unit (128 GB fit-gated)"
  - "CODER-V2-02: Qdrant/villa-embed-backed MCP semantic code search (behind a pre-declared eval it must WIN)"
  - "CODER-V2-03: sandboxed/containerized agent profile for untrusted repos"
---

# Milestone v1.4 "Coding Agent" — Audit Report

**Audited:** 2026-06-15 · **Status:** `tech_debt` — all requirements satisfied, all integration wired, all flows complete; **no blockers**. Accumulated non-critical debt (1 tracked maintenance bug + 1 Nyquist gap + fail-safe advisories) is documented for explicit acknowledgment at close.

> Refreshed 2026-06-15 (supersedes the 13:35 draft): Phase 25 VALIDATION was finalized (`nyquist_compliant: true`) and Phase 27 wave-0 was realized + audited (`wave_0_complete: true`) since the prior draft — Nyquist is now 4/5 compliant with only Phase 28 missing. The 27-04 backend-revert bug (absent from the prior draft) is recorded.

**Milestone goal (ROADMAP):** A villa-orchestrated, strictly-local terminal coding agent as an optional install addon — Crush v0.76.0 delivered as a pinned SHA-256-verified host binary with villa-rendered config, talking over loopback to a fit-guarded recommended coding model via a transactional swap-based coding mode. Codebase memory is agent-native; the Qdrant code-collection premise was rejected on evidence. Zero new outbound, proven at runtime, negative-control-first.

## Scope

5 phases (24–28), 18 plans, all complete on disk. Every phase VERIFICATION.md is `status: passed`:

| Phase | Name | Verification | Score |
|-------|------|--------------|-------|
| 24 | Coder Fit Math, Catalog & On-Hardware Model Qualification | passed | 14/14 |
| 25 | Coding-Mode Render & Transactional Swap Verb | passed | 9/9 |
| 26 | Agent Delivery Core & Lockdown Launcher | passed | 4/4 |
| 27 | Install Addon, Preflight Gates & `villa verify agent` | passed | 5/5 (was 2/5 → gap-closure 27-05/06) |
| 28 | Agent Surfacing & Contracts | passed | 5/5 (on-hardware UAT resolved 2026-06-15) |

## Requirements Coverage — 17/17 satisfied (3-source cross-reference)

Each requirement cross-referenced across (a) REQUIREMENTS.md traceability, (b) phase VERIFICATION.md, (c) SUMMARY frontmatter / VERIFICATION SC table.

| REQ-ID | Phase | VERIFICATION | SUMMARY | Traceability | Final |
|--------|-------|--------------|---------|--------------|-------|
| CODER-01 | 24 | passed | listed (24-01/04) | [x] | **satisfied** |
| CODER-02 | 24 | passed | listed (24-02) | [x] | **satisfied** |
| CODER-03 | 24 | passed | listed (24-03/04) | [x] | **satisfied** |
| CMODE-01 | 25 | passed | listed (25-01) | [x] | **satisfied** |
| CMODE-02 | 25 | passed | listed (25-02, closed on-hardware 2026-06-13) | [x] | **satisfied** |
| AGENT-01 | 26 | passed | listed (26-01/02/03) | [x] | **satisfied** |
| AGENT-02 | 26 | passed | listed (26-01) | [x] | **satisfied** (port-literal drift now guarded — see tech debt, RESOLVED) |
| AGENT-03 | 26 | passed | listed (26-02) | [x] | **satisfied** |
| AGENT-04 | 26 | passed | listed (26-01/02/03) | [x] | **satisfied** |
| INSTALL-03 | 27 | passed | listed (27-01/04) | [x] | **satisfied** |
| INSTALL-04 | 27 | passed | listed (27-02) | [x] | **satisfied** |
| PRIV-06 | 27 | passed | listed (27-03/04) | [x] | **satisfied** |
| SURF-01 | 28 | passed (SC1) | ⚠ tags only (28-03) | [x] | **satisfied** |
| SURF-02 | 28 | passed (SC2) | ⚠ tags only (28-01) | [x] | **satisfied** |
| SURF-03 | 28 | passed (SC3) | ⚠ tags only (28-02) | [x] | **satisfied** |
| USAGE-03 | 28 | passed (SC4) | ⚠ tags only (28-03) | [x] | **satisfied** |
| USAGE-04 | 28 | passed (SC5) | ⚠ tags only (28-02/03) | [x] | **satisfied** |

No unsatisfied requirements. No orphans (every REQ appears in a phase VERIFICATION). Phase-28 / 27-05-06 SUMMARYs encode requirements in `tags`/`provides` rather than `requirements-completed` — flagged as cross-reference hygiene, not a coverage gap (verified via the VERIFICATION SC tables).

## Cross-Phase Integration — PASS (gsd-integration-checker, source-traced)

Build baseline: `go build ./...` clean; `go test ./...` 1321 passed / 24 pkgs / 0 fail; `TestSeamGrepGate` green. **0 blockers, 0 broken flows, 0 orphaned seams.**

5 cross-phase seam groups, all WIRED with single-source data flow:
1. **`rec.Coder` read-never-re-derived:** P24 recommend → P27 preflight envelope gate → P27 install render → P25 coding-mode helpers → P28 status residency recompute (same memory inputs, no drift).
2. **CR-01 closure:** P27 `--coding-agent` install invokes the P25 coding-mode render and serves the staged coder; crush.json advertises the same model id (`agent/render.go servedModelID` from `cfg.CoderModel`).
3. **P26 core composed, not re-implemented:** `agent.Install` (checksum-before-extract) / `agent.Render` / `agent.LoadCrushPolicy` composed by P27 install; `villa code` + install share the same `crushConfigPath()` + `agentBinPath()`.
4. **Shared DRY driver:** P27 `liveAgentToolCallProbe` is the single definition reused by `villa verify agent` (P27) and `villa doctor` (P28) — no fork.
5. **P28 reads finished state:** status/dashboard/doctor/backup all gate on `cfg.AgentEnabled`; status/doctor share `agent.DetectDrift`; backup covers crush.json, excludes the binary by identity.

Invariants intact: `TestSeamGrepGate` green (no backend/image/host literal in cmd/villa); coding-off/agent-off goldens append-only (status diff = one `schema_version 3→4` line); single status schema bump (status=4 once; doctor=2, backup per-store — distinct contracts); `TestCrushProviderPortMatchesInferenceServerPort` drift-guards the provider port. E2E flows 6/6 complete: install addon · verify agent (negative-control-first) · code launcher · status/dashboard (hidden-until-data) · uninstall (ordered) · backup/restore.

## Nyquist Compliance (discovery only — non-blocking)

| Phase | VALIDATION.md | Compliant | wave_0 | Action |
|-------|---------------|-----------|--------|--------|
| 24 | exists | true | true | — |
| 25 | exists | true | true | — (validated since prior draft) |
| 26 | exists | true | true | — |
| 27 | exists | true | true | — (wave-0 realized + audited 2026-06-15) |
| 28 | **missing** | n/a | n/a | **`/gsd-validate-phase 28`** |

Phase 28 is the only v1.4 phase without a validation contract. Its automated coverage is real (VERIFICATION passed 5/5 with green status/doctor/metrics/golden suites), so this is expected to be a State-B reconstruct documenting existing tests rather than writing new ones.

## Conclusion

v1.4 delivered a complete, integrated, strictly-local coding-agent addon: qualified coder catalog + fit math (P24), transactional coding-mode swap (P25), pinned checksum-verified Crush install + `villa code` lockdown (P26), the install addon + honest preflight + negative-control-first `villa verify agent` (P27), and full surfacing/doctor/backup/usage (P28) — `status.Report` evolved exactly once (3→4). All 17 requirements satisfied, integration clean (0 blockers), 6/6 E2E flows complete.

**Recommendation:** safe to complete. Two items merit a decision before archiving — neither blocks completion:
1. **Close the Phase-28 Nyquist gap** via `/gsd-validate-phase 28` (most actionable).
2. **Schedule the 27-04 backend-revert bug** as a v1.5 / maintenance follow-up (or accept into backlog).

The remaining debt (3 fail-safe Phase-27 advisories, the resolved port-literal drift-guard, the by-design uninstall W-1, ROCm-only / non-interactive-TUI on-hardware caveats) is non-blocking.

---
*Audited 2026-06-15 by /gsd-audit-milestone · integration check by gsd-integration-checker · refreshed to supersede the 13:35 draft*
